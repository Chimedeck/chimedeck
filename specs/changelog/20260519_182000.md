## Update
- Updated `GET /api/v1/boards/:boardId/state-transitions/rules` to normalize graph rules against current active board lists before deriving rules (strip deleted list nodes/edges and refresh renamed node labels).
- Extended `server/extensions/stateTransitions/api/__tests__/getRules.test.ts` with feature-flag-off, forbidden membership, and stale-graph normalization coverage.

## New
- Added `server/extensions/stateTransitions/api/__tests__/put_sync_hooks.test.ts` to cover PUT sync behavior (deleted-node stripping + renamed-label update) and `state_transition_updated` websocket emission.

## Technical Debt
- `put_sync_hooks.test.ts` and `put.test.ts` currently include overlapping sync/ws assertions; these can be consolidated once iteration-level file naming constraints are relaxed.

## What Should Be Done Next
- In a follow-up cleanup iteration, fold duplicate PUT sync/ws assertions into a single canonical API test file while preserving iteration-required coverage.
