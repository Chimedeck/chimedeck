## Update
- Removed `monetizationType` prop from `BoardCanvas`, `SortableListColumn`, and `CardModal` components.
- Rewrote `BoardSettings.tsx` — stripped monetization radio group; now renders a minimal settings panel.
- Cleaned `BoardPage.tsx` — removed `handleSaveMonetizationType` callback and `patchBoardMonetizationType` import.
- Removed `monetization_type` field from the `Board` interface in `src/extensions/Board/api.ts`.
- Removed `optimisticUpdateBoardMonetization` action from `boardSlice`.
- Cleaned `CardModal` container — removed `selectBoard` usage and `monetizationType` / `listName` props.
- Removed `paymentRouter` import and dispatch from `server/index.ts`.
- Removed `STRIPE_SECRET_KEY` from `server/config/env.ts`.
- Removed `STRIPE_PUBLIC_KEY` from `src/config/index.ts`.
- Removed Stripe entries from `.env.example`.
- Removed `@stripe/react-stripe-js`, `@stripe/stripe-js`, and `stripe` from `package.json`.

## New
- Nothing added.

## Technical Debt
- `db/migrations/0020_board_monetization.ts` still exists and adds a `monetization_type` column to boards. A down-migration or a new migration dropping the column should be applied to keep the schema consistent with the codebase.

## What Should Be Done Next
- Run `db/migrations/0020_board_monetization.ts` down migration (or create a new migration) to drop the now-unused `monetization_type` column from the `boards` table.
- Remove the migration file itself once the column is gone.
- Uninstall Stripe packages from `node_modules` with `bun install` after the `package.json` changes take effect.
