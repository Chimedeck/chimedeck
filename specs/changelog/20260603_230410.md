## Update
Extended the BoardPage documentation test mock for `~/extensions/HealthCheck/config/healthCheckConfig` so it now matches the real export shape by including `HEALTH_CHECK_POLL_INTERVAL_MS` alongside `HEALTH_CHECK_ENABLED`.

## New
None.

## Technical Debt
The BoardPage documentation test still has an existing store-shape issue (`state.viewPreference.activeView` is undefined) that prevents the test from passing independently of the mock fix.

## What Should Be Done Next
Stabilize the BoardPage documentation test store fixture so it supplies the expected `viewPreference` state, then rerun the documentation and SpecsWorkspacePage test files together.
