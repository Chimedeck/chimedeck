# Changelog 20260424_101500

## Update

- Fixed production PDF attachment preview failures caused by CSP blocking blob-backed iframe rendering.
- Updated server CSP generation to always include `frame-src 'self' blob:` while still allowing extra plugin frame origins.

## New

- Added a security-header integration assertion that verifies `Content-Security-Policy` contains `frame-src 'self' blob:` to prevent regressions.

## Technical Debt

- CSP is currently assembled as a single string literal in `server/mods/helmet.ts`; migrating to directive-wise builders would reduce the risk of future directive regressions.

## What Should Be Done Next

- Verify PDF preview in staging/production by uploading a `.pdf` attachment, clicking View, and confirming the lightbox iframe renders without CSP console errors.
- Monitor CSP violation reports/logs for 24-48 hours to ensure no unintended frame policy side effects.
