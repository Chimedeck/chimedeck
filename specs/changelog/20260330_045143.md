## Update
- `server/extensions/email/templates/adminInvite.ts` — removed inline HTML, made function async, now calls `await renderTemplate(...)`.
- `server/extensions/auth/api/adminCreateUser.ts` — added `await` to `adminInviteEmail(...)` call to match new async signature.

## New
- `server/extensions/email/templates/html/adminInvite.html` — standalone Handlebars HTML template with `{{inviterName}}`, `{{newUserEmail}}`, `{{plainPassword}}`, `{{loginUrl}}` placeholders.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 4: Update any remaining call sites for `buildVerificationEmail`, add `server/extensions/email/templates/README.md`, and run final compile check.
