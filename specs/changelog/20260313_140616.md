## Update
- No changes to existing code or features; this iteration only adds a new migration for notification preferences.

## New
- Added `db/migrations/0037_notification_preferences.ts` to create the `notification_preferences` table:
  - Columns: `id` (UUID PK), `user_id` (UUID FK, CASCADE delete), `type` (TEXT), `in_app_enabled` (BOOLEAN, default `true`), `email_enabled` (BOOLEAN, default `true`), `updated_at` (TIMESTAMPTZ)
  - Unique constraint on (`user_id`, `type`) to enforce opt-out model
  - Index on `user_id` for efficient lookups
  - Clean rollback support via `dropTableIfExists`

## Technical Debt
- None

## What Should Be Done Next
- Implement API endpoints and UI for managing notification preferences
- Add integration and websocket tests for notification payloads
- Decide on canonical avatar storage/normalization strategy
- Ensure end-to-end tests for notification preference flows
