# Changelog — 2026-03-19 10:50:00

## Update

- `src/extensions/UserProfile/containers/EditProfilePage/EditProfilePage.tsx`: Replaced all hardcoded English strings with `translations['UserProfile.*']` bracket notation. Imports `translations` from the new `../../translations/en.json`.
- `src/extensions/AdminInvite/InviteExternalUserModal.tsx`: Replaced all hardcoded English strings (modal title, description, field labels, placeholders, radio labels, button copy, error messages, loading state, strength labels) with `translations['AdminInvite.*']` bracket notation. Imports `translations` from `./translations/en.json`.
- `src/extensions/AdminInvite/CredentialSheet.tsx`: Replaced all hardcoded English strings (sheet title, email-sent notice, verification status, credential field labels, copy/done buttons and aria-labels) with `translations['AdminInvite.*']` bracket notation.

## New

- `src/extensions/UserProfile/translations/en.json`: Per-feature translation file for the UserProfile extension. Contains 11 keys covering the back button, page title, signed-in-as label, notification preferences heading, avatar actions, save/cancel buttons, and success/error messages.
- `src/extensions/AdminInvite/translations/en.json`: Per-feature translation file for the AdminInvite extension. Contains 35 keys covering all modal copy (title, description, field labels, placeholders, radio labels, toggles, buttons, strength labels, client-side and server-side validation errors) and all CredentialSheet copy.
- `specs/tests/sprint-94-userprofile-admininvite-i18n.md`: Playwright MCP test plan for verifying UserProfile and AdminInvite i18n. Covers profile settings page, invite modal, credential sheet, and zero-hardcoded-string grep assertions.

## Technical Debt

- `src/extensions/User/translations/en.json` contains overlapping `ProfilePage.*` keys (changePhoto, removePhoto, saveChanges, etc.) that pre-date this sprint. The `UserProfile.*` keys created here are a superset for the `EditProfilePage` flow. A future cleanup pass should consolidate or deprecate the `ProfilePage.*` keys in `User/translations/en.json` once it is confirmed no component still references them directly.

## What Should Be Done Next

- Iteration 7: Implement Realtime, OfflineDrafts, and BoardViews i18n — create `translations/en.json` for each and replace hardcoded strings in their `.tsx` files.
- Run Playwright MCP smoke test against `http://localhost:3000` to verify UserProfile and AdminInvite UI renders translation-driven copy with no visible hardcoded strings.
