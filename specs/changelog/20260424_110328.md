# Update
- Fixed duplicate comment-notification fanout for mentioned users in card comments.
- Users mentioned in a newly-created comment are now excluded from the generic `card_commented` notification path.

# New
- Added optional `excludedUserIds` support to `dispatchDirectCardNotification` so callers can suppress board-activity notifications for specific recipients.

# Technical Debt
- Comment notification fanout logic still lives in a high-complexity create-comment handler; additional extraction could improve readability and maintainability.

# What Should Be Done Next
- Add an integration test that creates a comment with mentions and asserts each mentioned user receives only `mention` (not `card_commented`).
- Add a complementary test asserting non-mentioned board members still receive `card_commented`.
