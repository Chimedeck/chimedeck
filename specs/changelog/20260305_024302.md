# Changelog — 20260305_024302

## Update
- `src/extensions/Card/components/CardItem.tsx` — added `CardPluginBadges` and `CardPluginButtons` injection points at the bottom of each card tile
- `src/extensions/Card/components/CardModal.tsx` — added `CardPluginSection` in the main column (between checklist and activity feed) and `CardDetailPluginBadges` in the sidebar (after Members)

## New
- `src/extensions/Plugins/uiInjections/CardPluginBadges.tsx` — resolves `card-badges` capability via plugin bridge and renders colour-coded badge chips on card tiles
- `src/extensions/Plugins/uiInjections/CardPluginButtons.tsx` — resolves `card-buttons` capability and renders clickable plugin action buttons on card tiles; button click broadcasts `BUTTON_CLICKED` to all active plugins
- `src/extensions/Plugins/uiInjections/CardDetailPluginBadges.tsx` — resolves `card-detail-badges` capability and renders bordered badge chips in the card detail modal sidebar
- `src/extensions/Plugins/uiInjections/CardPluginSection.tsx` — resolves `card-section` capability and renders plugin-provided section panels in the card detail modal main column

## Technical Debt
- Button click currently broadcasts `BUTTON_CLICKED` to all active plugins (no attribution from bridge.resolve results). Iteration 10 should add per-plugin attribution so only the owning plugin receives the callback.
- `CardPluginSection` uses a custom `card-section` capability name not in the Trello spec. Future: align with standard or document as extension.

## What Should Be Done Next
- Iteration 10: Implement `PluginPopup` and `PluginModal` overlays; wire `BUTTON_CLICKED` → `UI_MODAL` flow end-to-end; add `show-settings` gear icon in plugin dashboard.
