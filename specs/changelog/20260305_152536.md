## Update
- Refactored `server/extensions/plugins/sdk/jh-instance.ts` to add a callback registry for plugin button callbacks and implemented `serializeResult()` to replace function callbacks with callback IDs.
- Updated the CAPABILITY_INVOKE handler to send a typed `RESOLVE_CAPABILITY_RESPONSE` and added a BUTTON_CLICKED handler to invoke registered callbacks.
- Modified `src/extensions/Plugins/uiInjections/CardPluginButtons.tsx` to send BUTTON_CLICKED messages with callback IDs to the plugin iframe on button click.
- Rebuilt `public/sdk/jh-instance.js` to include the new callback registry and BUTTON_CLICKED logic.

## New
- Introduced a callback registry in the plugin SDK to support dynamic button callbacks via opaque callback IDs.
- Added BUTTON_CLICKED postMessage handling for end-to-end plugin button invocation between host and plugin iframe.

## Technical Debt
- No automated Playwright tests exist for plugin button/capability flows; only a manual test plan is present in `specs/tests/plugins-sdk-mcp-evaluator.md`.
- Modal `sizeTo` callback and bridge-based `show-settings` flow remain unimplemented.

## What Should Be Done Next
- Implement Playwright E2E tests for plugin button/capability flows as outlined in the manual test plan.
- Add DB migration and server-side validation for board/resource scoping and plugin data storage.
- Complete modal `sizeTo` callback and bridge-based `show-settings` implementation.
- Address memory management and error handling for stale/missing callback IDs in the registry.