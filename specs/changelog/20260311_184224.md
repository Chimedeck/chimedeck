## Update
- Integrated `AttachmentPanel` into `CardModal`, displaying attachments and upload UI in the card detail view.
- Updated `AttachmentItem`, `AttachmentThumbnail`, `AttachmentDropZone`, and `PasteListener` to support the new panel and flows.

## New
- Created `src/extensions/Attachments/components/AttachmentPanel.tsx` with drag-and-drop, file picker, upload progress, thumbnail grid, and external link form.
- Added Playwright E2E test: `tests/e2e/attachments/attachmentPanel.spec.ts` covering all panel scenarios.

## Technical Debt
- Playwright test file initially written as Markdown; required conversion to `.spec.ts` for execution.
- No automation or migration code for attachments yet; MIME allowlist logic still missing.

## What Should Be Done Next
- Implement MIME type allowlist and validation for attachment uploads.
- Finalize UI/UX for error, empty, and confirmation states.
- Add automation features and related changelogs/specs.
- Ensure Playwright tests use the correct base URL and pass end-to-end.