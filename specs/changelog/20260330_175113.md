## Update
- Fixed activity notification recipient scoping in `server/extensions/activity/mods/mapActivityToNotification.ts`.
- Recipient fan-out now resolves from board-level participants only (`board_members` + `board_guest_access`) instead of all workspace memberships.

## New
- Added explicit board-scope guard comments in the activity-to-notification mapping logic to document why workspace-level recipient lookup is unsafe for guests.

## Technical Debt
- `mapActivityToNotification` still has elevated cognitive complexity and duplicated switch branches in `buildEmailTemplateData`; this should be refactored in a follow-up to satisfy static analysis rules without behavior changes.

## What Should Be Done Next
- Add regression tests for cross-board guest isolation:
  - Guest invited to Board A must not receive notifications from Board B.
  - Guest invited to Board A must still receive notifications from Board A.
- Validate existing production/staging data for stale rows that could still route notifications incorrectly (e.g. unexpected board participant records).
