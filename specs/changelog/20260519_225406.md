## Update
- Refactored stateTransitions API to normalize error envelopes and enforce feature flag gating.
- Improved sync logic in `sync.ts` for list deletion/rename, and updated event broadcast in `put.ts`.
- Enhanced validation and serialization for rules and sync edge cases.
- Updated routing in `index.ts` to mount new endpoints.

## New
- Implemented `GET /state-transitions/rules` endpoint with normalized rules output.
- Added sync helpers for stripping deleted nodes and renaming nodes.
- WebSocket broadcast of `state_transition_updated` after successful PUT.
- Added/extended types, validators, serializers, and error types for rules and sync.
- Added tests: `getRules.test.ts`, `put.test.ts`, `sync.test.ts` for rules, sync, and eventing.

## Technical Debt
- Error envelope inconsistency remains in legacy modules.
- Playwright cannot fully validate API errors due to CORS.
- UI and routing for `/state-transitions` and `/state-transitions/rules` not mounted; endpoints return 404.
- Login page not rendering; form missing in UI tests.

## What Should Be Done Next
- Fix routing and UI mounting for state transitions management and rules endpoints.
- Implement `/rules` and `/copy` endpoints, list sync hooks, and websocket broadcast for other flows.
- Expand test coverage for edge cases and error handling.
- Address error envelope consistency in legacy modules.
- Restore login page rendering and form fields in UI.