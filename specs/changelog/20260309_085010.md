# Sprint 42 — Split AWS Credentials: LocalStack S3 vs Real AWS SES

## Update

- `server/config/env.ts`: Added `S3_AWS_ACCESS_KEY_ID` and `S3_AWS_SECRET_ACCESS_KEY` env vars alongside the existing global `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`.
- `server/extensions/attachment/common/config/s3.ts`: S3 client now resolves credentials as `S3_AWS_ACCESS_KEY_ID || AWS_ACCESS_KEY_ID` (and same for secret), enabling LocalStack and real AWS SES to coexist in the same environment.
- `server/extensions/email/mods/ses.ts`: Added a clarifying comment explaining SES intentionally uses global AWS credentials, not the S3-specific ones.
- `.env`: Replaced `AWS_ACCESS_KEY_ID=test` / `AWS_SECRET_ACCESS_KEY=test` in the S3 block with `S3_AWS_ACCESS_KEY_ID=test` / `S3_AWS_SECRET_ACCESS_KEY=test`; global `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` are now blank (real credentials go here for SES).
- `.env.example`: Documented both credential sets with fallback behaviour; updated SES section comment.

## New

- `S3_AWS_ACCESS_KEY_ID` / `S3_AWS_SECRET_ACCESS_KEY` env vars — S3-specific credentials with fallback to global AWS credentials.

## Technical Debt

None introduced.

## What Should Be Done Next

- Add automated credential-matrix tests (S3_AWS_* set / unset combinations).
