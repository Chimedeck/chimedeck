# Changelog — 2026-03-23T08:39:08Z

## Update
- `package.json`: Added `bin.horiflow` entry pointing to `cli/index.ts`; installed `minimist` and `@types/minimist` dependencies.

## New
- `cli/config.ts`: `resolveConfig()` reads `--token` / `HORIFLOW_TOKEN` and `--api-url` / `HORIFLOW_API_URL`; exits 1 with a helpful error when token is missing.
- `cli/apiClient.ts`: `call<T>()` fetch wrapper that injects `Authorization: Bearer` header; handles 4xx/5xx and network errors with `process.exit(1)`.
- `cli/output.ts`: `print(data, jsonMode)` formats responses as raw JSON (`--json`) or a human-readable one-liner.
- `cli/index.ts`: Entry point (`#!/usr/bin/env bun`); parses global flags (`--token`, `--api-url`, `--json`, `--help`, `--version`) via minimist; dispatches to subcommands (stub switch — commands wired in future iterations).

## Technical Debt
- `cli/index.ts` switch has only a `default` branch — subcommand modules will be added in iterations 7–9.

## What Should Be Done Next
- Iteration 7: Wire in `move-card` and `comment` CLI subcommands.
