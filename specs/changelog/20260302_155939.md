## Update
- Refactored `BoardPage.tsx` to use new `boardSlice`, `BoardHeader`, and `BoardCanvas` components, enabling DnD and inline editing.
- Migrated `ListColumn.tsx` to use `@dnd-kit` for card drag-and-drop and updated to render new `CardItem` and `AddCardForm`.
- Enhanced `ListHeader.tsx` with dark kanban styling, Enter/Escape inline edit, and card count prop.
- Registered `boardReducer` in `reducers.ts` and `store/index.ts`.
- Updated Workspace test files to include the new board reducer.

## New
- Installed `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` for drag-and-drop support.
- Added `Board/slices/boardSlice.ts` for kanban board state and optimistic drag/rollback logic.
- Created API wrappers: `Board/api/board.ts`, `Board/api/list.ts`, `Board/api/card.ts`.
- Added UI components: `BoardHeader.tsx`, `BoardCanvas.tsx`, `BoardMemberAvatars.tsx`, `CardItem.tsx`, `AddCardForm.tsx`, `AddListForm.tsx`.
- Added markdown test specs: `specs/tests/board-view.md`, `board-drag-card.md`, `board-add-card.md`.
- Created changelog: `specs/changelog/20260302_084238.md`.

## Technical Debt
- Playwright E2E tests are not implemented; only markdown specs exist for board flows.
- Real-time sync, CardModal, and advanced board settings are deferred.
- Error handling for failed optimistic updates and accessibility for DnD/inline editing need further work.

## What Should Be Done Next
- Implement Playwright test files and config for board flows as per markdown specs.
- Add real-time sync, CardModal, and advanced board settings in future sprints.
- Improve error handling and accessibility for drag-and-drop and inline editing.
- Refine board snapshot/rollback and loading/empty states.