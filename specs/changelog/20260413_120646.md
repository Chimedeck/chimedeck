## Update
- Migrated all raw `<button>` elements for tag removal in `RegisterPluginModal.tsx` and `EditPluginModal.tsx` to use `<IconButton>` with appropriate `aria-label` for accessibility.
- Confirmed all remaining raw button usages in Sprint 133 target files are either migrated or explicitly excepted with comments (e.g., `[plugin-button-exception]`, `[switch-exception]`).
- Lint and type-check pass with no new errors introduced by these changes.

## New
- No new features or files added in this iteration; all work focused on migration and consolidation of existing button usage.

## Technical Debt
None

## What Should Be Done Next
- Ensure test data and permissions allow access to plugin modals for full manual smoke testing of tag removal flows.
- Continue monitoring for any missed edge-case raw button usages in future iterations.
- Address any regressions or accessibility issues if discovered during further manual or automated testing.