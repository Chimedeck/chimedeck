# Changelog

## Update

- Added a direct API-level test for workspace creation subscription policy.
- The test exercises POST /api/v1/workspaces through the workspace router path and verifies the full 402 response envelope when an owner already has a free workspace.

## New

- New integration test file:
  - tests/integration/workspaceCreateSubscriptionPolicy.test.ts

## Technical Debt

- Current integration test uses module mocks for auth/db/env; a future full-stack integration using a seeded database would provide stronger end-to-end confidence.

## What Should Be Done Next

- Add a companion API-level test for the allowed path (all owned workspaces paid) that expects 201.
- Add client-side handling for workspace creation 402 (`workspace-creation-blocked-by-free-owned-workspace`) to show billing CTA.
