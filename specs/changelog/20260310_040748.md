# Changelog — 20260310_040748

## Update
- `server/extensions/board/api/get.ts` — refactored `handleGetBoard` to use `applyBoardVisibility` instead of the manual authenticate → load board → requireWorkspaceMembership chain; role check now skipped for PUBLIC boards
- `server/extensions/board/api/members.ts` — same refactor to use visibility middleware
- `server/extensions/board/api/archived-cards.ts` — same refactor
- `server/extensions/board/api/labels.ts` — GET handler refactored; POST handler unchanged (still requires auth)
- `server/extensions/activity/api/boardActivity.ts` — refactored to use visibility middleware
- `server/extensions/board/api/comments.ts` — refactored to use visibility middleware
- `src/extensions/Board/api.ts` — added `BoardVisibility` type, `visibility` field to `Board` interface, and `patchBoardVisibility` function
- `src/extensions/Board/containers/BoardSettings/BoardSettings.tsx` — integrated `VisibilitySelector`; reads current visibility from Redux store, PATCHes on change with optimistic update and rollback on failure

## New
- `server/middlewares/boardVisibility.ts` — middleware `applyBoardVisibility()` that loads the board, allows unauthenticated access for PUBLIC boards, and enforces authentication + workspace membership for WORKSPACE/PRIVATE boards; exports `BoardVisibilityScopedRequest` type
- `src/extensions/Board/containers/BoardSettings/VisibilitySelector.tsx` — radio group component for selecting board visibility (PRIVATE / WORKSPACE / PUBLIC)
- `tests/integration/board/boardVisibility.test.ts` — bun:test unit/integration tests covering 404 for non-existent boards and auth guard behaviour
- `tests/e2e/board-visibility.spec.ts` — Playwright E2E tests verifying: PRIVATE board rejects unauthenticated requests (401), PRIVATE board rejects non-members (403), PUBLIC board accessible without auth (200), PATCH visibility round-trip

## Technical Debt
- The frontend `Board` interface in `src/extensions/Board/api.ts` still uses `workspaceId` (camelCase) while the server returns `workspace_id` (snake_case) — pre-existing inconsistency, not introduced here
- Board settings visibility section could be disabled for VIEWER/GUEST callers in the future when the client knows the caller's role

## What Should Be Done Next
- Iteration 3: Sprint 50 — Error Envelope Standardisation (update all 4xx/5xx to `{ error: { code, message } }` shape)
- Add role-aware disabling of the visibility selector (disabled for VIEWER and GUEST roles) once the client reliably receives `callerRole` from the API
