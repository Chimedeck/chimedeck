## Update
- Integrated Trello card endpoints and serialization into the Trello compat API: `/trello/1/cards/*` now supports CRUD, sub-resource includes, actions, member/label assignment, and checklist endpoints.
- Updated board and list serializers to reuse the new card serializer for card includes.
- Extended Trello types, error helpers, and auth middleware for card-specific logic and stricter user extraction.

## New
- Added `server/extensions/trelloCompat/api/cards/index.ts` with full Trello card routing and sub-resources.
- Added serializers: `card.ts`, `action.ts`, `checklist.ts` under `server/extensions/trelloCompat/serializers/`.
- Added `server/extensions/trelloCompat/api/lists/index.ts` for list card sub-resource routing.
- Added card-focused test suites: `cards.test.ts`, `actions.test.ts`, `members_labels.test.ts` under `api/cards/__tests__/`.

## Technical Debt
- Board actions endpoint remains a placeholder.
- Richer member role handling and some auth tests are blocked by missing/invalid test credentials.
- Playwright UI flows for cards are blocked by failed login with provided test credentials.

## What Should Be Done Next
- Fix test credential login to unblock Playwright UI and MCP card flow tests.
- Implement richer member role handling and complete deferred auth tests.
- Expand board actions endpoint beyond placeholder.
- Continue coverage for edge cases: card moves, badge mapping, and error handling.