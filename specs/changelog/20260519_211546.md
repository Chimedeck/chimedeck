## Update
- Refined State Transitions UI logic for error popup and allowed-next-state rendering.
- Hardened server API handlers (GET, PUT, COPY) for consistent error envelopes and fallback repair on malformed graphs.
- Improved enforcement logic and error class handling for safer, deduplicated state checks.
- Updated Playwright config to auto-discover both `*.spec.*` and `*.test.*` files in e2e tests.

## New
- Added Playwright/Jest specs:
  - `tests/stateTransitions/edgeCases.spec.ts` (invalid graphs, race conditions)
  - `tests/stateTransitions/permissions.spec.ts` (admin/owner, forbidden actions)
  - `tests/stateTransitions/uiFlows.spec.ts` (error popups, enforcement indicators)

## Technical Debt
- Playwright E2E flows could not be validated due to login failure with test credentials.
- Deferred: Audit for legacy endpoints exposing raw S3 URLs, registry/extensibility, plugin auth, and some admin logic improvements.

## What Should Be Done Next
- Fix login for test users to enable Playwright E2E validation.
- Complete E2E/UI test runs for State Transitions flows.
- Address deferred technical debt and expand edge case/permission test coverage as needed.