# Changelog — 2026-03-23T13:00:00Z

## Sprint 100 — Wire BoardNotificationTypePreferences into BoardSettings

### Update
- `src/extensions/Board/containers/BoardSettings/BoardSettings.tsx` — Imported and mounted `BoardNotificationTypePreferences` below `BoardNotificationToggle` inside the "User settings" section, following the JSX snippet from sprint-100.md §5.

### New
- No new files in this iteration (component, slice, translations, and store registration were all completed in Iteration 6).

### Technical Debt
- None introduced.

### What Should Be Done Next
- All Sprint 99 and Sprint 100 acceptance criteria are now fully implemented across all 7 iterations. End-to-end testing via Playwright MCP confirms the board settings panel shows the per-type toggle matrix, toggles fire PATCH requests, board overrides show indigo rings, and the reset flow clears all overrides.
