## Update
- InviteExternalUserModal: Added "Mark email as verified" checkbox (default: checked), hidden when EMAIL_VERIFICATION_ENABLED is false. Checkbox value sent as autoVerifyEmail in API request.
- CredentialSheet: Displays email verification status ("Email verified" or "Email not verified") based on email_verified_at from API response.
- types.ts: Updated AdminCreateUserRequest and AdminCreateUserResponse types to support autoVerifyEmail and email_verified_at.
- adminInvite.slice.ts: State now tracks emailVerifiedAt; setInviteCredentials persists it.
- featureFlagsSlice.ts: Added emailVerificationEnabled flag and selector.
- server/index.ts: Exposes emailVerificationEnabled in /api/v1/flags.

## New
- specs/tests/admin-auto-verify-email-ui.md: Playwright MCP test spec for admin invite auto-verify email UI and credential sheet status.

## Technical Debt
- Playwright test not recognized: .md spec not picked up by Playwright, likely due to config or file extension. Needs adjustment to run.
- Pre-existing type errors in featureFlagsSlice.ts remain (unrelated to this iteration).

## What Should Be Done Next
- Adjust Playwright config or convert test to .ts/.js to ensure admin-auto-verify-email-ui test runs.
- Investigate and resolve Playwright test discovery for .md specs.
- Continue with deferred sprints/features as planned.
