# Changelog — 20260330_044711

## Update
- `server/extensions/auth/api/login.ts`: Added `await` to `buildVerificationEmail` call (now async).
- `server/extensions/auth/api/register.ts`: Added `await` to `buildVerificationEmail` call.
- `server/extensions/auth/api/resendVerification.ts`: Added `await` to `buildVerificationEmail` call.
- `server/extensions/email/templates/verificationEmail.ts`: Removed inline HTML template literal; now calls `await renderTemplate(...)`. Function is now `async`.

## New
- `server/extensions/email/templates/html/verification.html`: Full HTML email document using `{{verificationUrl}}` Handlebars placeholder (no TypeScript template literals).

## Technical Debt
- Remaining email templates (`adminInvite`, `emailChangeConfirmation`, `passwordResetEmail`) still use inline HTML template literals — to be migrated in subsequent iterations.

## What Should Be Done Next
- Iteration 3: Extract `adminInvite.ts` inline HTML into `html/adminInvite.html` and migrate to `renderTemplate`.
- Iteration 4: Migrate remaining templates and update all call sites; add `server/extensions/email/templates/README.md`.
