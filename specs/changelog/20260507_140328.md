## Update
- Fixed hyperlink toolbar edit behavior in the shared Tiptap link popover so updating an existing link can also update its display text.
- Link popover now pre-fills the display-text field from the active link text when cursor is inside a link, not only when a range is manually selected.

## New
- No new API or schema changes.

## Technical Debt
- Link-edit behavior remains centralized in a UI popover command path; dedicated editor command helpers could improve reuse and testability.

## What Should Be Done Next
- Add a UI test covering: select existing link -> change display text + URL -> save -> persisted markdown reflects new label and href.
- Add a second test for cursor-inside-link (no selection) edit flow to ensure link text replacement works reliably.
