## Update
- Updated `server/extensions/mcp/README.md` to add a comprehensive "Remote HTTP Transport" section.
- Documented the `/api/mcp` endpoint, session lifecycle, authentication via `Authorization: Bearer hf_…`, and error response conventions.
- Added detailed `curl` usage examples for session initialization, tool calls, SSE streaming, and session termination.
- Included a comparison table of stdio vs HTTP transport (auth, session, install, use-cases).

## New
- Introduced clear documentation for the Remote HTTP transport, including endpoint usage, authentication, session management, and error handling.
- Provided example requests and a feature comparison table for users transitioning from stdio to HTTP transport.

## Technical Debt
- None

## What Should Be Done Next
- Expand documentation with advanced usage and troubleshooting guidance.
- Implement and document test infrastructure for HTTP transport (e.g., Playwright config).
- Address deferred Playwright test and theme audit issues from previous iterations.
- Update documentation as HTTP transport features evolve or change.
