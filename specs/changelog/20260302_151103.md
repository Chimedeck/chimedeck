## Update
- Refactored Redux store to use extension-based reducers and injected `apiClient` as thunk extra argument.
- Updated routing to use extension route registration and lazy loading.
- Fixed circular dependencies in API client/auth logic using a lazy token getter pattern.
- Updated selectors and actions to use new `authDuck` slice and `clearAuth` action throughout the app.
- Updated Vite config to use client tsconfig and `/api` proxy.

## New
- Added Vite 5 + React + Tailwind build pipeline (`vite.config.ts`, `tailwind.config.ts`, `postcss.config.js`, `index.html`, `tsconfig.client.json`).
- Installed frontend dependencies: react, react-dom, react-router-dom, react-redux, axios, clsx, tailwind-merge, class-variance-authority, @radix-ui/*, lucide-react.
- Created `src/extensions/Auth/` extension: routes, reducers, duck, PrivateRoute, stub Login/Signup containers.
- Created `src/extensions/common/api/` with API client and barrel exports.
- Implemented `src/common/api/client.ts` (typed Axios client, Bearer/401 handling, no localStorage), `interceptors.ts` (401 logout/refresh).
- Added `src/routing/` with PrivateRoute/PublicRoute and extension route registration.
- Added shared infra: typed hooks, `cn` util, base components (Button, Input, Spinner, ErrorBoundary), layout stubs.
- Added 4 new Playwright UI routing tests and 1 manual test spec (`specs/tests/ui-foundation.md`).

## Technical Debt
- No E2E Playwright coverage for all user flows yet (only routing tested).
- Auth UI is stub only; full login/signup flows deferred to Sprint 16.
- Redis adapter and activity log backfill tests still missing (server-side).
- Some extension registration patterns may need further isolation as more features are added.

## What Should Be Done Next
- Implement full Auth UI (login/signup forms, error handling, integration with API).
- Add Playwright E2E tests for all required user flows (login, protected routes, logout, etc.).
- Add tests for Redis adapter and activity log backfill (server).
- Continue migrating legacy code to extension-based structure and improve test coverage.
- Review extension registration and isolation patterns as new features are added.