## Update
- Integrated boardMembersApi slice and middleware into Redux store (src/reducers.ts, src/store/index.ts).
- Updated BoardSettings panel to conditionally render VisibilitySelector for admins/owners only.

## New
- Added src/extensions/Board/components/BoardSettings/VisibilitySelector.tsx: radio-group for board visibility (PRIVATE/WORKSPACE/PUBLIC).
- Added src/extensions/Board/components/BoardSettings/index.tsx: role-aware settings panel.
- Added src/extensions/Board/slices/boardMembersSlice.ts: RTK Query slice for board member CRUD.
- Added specs/tests/sprint-79-iteration-4-visibility-selector.md: Playwright MCP test for BoardSettings flows.

## Technical Debt
- Playwright tests are markdown, not executable.
- Guest/board member panel deferred to next iteration.

## What Should Be Done Next
- Implement board member panel UI and guest invite flows.
- Convert Playwright tests to runnable format.
- Complete workspace board filtering for guests.
- Address TypeScript errors in unrelated files.