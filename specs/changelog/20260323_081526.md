# Changelog — 2026-03-23T08:15:26Z

## Update
- `server/extensions/mcp/index.ts`: Registered `create_card` and `edit_card_description` tools; updated TODO comment to reflect remaining tools.

## New
- `server/extensions/mcp/tools/createCard.ts`: `create_card` MCP tool wrapping `POST /api/v1/lists/:listId/cards`. Accepts `listId`, `title`, and optional `description`. Returns created card data or structured error.
- `server/extensions/mcp/tools/editDescription.ts`: `edit_card_description` MCP tool wrapping `PATCH /api/v1/cards/:cardId/description`. Accepts `cardId` and `description`. Returns updated card data or structured error.
- `server/extensions/mcp/tests/createCard.md`: Playwright MCP test spec for T3 (create_card).
- `server/extensions/mcp/tests/editDescription.md`: Playwright MCP test spec for T4 (edit_card_description).

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 4: Implement `set_card_price` and `invite_to_board` tools.
