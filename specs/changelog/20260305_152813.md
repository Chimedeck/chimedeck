## Update
- Updated the `plugin_data` table schema: added a nullable `board_id` foreign key column, dropped the old unique constraint, and replaced it with a new unique constraint that includes `board_id`.
- Added an index on `board_id` for efficient lookups.

## New
- Created migration file `db/migrations/0022_plugin_data_board_id.ts` to implement the above schema changes.

## Technical Debt
- None

## What Should Be Done Next
- Implement server-side validation to ensure plugin data is always scoped to the correct board.
- Audit all plugin-data endpoints for correct board/resource validation.
- Plan and implement E2E tests for plugin flows once Playwright/MCP integration issues are resolved.
- Review and update existing plugins to handle the new `board_id` scoping as needed.