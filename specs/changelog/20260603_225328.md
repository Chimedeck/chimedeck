## Update
- Stabilized `SpecsWorkspacePage` tests by replacing timer-sleep sequencing with deterministic async assertions for autosave, conflict recovery, and commit flows.
- Hardened shared test cleanup in `tests/setup.ts` with DOM cleanup, real-timer reset, and mock restoration between cases.

## New
- None.

## Technical Debt
- `BoardPage.documentation.test.tsx` still has an existing mock gap around `HEALTH_CHECK_POLL_INTERVAL_MS` that keeps the combined docs suite from going green.

## What Should Be Done Next
- Fix the health-check config mock in the board documentation test suite.
- Resume the remaining board-chat persistence and assistant pipeline work once the docs workspace tests are fully stable.
