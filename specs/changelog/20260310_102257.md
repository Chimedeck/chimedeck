## Update
- Updated `list.ts` to include `isStarred` boolean per board for the authenticated user (LEFT JOIN on `board_stars`).
- Updated `index.ts` (board router) to register new star/follow endpoints.
- Extended `types.ts` with `BoardStar`, `BoardFollower`, and `BoardWithIsStarred` types.

## New
- Migration `0029_board_stars_followers.ts`: Adds `board_stars` and `board_followers` tables with composite PK (`user_id`, `board_id`) and CASCADE deletes.
- `star.ts`: Implements idempotent `POST/DELETE /api/v1/boards/:id/star` endpoints.
- `follow.ts`: Implements idempotent `POST/DELETE /api/v1/boards/:id/follow` endpoints.
- `me-starred-boards.ts`: Implements `GET /api/v1/me/starred-boards` endpoint.
- Integration test: `starFollow.test.ts` (8/8 passing) for all new endpoints and auth guards.

## Technical Debt
- Playwright E2E tests for new board star/follow flows are not yet implemented.
- Some uuidv4 usages remain in seeds and outlier files.

## What Should Be Done Next
- Implement Playwright E2E tests for board star/follow endpoints and user flows.
- Complete uuidv7 migration in seeds and outliers.
- Implement board-level views API and UI (activity, comments, archived cards, starred boards UI).
- Add/enable automated tests for plugin context flow and unwired code paths.