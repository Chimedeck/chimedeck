## Update
- Wired the Board Chat drawer into `BoardPage` so the header action opens and closes the right-side panel.
- Added drawer lifecycle handling for Escape, backdrop clicks, and board navigation changes.
- Kept the current history contract isolated behind a hook so the UI can evolve without changing the drawer API.

## New
- Added `BoardChatDrawer.tsx` with a slide-in panel shell, scrollable history area, and sticky composer footer.
- Added `useBoardChatHistory.ts` as the history-state hook for loading, empty, error, and loaded cases.
- Added `BoardChatDrawer.test.tsx` and expanded board page chat coverage for open/close behavior.

## Technical Debt
- The history hook is still stubbed and will need the real backend data source in the next iteration.
- Composer send handling remains non-functional until the message API lands.

## What Should Be Done Next
- Wire the chat history endpoint and real message payloads.
- Add send-message persistence and permission checks for board chat access.
- Replace the stubbed history states with live loading and paging behavior.
