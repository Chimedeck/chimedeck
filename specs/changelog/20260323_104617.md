## Update
- Extended `NotificationType` union in `src/extensions/Notification/api/index.ts` to include `card_updated`, `card_deleted`, and `card_archived`.
- Updated `NOTIFICATION_TYPES` and `NOTIFICATION_TYPE_LABELS` in `src/extensions/Notifications/NotificationPreferences/types.ts` to support 9 notification types with appropriate labels.
- Enhanced `buildCopy` and icon/color mappings in `src/extensions/Notification/components/NotificationItem.tsx` for the new notification types.
- Refined `mapActivityToNotification.ts` to handle new activity actions and payload fields for card update, delete, and archive events.

## New
- Added support for `card_updated`, `card_deleted`, and `card_archived` notification types across client and server logic.
- Introduced icon and color mappings for the new notification types in the notification UI.
- Implemented copy builders for the new notification types, including field change summaries and archive state.

## Technical Debt
- No Playwright UI tests exist for notification flows; only integration tests are present.
- Notification UI and user flows are not currently covered by automated end-to-end tests.

## What Should Be Done Next
- Create Playwright UI tests in `specs/tests/` to cover notification flows: card create/update/delete/archive, notification rendering, preferences, and navigation.
- Verify email notification flows and links in manual QA.
- Review and address any edge cases for notification self-exclusion and icon/color consistency.
- Continue with advanced notification filtering and unread badge logic if not yet implemented.