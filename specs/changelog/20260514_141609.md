## Update
- Applied comment-surface parity for recent link fixes from card description editor.
- Comment editor now preserves and restores explicit link mode metadata across save/reopen/edit cycles.
- Comment markdown save path now unwraps malformed nested markdown link destinations before persistence.
- Comment view rendering now normalizes anchor href values so native browser "Open link in new tab" opens the raw URL (not wrapped markdown tokens).

## New
- Added title-based mode metadata persistence in comment editor link marks (`cd-mode:url|button|card`) and mode detection fallback from title.
- Added wrapped-destination normalization function in comment markdown serialization.
- Added rendered-comment href sanitization pass in `normalizeRenderedLinkHtml`.

## Technical Debt
- Link mode/destination normalization logic now exists in both card and comment surfaces and should be extracted to shared utilities to avoid drift.

## What Should Be Done Next
- Add regression tests for comment flow mirroring card flow:
  - mode-1 + custom display text survives save/view/re-edit
  - malformed wrapped destination in markdown is normalized to plain URL
  - right-click open-in-new-tab in comment view opens clean URL
