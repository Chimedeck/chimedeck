## Update
- Updated `server/extensions/stateTransitions/api/index.ts` to enforce `STATE_TRANSITIONS_ENABLED` at router level so `/api/v1/boards/:boardId/state-transitions` returns `501` immediately when disabled.
- Updated `server/extensions/stateTransitions/api/put.ts` permission flow to allow board-level ADMIN users (in addition to workspace ADMIN/OWNER), while keeping existing workspace membership and writable-board checks.
- Hardened `put.ts` persistence flow with safer typed handling of insert/update return rows and explicit failure handling for unexpected empty updates.

## New
- Added API tests:
  - `tests/server/extensions/stateTransitions/api/get.test.ts`
  - `tests/server/extensions/stateTransitions/api/put.test.ts`
- Test coverage now verifies:
  - GET lazy default graph creation and existing graph retrieval
  - PUT valid graph persistence
  - PUT malformed graph rejection (`state-transition-graph-invalid`)
  - PUT unknown-list rejection (`state-transition-node-unknown-list`)
  - feature-flag-off behavior (`501`) for both handlers

## Technical Debt
- Global repo typecheck still fails on unrelated pre-existing TypeScript issues outside the stateTransitions module.
- ESLint project config currently does not include `tests/server/**` in `tsconfig.json`, so direct linting of new server test files reports parser-project errors.

## What Should Be Done Next
- Continue Iteration 3 scope: implement `/rules`, websocket update broadcast, and list rename/delete sync hooks.
- Decide whether to include `tests/server/**/*` in TypeScript project/lint configuration to remove parser-project lint gaps for server-side tests.
