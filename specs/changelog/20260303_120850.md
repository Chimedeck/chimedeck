## Update
- Updated `register.ts` to generate/store verification tokens and send verification emails when the feature flag is enabled.
- Updated `login.ts` to block unverified users and return a 403 `email-not-verified` error.
- Updated `authDuck.ts` to support `pending-verification` status and `pendingEmail` state.
- Updated `auth.ts` to add `verifyEmail` and `resendVerification` API methods.
- Updated `SignupPage.tsx` to show the verification pending screen when required.
- Updated `auth/api/index.ts` to add new verification routes.
- Updated `server/config/env.ts`, `server/mods/flags/defaults.ts`, and `.env.example` to include SES and email verification flags/vars.
- Updated `routing/index.tsx` to add `/verify-email` route.
- Updated `store/index.ts` to register the `verifyEmail` reducer.

## New
- Added `db/migrations/0014_email_verification.ts` to add `email_verified`, `verification_token`, and `verification_token_expires_at` columns to `users`.
- Added `server/extensions/email/` module with SES and console modes, config, and verification email template.
- Added `verifyEmail.ts` and `resendVerification.ts` endpoints for email verification flows.
- Added `requireVerified.ts` middleware to enforce email verification on private routes.
- Added `VerifyEmailPage.tsx` and `VerifyEmailPage.duck.ts` for handling verification links.
- Added `VerificationPending.tsx` and `ResendVerificationButton.tsx` components for the client.
- Added `specs/tests/email-verification.md` and `specs/tests/email-verification-flag.md` for E2E and unit test coverage.

## Technical Debt
- Playwright E2E tests for email verification are not implemented as `.spec.ts` files; only `.md` spec files exist, which are not directly runnable by Playwright.
- Some pre-existing TypeScript errors remain unrelated to this iteration.

## What Should Be Done Next
- Implement Playwright `.spec.ts` files for email verification flows to enable automated E2E testing.
- Confirm and enforce single-use, 24h expiry for verification tokens per requirements.
- Review and harden rate limiting for resend endpoint.
- Audit all private routes to ensure `requireVerified` middleware is correctly applied.
- Confirm if explicit nickname support is required and implement if needed.