## Update
- Tightened `SpecsWorkspacePage` reducer transitions so `pendingCommitPaths` now stays in sync with dirty edits: a path is removed from commit-ready state while unsaved changes exist, restored when content returns to the last saved value, and only committed paths are cleared on commit success.
- Added explicit save precondition handling for existing specs markdown files: missing `If-Match` now returns a 412 instead of allowing blind overwrite.

## New
- Added reducer-level regression coverage for:
  - edit-to-unsaved removal from pending commit state,
  - restoring pending state when content returns to saved,
  - partial `commit/success` clearing behavior.
- Added API coverage for `PUT /github/specs/file` when `If-Match` is missing on an existing file.
- Extended workspace component test to assert autosave-to-commit handoff by checking commit button disable/enable around save completion.

## Technical Debt
- Repository-wide lint/typecheck baselines still fail due existing unrelated issues outside this iteration scope (CLI ESLint project mapping and existing TypeScript errors).

## What Should Be Done Next
- Stabilize the global lint/typecheck baseline so full-suite validation can be enforced per iteration without known unrelated blockers.
