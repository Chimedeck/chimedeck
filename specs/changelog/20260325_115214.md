## Update
- Enhanced `AttachmentItem.tsx` to support inline renaming with an Edit button, input field, and validation.
- Updated `AttachmentPanel.tsx` to wire `onRename` handler, implement optimistic alias updates, and handle rollback on server error.
- Fixed usage of `attachment.url` to `attachment.view_url` in affected components.
- Improved translation keys in `en.json` for edit/rename actions.

## New
- Added Playwright MCP markdown test: `tests/e2e/sprint-117-attachment-edit.md` covering all inline rename scenarios.
- Introduced translation keys: `attachment.edit`, `attachment.item.action.edit.ariaLabel`, `attachment.rename.placeholder`, `attachment.rename.cancel`.

## Technical Debt
- Playwright does not recognize `.md` test files; tests are not executed until converted to `.ts` or supported format.
- Pre-existing TS errors in unrelated files (`AttachmentThumbnail.tsx`, `attachmentMarkdown.ts`).

## What Should Be Done Next
- Convert the markdown test file to a supported Playwright test format and re-run tests.
- Begin implementation of AttachmentItem Comment action and ActivityFeed integration (Iteration 4).
- Update Playwright config to support .md tests if desired.
- Address pre-existing TypeScript errors in unrelated files.