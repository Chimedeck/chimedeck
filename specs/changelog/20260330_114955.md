## Update
- Refactored `server/extensions/email/templates/verificationEmail.ts` to remove inline HTML and use the new async `renderTemplate` helper with Handlebars.
- Updated all call sites in `server/extensions/auth/api/login.ts`, `register.ts`, and `resendVerification.ts` to `await` the now-async `buildVerificationEmail`.

## New
- Added `server/extensions/email/templates/html/verification.html`: standalone Handlebars HTML template for verification emails using `{{verificationUrl}}`.

## Technical Debt
- No UI or feedback is shown to users after registration or failed login regarding email verification status.
- No resend verification link/button is present in the UI.
- Email sending and verification flows are not fully testable due to missing UI hooks.

## What Should Be Done Next
- Implement user-facing UI for verification prompts after registration and failed login.
- Add resend verification link/button to login and signup flows.
- Ensure verification emails are sent and links are accessible for manual and automated testing.
- Update tests to cover the full verification flow, including UI feedback and email delivery.