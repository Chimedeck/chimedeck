# Changelog — 2026-03-18T10:03:04Z

## Update
- Refactored `src/layout/AppShell.tsx` to support mobile off-canvas sidebar with overlay, focus trap, Escape key handling, and ARIA accessibility attributes.
- Updated `src/layout/Sidebar.tsx` to accept an `onClose` prop for mobile mode, hide collapse toggle, and ensure navigation closes the drawer on mobile.
- Expanded `specs/tests/sidebar-collapsible-drawer.md` with new scenarios and acceptance criteria for mobile drawer, accessibility, and keyboard flows.

## New
- Implemented mobile sidebar drawer with animated slide-in, backdrop, and focus trap.
- Added ARIA roles and attributes for accessibility compliance in mobile drawer mode.
- Added Playwright test scenarios for mobile drawer open/close, focus trap, Escape, and ARIA.

## Technical Debt
- Playwright tests cannot be executed due to a Bun/Playwright ESM loader compatibility issue; no user flows are currently testable.
- TypeScript errors exist in unrelated files (pre-existing).

## What Should Be Done Next
- Investigate and resolve Playwright/Bun compatibility so tests can run and user flows can be validated.
- Proceed to responsive hardening and regression safety (Iteration 6).
- Address any pre-existing TypeScript errors unrelated to this iteration.