## Update
None

## New
- Added `terraform/modules/security-groups/` module:
  - `main.tf`: Defines four security groups (ALB, App, DB, Redis) with correct ingress/egress rules per sprint-125.
  - `variables.tf`: Inputs for `name_prefix`, `vpc_id`, and `tags`.
  - `outputs.tf`: Exposes `alb_sg_id`, `app_sg_id`, `db_sg_id`, `redis_sg_id`.
  - `README.md`: Module usage and details.
- Added `terraform/modules/rds-postgres/` module:
  - `main.tf`: DB subnet group, Postgres 16 parameter group (with `pg_cron`), and `aws_db_instance` (gp3, encrypted, auto-scaling).
  - `variables.tf`: All required variables per sprint-125.
  - `outputs.tf`: Exposes `endpoint`, `db_name`, `db_username`, `instance_id`.
  - `README.md`: Includes sharding pattern documentation.

## Technical Debt
None

## What Should Be Done Next
- Integrate new modules into main Terraform stack and test with real AWS resources.
- Add automated tests for security group and RDS configuration.
- Document example usage in main project README.
- Review for additional parameterization or module reusability needs.