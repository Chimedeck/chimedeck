# 20260422_101500

## Update

- Upgraded the list actions menu to support richer list management aligned with the Trello-style flow.
- Replaced the old "Move list to top/bottom" behavior with a position picker submenu that lets users place a list at a specific index.
- Switched list color handling from browser-local storage to server persistence so all board members see the same color state.

## New

- Added migration `db/migrations/0111_list_color.ts` to introduce nullable `lists.color`.
- Added list color update endpoint `PATCH /api/v1/lists/:id/color` via `server/extensions/list/api/color.ts` and router wiring in `server/extensions/list/api/index.ts`.
- Added client API helper `updateListColor` in `src/extensions/List/api.ts`.
- Updated board/list UI wiring so list colors are read from canonical list data and update optimistically with server fallback.
- Added list position picker submenu in `src/extensions/List/components/ListHeader.tsx` and index-based move handling through BoardPage + BoardCanvas.

## Technical Debt

- Existing Sonar/lint complexity warnings in `src/extensions/Board/components/BoardCanvas.tsx` and nested ternary warnings in `src/extensions/Board/containers/BoardPage/BoardPage.tsx` remain unchanged and should be refactored separately.
- Bulk list operations currently execute per-card network calls sequentially (`move all cards`, `archive all cards`); batching endpoints would improve large-list performance.

## What Should Be Done Next

- Run migrations in all environments so `lists.color` exists before using color changes.
- Add backend integration tests for `PATCH /lists/:id/color` validation and authorization.
- Add UI tests for the move-list position picker and shared list color behavior across two sessions.
- Consider adding dedicated server endpoints for bulk move/archive list operations to reduce request volume on large boards.
