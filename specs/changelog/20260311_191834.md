## Update
- Registered two new triggers (`board.member_added`, `list.card_added`) in the automation engine and updated the registry count to 15.
- Updated `api/create.ts` and `api/update.ts` to validate trigger type/config against the registry using `validateTrigger`.
- Updated API router to include `/api/v1/automation/trigger-types` endpoint.
- Expanded integration tests to cover new triggers and registry count.

## New
- Added `server/extensions/automation/engine/triggers/board/memberAdded.ts` for `board.member_added` trigger.
- Added `server/extensions/automation/engine/triggers/list/cardAdded.ts` for `list.card_added` trigger.
- Created `server/extensions/automation/api/triggerTypes.ts` endpoint to list all registered trigger types and their config schemas.

## Technical Debt
- No Playwright UI/E2E tests exist for these flows; only integration tests cover the new triggers and endpoints.
- Registry extensibility and direct engine tests remain minimal; some technical debt persists in automation engine structure.

## What Should Be Done Next
- Implement Playwright or E2E tests for automation trigger flows if UI coverage is required.
- Address technical debt in automation engine (extensibility, direct tests).
- Proceed with backend endpoints, DB migration, and MIME validation for attachments as planned in Sprint 59 Iteration 1.