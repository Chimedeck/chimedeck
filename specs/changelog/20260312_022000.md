# Sprint 65 — Automation: HeaderButton + Panel shell + tabs

## Update

- `src/extensions/Board/components/BoardHeader.tsx`: Added `onOpenAutomation` and `activeAutomationCount` props; renders `AutomationHeaderButton` immediately left of the ··· settings menu.
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx`: Wires up `useAutomationPanel` hook; passes `onOpenAutomation` to `BoardHeader`; renders `AutomationPanel` overlay inside `PluginIframeContainer`.

## New

- `src/extensions/Automation/types.ts`: Domain types — `Automation`, `AutomationTrigger`, `AutomationAction`, `TriggerType`, `ActionType`, `AutomationTab`, `CreateAutomationPayload`, `UpdateAutomationPayload`.
- `src/extensions/Automation/api.ts`: Client API functions — `getAutomations`, `createAutomation`, `updateAutomation`, `deleteAutomation`, `getTriggerTypes`, `getActionTypes` — all using shared `apiClient` (Axios).
- `src/extensions/Automation/hooks/useAutomationPanel.ts`: Hook managing panel `isOpen`, `activeTab`, `openPanel`, `closePanel`, `setActiveTab`.
- `src/extensions/Automation/components/AutomationHeaderButton.tsx`: BoltIcon (solid 20px) button with active-count badge (shows when > 0, caps at 99+).
- `src/extensions/Automation/components/AutomationPanel/index.tsx`: Slide-in right drawer (fixed overlay). Tabs: Rules (active), Buttons / Schedule / Log (show "Coming soon"). Loads automations on open, handles loading/error/empty/list states. Closes on Escape or backdrop click.
- `src/extensions/Automation/components/AutomationPanel/AutomationList.tsx`: List of RULE automations — each row shows name, trigger label, action count, enable/disable toggle (optimistic PATCH), edit button (stub for iter 3 RuleBuilder), and two-click delete.
- `src/extensions/Automation/components/AutomationPanel/AutomationEmptyState.tsx`: Empty state with BoltIcon illustration and "Create rule" CTA.

## Technical Debt

- `AutomationList` `onCreateRule` / `onEditRule` callbacks are stubs (`{/* RuleBuilder in Sprint 65 iter 3 */}`); RuleBuilder component not yet implemented.
- `BoardHeader.activeAutomationCount` is always `0` until a selector over the automation slice is wired up (Sprint 65 iter 3 or later).

## What Should Be Done Next

- Iteration 3: Implement `RuleBuilder` multi-step flow (TriggerPicker, TriggerConfig, ActionList, ActionPicker, ActionConfig, ActionItem, RuleBuilderFooter) + enable/disable + delete, and write Playwright MCP test file.
