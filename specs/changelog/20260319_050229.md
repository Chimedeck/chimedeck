# Sprint 90 · Iteration 2 — Activity Extension i18n

## Update
- `src/extensions/Activity/components/ActivityFeed.tsx`: replaced all hard-coded strings ("Activity", "No activity yet.", "Loading…", "Load more") with `translations['key']` bracket-notation lookups, importing from `../translations/en.json`.
- `src/extensions/Activity/components/ActivityItem.tsx`: replaced the `switch`/template-literal `describeAction` function with a JSON-driven template lookup using a local `interpolate` helper that substitutes `{placeholders}` at runtime. All action description templates are now sourced from the translation file.

## New
- `src/extensions/Activity/translations/en.json`: created with 24 keys covering the section title, empty/loading states, load-more button, all 16 action description templates (with `{actor}`, `{title}`, `{cardTitle}`, `{fromList}`, `{toList}`, `{action}` placeholders), and fallback "unknown" values for list names.
- `specs/tests/sprint-90-iteration-2-activity-i18n.md`: Playwright MCP test covering empty state, activity items, loading indicator, load-more pagination, template interpolation for all action types, and no-regression check.

## Technical Debt
- The `exactOptionalPropertyTypes` TS error on `actorName={actorNames[activity.actor_id]}` in `ActivityFeed.tsx` is pre-existing and unrelated to this iteration.

## What Should Be Done Next
- Iteration 3: Attachment extension i18n — create `src/extensions/Attachment/translations/en.json` and update all Attachment components.
