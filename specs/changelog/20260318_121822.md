# Changelog — 2026-03-18T12:18:22Z

## Update
- Extended `src/extensions/Card/config/activityEventsConfig.ts` to include `card_created`, `card_moved`, `card_member_assigned`, and `card_member_unassigned` in the visible event types.
- Enhanced `src/extensions/Card/config/activityEventLabels.ts` to support dynamic, context-aware message rendering for new card activity events, including self-assignment and member name resolution.
- Updated `src/extensions/Card/containers/CardModal/ActivityFeed.tsx` to pass member context and current user ID to event label logic, enabling accurate, user-friendly activity feed messages.

## New
- Added Playwright MCP UI assertion scenarios to `specs/tests/card-activity-create-move-assign.md` for all new activity event types and edge cases (self-assign, unknown event, member left, etc.).

## Technical Debt
- Playwright UI tests for Markdown-based specs do not execute due to runner syntax errors; no UI or API assertions were run for the new scenarios. TypeScript errors remain in unrelated files. No new errors in changed files.

## What Should Be Done Next
- Enable or port Playwright UI tests for activity feed scenarios to `.ts` files or clarify required runner setup for Markdown specs.
- Complete client-side activity feed rendering for all event types and ensure i18n/accessibility.
- Finalize Playwright test coverage and address deferred automation.
- Begin work on real-time activity updates in the open card modal.
