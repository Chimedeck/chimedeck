# Changelog — 2026-03-23T03:36:00Z

## Update
- Wired `dispatchDirectCardNotification` fire-and-forget calls into all three card mutation endpoints.

## New
- **card_updated**: `server/extensions/card/api/update.ts` — fires after persist with `changedFields` (keys that were actually updated, excluding `updated_at`).
- **card_deleted**: `server/extensions/card/api/delete.ts` — fires before row removal so card title is still readable.
- **card_archived**: `server/extensions/card/api/archive.ts` — fires after persist with the new `archived` boolean.

## Technical Debt
- `resolveCoverImageUrl` input type narrows to `{ id, cover_attachment_id }`, causing pre-existing TS errors for `.amount`, `.currency`, `.title` on the returned value; a proper generic card type would eliminate these casts.

## What Should Be Done Next
- Iteration 6: Client types, copy builders, notification icons for the three new notification types.
