## Update
- Migrated `src/containers/TopbarContainer/TopbarContainer.tsx` to use semantic design tokens:
  - Replaced `bg-gray-900` with `bg-bg-surface`.
  - Replaced `border-white/10` with `border-border`.
  - Replaced `text-white` (app name) with `text-base`.
  - Updated logout button from bare `<button>` to `<Button variant="ghost" size="sm">`.

## New
- Added `specs/tests/sprint-119-containers-light-mode.md` with 4 Playwright MCP test scenarios for topbar light mode, ghost button styling, dark mode regression, and theme persistence.

## Technical Debt
- Playwright could not find any tests in `specs/tests/sprint-119-containers-light-mode.md`. The file may require conversion to a valid Playwright test format.
- `FooterContainer` returns `null` and was not migrated.

## What Should Be Done Next
- Convert the Playwright MCP test spec to a valid `.ts` or `.js` test file and rerun tests.
- Review and migrate any remaining containers as needed.
- Continue with the next iteration per the sprint plan.
