## Update
- Updated `src/routing/index.tsx` to add `/settings/profile` route.
- Updated `src/extensions/Workspace/components/Sidebar.tsx` to display avatar, nickname, and profile settings link.
- Registered profile reducer in `src/store/index.ts` and `src/reducers.ts`.
- Updated `server/extensions/users/api/index.ts` to include new profile and avatar routes.

## New
- Added `db/migrations/0015_user_profile.ts` for `nickname` (UNIQUE) and `avatar` fields on users table.
- Implemented server-side: `server/mods/imageProcessor/index.ts`, `server/extensions/users/api/profile/get.ts`, `server/extensions/users/api/profile/update.ts`, `server/extensions/users/api/avatar/upload.ts`, `server/extensions/users/api/avatar/remove.ts`.
- Created client-side User extension: `src/extensions/User/api/user.ts`, `ProfilePage.duck.ts`, `ProfilePage.tsx`, `AvatarUploader.tsx`, `NicknameField.tsx`, `ProfileForm.tsx`, and translations.
- Added test specs: `specs/tests/profile-avatar.md` and `specs/tests/profile-nickname.md`.

## Technical Debt
- Playwright E2E tests for profile avatar/nickname are not yet implemented (specs only).
- Real-time propagation of profile changes is limited to sidebar; other UI may require further updates.
- Resend email verification rate limit is still in-process only (not Redis-backed).
- `requireVerified` middleware is not yet globally applied to all private routes.

## What Should Be Done Next
- Implement Playwright E2E tests for profile avatar and nickname flows per the new specs.
- Expand real-time profile update propagation to all relevant UI (e.g., boards, mentions).
- Migrate resend email verification rate limit to Redis for scalability.
- Apply `requireVerified` middleware globally to all private/protected routes.