## Update
- Normalized error envelope in `getRules.ts` to `{ name, data }` format for board-not-found.
- Replaced inline state-transition graph sync logic in `list/api/update.ts` and `list/api/delete.ts` with centralized list-sync hooks.
- Added sync-helper typing in `stateTransitions/common/types.ts`.

## New
- Added `server/extensions/stateTransitions/hooks/listSync.ts` for list rename/delete sync logic.
- Created `tests/api/stateTransitions-rules.test.ts` for API-level rules, sync, and WS event coverage.

## Technical Debt
- Repo-wide lint/typecheck failures persist due to unrelated legacy issues.
- Playwright/browser-based tests blocked by login failure (test credentials or backend issue).

## What Should Be Done Next
- Fix login/test credential issue to unblock browser-based tests.
- Proceed with `/copy` endpoint, Kanban DnD enforcement UI, and further real-time/presence hardening.
- Address error envelope consistency in legacy modules and global lint/typecheck failures.