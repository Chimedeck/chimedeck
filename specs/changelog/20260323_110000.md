# Changelog — 2026-03-23T11:00:00Z

## Update
- `server/extensions/notifications/mods/emailDispatch.ts`: Added `card_updated`, `card_deleted`, and `card_archived` cases to `renderTemplate` switch. `changedFields` is parsed from a JSON-encoded string; `archived` is parsed from the string `'false'` (defaults to `true`).
- `server/extensions/notifications/mods/boardActivityDispatch.ts`: Extended `dispatchDirectCardNotification` to build email template data and call `dispatchNotificationEmail` for `card_updated`, `card_deleted`, and `card_archived` payloads. Previously only `card_commented` dispatched email.

## New
- Three new email template render functions are now reachable via the dispatch layer: `renderCardUpdatedEmail`, `renderCardDeletedEmail`, `renderCardArchivedEmail`.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Sprint 100 iteration 3: DB migration `0092_board_notification_type_preferences` for per-board per-type notification preferences.
