## Update
- Extended plugin API types to include `whitelistedDomains` and board plugin config to include `allowedDomains`.
- Modified `PluginDashboardPage` and related duck to support plugin editing via PATCH endpoint and update state management.
- Updated plugin list and card components to support admin-only edit actions and modal invocation.

## New
- Created `src/extensions/Plugins/types.ts` defining `BoardPluginConfig` and extended plugin types.
- Added `EditPluginModal.tsx` for platform admin plugin editing, including tag input for whitelisted domains.
- Implemented `updatePluginThunk` and related reducers/selectors for PATCH `/api/v1/plugins/:pluginId`.

## Technical Debt
- No E2E/Playwright tests for plugin edit flows; UI error handling and toast notifications are minimal.
- Some endpoints may not include `whitelistedDomains` in all responses; legacy plugin compatibility is not fully verified.
- Dev server/API routes for plugin dashboard returned 404 during test verification—entry route or config may need review.

## What Should Be Done Next
- Implement board-level Allowed Domains panel and iframe bridge enforcement.
- Add E2E/Playwright tests for plugin edit flows and error scenarios.
- Improve error handling, toast notifications, and UI polish for plugin editing.
- Review and update API responses for full backward compatibility and field coverage.