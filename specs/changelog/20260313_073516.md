# Changelog — 20260313_073516

## Update
- `server/config/env.ts`: Added `EMAIL_NOTIFICATIONS_ENABLED` env var (boolean, defaults to `false`).
- `server/mods/flags/defaults.ts`: Added `EMAIL_NOTIFICATIONS_ENABLED: false` to feature flag defaults.

## New
- `server/extensions/notifications/mods/emailTemplates/shared.ts`: Base HTML email wrapper (`wrapInBaseTemplate`) with branded header, footer, and unsubscribe link to `/settings/profile#notifications`.
- `server/extensions/notifications/mods/emailTemplates/mention.ts`: `renderMentionEmail` — "{actorName} mentioned you in {cardTitle}".
- `server/extensions/notifications/mods/emailTemplates/cardCreated.ts`: `renderCardCreatedEmail` — "New card was created in {boardName}".
- `server/extensions/notifications/mods/emailTemplates/cardMoved.ts`: `renderCardMovedEmail` — "Card was moved from {fromList} to {toList}".
- `server/extensions/notifications/mods/emailTemplates/cardCommented.ts`: `renderCardCommentedEmail` — "{actorName} commented on {cardTitle}" with inline comment preview.
- `server/extensions/notifications/mods/emailDispatch.ts`: `dispatchNotificationEmail` helper — gates dispatch on `SES_ENABLED` + `EMAIL_NOTIFICATIONS_ENABLED` feature flags and `email_enabled` user preference, then renders the correct template and calls `sendViaSes`. All failures are fire-and-forget (warn-only).

## Technical Debt
- `src/slices/featureFlagsSlice.ts` has pre-existing TS errors where `action.payload.X` should be `action.payload.data.X` — not introduced by this iteration.

## What Should Be Done Next
- Iteration 7: Wire `emailDispatch` into the events pipeline (`boardActivityDispatch.ts`) and into the mention dispatch hook. Write `specs/tests/email-notifications.md` and integration tests.
