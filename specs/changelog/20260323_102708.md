## Update
- Updated database constraints for notification types: dropped and recreated `notifications_type_check` and `notification_preferences_type_check` to include all 9 canonical types (`mention`, `card_created`, `card_moved`, `card_commented`, `card_member_assigned`, `card_member_unassigned`, `card_updated`, `card_deleted`, `card_archived`).
- Migration applied successfully; app loads at `http://localhost:5173` (HTTP 200).

## New
- Added migration: `db/migrations/0091_notification_types_extend2.ts` to extend allowed notification types in DB constraints.

## Technical Debt
None

## What Should Be Done Next
- Update server and client code to use the new notification types and ensure type lists are in sync.
- Add tests to verify all allowed types are handled correctly in both code and DB.
- Review for any code/DB drift or naming mismatches (e.g., `card_updated` vs `card.updated`).
