## Update
- Verified that `playwright.config.ts` already includes both `tests/e2e/` and `specs/tests/` via the `projects` array, ensuring all E2E and MCP tests are discovered.
- Confirmed that Playwright test discovery works as expected (`npx playwright test --list` finds all tests).
- Investigated registration test failures and determined they are due to a pre-existing PostgreSQL connection pool exhaustion ("too many clients"), not related to Playwright config.

## New
- None.

## Technical Debt
None.

## What Should Be Done Next
- Address PostgreSQL connection pool exhaustion to resolve registration test failures.
- Proceed with Routes + Navigation work for ApiTokenPage in the next iteration.
- Complete any deferred client-side polish or additional integration tests for notification preferences as needed.