## Update
- Aligned test specs for state transitions to match current implementation and iteration plan.
- Verified that all planned endpoints, sync logic, and WS broadcast are covered by tests and documentation.

## New
- Added `specs/tests/stateTransitions/rules.md`: covers normalized rules endpoint scenarios, including empty graph, derivation, sync normalization, and feature-flag/error cases.
- Added `specs/tests/stateTransitions/list-sync.md`: documents list rename/delete graph sync expectations, idempotency, and empty-graph edge case.
- Added `specs/tests/stateTransitions/ws-broadcast.md`: details `state_transition_updated` WS broadcast expectations, cache invalidation, and no-event-on-failure.

## Technical Debt
None

## What Should Be Done Next
- Implement `/copy` endpoint and card move enforcement for state transitions.
- Expand edge case tests for concurrent edits and rename collisions.
- Review error envelope consistency across all state transition endpoints.
