## Update
- Normalized Trello actions sub-resource endpoints in `server/extensions/trelloCompat/api/actions/index.ts` to route `/organization`, `/reactions`, and `/reactionsSummary` to new handler modules.
- Updated contract tests and fixtures to validate organization and reaction response shapes.
- Expanded Trello types to include `TrelloReaction` and `TrelloReactionSummary`.
- Updated `specs/trello-adapter-normalization/matrix.md` with current endpoint statuses.

## New
- Added `server/extensions/trelloCompat/api/actions/organization.ts` for GET `/actions/{id}/organization`.
- Added `server/extensions/trelloCompat/api/actions/reactions.ts` for GET/POST `/actions/{id}/reactions` and GET `/actions/{id}/reactions/{id}`.
- Added `server/extensions/trelloCompat/api/actions/reactionsSummary.ts` for GET `/actions/{id}/reactionsSummary`.
- Created `server/extensions/trelloCompat/serializers/reaction.ts` for Trello-compatible reaction serialization.
- Added/updated contract tests and fixtures for new endpoints and shapes.

## Technical Debt
- Browser-based GET endpoints for `/organization`, `/reactions`, `/reactions/{id}`, and `/reactionsSummary` currently return errors (not reachable in Playwright tests).
- `/reactionsSummary` shape may drift from Trello; further validation needed.

## What Should Be Done Next
- Investigate and fix backend errors preventing browser access to new endpoints.
- Ensure all endpoints return valid payloads and pass Playwright UI tests.
- Complete normalization for any remaining partial endpoints and update contract/matrix accordingly.
- Monitor Trello API for changes in reaction summary shape and update serializers/tests as needed.