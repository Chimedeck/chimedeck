# Changelog — 2026-03-13T09:20:00Z

## Update
- `server/extensions/search/api/query.ts`: Added `background` field to board search results SELECT query so clients can render board thumbnails from search results. Type filtering (`type=board|card`) was already implemented.

## New
- `specs/tests/search-type-filter.md`: Playwright MCP test covering (1) `type=board` returns only boards, (2) `type=card` returns only cards, (3) no type returns both, (4) board results include `background` field, (5) invalid type edge case.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 9: UI — CommandPalette scope tabs (All / Boards / Cards), `type` query param wiring, sessionStorage persistence, scoped empty states, placeholder text, keyboard nav.
