## Update
- Expanded test coverage for state transitions GET endpoint to include unauthenticated (401) and board-not-found (404) error cases.
- Confirmed GET/PUT endpoints and validation logic match approved API/requirements spec for Iteration 2.

## New
- Added tests for unauthenticated and board-not-found error cases in `tests/server/extensions/stateTransitions/api/get.test.ts`.

## Technical Debt
- API returns a redirect to `/login` instead of a JSON 401 error for unauthenticated API requests (should return JSON per spec).
- Login form fields are missing or undetectable, blocking automated login and further authenticated API testing.

## What Should Be Done Next
- Fix login form markup to expose `#email` and `#password` fields for automated login.
- Update API unauthenticated error handling to return JSON 401 errors (not redirects) for API endpoints.
- Proceed to Iteration 3: implement rules endpoint, WS broadcast, and list sync hooks.