## Update
None

## New
- Added `db/migrations/0083_user_card_drafts.ts` migration to create the `user_card_drafts` table for offline draft persistence. Includes all required columns, enums (`draft_type`, `intent`), a unique constraint on `(user_id, card_id, draft_type)`, and indexes on `(user_id, updated_at)` and `(card_id)`.
- Migration applied successfully (`Batch 2 run: 1 migrations`).

## Technical Debt
None

## What Should Be Done Next
- Implement server API and client logic for offline draft creation, update, and reconciliation.
- Integrate offline draft support in CardDescriptionTiptap and CommentEditor.
- Enable and run Playwright MCP scenarios for draft flows.
- Review accessibility and edge-case handling for editor toolbars and uploads.