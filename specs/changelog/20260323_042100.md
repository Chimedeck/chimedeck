# Changelog — 2026-03-23T04:21:00Z

## Update
- N/A (new files only)

## New
- `server/extensions/notifications/mods/emailTemplates/cardUpdated.ts` — Email template for `card_updated` notification. Renders subject/html/text with changed-field list and CTA linking to the card.
- `server/extensions/notifications/mods/emailTemplates/cardDeleted.ts` — Email template for `card_deleted` notification. CTA links to `boardUrl` since the card no longer exists.
- `server/extensions/notifications/mods/emailTemplates/cardArchived.ts` — Email template for `card_archived` notification. Handles both `archived=true` ("archived") and `archived=false` ("unarchived") cases.

## Technical Debt
- Templates are not yet wired into `emailDispatch.ts` — deferred to Iteration 2.

## What Should Be Done Next
- Wire the three new templates into `emailDispatch.ts` (Iteration 2).
- Update unsubscribe URL in `shared.ts` to absolute path (Iteration 2).
