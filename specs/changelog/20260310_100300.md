## Update
- Updated `boards` table: added `visibility` (enum: PUBLIC/PRIVATE/WORKSPACE, default PRIVATE), `description` (nullable text), and `background` (nullable varchar) columns via migration 0027.
- Updated `cards` table: added `start_date` (nullable timestamptz) column via migration 0028.
- Updated `Board` interface in `server/extensions/board/types.ts` to include new fields.

## New
- Created `db/migrations/0027_board_extensions.ts` for board schema changes.
- Created `db/migrations/0028_card_start_date.ts` for card schema changes.
- Added `server/extensions/card/types.ts` defining the `Card` interface with `start_date`.

## Technical Debt
- None

## What Should Be Done Next
- Update API and UI to support new board visibility, description, background, and card start date fields.
- Add/expand tests for new board/card properties in API and UI.
- Address pre-existing auth test failure ("Auth store > initialises with unauthenticated status").
- Review downstream code for compatibility with new types and columns.