## Update
- Added a Documentation tab to `BoardPage` and wired it to the board integrations lookup so it only appears when `github_project_url` is available.
- Updated the board tab flow to render the new documentation workspace after Health Check.

## New
- Added `SpecsWorkspacePage` for the documentation workspace shell, manifest loading, file selection, and dirty-state tracking.
- Added `SpecsFileTree` to render the specs manifest as a nested tree with selection and dirty markers.
- Added `SpecsMarkdownEditor` as the TipTap-based markdown editor for spec files.
- Added board and workspace tests covering tab visibility, placement, loading, retry, selection, and dirty-state behavior.

## Technical Debt
- Save-to-commit sync is still deferred to the next iteration.
- React component tests still require a DOM test environment; `bun test` continues to report `document is not defined` for `.test.tsx` files.

## What Should Be Done Next
- Implement the save and commit sync workflow for specs files.
- Add stale-precondition handling and round-trip tests for save/commit.
- Set up a DOM test environment so the new React component tests can run under the local test command.
