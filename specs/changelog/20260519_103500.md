## Update
- Normalized Actions serializer handling in `server/extensions/trelloCompat/serializers/action.ts` so action `data` is always object-safe for Trello contracts while preserving required keys (`id`, `idMemberCreator`, `data`, `type`, `date`, `limits`, `memberCreator`).
- Added generic field projection behavior for `GET /actions/{id}/{field}` in `server/extensions/trelloCompat/api/actions/index.ts` for top-level action fields, with Trello-style invalid-field errors.
- Expanded Actions contract coverage in `tests/unit/server/extensions/trelloCompat/actions.contract.test.ts` to cover GET/PUT/DELETE response shapes and field projection behavior.

## New
- Added a reusable `projectActionField` helper in `server/extensions/trelloCompat/api/actions/index.ts` to keep projection behavior consistent and testable.
- Added an edge-case fixture (`createCommentActionFixtureWithoutList`) in `server/extensions/trelloCompat/helpers/fixtures.ts` to validate missing optional list context.
- Strengthened `assertTrelloShape` action checks in `server/extensions/trelloCompat/helpers/assertTrelloShape.ts` with required type validation for core keys and ISO date validation.

## Technical Debt
- Field projection currently targets top-level action keys only; nested projections (for example `data.text`) are not part of this sprint scope.

## What Should Be Done Next
- In Sprint 150 Phase 2, normalize action sub-resources (`/board`, `/card`, `/list`, `/member`, `/memberCreator`, `/organization`) and reaction payload endpoints against checklist contract details.
