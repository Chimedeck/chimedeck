# Update
- Updated client activity-to-notification copy mapping for member and checklist assignment/removal events to distinguish between current-user targets and other-user targets.

# New
- `mapActivityToNotification` now accepts `currentUserId` and payload fields for assignment target identity (`assigneeName`, `userId`, `previousUserId`).
- Copy now resolves to `you` only when the target matches `currentUserId`; otherwise it renders the assignee name.

# Technical Debt
- If `assigneeName` is not provided in an activity payload for non-current-user events, copy falls back to `a member`.

# What Should Be Done Next
- Ensure all relevant activity payload producers consistently include `assigneeName` for assignment/unassignment/checklist assignment events.
- Add unit tests for copy variants: target=current user, target=other user, target with missing name.
