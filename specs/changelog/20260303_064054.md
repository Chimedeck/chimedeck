# Changelog — 20260303_064054

## Update

- `server/extensions/comment/api/create.ts` — now calls `createNotificationsForMentions` after syncing mentions on new comment
- `server/extensions/comment/api/update.ts` — same on comment edit
- `server/extensions/card/api/update.ts` — same on card description save
- `server/extensions/realtime/api/index.ts` — registers/deregisters user sockets on WS open/close
- `server/index.ts` — added `notificationsRouter`
- `src/layout/AppShell.tsx` — added `NotificationContainer` in desktop + mobile topbars
- `src/reducers.ts` / `src/store/index.ts` — added `notificationReducer`

## New

- `db/migrations/0017_notifications.ts` — `notifications` table with FK cascade to users, cards, boards; index on `(user_id, read, created_at DESC)`
- `server/extensions/notifications/api/list.ts` — `GET /api/v1/notifications` (cursor pagination, `?unread=true`)
- `server/extensions/notifications/api/markRead.ts` — `PATCH /api/v1/notifications/:id/read`
- `server/extensions/notifications/api/markAllRead.ts` — `PATCH /api/v1/notifications/read-all`
- `server/extensions/notifications/api/delete.ts` — `DELETE /api/v1/notifications/:id`
- `server/extensions/notifications/api/index.ts` — notifications router
- `server/extensions/notifications/mods/createNotifications.ts` — inserts notification rows + publishes `notification_created` WS event to each recipient's user socket
- `server/extensions/realtime/userChannel.ts` — `userSockets` registry, `registerUserSocket`, `deregisterUserSocket`, `publishToUser`
- `src/extensions/Notification/api/index.ts` — client API wrappers (list, markRead, markAllRead, deleteOne)
- `src/extensions/Notification/slices/notificationSlice.ts` — Redux slice with thunks (fetch, fetchMore, markRead, markAllRead, delete) and `addNotification` action
- `src/extensions/Notification/hooks/useNotificationSync.ts` — subscribes to WS for `notification_created` events
- `src/extensions/Notification/components/NotificationBell.tsx` — bell icon with red unread badge (capped at 99+)
- `src/extensions/Notification/components/NotificationPanel.tsx` — slide-in panel with header "Mark all read" and cursor-paginated list
- `src/extensions/Notification/components/NotificationItem.tsx` — single notification row with unread dot, click-to-navigate, dismiss button
- `src/extensions/Notification/containers/NotificationContainer.tsx` — mounts bell/panel, fetches on mount, subscribes to WS sync
- `src/extensions/Notification/translations/en.json` — i18n strings
- `specs/tests/notifications-mention.md`, `notifications-realtime.md`, `notifications-mark-read.md` — E2E test specs

## Technical Debt

- `WorkspaceModal.test.tsx` / `WorkspaceShell.test.tsx` partial state mocks accumulate a type error each time a new reducer is added; a shared `buildTestState()` helper should be introduced.
- `notificationApi.list` response is not yet i18n-aware (actor names are raw DB values); future work could add display-name resolution.

## What Should Be Done Next

- Wire `NotificationItem` through the i18n intl system (`useIntl`) to use `Notifications.mention` translation key
- Add Playwright E2E tests from `specs/tests/notifications-mention.md` and `specs/tests/notifications-realtime.md`
- Fix the pre-existing `WorkspaceShell.test.tsx` / `WorkspaceModal.test.tsx` partial-state typing pattern
- Implement global `requireVerified` middleware (deferred from Sprint 26)
- Redis rate limiting (deferred)
