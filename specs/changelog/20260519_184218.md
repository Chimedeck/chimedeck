## Update
- Updated state-transition rules caching to derive from list-synced graph snapshots and expose cached normalized rule payloads for both enforcement and rules API usage.
- Updated `GET /api/v1/boards/:boardId/state-transitions/rules` to source its response from the shared enforcement rules cache layer.
- Updated websocket state-transition broadcast flow to trigger cache invalidation through the `state_transition_updated` event contract.
- Updated Trello card move integration to route state-transition 422 mapping through `server/extensions/trelloCompat/cardMove.ts`.

## New
- Added `invalidateRulesCacheFromStateTransitionEvent(...)` in `server/extensions/stateTransitions/enforcement/rules.ts`.
- Added `server/extensions/trelloCompat/cardMove.ts` helper for Trello-compatible forbidden-move response mapping.
- Added `specs/tests/stateTransitions/enforcement.md` with unit/integration/cache invalidation scenarios.
- Added rules-cache behavior coverage in `server/extensions/stateTransitions/api/__tests__/getRules.test.ts`.

## Technical Debt
- Cache invalidation remains process-local; multi-process deployments still rely on TTL fallback unless broader cross-process invalidation is introduced.

## What Should Be Done Next
- Add an integration test that exercises cache invalidation via the real realtime/pubsub message path (not direct function invocation) to validate runtime wiring end-to-end.
