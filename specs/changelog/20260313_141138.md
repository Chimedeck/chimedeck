## Update
- Extended `server/extensions/notifications/api/index.ts` to mount the new preferences sub-router before existing routes.
- Added `NOTIFICATION_PREFERENCES_ENABLED: true` to `server/mods/flags/defaults.ts`.
- Exposed `NOTIFICATION_PREFERENCES_ENABLED` in `server/config/env.ts` (defaults on; can be disabled via env var).

## New
- Created `api/preferences/get.ts`: `GET /api/v1/notifications/preferences` returns all notification types with current preferences (defaults to enabled if missing).
- Created `api/preferences/update.ts`: `PATCH /api/v1/notifications/preferences` upserts a preference row with strict type/boolean validation.
- Created `api/preferences/index.ts`: router for preferences endpoints, returns 501 if feature flag is off.
- Created `mods/preferenceGuard.ts`: exports `NOTIFICATION_TYPES`, `NotificationType`, and `preferenceGuard` helper with fallback defaults.
- Added `specs/tests/notification-preferences-api.md`: Playwright MCP spec for API (10 scenarios: defaults, upsert, validation, 401, 501, per-user isolation).

## Technical Debt
- Playwright test file was not recognized as a valid test by Playwright; test format or location may need adjustment.

## What Should Be Done Next
- Verify and, if needed, convert `specs/tests/notification-preferences-api.md` to a valid Playwright test file.
- Wire `preferenceGuard` into notification dispatch logic (e.g., mention notifications).
- Implement UI panel and RTK slice for user preferences.
- Add email/in-app dispatch logic respecting preferences.
- Expand Playwright/integration tests for end-to-end flows.