## Update
- Card modal participant loading now merges board members and board guests before rendering assignment-related pickers.
- Checklist item assignment validation now accepts assignee IDs from either board members or board guests for the same board.

## New
- Added `getBoardGuests` API helper in the card detail API layer and integrated it into card modal participant bootstrap.

## Technical Debt
- `board_members` naming in card modal now includes guests for UI compatibility; consider renaming to `boardParticipants` across card detail state and props to improve clarity.

## What Should Be Done Next
- Add focused integration tests for checklist assignment by guest/member combinations.
- Add API tests to cover acceptance/rejection matrix for `assigned_member_id` with member, guest, non-board user, and null.
