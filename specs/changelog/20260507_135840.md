## Update
- Updated notification panel stacking logic so contiguous `comment_reaction` notifications are grouped by `card_id`, the same way discussion notifications are grouped.
- Mixed contiguous discussion entries on the same card (comment + reaction, and mention where applicable) now appear in a single grouped bundle.

## New
- No new APIs or schema changes.

## Technical Debt
- Grouping remains a UI-only heuristic based on contiguous feed order; non-adjacent same-card discussion events are intentionally not merged.

## What Should Be Done Next
- Add a UI unit test for notification grouping that covers `card_commented` + `comment_reaction` contiguous sequences on the same card.
- Verify behavior against real-time inserts to ensure grouping remains stable while new notifications arrive.
