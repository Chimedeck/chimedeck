# Changelog — 2026-03-05T03:21

## Update

- `server/config/env.ts`: Added `PLATFORM_ADMIN_EMAILS` env var (comma-separated, fail-safe default empty).
- `server/extensions/plugins/api/registry/create.ts`: Replaced broken `isRegistryAdmin` (memberships check) with `platformAdminGuard`. Added `connectorUrl` must-be-https validation (422 `invalid-connector-url`). Fixed duplicate slug error name from `duplicate-slug` → `plugin-slug-taken`.
- `server/extensions/plugins/api/registry/update.ts`: Same guard replacement.
- `server/extensions/plugins/api/registry/delete.ts`: Same guard replacement. Fixed response from `{ data: null }` → `{ data: {} }`.
- `.env`: Added `PLATFORM_ADMIN_EMAILS=tam.vu+58ce49cc971aa59ff0ef284c@journeyh.io` for local dev.
- `.env.example`: Added `PLATFORM_ADMIN_EMAILS` and `VITE_PLATFORM_ADMIN_EMAILS` template entries.

## New

- `server/config/platformAdmin.ts`: Reads `PLATFORM_ADMIN_EMAILS` from env, exports `platformAdminEmails` array and `isPlatformAdmin(email)` helper. Fail-safe: empty list denies all.
- `server/middlewares/platformAdminGuard.ts`: Middleware that authenticates the request then checks the caller's email against `platformAdminEmails`. Returns 403 `not-platform-admin` if not in list.

## Technical Debt

- The `index.ts` plugin router still imports handlers via `registry/` subdirectory. The approved plan referenced flat paths (`api/create.ts`, etc.) but the existing structure uses `registry/`. We updated the existing files rather than duplicating to avoid dead code.

## What Should Be Done Next

- Iteration 2: Extend `GET /api/v1/plugins` with `q`/`category`/`isPublic`/`page`/`perPage` query params and add `GET /api/v1/plugins/categories`.
- Soft-deleted plugins (`is_active=false`) should be excluded from GET list results — confirm this is enforced in `registry/list.ts`.
