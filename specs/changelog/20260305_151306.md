## Update
- Modified `src/extensions/Plugins/iframeHost/usePluginBridge.ts` to add explicit handlers for `CTX_CARD`, `CTX_LIST`, `CTX_BOARD`, and `CTX_MEMBER` messages.
- Introduced a `pluginContextRef` to track the last context sent to each plugin and ensure context-read calls resolve with the correct data.
- Added a helper function to extract only requested fields from context objects and updated the message handler logic to use it.

## New
- Plugins can now reliably call `t.card()`, `t.list()`, `t.board()`, and `t.member()` and receive the correct context object from the host, matching the most recent `CAPABILITY_INVOKE` context.

## Technical Debt
- Playwright MCP plugin context flow tests are not configured or present; no automated tests were executed for this feature.
- No e2e or integration tests were added for the new context query logic; only manual verification is possible at this stage.

## What Should Be Done Next
- Update Playwright configuration to include MCP plugin context flow tests and add/enable automated tests for plugin context queries.
- Review and improve error handling for cases where a plugin requests context before any context has been set.
- Audit context object shapes to ensure no sensitive or extraneous data is exposed to plugins.
- Continue addressing deferred technical debt around registry caching, SDK versioning, and plugin authentication.