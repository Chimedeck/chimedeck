## Update
- Updated `server/config/env.ts` to add `ADMIN_EMAIL_DOMAINS` and `ADMIN_INVITE_EMAIL_ENABLED` with proper typing and documentation.
- Registered the new admin user creation endpoint in `server/extensions/auth/api/index.ts`.

## New
- Added `server/extensions/auth/common/isAdminEmailDomain.ts`: helper to check if an email is from an admin domain.
- Added `server/extensions/auth/common/generatePassword.ts`: secure random password generator.
- Implemented `server/extensions/auth/api/adminCreateUser.ts`: `POST /api/v1/admin/users` endpoint with admin-only guard, password generation, email invite, and error handling.
- Added `server/extensions/email/templates/adminInvite.ts`: admin invite email template (plain and HTML).
- Created `tests/integration/auth/adminCreateUser.test.ts`: 23 integration tests for all scenarios (admin/non-admin, password, email, errors).

## Technical Debt
- Deferred Playwright/E2E tests for admin/invite flows (no Playwright config or tests present).
- No sample-project/ reference for admin/invite flows; implementation based on architecture specs and existing patterns.
- Password reset UI edge cases and AWS credential refactoring remain outstanding from previous sprints.

## What Should Be Done Next
- Implement Sprint 45: Admin Invite External Users UI (sidebar, modal, RTK Query, Redux, credential sheet, error handling, and tests).
- Add Playwright/E2E tests for admin/invite flows once UI is implemented.
- Validate SES/email/feature flag config in a production-like environment.
- Address deferred password reset UI edge cases and AWS credential refactoring.