# Changelog — 20260610_120000

## Update

- Extended the master sprint roadmap in `specs/sprints/sprint-plan.md` with six new sprints (171-176) for the agentic workflow lifecycle.
- Added a new roadmap subsection: `Agentic Workflow + Dynamic State-Phase Automation`.
- Added feature-flag coverage entries for:
  - `INNER_CARD_CHAT_ENABLED` (Sprint 171)
  - `AGENTIC_WORKFLOW_ENABLED` (Sprint 173)

## New

- `specs/sprints/sprint-171.md` — Inner Card Chat + AI Assist refinement loop with BA persona and quality scoring to 90+.
- `specs/sprints/sprint-172.md` — Dynamic workflow phase metadata on columns via the state-transition graph editor.
- `specs/sprints/sprint-173.md` — Move-time metadata trigger engine with tier-aware execution.
- `specs/sprints/sprint-174.md` — Deep context gathering and impact analysis service across docs/code/cards/commits.
- `specs/sprints/sprint-175.md` — Multi-turn AI editing orchestrator (`POST /ai/edit -> gather context -> scope -> create -> edit -> commit`).
- `specs/sprints/sprint-176.md` — Requirement-to-sprint generation plus Update-As-Built documentation sync.

## Technical Debt

- Sprint dependency and numbering consistency should be periodically audited because adjacent roadmap sections include dense cross-links.
- Feature-flag defaults for new agentic flags still need explicit values in runtime config docs and `.env.example` when implementation begins.

## What Should Be Done Next

- Review the new sprint sequence with stakeholders and confirm ordering constraints against currently active board-chat/state-transition implementation work.
- During implementation kickoff, add these new flags to `server/mods/flags/defaults.ts`, `server/mods/flags/types.ts`, and `server/config/flags.ts`.
- Decide whether sprint generation should append only to `specs/sprints/` or also open review PRs by default for generated sprint docs.
