## Update
- Added PATCH endpoint for `/api/v1/attachments/:id` to update the alias field, with validation and board membership authorization.
- Updated `server/extensions/attachment/api/index.ts` to register the PATCH route and handler.
- Extended `Attachment` type in `src/extensions/Attachments/types.ts` to include `alias` and `view_url` fields, replacing the old `url` field.
- Added `patchAttachment` API helper in `src/extensions/Attachments/api.ts`.

## New
- Created `server/extensions/attachment/api/patch.ts` implementing PATCH logic and validation.
- Added `PatchAttachmentRequest` type to client types.
- Added Playwright MCP markdown test: `tests/e2e/sprint-117-attachment-alias.md` for PATCH alias flows and validation.

## Technical Debt
- Playwright config currently excludes `.md` test files, so new markdown tests are not executed. No other major shortcuts taken.

## What Should Be Done Next
- Update `playwright.config.ts` to include `.md` files in `testMatch` so markdown tests are picked up and run.
- Re-run Playwright tests to verify PATCH alias flows.
- Monitor for edge cases in alias validation and error handling.