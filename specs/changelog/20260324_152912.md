## Update
- Updated `server/config/env.ts` to add `HEALTH_CHECK_ENABLED`, `HEALTH_CHECK_TIMEOUT_MS`, and `HEALTH_CHECK_AMBER_THRESHOLD_MS` environment variables with defaults.
- Used `string` (varchar) for all id and foreign key columns in new migrations to match existing codebase conventions.

## New
- Added `db/migrations/0095_create_board_health_checks.ts` to create the `board_health_checks` table with required columns and index.
- Added `db/migrations/0096_create_board_health_check_results.ts` to create the `board_health_check_results` table with required columns and indexes.
- Added `server/config/health-check-services.json` with six pre-configured health check services (Stripe, SendGrid, AWS S3, AWS SES, GitHub API, Cloudflare).

## Technical Debt
None

## What Should Be Done Next
- Implement board health check logic and API endpoints.
- Add tests for health check creation, result recording, and config validation.
- Remove legacy PluginList.tsx/PluginCard.tsx and unused thunk in the duck as noted in previous changelogs.
- Review and document health check config and migration patterns for future features.