## Update
- Added the workspace entitlements API and wired it into the subscription router.
- Implemented usage aggregation for plan-limited resources and fixed the live schema mismatches found during browser validation.
- Kept subscription fallback behavior aligned with `SUBSCRIPTIONS_ENABLED` and the default-unlimited tier setting.

## New
- `server/extensions/subscription/common/entitlements.ts`
- `server/extensions/subscription/common/usage.ts`
- `server/extensions/subscription/api/getEntitlements.ts`
- `GET /api/v1/workspaces/:workspaceId/entitlements`

## Technical Debt
- Usage aggregation still depends on direct table-count queries and should be revisited if schema coverage expands.
- The fallback and tier-mapping logic should be kept in sync with any future subscription tier renames or new stored tier IDs.

## What Should Be Done Next
- Add feature-gate middleware for entitlement-based access control.
- Add limit guards for the resources surfaced by usage aggregation.
- Build the billing UI and upgrade flow that consumes the new entitlement payload.
