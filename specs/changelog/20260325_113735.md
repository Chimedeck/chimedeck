# Sprint 117 Iteration 1 — DB Migration + Secure Attachment Proxy

## Update
- Modified `server/extensions/attachment/api/list.ts` to remove raw S3 presigned URLs from responses. Now returns proxy URLs (`view_url`, `thumbnail_url`) and includes the new `alias` field.
- Updated `server/extensions/attachment/api/index.ts` to register new proxy routes for attachment viewing and thumbnails.

## New
- Added `db/migrations/0099_attachment_alias.ts` to introduce a nullable `alias` column to the attachments table.
- Created `server/extensions/attachment/api/view.ts` implementing the secure GET /attachments/:id/view proxy endpoint.
- Created `server/extensions/attachment/api/thumbnail.ts` for GET /attachments/:id/thumbnail proxy endpoint.
- Added `tests/e2e/sprint-117-attachment-proxy.md` with Playwright MCP markdown test scenarios for the new proxy endpoints and alias field.

## Technical Debt
- Playwright markdown spec is not recognized as a runnable test; actual test implementation is pending.
- Some pre-existing unrelated TypeScript errors remain.

## What Should Be Done Next
- Implement runnable Playwright tests for the attachment proxy and alias flows.
- Add PATCH endpoint for alias updates and corresponding client changes.
- Audit for any legacy code paths that may still leak raw S3 URLs.
- Expand UI and API coverage for attachment alias management.