## Update
- Completed Trello Compat CustomFields endpoints under `server/extensions/trelloCompat/api/customFields/index.ts` for field definition CRUD and dropdown option CRUD.
- Completed card custom-field value endpoints in `server/extensions/trelloCompat/api/cards/index.ts` for `PUT /cards/{idCard}/customField/{idCustomField}/item` and `GET /cards/{id}/customFieldItems`.
- Fixed the custom-field value patch helper scoping in cards API so custom-field upsert logic is reachable and consistent.

## New
- Added Trello compat API tests for custom fields:
  - `tests/server/extensions/trelloCompat/api/customFields.test.ts`
- Added Trello compat API tests for card custom-field item upsert/list:
  - `tests/server/extensions/trelloCompat/api/cards.test.ts`

## Technical Debt
- Trello compat board/card permission helper logic remains duplicated across routers (`boards`, `cards`, `customFields`) and should be centralized.

## What Should Be Done Next
- Add additional negative-path tests for invalid custom field payload variants (type mismatches, invalid list option IDs, malformed date payloads).
- Consolidate shared Trello compat access-control helpers into a common module to reduce drift across endpoint routers.
