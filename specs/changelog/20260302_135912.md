## Update
- Updated server to register new comment and activity routers.
- Integrated Activity log writes into all comment mutations (create, update, delete).
- Enforced append-only immutability for Activity at both DB and application layers.
- Enhanced RBAC checks for comment edit/delete (owner or ADMIN only).

## New
- Migration `0010_comments_activity.ts`: Adds `comments` (versioned, soft-delete) and `activities` (append-only) tables.
- Comment API: `POST /api/v1/cards/:id/comments`, `PATCH /api/v1/comments/:id`, `DELETE /api/v1/comments/:id`.
- Activity API: `GET /api/v1/cards/:id/activity`, `GET /api/v1/boards/:id/activity` (cursor pagination).
- Frontend: `CommentThread`, `CommentItem`, `CommentEditor`, `CommentDeletedItem`, `ActivityFeed`, `ActivityItem` components.
- Tests: 16 unit/integration tests for comment CRUD, versioning, activity immutability, RBAC, and pagination.

## Technical Debt
- None

## What Should Be Done Next
- Implement Playwright/browser E2E tests for comment/activity/realtime/RBAC flows.
- Complete Sprint 10 (client-side real-time sync, optimistic UI, offline queue).
- Add advanced feature flag logic, Redis integration, RBAC middleware, and workspace endpoints.
- Address any deferred UI/UX improvements for comments and activity feeds.
