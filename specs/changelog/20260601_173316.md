## Update
- Extended `server/extensions/subscription/common/usage.ts` with `getWorkspaceCountForUser(userId)` and `getColumnCountForBoard(boardId)` helpers for targeted limit queries.
- Wired workspace creation cap (`maxWorkspaces`) into `server/extensions/workspace/api/create.ts`.
- Wired board creation caps (`maxBoardsPerWorkspace`, `maxBoardsTotal`) into `server/extensions/board/api/create.ts`.
- Wired column creation cap (`maxColumnsPerBoard`) into `server/extensions/list/api/create.ts`.

## New
- New `server/middlewares/limitGuard.ts` with exported `resolveQuota`, `buildLimitResponse`, `applyLimitGuard` (for board/list caps), and `applyWorkspaceLimitGuard` (for workspace caps via user-tier resolution).
- New `server/middlewares/limitGuard.test.ts` — 24 unit tests covering quota resolution and 402 response shape for all tiers and limit keys.
- New `server/extensions/workspace/api/__tests__/create.test.ts` — workspace limit enforcement tests.
- New `server/extensions/board/api/__tests__/create.test.ts` — board per-workspace and total limit tests.
- New `server/extensions/list/api/__tests__/create.test.ts` — column-per-board limit tests.

## Technical Debt
- `getBoardCountTotal(workspaceId)` still performs a per-workspace count; cross-workspace total board cap not yet enforced globally.
- `applyWorkspaceLimitGuard` resolves the user's best tier via a raw JOIN on `memberships`; should be moved to `subscriptionRepo` in a future cleanup pass.
- Member, guest, and storage limit enforcement is deferred to Iteration 8.

## What Should Be Done Next
- Enforce invited-member, guest, and storage upload caps (Iteration 8).
- Add workspace-shared rate limiting per tier (Iteration 9).
- Build the billing UI and upgrade modal for 402 responses (Iterations 10–12).
