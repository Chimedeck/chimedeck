# Changelog — 2026-03-17T05:14:00Z

## Update
- `CardModal.tsx`: Pass `cardId={card.id}` to both `CardDescriptionTiptap` instances (activity-visible and hidden layouts). Attachment upload in the description editor now has the correct card scope.
- `ActivityFeed.tsx` (CardModal container): Pass `cardId={cardId}` to the new-comment `CommentEditor`. The paperclip button is now visible and functional for new comments.
- `CommentItem.tsx`: Pass `cardId={comment.card_id}` to the edit-mode `CommentEditor`. Attachment uploads from the edit-comment form are correctly scoped to the owning card.

## New
- `specs/tests/rich-text-toolbar-single-line-overflow.md`: Added three end-to-end Playwright MCP test scenarios:
  - *cardId Threading — Description Editor Attachment Upload*
  - *cardId Threading — New Comment Attachment Upload*
  - *cardId Threading — Edit Comment Attachment Upload*

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 6: DB migration for `user_card_drafts` table (Sprint 83 offline draft infrastructure).
