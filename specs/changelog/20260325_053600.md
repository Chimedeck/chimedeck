# Sprint 117 — Iteration 6: Client Avatar Migration

## Update
- `server/extensions/users/api/avatar/upload.ts`: Fixed POST `/users/me/avatar` to return the stable proxy path (`/api/v1/users/:id/avatar`) instead of a short-lived presigned S3 URL. Changed import from `resolveAvatarUrl` to `buildAvatarProxyUrl`.

## New
- `tests/e2e/sprint-117-avatar-proxy.md`: Added UI-level tests (T18–T21) verifying that avatar `<img src>` attributes in profile settings and activity feed use the proxy path, not raw S3 URLs.

## Technical Debt
- `resolveAvatarUrlsInCollection` in `resolveAvatarUrl.ts` still generates presigned URLs; only used for legacy board background resolution — not for avatars.

## What Should Be Done Next
- Iteration 7: Rewrite S3 URLs in card description and comment renderers.
