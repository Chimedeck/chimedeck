# Changelog - 2026-06-03 14:15:00

## Update

- Locked GitHub integration decisions directly in Sprint 168 so downstream implementation is unambiguous.

## New

- Added a "Forward-Locked Integration Decisions" section to `specs/sprints/sprint-168.md`.
- Explicitly fixed downstream contract:
  - Fetch auth: GitHub App installation token
  - Commit identity: app bot alias
  - Audit trace: initiating actor metadata preserved
  - Git execution: server-side Git service wrapper only

## Technical Debt

- Exact bot alias naming convention is documented as policy but final canonical value is not yet centralized in config docs.

## What Should Be Done Next

- Add a single canonical config reference for app bot alias naming and commit footer format.
- Add API examples showing commit metadata fields carrying initiating actor id.
