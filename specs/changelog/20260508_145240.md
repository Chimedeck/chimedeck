# Changelog - 2026-05-08 14:52:40

## Update
- Extended notification panel stacking logic to include continuous `card_updated` events by `board_id`.
- Kept existing contiguous same-card discussion stacking (`mention`, `card_commented`, `comment_reaction`) unchanged.
- Updated notification item header behavior for grouped board updates so grouped rows present as board activity instead of a single card title.

## New
- Added stack-key resolution in the notification grouping pipeline to support multiple stack strategies:
  - `discussion:<card_id>` for discussion notifications
  - `board-update:<board_id>` for card update notifications

## Technical Debt
- Board-level grouped update header copy is currently hardcoded (`Board activity`, `<n> card updates`) and not localized yet.

## What Should Be Done Next
- Add/extend unit tests for notification grouping covering:
  - contiguous `card_updated` in same board groups together
  - `card_updated` in different boards does not group
  - non-contiguous `card_updated` does not group
  - mixed discussion/update types do not group together
- Consider adding translation keys for grouped board update header/subtitle copy.
