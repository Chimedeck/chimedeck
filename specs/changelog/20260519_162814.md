## Update
- Integrated card move enforcement into the state transitions feature: moves now checked against board state transition graph.
- Updated normalization in `serializer.ts` to use listId-based rule IDs for consistency.
- Updated validator to enforce `node.id === node.listId`.
- Updated `put.ts` to invalidate rules cache after state transition updates.
- Wired enforcement into card move handler; forbidden moves return 422 with error details.

## New
- Added `enforcement/index.ts` with `validateCardMove` for move validation logic.
- Added `enforcement/rules.ts` for in-memory rules cache (60s TTL, invalidation helpers).
- Added `common/errors.ts` with `StateTransitionForbiddenError`.
- Added/updated tests for enforcement, cache invalidation, and error handling.
- Added changelog entry for this iteration.

## Technical Debt
- Trello compat permission helpers remain duplicated (not yet centralized).
- Some OpenAPI schemas are still generic and need tightening.
- Placeholder endpoints for Trello actions/notifications remain.

## What Should Be Done Next
- Implement activity log entries for blocked moves and Trello-compat error mapping (Sprint 154 §4–§6).
- Add UI enforcement and collaborative editing (Sprints 155–157).
- Investigate and resolve login failure blocking Playwright MCP flow tests.
- Ensure cache invalidation and error mapping are robust for real-time enforcement.
