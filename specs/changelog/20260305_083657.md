# Changelog — 20260305_083657

## Update
- Audited `usePluginBridge.ts`: confirmed `boardId` is always included in `DATA_GET` and `DATA_SET` proxied API calls, and `resourceId` is correctly resolved from the SDK message payload.
- Verified all end-to-end integration points from Sprint 37 and Sprint 38 are complete and consistent:
  - `CTX_CARD`, `CTX_LIST`, `CTX_BOARD`, `CTX_MEMBER` handlers present in host bridge.
  - `BUTTON_CLICKED` dispatched by `CardPluginButtons.tsx` with `__callbackId`; handled by SDK callback registry in `jh-instance.ts` / `public/sdk/jh-instance.js`.
  - `DATA_GET`/`DATA_SET` in the SDK auto-resolve `resourceId` and `boardId` from `this.args`.
  - Server `GET /api/v1/plugins/data` and `PUT /api/v1/plugins/data` both enforce `boardId` presence and validate resource-board ownership via `validateResourceBelongsToBoard`.
  - Migration `0022_plugin_data_board_id.ts` adds `board_id` column, drops old unique constraint, and adds a new one including `board_id`.

## New
- No new files in this iteration (audit/verification only).

## Technical Debt
- Playwright E2E test infrastructure for plugin flows (badge rendering, button clicks, modal opens, data persistence) is deferred.

## What Should Be Done Next
- Automated Playwright E2E tests for plugin data and button flows, modal `sizeTo` callback, and bridge-based show-settings flow.
