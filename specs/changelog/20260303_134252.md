## Update
- Integrated notification creation and real-time delivery into comment and card update APIs (server/extensions/comment/api/create.ts, update.ts, card/api/update.ts).
- Registered notifications API router and user WebSocket channel management in server entry and realtime modules.
- Updated Redux store and AppShell to mount NotificationContainer and support notification state.

## New
- Added notifications DB migration (db/migrations/0017_notifications.ts) with CASCADE deletes.
- Implemented server-side notification REST API: list, markRead, markAllRead, delete (server/extensions/notifications/api/).
- Added notification creation helper (server/extensions/notifications/mods/createNotifications.ts).
- Created per-user WebSocket channel utilities (server/extensions/realtime/userChannel.ts).
- Client Notification extension: Redux slice, thunks, API client, WebSocket sync hook, NotificationBell, NotificationPanel, NotificationItem, NotificationContainer, i18n strings.
- Added E2E test specs: specs/tests/notifications-mention.md, notifications-realtime.md, notifications-mark-read.md.

## Technical Debt
- E2E Playwright tests for notifications are present but not runnable ("No tests found").
- Deferred: global requireVerified middleware, Redis rate limiting, real-time profile propagation, accessibility polish, edge-case handling (nickname collisions, membership changes), UI/UX polish, and error handling improvements.

## What Should Be Done Next
- Fix and enable E2E Playwright tests for notification flows.
- Complete mark-as-read and bulk-clear actions in UI and API.
- Address accessibility for notification panel and mention autocomplete.
- Handle edge cases: nickname collisions, deleted users, membership changes, cascade deletes.
- Implement deferred technical debt: global middleware, rate limiting, real-time profile propagation, and UI/UX polish.