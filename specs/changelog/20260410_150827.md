## Update
- Extended `CommentData` and `Comment` interfaces with `parent_id` and `reply_count` fields.
- Added `getReplies` and `postReply` API helpers to `src/extensions/Comment/api.ts`.
- Updated `CommentItem.tsx` to show Reply button, reply count, and render `CommentReplyThread` for top-level comments.
- Modified `CommentThread.tsx`, `ActivityFeed.tsx`, and `CardModal.tsx` to pass and handle reply actions.
- Added `addReply` action to `cardDetailSlice.ts` and handled `comment_reply_added` WS events in `useBoardSync.ts`.
- Updated translations for reply UI in `en.json`.

## New
- Created `CommentReplyThread.tsx` for displaying and managing threaded replies with expand/collapse, load-on-demand, and inline reply composer (max depth = 1).

## Technical Debt
None

## What Should Be Done Next
- Ensure at least one card exists and is visible/clickable for UI/E2E testing of threaded replies.
- Complete Playwright MCP UI tests for reply flows, real-time sync, and edge cases (e.g., reply depth enforcement, error handling).
- Defer advanced reply features (editing, deep threading, moderation) to a future sprint.
