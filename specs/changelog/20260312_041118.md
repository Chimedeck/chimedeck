# Sprint 68 — Log Tab UI (QuotaBar, RunLogTable, RunLogDetail)

## Update
- Activated the Log tab in `AutomationPanel` — replaces the "Coming soon" placeholder with the real `LogPanel`.

## New
- `LogPanel/index.tsx` — entry point for Log tab; fetches board-wide quota and runs on mount; composes QuotaBar + RunLogTable.
- `LogPanel/QuotaBar.tsx` — progress bar showing monthly run quota; colour-coded: emerald → amber at ≥80% → red at ≥95%; shows reset countdown.
- `LogPanel/RunLogTable.tsx` — paginated table for automation run logs; supports real-time row prepending via `prependedRuns` prop.
- `LogPanel/RunLogRow.tsx` — single expandable run row; status icon, automation name + type chip, card link, triggered-by, relative timestamp.
- `LogPanel/RunLogDetail.tsx` — inline detail section showing context JSON and error message.
- `LogPanel/AutomationRunsPanel.tsx` — per-automation scoped run history panel (for use in edit views).
- `api.ts` — added `getAutomationRuns`, `getBoardRuns`, `getAutomationQuota` functions.
- `types.ts` — added `AutomationRunLog`, `PaginatedRunLogs`, `AutomationQuota`, `RunStatus` types.
- `tests/e2e/automation/logPanel.md` — Playwright MCP test spec covering quota bar, table, expand/collapse, pagination, empty state.

## Technical Debt
- `prependedRuns` prop in `RunLogTable` is wired for WebSocket real-time updates but the WS subscription is not yet connected (done in Iteration 10).
- `AutomationRunsPanel` is created but not yet wired into the edit views of Rules/Buttons/Schedule rows (out of scope for this iteration).

## What Should Be Done Next
- Iteration 10: Wire `automation_ran` WS event to prepend rows in `RunLogTable` and increment per-automation run-count chips.
