## Update
- Refactored `src/layout/AppShell.tsx` to use a new `TopBar` component, added `min-w-0` to the main content flex wrapper for better responsive behavior, and removed unused imports.
- Fixed user menu dropdown clipping in collapsed sidebar mode in `src/layout/Sidebar.tsx` by repositioning the dropdown to the right of the sidebar.

## New
- Added `src/layout/TopBar.tsx` component, extracting both mobile and desktop topbar logic with typed props and unified height.
- Expanded `specs/tests/sidebar-collapsible-drawer.md` with a viewport matrix, new scenarios for board/card pages, a regression checklist, and extended acceptance criteria.

## Technical Debt
- Playwright E2E tests are limited; most layout/sidebar tests still use Bun's test runner and are not Playwright-compatible.
- Pre-existing TypeScript errors remain in unrelated files.

## What Should Be Done Next
- Migrate sidebar/layout tests to Playwright for full E2E coverage.
- Complete board-scoped search API/UI integration and result interactions.
- Finalize responsive and accessibility hardening for sidebar/AppShell/TopBar.
- Address technical debt: remove old sidebar, resolve TypeScript errors, and ensure all new files are tracked.