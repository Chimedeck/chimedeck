# Changelog — 2026-03-24 03:37:36

## Update
- Added `tests/README.md` documenting the three-directory test structure.

## New
- `tests/README.md`: covers `integration/` (bun:test, `.test.ts`), `e2e/` (Playwright, `.spec.ts`), and `load/` (`.js`) directories; explains MCP scenario file location in `specs/tests/`; documents all `bun run test:*` scripts and env-var overrides (`SKIP_UNIT`, `SKIP_E2E`, `SKIP_MCP`, `BASE_URL`); includes a file-type rules summary table.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 9: Write missing MCP scenario `.md` files (batch 1 of 2) in `specs/tests/`.
