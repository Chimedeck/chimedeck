## Update
- Added the board-chat semantic search read path with board-scoped permission checks and ranked message hits.
- Wired `POST /api/v1/boards/:boardId/chat/search` into the board API router.

## New
- `server/extensions/board/api/chatSearch/*` for the new search endpoint.
- `server/extensions/board/mods/chat/search/*` for query embedding, similarity scoring, and result shaping.
- `server/extensions/board/api/__tests__/chatSearch.test.ts` and `server/extensions/board/mods/chat/search/__tests__/query.test.ts` for API and search coverage.
- Search result/type additions in `server/extensions/board/types.ts`.

## Technical Debt
- Search scoring still runs in application code over persisted vectors instead of a database-native vector index.
- Semantic search returns top hits only; pagination for larger result sets is deferred.

## What Should Be Done Next
- Add the next board-chat UX for searching inside the drawer.
- Revisit a database-backed vector index once the search surface grows.
