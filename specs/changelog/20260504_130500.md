## Update
- Fixed board member add authorization so board-level ADMIN/OWNER can add workspace members to a board even when caller is not workspace ADMIN/OWNER.
- Aligned runtime behavior with existing endpoint intent/comments in board members create handler.

## New
- Added explicit fallback auth path in board member creation:
  - First checks workspace ADMIN+ role.
  - If that fails, checks explicit board role in `board_members` and allows `ADMIN` or `OWNER`.
- Added explicit 401 response when add-member route is reached without an authenticated caller context.

## Technical Debt
- Board member update/remove and guest-management endpoints still gate on workspace ADMIN+ only, while UI permission model is board-admin centric. These flows should be reviewed for consistency.

## What Should Be Done Next
- Add/extend tests for board-member add auth matrix:
  - workspace MEMBER + board ADMIN should succeed.
  - workspace MEMBER + board MEMBER should fail with 403.
  - workspace ADMIN should continue to succeed without explicit board_members row.
- Evaluate whether PATCH/DELETE member routes and guest invite/revoke should mirror the same board-admin fallback.
