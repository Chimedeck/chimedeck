## Update
- Updated `playwright.config.ts` to remove the obsolete `specs` project entry.
- Kept only the `e2e` project, targeting `./tests/e2e`.
- Set reporter to `[['line'], ['html', { open: 'never' }]]`.
- Added `use: { baseURL: process.env.TEST_BASE_URL ?? 'http://localhost:3000' }` to the config.

## New
- No new features or files added in this iteration.

## Technical Debt
- None

## What Should Be Done Next
- Fix Playwright config and test recognition for scenario/E2E tests if issues persist.
- Validate scenario test syntax and ensure all intended tests are executed.
- Address deferred features (route/sidebar wiring for docs page, board-level views, notification preferences, uuidv7 migration).
- Consider adding coverage tooling.
- Ensure no other scripts or CI configs reference the removed `specs` project.