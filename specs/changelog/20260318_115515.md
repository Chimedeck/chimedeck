## Update
- Made `boardId` optional in `connect()` in `src/extensions/Realtime/client/socket.ts` to support workspace-level realtime updates.
- Added `boardRemovedByRealtime` reducer action in `BoardListPage.duck.ts` to handle board removal from Redux state and prevent rollback of genuinely deleted boards.
- Wired `useWorkspaceSync` into `BoardListPage.tsx` to enable realtime board list updates for all users in a workspace.

## New
- Created `src/extensions/Board/realtime.ts` with the `useWorkspaceSync` hook for subscribing to workspace board deletion events and dispatching state updates.
- Added `src/pages/WorkspaceBoardsPage.tsx` as a canonical re-export of the board list page.
- Expanded `specs/tests/board-delete-auto-refresh.md` with scenarios BDAR-RT-01 to BDAR-RT-05 for multi-session, cross-user realtime board removal and UI refresh.

## Technical Debt
- Pre-existing TypeScript errors remain in thunk exports in `BoardListPage.duck.ts` (not worsened by these changes).
- Playwright test runner still requires Node.js due to ESM loader issues with Bun.
- Some legacy thunk/middleware patterns and logging remain unrefactored.

## What Should Be Done Next
- Implement client redirect/toast and UI feedback for board deletion.
- Add real-time board removal UI updates for all connected users.
- Refactor legacy thunk exports and middleware/logging.
- Update Playwright test command to run natively with Bun if possible.
- Address any Playwright test flakiness or config issues unrelated to this iteration.