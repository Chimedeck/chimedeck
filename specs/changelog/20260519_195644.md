## Update
- Normalized error envelopes for stateTransitions GET/PUT endpoints and router: all now return `{ name, data }` per API spec (replacing legacy `{ error: ... }`).
- Confirmed feature flag gating for STATE_TRANSITIONS_ENABLED is correct.
- No changes to unrelated modules; scope limited to stateTransitions API.

## New
- None (no new endpoints or files added; all changes were in-scope refactors and envelope alignment).

## Technical Debt
- Error envelope inconsistency remains in legacy modules outside stateTransitions; not addressed in this iteration.
- Playwright browser navigation cannot validate API error envelopes due to CORS/non-HTML responses—APIRequestContext or REST client needed for full API test coverage.

## What Should Be Done Next
- Implement `/rules` and `/copy` endpoints for stateTransitions.
- Add list rename/delete sync hooks and websocket `state_transition_updated` event.
- Consider refactoring legacy modules to use the new error envelope format for consistency.
- Expand API test coverage using direct API requests for error envelope validation.
