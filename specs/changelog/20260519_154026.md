## Update
- Added board-scoped state transition API routing for `GET` and `PUT` at `/api/v1/boards/:boardId/state-transitions`.
- Wired the new state transitions router into the main server router dispatch pipeline.

## New
- Implemented `GET` state transitions handler with lazy row creation and default graph generation from active board lists.
- Implemented `PUT` state transitions handler with feature-flag gating, admin/owner permission enforcement, graph validation, unknown-list detection, and persistence.
- Added state transition serializer helpers for default/empty graph creation and consistent API response formatting.
- Added state transition validator helpers for graph shape validation and unknown node/list reference detection.
- Added API tests for:
  - GET default graph generation, existing graph retrieval, and feature-flag-off behavior
  - PUT valid update, malformed graph rejection, unknown-list rejection, and feature-flag-off behavior

## Technical Debt
- Route and handler error envelopes still coexist with both `{ error: { ... } }` and `{ name, data }` formats in the broader codebase; this module follows the state-transition-specific error naming required by the sprint spec.

## What Should Be Done Next
- Implement the `/rules` and `/copy` state transition endpoints (Iteration 3+ scope).
- Add list rename/delete sync hooks and websocket broadcast (`state_transition_updated`) on successful transition updates.
