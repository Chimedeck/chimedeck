## Update
- Added Sentry-related environment variables to `.env.example` and documented them in the README under the Production Environment section.
- Updated `src/config/index.ts` to expose Sentry client config fields from `import.meta.env`.
- Updated `server/config/env.ts` to expose Sentry server config fields from `Bun.env`.
- Added Sentry dependencies to `package.json` (`@sentry/react`, `@sentry/node`).

## New
- Introduced Sentry configuration fields and environment variable documentation for both client and server.
- Added a dedicated Sentry section to the environment variable documentation table in the README.

## Technical Debt
None

## What Should Be Done Next
- Implement Sentry initialization logic in both client and server, guarded by the new config fields.
- Add runtime tests to verify Sentry is only initialized when enabled and DSN is set.
- Review and update error handling to leverage Sentry where appropriate.
- Document Sentry usage and troubleshooting for developers.