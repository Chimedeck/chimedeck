## Update
- Converted the server rate limiter to use workspace-shared READ/WRITE buckets when subscriptions are enabled.
- Preserved the legacy auth/upload and subscription-disabled fallback path so existing per-identifier throttling still works.
- Wired request workspace context into the server pipeline before feature gating.

## New
- Added workspace-scoped rate-limit keying and tier-driven quota lookup.
- Added focused coverage for shared workspace buckets, workspace separation, read/write independence, and the subscription-disabled fallback.
- Added a small request-context test to pin workspace resolution behavior.

## Technical Debt
- The limiter still degrades to allow traffic when Redis is unavailable, matching prior behavior.
- Remaining repository typecheck failures are unrelated to this iteration.

## What Should Be Done Next
- Continue with the billing UI foundation and plan presentation work.
