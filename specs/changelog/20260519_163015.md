## Update

- Added blocked-move activity logging for state transition enforcement failures and wired it into both native card move and Trello-compat card update move flows.
- Extended `StateTransitionForbiddenError` context with `boardId` so error objects carry complete transition metadata for logging and API surfaces.
- Added Trello-compat 422 mapping for blocked transitions with Trello-style body:
  `{ message, error: "STATE_TRANSITION_FORBIDDEN" }`.

## New

- New helper: `server/extensions/stateTransitions/common/activityLog.ts` with `emitCardMoveBlockedActivity(...)` to centralize append-only `card_move_blocked` activity writes and feed publishing.
- New visible activity event type on server: `card_move_blocked` in `server/extensions/activity/config/visibleEventTypes.ts`.

## Technical Debt

- Trello-compat card update route still performs direct card persistence logic rather than reusing the canonical `/api/v1/cards/:id/move` path, so move behavior is duplicated.

## What Should Be Done Next

- Add client-side activity label/copy rendering for `card_move_blocked` so blocked transitions show a dedicated human-readable sentence in the card activity UI.
