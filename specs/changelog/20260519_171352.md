## Update
- Refactored Graph Editor to support custom edge rendering, edge inspector, and edge deletion in the StateTransitions extension.
- Updated edge state management, validation, and persistence logic to handle new edge properties (direction, action, style) and enforce no self-loops or duplicate directional edges.
- Expanded i18n strings for edge actions and inspector UI.
- Updated validator and tests to cover new edge rules.

## New
- Added `TransitionEdge.tsx`, `EdgeActionLabel.tsx`, `EdgeDeleteButton.tsx`, and `EdgeInspector.tsx` for custom edge UI and editing.
- Introduced `config/actionTypes.ts` for extensible edge action types.
- Implemented shake animation utility in `src/index.css` for invalid edge feedback.
- Added changelog entry for this iteration.

## Technical Debt
- List sync hooks remain synchronous (should be async for perf).
- UI: Toolbar, sticky notes, keyboard shortcuts, real-time collab, Kanban DnD enforcement, copy modal, and banner are deferred.

## What Should Be Done Next
- Fix authentication so Playwright tests can log in and verify edge flows.
- Implement deferred UI features: toolbar, sticky notes, keyboard shortcuts, real-time collab, Kanban DnD enforcement, copy modal, and banner.
- Continue edge case testing for simultaneous edge creation and animation polish.