## Update
- None. No changes made to existing code or features in this iteration.

## New
- Added `specs/tests/health-check-board.md`: Playwright MCP test spec covering all backend health check API acceptance criteria from Sprint 115 (AC-1 to AC-10).
- The spec includes scenarios for: preset listing, adding custom/preset checks, duplicate URL handling, probe results (green/red), SSRF protection, feature flag 404s, URL validation, rate limiting, DELETE lifecycle, and unauthenticated access.

## Technical Debt
- Rate limiting remains in-memory only (not persistent).
- No Playwright MCP/E2E tests for backend probe endpoints or rate limiting yet.
- SSRF allow-list and edge-case validation may need further review.
- Amber probe state (slow response) is noted as integration-only and not directly testable in the MCP spec.

## What Should Be Done Next
- Implement Playwright MCP/E2E tests for backend probe endpoints and rate limiting.
- Review and document SSRF allow-list and edge-case handling.
- Begin frontend (Redux, UI, E2E UI test) implementation for the health check feature in the next iteration.
- Address any ambiguities or missing requirements in the architecture/requirements specs as discovered during further testing.