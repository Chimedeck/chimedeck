## Update
- Refactored `server/extensions/search/api/query.ts` to delegate all workspace search logic to the new access-aware query mod, removing legacy unfiltered queries.

## New
- Added `server/extensions/search/mods/queryWorkspaceSearch.ts`: Implements board-level access filtering for workspace search, enforcing RBAC at the SQL layer for OWNER, ADMIN, MEMBER, VIEWER, and GUEST roles.
- Created `specs/tests/search-board-access-rights.md`: Comprehensive API and UI test scenarios for board search access rights, including edge cases and acceptance checklist.

## Technical Debt
- Playwright MCP evaluator project is not configured; UI test automation for access rights is not runnable yet.
- Pre-existing TypeScript errors remain unresolved (not introduced by this iteration).

## What Should Be Done Next
- Enable and configure Playwright MCP evaluator project to run UI tests.
- Complete board search feature (access filtering, error handling, stale result handling).
- Address outstanding TypeScript errors and technical debt.
- Finalize responsive and accessibility improvements; complete theme audit for all components.
- Remove obsolete sidebar code and migrate sidebar/layout tests to Playwright.