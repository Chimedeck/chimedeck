# 20260422_160500

## Update

- Removed comment reaction events from the card detail activity feed so reactions only appear in the board Activities tab.
- Fixed notification list support for reaction notifications by aligning API type filter and DB type constraints with `comment_reaction`.

## New

- Added migration `db/migrations/0112_notification_types_extend3_comment_reaction.ts` to include `comment_reaction` in:
  - `notifications` type check
  - `notification_preferences` type check
  - `board_notification_type_preferences` type check
- Expanded notification list API type allowlist in `server/extensions/notifications/api/list.ts`.
- Updated card activity feed allowlist in `src/extensions/Card/config/activityEventsConfig.ts`.

## Technical Debt

- Existing complexity warnings in `server/extensions/notifications/mods/boardActivityDispatch.ts` remain unchanged.
- Existing nested-ternary warnings in `src/extensions/Board/containers/BoardPage/BoardPage.tsx` remain unchanged.

## What Should Be Done Next

- Run migrations in all environments before validating reaction notifications in production-like data.
- Add API test coverage for `GET /api/v1/notifications` including `comment_reaction` in persisted data.
- Add UI test to confirm card modal activity excludes reactions while board Activities tab includes them.
