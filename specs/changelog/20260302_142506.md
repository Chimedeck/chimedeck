## Update
- Strengthened security headers in `server/mods/helmet.ts` (added Permissions-Policy, improved CSP, frame-ancestors).
- Extended activity audit logging in `server/extensions/activity/mods/write.ts` to include `ipAddress` and `userAgent`.
- Added `OTEL_ENABLED`, `OTEL_EXPORTER_URL`, and `RATE_LIMIT_ENABLED` to `server/config/env.ts`.
- Updated `.github/workflows/ci.yml` to run `bun audit --audit-level=high`.
- Added `test:load` script to `package.json` for k6 load tests.

## New
- OpenTelemetry observability: `server/mods/observability/tracer.ts`, `metrics.ts`, `exporters.ts` (tracing/metrics, OTLP exporter, env-gated, no-op fallback).
- Redis-based sliding-window rate limiter: `server/middlewares/rateLimiter.ts` (Lua atomic, per-route, 429/Retry-After, graceful degradation).
- Activity audit DB migration: `db/migrations/0013_activity_audit.ts` (adds `ip_address`, `user_agent` columns).
- Board UI performance: `src/extensions/Board/components/VirtualCardList.tsx` (virtualized list), `BoardSkeleton.tsx` and `CardSkeleton.tsx` (shimmer loaders).
- Load tests: `tests/load/board-load.js`, `drag-latency.js`, `ws-broadcast.js` (k6 scenarios).
- Integration tests: `tests/integration/securityHeaders.test.ts`, `rateLimiter.test.ts`, `otel.test.ts` (16 new tests, all passing).

## Technical Debt
- No Playwright (@PLAYWRIGHT_REQUIRED) E2E tests for required user flows; MCP evaluator cannot verify flows until these are implemented.
- Redis adapter tests skipped if Redis unavailable.
- Activity log immutability enforced only in app, not DB.
- Activity backfill for sprints 03–08 mutations is pending.

## What Should Be Done Next
- Implement Playwright E2E tests for all required user flows to enable MCP evaluation.
- Backfill activity log for historical mutations (sprints 03–08).
- Enforce activity log immutability at the DB level.
- Add advanced feature flag logic and RBAC middleware for deferred items.
- Address any deferred or skipped tests for Redis and rate-limiting edge cases.