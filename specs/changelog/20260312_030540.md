# Changelog — 2026-03-12T03:05:40Z

## Update
- `src/extensions/Automation/components/AutomationPanel/index.tsx` — wired up the Buttons tab (replaced "Coming soon" placeholder with `ButtonsTab`).
- `src/extensions/Board/components/BoardHeader.tsx` — inserted `BoardButtonsBar` immediately to the left of `AutomationHeaderButton`.
- `src/extensions/Automation/api.ts` — added `runBoardButton()` function.
- `src/extensions/Automation/types.ts` — added `BoardButtonRunResult` type.
- `server/extensions/automation/api/index.ts` — registered the `POST /api/v1/boards/:boardId/automation-buttons/:automationId/run` route.

## New
- `server/extensions/automation/api/runBoardButton.ts` — server endpoint; resolves target scope (board/list/filter) to card IDs, runs executor per card, caps at 50 cards, returns `{ runLogId, cardCount, status }`.
- `src/extensions/Automation/components/BoardButtons/BoardButtonItem.tsx` — icon-only button with hover tooltip; shows spinner while running and colour feedback after.
- `src/extensions/Automation/components/BoardButtons/BoardButtonBuilder.tsx` — modal builder for BOARD_BUTTON automations: name, icon picker, scope selector (board/list/filter), action list.
- `src/extensions/Automation/components/BoardButtons/BoardButtonsBar.tsx` — horizontal strip rendered in the board header; loads enabled BOARD_BUTTON automations, shows max 5 with overflow indicator.
- `src/extensions/Automation/components/AutomationPanel/ButtonsTab.tsx` — Buttons tab content with two sections (Card Buttons / Board Buttons); per-row enable toggle, edit, delete, and "Add" CTAs opening respective builders.
- `tests/e2e/automation/boardButtons.md` — 12 Playwright MCP test scenarios for board button creation, running, scope, overflow, enable/disable, and delete.

## Technical Debt
- `BoardButtonBuilder` scope "filter" only stores `targetScope: 'filter'` without labelIds/memberIds UI input — a comment directs users to configure those post-save. A future iteration should add a proper filter editor.
- Board header's `BoardButtonsBar` re-fetches automations independently from the panel — a shared context/store would be cleaner once more consumers exist.

## What Should Be Done Next
- Iteration 6: ScheduledCommandBuilder + DueDateCommandBuilder + scheduleSummary (Sprint 67).
