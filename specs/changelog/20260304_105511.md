## Update
- Updated `server/config/env.ts` to add `STRIPE_SECRET_KEY` config entry.
- Modified `server/index.ts` to register the new payment router.
- Updated `.env.example` to include `STRIPE_SECRET_KEY` and `VITE_STRIPE_PUBLIC_KEY` placeholders.

## New
- Installed `stripe@20.4.0` as a runtime dependency using `bun add stripe`.
- Added `server/extensions/payment/config/stripe.ts`: Stripe SDK singleton, exports configured Stripe instance or `null` if not configured.
- Added `server/extensions/payment/api/index.ts`: Implements `POST /api/v1/payments/stripe/create-payment-intent` endpoint with input validation, error handling, and PaymentIntent creation.

## Technical Debt
- No Playwright or automated tests exist for the payment/stripe endpoint; flows are not currently validated by any test runner.
- RBAC/event sourcing for payment flows not reviewed.
- No architectural guidance for payment/Stripe flows in `specs/architecture/`.

## What Should Be Done Next
- Implement Playwright or integration tests for the payment/stripe endpoint and flows.
- Review and enhance RBAC/event sourcing for payment logic.
- Document payment/Stripe architecture and requirements in `specs/architecture/`.
- Begin client-side Stripe integration and UI wiring for payment flows.