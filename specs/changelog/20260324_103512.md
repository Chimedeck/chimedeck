## Update
- Converted 5 scenario .md files in `tests/e2e/` to Playwright `.spec.ts` files, aligning with architecture and test coverage requirements.
- Ensured all new spec files are recognized by Playwright and included in the test suite.

## New
- Added the following Playwright spec files:
  - `business-logic-invariants.spec.ts` (API: archived-board guard, workspace owner invariant)
  - `card-description-inline-edit.spec.ts` (UI: inline description editing)
  - `csrf-guard.spec.ts` (API: CSRF origin-header middleware)
  - `delete-confirmation.spec.ts` (API/UI: delete with `confirm:true` flag)
  - `input-sanitization.spec.ts` (API: XSS sanitization for all field types)
- Removed all corresponding .md scenario files from `tests/e2e/`.

## Technical Debt
- No individual Playwright report folders or screenshots found for new specs; review HTML report for details if needed.
- Some unrelated e2e tests failed (timeouts/missing elements), but not in the new flows.

## What Should Be Done Next
- Review Playwright HTML report for detailed results and screenshots if required.
- Investigate and address unrelated e2e test failures.
- Refine new specs as needed based on future feedback or edge cases.
