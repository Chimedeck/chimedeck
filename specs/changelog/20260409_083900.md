## Update
- Fixed cross-list card drag resolution on dense boards in Kanban view by preventing same-list pointer midpoint logic from overriding destination-list placeholder updates.
- Added pointer-aware list detection during drag-over so drop target list is derived from the live pointer position when available.
- Added pointer-aware collision detection for card dragging to prioritize droppables under the cursor.
- Added `over === null` handling in card drag-over to keep destination placeholder/index synced from live pointer location when DnD collision temporarily drops out.
- Added drag-end pointer fallback so final commit resolves to the list under the pointer when placeholder data is stale.
- Made drag overlay cards non-interactive (`pointer-events-none`) so pointer hit-testing resolves underlying board lists/cards reliably.

## New
- Added helper `getBoardListIdFromElement` in BoardCanvas to resolve list id from DOM under the pointer.

## Technical Debt
- `handleDragOver` and `handleDragEnd` in BoardCanvas remain high-complexity and should be split into smaller units (same-list positioning, cross-list target resolution, and final commit payload building).

## What Should Be Done Next
- Add focused unit/integration tests for cross-list drag in dense-board mode (`disableLiveDragPreview=true`) covering:
  - destination placeholder visibility while dragging over another column,
  - `targetListId` in move payload matching hovered list,
  - same-list reorder behavior still intact.
