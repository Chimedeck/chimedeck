## Update
- Normalized `server/extensions/trelloCompat/serializers/organization.ts` to emit canonical Trello organization shape with correct defaults and mappings (prefs, billableMemberCount, idMemberCreator, powerUps, nodeId, website).
- Updated `server/extensions/trelloCompat/api/organizations/index.ts` to ensure all organization responses use the canonical serializer, including field endpoints (now return 404/not implemented).
- Updated `specs/trello-adapter-normalization/matrix.md` with PASS statuses for organization endpoints.

## New
- Added `server/extensions/trelloCompat/tests/unit/server/extensions/trelloCompat/organizations.contract.test.ts` for contract coverage of organization endpoints.
- Added changelog entry for this iteration.

## Technical Debt
- Field endpoints for organizations (e.g., /website, /name, /prefs) are not implemented and return 404/not implemented.
- Some legacy code paths may still emit non-canonical organization shapes in embedded contexts.

## What Should Be Done Next
- Implement and test field endpoints for organizations (e.g., /website, /name, /prefs).
- Audit for non-canonical organization shapes in embedded contexts and refactor as needed.
- Expand contract/integration tests for edge cases and legacy data.
- Continue normalization for any remaining endpoints or sub-resources.