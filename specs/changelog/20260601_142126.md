## Update
- Extended `subscriptionRepo.ts` with Stripe lookup, upsert, and idempotency helpers for workspace subscription sync.
- Updated `api/index.ts` to register the new webhook endpoint.

## New
- Added `api/webhook.ts`: POST endpoint for Stripe webhooks with signature verification and error envelope.
- Added `common/syncFromStripe.ts`: Idempotent event processor for Stripe subscription events.
- Added `common/priceTierMap.ts`: Maps Stripe price IDs to internal tier keys.
- Added tests: `api/webhook.test.ts` (endpoint, signature, idempotency) and `common/syncFromStripe.test.ts` (event handling, tier fallback).

## Technical Debt
- Deferred: Full regression tests for HTML sanitization and audit doc clarifications.
- Some server-side error envelope standardization and sanitization remain outstanding.

## What Should Be Done Next
- Add UI feedback for webhook failures.
- Complete regression on HTML sanitization.
- Clarify audit documentation for subscription events and error handling.
- Confirm correct API base URL/port for webhook endpoint exposure.