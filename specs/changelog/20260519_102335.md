## Update
- Completed gap analysis for Actions serializer and updated `specs/trello-adapter-normalization/matrix.md` with PASS/PARTIAL/MISMATCH status for all Actions endpoints.
- Reviewed architecture, requirements, and endpoint-response-checklist specs to align test scaffolding plan.

## New
- Documented the absence of contract test scaffolding for Trello adapter normalization (no `fixtures.ts` or `assertTrelloShape.ts` helpers yet).
- Outlined implementation plan for test helpers and smoke contract tests for `GET /actions/{id}`.

## Technical Debt
- No contract tests exist for Trello adapter normalization; test infrastructure is missing and must be created.
- Some Actions endpoints remain PARTIAL/MISMATCH in the status matrix.

## What Should Be Done Next
- Implement `fixtures.ts` and `assertTrelloShape.ts` helpers in `server/extensions/trelloCompat/tests/unit/helpers/`.
- Write smoke contract tests for `GET /actions/{id}` (comment and activity shapes).
- Expand contract test coverage and address normalization gaps for PARTIAL/MISMATCH endpoints in future iterations.