## Update
- Updated `server/extensions/trelloCompat/serializers/organization.ts` to normalize organization payloads with Trello-compatible defaults and mappings for `prefs`, `billableMemberCount`, `idMemberCreator`, `powerUps`, `nodeId`, and `website`.
- Updated `server/extensions/trelloCompat/api/organizations/index.ts` field projection handling to return values from the canonical organization serializer output.
- Updated `specs/trello-adapter-normalization/matrix.md` with Sprint 152 organization endpoint statuses.

## New
- Added `server/extensions/trelloCompat/tests/unit/server/extensions/trelloCompat/organizations.contract.test.ts` to cover organization serializer contract behavior for `GET /organizations/{id}` and related organization response expectations.

## Technical Debt
- Organization visibility is currently optional in adapter inputs; data source parity for workspace-level visibility should be audited if upstream schema evolves.
- Full endpoint-level integration coverage for organization routes remains limited compared with serializer-focused contract tests.

## What Should Be Done Next
- Continue Sprint 152 normalization with search/custom field/error-envelope endpoints per checklist.
- Add integration-level organization route tests once shared trelloCompat route harness coverage expands.
