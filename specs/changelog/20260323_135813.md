## Update
- Updated `playwright.config.ts` to use Playwright `projects` for test discovery in both `tests/e2e` and `specs/tests` directories.
- Restricted Playwright to only run `*.spec.ts` files, preventing accidental execution of Bun unit tests (e.g., `board.test.ts`).
- Verified that 61 tests across 13 spec files are now discovered by Playwright.

## New
None.

## Technical Debt
None.

## What Should Be Done Next
- Re-run Playwright E2E tests to confirm all tests are discovered and passing.
- Continue with any remaining client-side polish, additional translations, or deferred integration tests for notification preferences.
- Begin Sprint 101 (API Token Infrastructure) as outlined in the overall plan.
