# Changelog — 2026-03-18T10:09:00Z

## Update
- Extracted `src/layout/TopBar.tsx` from `AppShell.tsx`: both the mobile and desktop topbar bars now live in a dedicated component (`TopBar`) with typed props (`onOpenDrawer`, `drawerOpen`). AppShell imports `TopBar` and passes the drawer state through.
- Normalised topbar height: mobile and desktop bars are now both `h-14` (56 px) for layout consistency.
- Added `min-w-0` to the main content wrapper in `AppShell` to prevent flex children from overflowing on narrow viewports.
- Fixed user-menu dropdown clipping in `Sidebar.tsx`: when the sidebar is collapsed (`w-16`, `overflow-hidden`), the `w-40` user-menu dropdown now positions to the right of the sidebar (`left-full bottom-0 ml-2`) instead of being clipped inside the narrow container.
- Finalised `specs/tests/sidebar-collapsible-drawer.md` with viewport matrix (six breakpoints: 375 → 1920 px), board/card page scenarios (Scenarios 22-26), a regression checklist (R-1 to R-8), and extended acceptance criteria (AC-20 to AC-24).

## New
- `src/layout/TopBar.tsx`: standalone topbar component replacing the inline topbar markup in `AppShell`.
- Viewport matrix in the test spec covering six canonical breakpoints with expected sidebar/topbar behaviour.
- Regression checklist for board/card page layout covering overflow, topbar stickiness, modal accessibility, focus trap isolation, and user-menu visibility in collapsed mode.

## Technical Debt
- Six pre-existing `exactOptionalPropertyTypes` TypeScript errors in `Sidebar.tsx` (NavItem/NavButton props) remain unfixed — pre-date this iteration and are unrelated to layout hardening.
- Playwright tests remain unexecutable due to the Bun/Playwright ESM loader compatibility issue documented in previous iteration.

## What Should Be Done Next
- Proceed to Iteration 7 (Sprint 86 server-side access-aware search filtering).
- Address the `exactOptionalPropertyTypes` TS errors in `Sidebar.tsx` as a separate chore.
- Investigate Playwright/Bun compatibility to enable automated test runs.
