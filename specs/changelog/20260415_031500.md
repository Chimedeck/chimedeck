# Changelog — 2026-04-15T03:15:00Z

## Update
- `src/layout/Sidebar.tsx`: Added "Webhooks" nav item below "API Tokens" in the user settings dropdown menu.
- `src/extensions/Workspace/translations/en.json`: Added `Sidebar.webhooks` translation key.
- `src/routing/index.tsx`: Registered lazy-loaded `WebhooksRegisterPage` at `/settings/webhooks`.
- `src/reducers.ts`: Exported `webhooksApi` from Webhooks slice.
- `src/store/index.ts`: Registered `webhooksApi` reducer and middleware in the Redux store.

## New
- `src/extensions/Webhooks/containers/WebhooksRegisterPage/WebhooksRegisterPage.tsx`: Main page listing webhooks in a table with empty state handling. Reads workspaceId from URL path, uses `useListWebhooksQuery` RTK Query hook. Edit/delete/register modals deferred to iteration 6–7.
- `src/extensions/Webhooks/routes.ts`: Route config for `/settings/webhooks` → `WebhooksRegisterPage`.
- `src/extensions/Webhooks/translations/en.json`: All UI strings for WebhooksRegisterPage (title, description, table columns, status badges, buttons, empty state).
- `tests/unit/webhooks/WebhooksRegisterPage.test.tsx`: Unit tests covering slice endpoint definitions, tag types, WebhookItem shape, and empty-state/table-render logic (13 tests, all passing).

## Technical Debt
- `WebhooksRegisterPage` "Register webhook", "Edit", and "Delete" buttons are wired to no-op handlers (`() => {}`); modals will be implemented in iterations 6 and 7.
- `useWorkspaceId()` helper in the page extracts workspace ID from URL — works for boards context; may need a Redux selector when used from a non-board route.

## What Should Be Done Next
- Iteration 6: `RegisterWebhookModal` + `WebhookCreatedModal`.
- Iteration 7: `EditWebhookModal` + `DeleteWebhookDialog`.
