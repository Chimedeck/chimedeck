# Changelog — 20260309_103341

## Update
- `server/config/env.ts`: Added `ALLOWED_EMAIL_DOMAINS` (comma-separated, defaults to `journeyh.io`) and `EMAIL_DOMAIN_RESTRICTION_ENABLED` (defaults to `true`) env vars.
- `server/extensions/auth/api/register.ts`: Added domain guard — returns `422 email-domain-not-allowed` before any DB work when domain restriction is enabled and the email domain is not in the allowlist.
- `server/extensions/auth/api/changeEmail.ts`: Same domain guard applied after password validation and before token generation.
- `src/extensions/Auth/components/SignupForm.tsx`: Added `email-domain-not-allowed` → `emailDomainNotAllowed` mapping in `API_ERROR_MAP`.
- `src/extensions/Auth/components/ChangeEmailForm.tsx`: Added `email-domain-not-allowed` case in the catch block to surface the user-friendly message.
- `src/extensions/Auth/translations/en.json`: Added `errors.emailDomainNotAllowed` and `changeEmail.emailDomainNotAllowed` translation keys.

## New
- `server/extensions/auth/common/emailDomain.ts`: New shared helper module with `extractDomain(email)` and `isEmailDomainAllowed(email)` — used by both registration and email-change routes.
- `tests/integration/auth/emailDomainRestriction.test.ts`: Integration tests covering helper unit tests and HTTP-level 422 behaviour for disallowed domains on registration.

## Technical Debt
- The `handleChangeEmail` domain-guard path is covered by the unit tests for `isEmailDomainAllowed`; a full HTTP-level test for `changeEmail` would require a running DB (or a more thorough mock).

## What Should Be Done Next
- Sprint 44: Add `POST /api/v1/admin/users` endpoint with admin-domain authentication and auto-generated password.
