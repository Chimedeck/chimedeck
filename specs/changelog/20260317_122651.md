## Update
- Registered the new offline drafts API router in `server/index.ts`.
- Updated service and API layers to enforce user isolation and error response conventions.

## New
- Added `server/extensions/offlineDrafts/mods/drafts.ts` with service functions for upsert, get, and delete of user card drafts.
- Added `server/extensions/offlineDrafts/api/index.ts` implementing GET, PUT, and DELETE endpoints for card drafts, fully scoped to the authenticated user.
- Created `specs/tests/offline-description-draft-recovery.md` with 17 Playwright MCP scenarios for server-side draft API.
- Added migration `db/migrations/0083_user_card_drafts.ts` for the `user_card_drafts` table with constraints and indexes.

## Technical Debt
- Playwright test file is not recognized due to unsupported file extension or directory; needs conversion or relocation for test execution.

## What Should Be Done Next
- Convert or move the Playwright test to a supported format/location so it runs with the test suite.
- Begin client-side offline draft storage and API integration.
- Review and address any edge cases around concurrent draft updates and error handling.
- Monitor for further technical debt as client integration proceeds.