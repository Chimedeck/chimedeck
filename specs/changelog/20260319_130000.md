## Update
- Extracted remaining hardcoded UI string from `ApiKeyRevealModal.tsx` — the
  warning copy ("This API key will never be shown again…") is now sourced from
  `src/extensions/Plugins/translations/en.json` under key
  `plugins.apiKeyModal.warning`.
- All `.tsx` files in `src/extensions/Plugins/` now have zero hardcoded
  English strings; every user-visible label, placeholder, aria-label, button
  text, and status message is driven by the translations JSON.

## New
- Added `specs/tests/sprint-92-iteration-7-plugins-i18n.md` — Playwright MCP
  markdown test verifying EditPluginModal, BoardPluginsPanel, PluginDomainAllowlist,
  and ApiKeyRevealModal UI copy is sourced from translations and that all
  functionality (editing, deleting, domain management, API key reveal) remains
  intact.
- Added this changelog: `specs/changelog/20260319_130000.md`.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Sprints 90–92 i18n extraction is fully complete across all five extensions
  (Comment, Activity, Attachment/Attachments, Automation, Plugins).
- Future work: consider a build-time lint rule (e.g. ESLint no-hardcoded-literals
  plugin) to prevent new hardcoded strings from being introduced.
