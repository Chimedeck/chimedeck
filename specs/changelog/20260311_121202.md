# Changelog — 2026-03-11T12:12:02Z

## Update
- `server/extensions/automation/engine/triggers/index.ts`: registers `board.member_added` and `list.card_added` triggers alongside the existing 13 card triggers; re-exports both new handlers
- `server/extensions/automation/api/index.ts`: added route `GET /api/v1/automation/trigger-types` handled by `handleGetTriggerTypes`
- `server/extensions/automation/api/create.ts`: imports `validateTrigger` and validates trigger type + config against the registry before persisting; imports trigger index to populate registry at request time
- `server/extensions/automation/api/update.ts`: same registry-backed trigger validation added to the PATCH path
- `tests/integration/automation/triggers.test.ts`: expanded registry assertion to 15 types; added positive/negative tests for `board.member_added` and `list.card_added`

## New
- `server/extensions/automation/engine/triggers/board/memberAdded.ts`: `board.member_added` trigger handler with optional `memberId` filter
- `server/extensions/automation/engine/triggers/list/cardAdded.ts`: `list.card_added` trigger handler matching both `card.created` and `card.moved` events against a required `listId`
- `server/extensions/automation/api/triggerTypes.ts`: `GET /api/v1/automation/trigger-types` endpoint returning all registered trigger types with JSON Schema config shapes (via `z.toJSONSchema`)

## Technical Debt
- Trigger config validation in `create.ts` / `update.ts` does not yet validate action configs against a registry (deferred to Sprint 63 action handler iteration)

## What Should Be Done Next
- Iteration 12: Implement 14 card action handlers under `server/extensions/automation/engine/actions/card/` and the `variables.ts` substitution module (Sprint 63)
