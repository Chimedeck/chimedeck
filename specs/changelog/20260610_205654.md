## Update
- **`server/config/subscription-tiers.ts`** — Added `'agenticWorkflow'` to `BooleanFeatureKey` union; set `true` on Business (`tier_4`) and Enterprise tiers, `false` on Personal, Hobby, and Pro.
- **`server/extensions/subscription/common/featureKeys.ts`** — Registered canonical key `phase: { agenticWorkflow: 'phase:agentic-workflow' }` for tier-gate resolution.

## New
- **`server/extensions/triggers/`** — New extension with 11 source files implementing the Sprint 173 trigger engine:
  - `common/config/` — Feature flag key `TRIGGER_ENGINE_ENABLED`, `PHASE_TIER_MINIMUMS` map (`SYNC_DOCUMENT: tier_2`, `GENERATE_SPRINT: tier_3`, `UPDATE_AS_BUILT: tier_4`), retry policy (max 3 attempts, 1s/4s/16s backoff).
  - `mods/tierGate/` — `evaluatePhaseTierEligibility` gates on `agenticWorkflow` boolean + phase minimum tier, returns `{ allowed, requiredTier, reason }` with upgrade hint.
  - `mods/worker/` — `enqueuePhaseTriggerRun` validates tier eligibility, persists QUEUED run, fires async `runTrigger`. Dispatch calls stub per-phase processors (real workflows deferred to Sprint 175/176), retries with exponential backoff, marks FAILED after max attempts, and creates an attempt row per retry.
  - `mods/idempotency/` — Deterministic keys `cardId:listId:phase:moveEventId` with app-level UNIQUE guard in transaction.
  - `mods/persistence/` — DB helpers with state machine validation: QUEUED → RUNNING → SUCCEEDED | FAILED.
  - `mods/deadLetter/` — In-memory dead-letter log with `console.error` for exhausted retries.
  - `mods/activities/` — Fire-and-forget activity event emission via `dispatchEvent`.
- **Tests: 38 tests across 4 files** — tierGate (12), dispatch (6), persistence (13), idempotency (7). All passing.

## Technical Debt
- Idempotency enforced at application layer (transaction + `SELECT … FOR UPDATE`) — assumes iteration 6 migration adds UNIQUE `(card_id, list_id, phase, move_event_id)` constraint.
- Downstream phase processors are stubs (`logIntent`) — real workflow plugins deferred to Sprint 175/176.
- Dead-letter log is in-memory only — durable persistence to DB table deferred to Sprint 174.
- No stuck-run sweeper — server restart mid-run leaves runs in RUNNING status (deferred to Sprint 174).
- Timeline API (`GET /cards/:cardId/phase-trigger-runs`) and activity event emission on trigger transitions deferred to next iteration.

## What Should Be Done Next
- **Iteration 6 (evaluator hook):** Build card-move hook that calls `resolveColumnWorkflowPhases` → `enqueuePhaseTriggerRun`, wiring trigger engine into the move-event path.
- **DB migration:** Create `card_phase_trigger_runs` and `card_phase_trigger_attempts` tables with the idempotency UNIQUE constraint that iteration 7's code assumes.
- **Timeline API:** `GET /api/v1/cards/:cardId/phase-trigger-runs` endpoint returning paginated run+attempt history.
- **Observability:** Activity events for trigger run status transitions (`card_phase_trigger_queued`, `card_phase_trigger_running`, etc.).
- **Sprint 174 cleanup:** Stuck-run sweeper, durable dead-letter persistence.
