# Changelog — Sprint 94 Iteration 7: Realtime + OfflineDrafts + BoardViews i18n

**Date:** 2026-03-19  
**Sprint:** 94  
**Phase:** i18n Phase 5 — Realtime, OfflineDrafts, BoardViews

---

## Update

- `src/common/components/ConnectionBadge.tsx`: Replaced hardcoded "Live", "Reconnecting…", "Offline" strings with `translations['Realtime.statusLive']`, `translations['Realtime.statusReconnecting']`, `translations['Realtime.statusOffline']` bracket notation. Added import from Realtime translations.
- `src/extensions/Realtime/PollingIndicator.tsx`: Replaced hardcoded "Polling" text and tooltip string with `translations['Realtime.statusPolling']` / `translations['Realtime.statusPollingTitle']`.
- `src/extensions/Realtime/components/PresenceAvatars.tsx`: Replaced hardcoded `aria-label="Active board members"` with `translations['Realtime.ariaActiveBoardMembers']`. Added translations import.
- `src/extensions/BoardViews/BoardCommentsPanel.tsx`: Replaced "Comments", "No comments yet.", "Loading…", "Load more", "Failed to load comments." with translations bracket notation.
- `src/extensions/BoardViews/BoardActivityPanel.tsx`: Replaced "Failed to load activity." with translation key.
- `src/extensions/BoardViews/BoardArchivedCardsPanel.tsx`: Replaced "Archived Cards", "Loading…", "No archived cards.", "in {list_title}", "Restoring…", "Restore", "Failed to load archived cards.", "Failed to restore card." with translations bracket notation.

---

## New

- `src/extensions/Realtime/translations/en.json`: Created with 6 keys covering connection status labels (Live, Connecting, Reconnecting, Offline, Polling) and presence aria label.
- `src/extensions/OfflineDrafts/translations/en.json`: Created with 6 keys covering draft banner, restore/discard buttons, saved/syncing indicators, and close aria label.
- `src/extensions/BoardViews/translations/en.json`: Created with 20 keys covering view switcher labels (Kanban/Table/Calendar/Timeline) and all panel UI strings (comments, archived cards, activity, load more, restore).
- `specs/tests/sprint-94-realtime-offlinedrafts-boardviews-i18n.md`: Playwright MCP test plan with 6 test scenarios and a grep verification command.

---

## Technical Debt

- `OfflineDrafts` extension has no `.tsx` UI components yet (only hooks and utility `.ts` files). The translations file is created and ready for when UI components are added.
- `BoardViewSwitcher/translations/en.json` exists but `BoardViewTab.tsx` and `BoardViewSwitcher.tsx` still use hardcoded label constants and `aria-label="Board view"`. These are covered by the `BoardViews/translations/en.json` scope but were not migrated in this iteration since they belong to a different extension folder. Should be addressed in Iteration 8 or a follow-up.
- Connection status keys "Connecting…" / "Connection error" from the sprint-94 spec are not yet used in any `.tsx` component; they are documented in the translation file for future use.

---

## What Should Be Done Next

- **Iteration 8**: Migrate `src/common/` and `src/layout/` shared components (modal, toast, nav, empty-state strings) to `src/common/translations/en.json`. Run final smoke test grep to verify zero hardcoded strings remain across all `src/extensions/`, `src/common/`, and `src/layout/`.
- Update `BoardViewSwitcher/BoardViewTab.tsx` and `BoardViewSwitcher/BoardViewSwitcher.tsx` to use the existing `BoardViewSwitcher/translations/en.json` (currently populated but unused).
- When `OfflineDrafts` UI components are added, wire them to `OfflineDrafts/translations/en.json`.
