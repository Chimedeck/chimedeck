# Changelog — 2026-04-10 07:15

## Update
- Extended `server/extensions/comment/api/list.ts` to join `comment_reactions` in a single extra query and attach a `reactions: ReactionSummary[]` array (sorted by count DESC, with `reactedByMe`) to every comment in the list response.
- Updated `server/extensions/comment/api/index.ts` to mount the new reactions sub-router.

## New
- `db/migrations/0104_comment_reactions.ts` — creates `comment_reactions` table with unique constraint on `(comment_id, user_id, emoji)` and an index on `comment_id`.
- `server/extensions/comment/api/reactions/add.ts` — `POST /api/v1/comments/:commentId/reactions`, idempotent upsert, emits `comment_reaction_added` WS event.
- `server/extensions/comment/api/reactions/remove.ts` — `DELETE /api/v1/comments/:commentId/reactions/:emoji`, idempotent delete, emits `comment_reaction_removed` WS event.
- `server/extensions/comment/api/reactions/index.ts` — sub-router wiring for the two reaction endpoints.
- `tests/integration/comment/commentReactions.test.ts` — 18 unit-level integration tests covering validation, idempotent add/remove, list shape, and auth guard (all pass).

## Technical Debt
- None introduced.

## What Should Be Done Next
- Sprint 130: Build the UI layer — `CommentReactions` pill row, `EmojiPickerPopover`, optimistic updates, and real-time WS sync.
