# Changelog — 20260324_081334

## Update
- Verified `specs/tests/board-plugin-discovery.md` covers all Sprint 114 Board Plugin Discovery acceptance criteria.

## New
- `specs/tests/board-plugin-discovery.md`: Playwright MCP scenario markdown covering six flows — (1) empty Enabled state on a clean board, (2) enabling a plugin moves it from Discover to Enabled, (3) disabling moves it back to Discover, (4) search input filters Discover results with debounce, (5) category filter narrows Discover results, (6) empty Discover state when the global registry has no active plugins. No `.spec.ts` code — MCP markdown only.

## Technical Debt
- None introduced this iteration.

## What Should Be Done Next
- Run the Playwright MCP scenario in `board-plugin-discovery.md` against the dev server to confirm all steps PASS.
- Begin Sprint 115 if all Sprint 113/114 scenarios are green.
