## Update
- Refactored `server/extensions/trelloCompat/api/search/index.ts` to use robust modelType/modelTypes param parsing (accepts singular/plural forms).
- Routed `/search` and `/search/members` responses through canonical normalized serializers.
- Updated `specs/trello-adapter-normalization/matrix.md` with PASS/PARTIAL/MISMATCH statuses for Search endpoints after changes.

## New
- Added `server/extensions/trelloCompat/serializers/search.ts` with canonical search response serializers.
- Added `server/extensions/trelloCompat/tests/unit/server/extensions/trelloCompat/search.contract.test.ts` for contract coverage of `/search` and `/search/members` endpoints, including edge cases and param variants.

## Technical Debt
- Some field projection endpoints for cards/boards remain only partially Trello-compatible.
- Nested resource normalization is still partial in some cases.
- API endpoints for `/search` and `/search/members` are not reachable on the running dev server (net::ERR_ABORTED/404); root cause not yet identified.

## What Should Be Done Next
- Investigate and resolve why `/search` and `/search/members` endpoints are not available on the dev server.
- Ensure all four arrays are always present in `/search` responses, even if empty.
- Complete field projection and nested resource normalization for full Trello compatibility.
- Expand contract tests to cover additional edge cases and error handling.
- Re-verify endpoint-response-checklist compliance after fixes.
