## Update
- Fixed card description link-mode persistence bug where mode-1 links (URL mode) with custom display text were reclassified as mode-2 after save/view/edit round-trip.
- Mode selection is now restored from persisted metadata instead of relying on display-text heuristics.

## New
- Persisted link mode in markdown-safe metadata via link `title` token (`cd-mode:url|button|card`).
- Added mode restore from title metadata in:
  - edit-mode link detection
  - view-mode preview hydration
  - editor rehydration path (`hydrateEditorLinkMarkClasses`)

## Technical Debt
- The editor file contains pre-existing lint issues unrelated to this fix; these should be cleaned in a dedicated lint pass.

## What Should Be Done Next
- Add regression test for this exact flow:
  - set link to mode-1
  - set display text to "Test"
  - save and reopen view
  - verify still mode-1 in view and edit
