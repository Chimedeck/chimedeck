## Update
- Updated `src/layout/AppShell.tsx` to use the new layout-level `Sidebar` component and integrate sidebar collapsed state.

## New
- Added `src/layout/hooks/useSidebarState.ts`: Custom React hook for managing sidebar collapsed state with `localStorage` persistence (`sidebar_collapsed` key).
- Added `src/layout/Sidebar.tsx`: Collapsible sidebar with expand/collapse toggle, icon-only mode, tooltips, keyboard accessibility, and smooth width transitions.
- Added `specs/tests/sidebar-collapsible-drawer.md`: Playwright MCP test spec with 12 scenarios for sidebar toggle, icon/label visibility, tooltip, keyboard accessibility, and persistence.

## Technical Debt
- Playwright `.test.ts` files for sidebar are not yet generated; only Markdown spec exists.
- Automated test execution blocked by dev server permission issues.

## What Should Be Done Next
- Convert Markdown sidebar test spec to executable Playwright `.test.ts` files and run automated tests.
- Resolve dev server permission issues to enable test runs.
- Address any UI/UX polish and accessibility edge cases found during testing.
- Plan and implement mobile drawer behavior in the next iteration.