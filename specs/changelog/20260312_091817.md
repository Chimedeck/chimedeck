## Update
- Added `usePgCron` and `schedulerEnabled` flags to `server/extensions/automation/config/index.ts` for automation scheduler configuration.
- Updated `server/extensions/automation/engine/index.ts` to expose `execute()` for direct scheduler invocation.
- Modified `server/index.ts` to wire in `startAutomationScheduler()` at startup, conditionally using LISTEN or Worker fallback.
- Extended `.env.example` to document `AUTOMATION_USE_PGCRON` and `AUTOMATION_SCHEDULER_ENABLED`.

## New
- `scheduler/listener.ts`: Bun-native PostgreSQL LISTEN client for `automation_tick` channel with reconnect logic.
- `scheduler/workerFallback.ts`: Worker fallback using setInterval for local/dev environments.
- `scheduler/index.ts`: Entry point to start scheduler in LISTEN or Worker mode.
- `tests/integration/automation/scheduler.test.ts`: Integration tests for scheduler (9 tests, all passing).

## Technical Debt
- No Playwright E2E tests for automation scheduler flows yet.
- Some error handling and reconnection edge cases may need further refinement.
- Deferred features: TimelineView arrows, registry caching, and some server-side invariants.

## What Should Be Done Next
- Scaffold and implement Playwright E2E tests for automation scheduler flows (startup, fallback, config toggling, DB reconnect, end-to-end integration).
- Continue with UI and server endpoints for Rules Builder, Buttons, Schedule, Log, etc., in subsequent sprints.
- Monitor and address technical debt and edge cases as automation features evolve.
