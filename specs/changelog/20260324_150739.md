## Update
- Updated `api.ts` to add `fetchBoardAvailablePlugins` for board-scoped plugin discovery.
- Modified `PluginDashboardPage.duck.ts` to add `fetchDiscoverablePluginsThunk` and integrate board-specific plugin fetching.
- Updated `useBoardPlugins.ts` to use the new thunk for loading discoverable plugins.
- Refactored `PluginDashboardPage.tsx` to a two-section layout (Enabled/Discover), with new empty states and improved filter/search handling.
- Extended `translations/en.json` with new keys for dashboard sections and actions.

## New
- Added `EnabledPluginRow.tsx` (enabled plugin row component).
- Added `DiscoverPluginRow.tsx` (discoverable plugin row component).
- Added `DiscoverPluginSearch.tsx` (debounced search and category filter component).
- Created Playwright MCP test spec: `specs/tests/board-plugin-discovery.md` (covers all dashboard flows and edge cases).

## Technical Debt
None

## What Should Be Done Next
- Add MCP config (e.g., `.vscode/mcp.json`) to enable Playwright MCP test execution.
- Rerun Playwright MCP tests for `plugin-registry-admin.md` and `board-plugin-discovery.md` once MCP is available.
- Report test results and screenshots for all flows after MCP is configured.
