# Changelog — 2026-03-18T03:25:57Z

## Update
- `src/reducers.ts` — exports new `searchReducer` from Search slice.
- `src/store/index.ts` — registers `search: searchReducer` in the Redux store.
- `specs/tests/search-board-access-rights.md` — extended with stale-cache UI scenarios (SBAR-UI-05 through SBAR-UI-10) and acceptance checklist items 15–19.

## New
- `src/extensions/Search/slices/searchSlice.ts` — Redux slice for client-side search result cache with `setSearchResults`, `purgeInaccessibleResult`, and `clearSearchCache` actions.
- `src/extensions/Search/components/SearchResults.tsx` — grouped results component with stale-result safety: verifies board access on click; on 403/404 shows a neutral amber banner, purges the stale entry from the cache via `purgeInaccessibleResult`, and redirects to `/workspaces/:workspaceId/boards` after 1.8 s. Card results and 5xx errors bypass the pre-check to avoid false positives.

## Technical Debt
- `WorkspaceModal.test.tsx` carries pre-existing mock-store shape errors (missing many slice keys); these are not caused by this iteration and should be fixed in a dedicated test-hardening pass.

## What Should Be Done Next
- Iteration 9: Add structured server-side logs/metrics for permission-filtered search results and denied board-open attempts, then finalise all three Markdown test specs for Playwright MCP execution with explicit PASS/FAIL checklist entries tied to each sprint acceptance criterion.
