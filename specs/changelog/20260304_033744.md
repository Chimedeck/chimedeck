# Changelog — 2026-03-04T03:37:44Z

## Update
- `server/extensions/board/api/index.ts`: PATCH `/api/v1/boards/:id` now routes to `handlePatchBoard` (supports both `title` and `monetization_type`).

## New
- `db/migrations/0020_board_monetization.ts`: Adds nullable `monetization_type` column (`'pre-paid' | 'pay-to-paid' | null`) to the `boards` table.
- `server/extensions/board/types.ts`: Defines `MonetizationType` and `Board` TypeScript types for the board feature.
- `server/extensions/board/api/patch.ts`: PATCH handler for board settings; validates `monetization_type` against the allowed enum (`null`, `'pre-paid'`, `'pay-to-paid'`), returns 400 for invalid values, and emits a `board_updated` event. Also handles `title` updates (supersedes `update.ts` for the PATCH route).

## Technical Debt
- `server/extensions/board/api/update.ts` is no longer wired to the PATCH route but remains in the codebase. It can be removed or consolidated into `patch.ts` in a future cleanup iteration.

## What Should Be Done Next
- Iteration 2: Add `monetization_type` to the client `Board` type and implement the Monetization radio section in `BoardSettings`.
