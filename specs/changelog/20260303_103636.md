## Update
- Fixed incorrect relative import paths in `src/extensions/Card/containers/CardModal/sections/` (ChecklistSection, LabelsSection, MembersSection) — changed `../../` to `../../../` to correctly resolve Card-level modules.
- Fixed `WorkspaceMember` type mismatch in `MembersSection.tsx`: aligned local interface `{ name: string | null }` with `MemberAssignModal`'s expected shape (was `display_name`).
- Fixed `exactOptionalPropertyTypes` TS error in `CardMemberAvatars.tsx`: switched to spread-based conditional prop passing for `onRemove` to avoid passing `undefined` to a non-undefined optional.
- Fixed `exactOptionalPropertyTypes` TS error in `LabelsSection.tsx`: switched to spread-based conditional prop passing for `LabelChip.onRemove`.

## New
- Sprint 28 implementation was already present from previous agent run: `MemberAvatarPopover`, `CardMemberAvatars` (button-based), `CardItem` (Redux-connected), `MembersSection` (modal, with popover), `EditProfilePage` (stub), and `/profile/edit` route.
- Server `list.ts` already aggregates both labels and members per card via LEFT JOINs.

## Technical Debt
- Remaining pre-existing `exactOptionalPropertyTypes` errors in `CardChecklist.tsx`, `CardLabels.tsx`, `ChecklistSection.tsx` (components layer) are out of scope for this iteration.
- Playwright E2E tests for Sprint 27/28 still deferred.
- Real-time member removal propagation (refreshing card list after remove) is not yet implemented.

## What Should Be Done Next
- Implement Playwright E2E tests for label chip toggle and member avatar popover flows.
- Refresh card list state after a member is removed from a card tile.
- Fix remaining `exactOptionalPropertyTypes` TS errors in Card components layer.
- Complete real-time profile propagation and global middleware/rate-limiting.
