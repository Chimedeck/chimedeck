## Update
- Fixed card description view-mode links so browser-native right-click "Open link in new tab" opens the clean URL, not a wrapped markdown token URL.
- Added href normalization in rendered HTML pass for all preview anchors before display.

## New
- `normalizeRenderedLinkHtml` now rewrites each rendered `<a href>` via `normalizePreviewLinkHref` before duplicate-link merge/hydration styling.

## Technical Debt
- Card/comment link normalization logic is still duplicated; consolidate into shared utility to reduce drift.

## What Should Be Done Next
- Add a regression test for card description view mode that validates:
  - markdown destination accidentally containing `[label](url)` is normalized to `url`
  - native browser new-tab navigation uses the normalized href
