## Update
- Refactored `scripts/deploy.sh` to remove Docker build/ECR push logic; now sources `.ecr-build-output` and dispatches on `USE_SSH` for SSH or Terraform deploys.
- Enhanced rollback logic for Terraform deploys: snapshots `terraform.tfvars`, flips slot weights/tags, and restores on failure.
- Updated `scripts/set_environment.sh` with comment-only documentation for new environment variables (`USE_SSH`, `DEPLOYMENT_MODE`, `USE_RDS`, `ENV_NAME`).
- Extended `.gitignore` to cover `.ecr-build-output`, Terraform state/plan/rollback files, and `/tmp/*.pem`.

## New
- Added `scripts/build.sh`: builds Docker image, pushes to ECR, writes `.ecr-build-output` with image/tag info, and cleans up AWS credentials.

## Technical Debt
None

## What Should Be Done Next
- Scaffold `terraform/` top-level: add `.terraform-version`, `README.md`, and `bootstrap/` (S3 state bucket, DynamoDB lock table).
- Implement .umbra/ CI templates (production-fleet.yml, production-asg.yml) for deployment automation.
- Review and test blue/green slot flipping and rollback logic in real deploy scenarios.
- Ensure all secrets remain excluded from `.ecr-build-output` and logs.