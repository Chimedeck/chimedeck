## Update
- **Wired sprintGeneration activities to real activity persistence.** Replaced `console.log` stubs in `server/extensions/sprintGeneration/mods/activities/index.ts` with calls to `writeActivity()` + `publishCardActivityEvent()`, making all sprint generation lifecycle events (started, artifact_created, card_created, quota_exceeded, completed, failed) persist to the DB and broadcast in realtime.
- **Wired asBuiltSync activities to real activity persistence.** Replaced `console.log` stubs in `server/extensions/asBuiltSync/mods/activities/index.ts` with calls to `writeActivity()` + `publishCardActivityEvent()`, covering all sync lifecycle events (started, evidence_collected, docs_updated, committed, completed, failed).
- **Registered 12 new activity event types** in the server visibility whitelist (`server/extensions/activity/config/visibleEventTypes.ts`) and client visibility + label configs (`src/extensions/Card/config/activityEventsConfig.ts`, `activityEventLabels.ts`): `sprint_generation_started`, `sprint_generation_artifact_created`, `sprint_generation_card_created`, `sprint_generation_quota_exceeded`, `sprint_generation_completed`, `sprint_generation_failed`, `as_built_sync_started`, `as_built_sync_evidence_collected`, `as_built_sync_docs_updated`, `as_built_sync_committed`, `as_built_sync_completed`, `as_built_sync_failed`.
- **Integrated CardActivity renderer into card modal ActivityFeed.** Modified `src/extensions/Card/containers/CardModal/ActivityFeed.tsx` to accept callback props (`onFileClick`, `onApprove`, `onRerun`, `onEdit`) and delegate sprint-gen/as-built events to `ActivityEventRenderer` for rich rendering instead of the default text-only label.
- **Fixed cross-test contamination in sprintGeneration tests.** Resolved 17 test failures caused by conflicting `mock.module` registrations between `quotaEnforcer.test.ts` (now uses dependency injection instead of mocking `../index`) and `generateArtifacts.test.ts` (fixed stale assertions: `## Test Plan` → `## Test Scenarios`, dependency graph/test matrix checks now reference top-level response fields, round-robin count made flexible).

## New
- **CardActivity client extension** (`src/extensions/CardActivity/`) with rich event rendering:
  - `components/ActivityEventRenderer.tsx` — dispatcher that maps event types to rich components, falling through to default label rendering for unrecognized types.
  - `renderers/SprintGenEvent.tsx` — renders sprint generation events with `GeneratedDiffSummary`, `SprintArtifactLinks`, and `ApproveRerunControls` based on event payload.
  - `renderers/AsBuiltEvent.tsx` — renders as-built sync events with diff summary and controls.
  - `index.ts` — barrel export for `ActivityEventRenderer` and `isRichEventType` helper.

## Technical Debt
- `isRichEventType` export is declared in `CardActivity/index.ts` but not yet implemented; `CardChatDrawer.test.tsx` fails due to `document` globals not polyfilled in Bun test runner (pre-existing).
- `quotaEnforcer.ts` has a module-scoped `quotaEnforcerDeps` that tests mutate directly — should be refactored to use proper dependency injection (factory or constructor pattern).
- `generateArtifacts` round-robin distribution heuristic may duplicate requirements on the last sprint when `ceil(N/sprintCount)` leaves an empty trailing bucket; the fallback assigns the full requirement set.

## What Should Be Done Next
- Implement `isRichEventType()` helper in `CardActivity/index.ts` to correctly gate which event types receive rich rendering.
- Write unit tests for `CardActivity` renderers (`ActivityEventRenderer`, `SprintGenEvent`, `AsBuiltEvent`).
- Wire `onFileClick`, `onApprove`, `onRerun`, `onEdit` callbacks from the CardModal container into `ActivityFeed`.
- Integrate `requiresHumanApproval` flag from tier policy metadata into `ApproveRerunControls`.
- Add `@happy-dom/global-registrator` or equivalent DOM polyfill for Bun test runner to fix 6 failing client tests in `CardChatDrawer`, `RefinementStatusBadge`, and related components.
- Deploy server and verify activity events appear in card feed with rich rendering in staging.
