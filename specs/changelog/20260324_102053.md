## Update
- None.

## New
- Created `tests/e2e/_helpers.ts` with shared Playwright helpers for user registration, workspace, board, list, and card creation.
- Added `tests/e2e/custom-fields-api.spec.ts` with 12 API tests for custom field CRUD, validation, and access guards.
- Added `tests/e2e/custom-field-values-api.spec.ts` with 12 API tests for value upsert, retrieval, deletion, and access guards.
- Added `tests/e2e/custom-fields-board-panel.spec.ts` with 6 UI tests for the Board Settings custom fields panel.
- Added `tests/e2e/custom-fields-ui.spec.ts` with 6 UI tests for card modal custom field editing and badge display.
- Copied all four related `.md` scenario files to `specs/tests/` as references and deleted them from `tests/e2e/`.

## Technical Debt
- Playwright e2e tests for custom fields fail immediately due to `TypeError: Cannot read properties of undefined (reading 'access_token')` in the `registerAndLogin` helper. No UI or API flows were exercised. Root cause is likely a backend or test setup issue.

## What Should Be Done Next
- Investigate and fix the login helper so that it reliably returns a valid `access_token`.
- Re-run all new Playwright e2e tests for custom fields after fixing authentication.
- Use the recap agent's summary to inform the next actionable iteration, following the scope constraint (max 2 intertwined features or 1 standalone feature).