## Update

- **CardModal → ActivityFeed callback wiring:** Added `onFileClick`, `onApprove`, `onRerun`, and `onEdit` to the CardModal component Props chain (type → destructure → ActivityFeed pass-through). Defined handler stubs in the CardModal container with TODO markers for real API wiring.
- **SprintGeneration client test imports:** Converted `ApproveRerunControls.test.tsx` and `GeneratedDiffSummary.test.tsx` from `bun:test`/`mock()` to `vitest`/`vi.fn()` — both now pass in vitest (12 tests).
- **Test runner expanded:** `run-cardchat-tests.sh` now covers 14 files/141 tests, adding `pathGuard` and `tierPolicy` tests that fail in bulk due to Bun `mock.module` cross-file module-cache contamination.

## New

- **CardActivity renderer tests** (`src/extensions/CardActivity/components/__tests__/ActivityEventRenderer.test.tsx` — 21 tests): `isRichEventType` recognises all 12 registered event-type prefixes (sprint-gen & as-built) and rejects false positives; `ActivityEventRenderer` dispatches SprintGenEvent, AsBuiltEvent, null metadata fallback, and passes through click/approve/re-run/edit callbacks.
- **Comprehensive test isolation script** (`run-cardchat-tests.sh`): runs each Bun test file in a separate process to avoid `mock.module` cross-file deadlocks, covering all 141 sprint 171–176 server tests.

## Technical Debt

- Approve/re-run/edit handlers in CardModal container are empty stubs with TODO markers — real API calls (e.g., POST to `/api/v1/sprints/:id/approve`) need wiring in a future iteration.
- `requiresHumanApproval` from tier-policy metadata is not yet plumbed into `ApproveRerunControls` — the component currently renders unconditionally.
- Bun `mock.module` cross-file module-cache contamination is a known limitation (not sprint-specific) — pre-existing `stateTransitions` tests also exhibit this. The `run-cardchat-tests.sh` per-file isolation pattern should be extended to stateTransitions in a separate cleanup pass.

## What Should Be Done Next

- Wire real API handlers for approve, re-run, and edit callbacks in the CardModal container.
- Plumb `requiresHumanApproval` from `enforceQuota` tier-policy metadata into `ApproveRerunControls` UI.
- Add `SprintArtifactLinks` component tests.
- Extend the test-isolation runner pattern to `stateTransitions` to resolve pre-existing cross-file failures (46 tests, not sprint 171–176).
