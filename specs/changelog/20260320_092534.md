## Update
- Modified `BoardSettings.tsx` to add a "User settings" section with a notification toggle for board members.
- Updated `en.json` with four new translation keys for notification toggle UI and error messages.

## New
- Added `BoardNotificationToggle.tsx`: toggle component for per-board notification preference, with optimistic PATCH, error handling, and accessible UI.

## Technical Debt
- None

## What Should Be Done Next
- Resolve dev server port conflict and ensure Playwright tests for BoardSettings are present and passing.
- Add global notification toggle in profile settings for next iteration.
- Improve error display and fallback logic if needed after user feedback.
