## Update
- Implemented Trello compatibility member routes at `/trello/1/members/*` with support for profile read/update, field reads, and sub-resources for boards, cards, organizations, notifications, and actions.
- Implemented Trello compatibility organization routes at `/trello/1/organizations/*` with workspace CRUD, board/member/memberShip reads, invite-by-email, member role updates, and member removal.
- Wired the new members and organizations routers into the main Trello compatibility router.

## New
- Added `server/extensions/trelloCompat/api/members/index.ts`.
- Added `server/extensions/trelloCompat/api/organizations/index.ts`.
- Added `server/extensions/trelloCompat/serializers/organization.ts`.
- Added integration tests:
  - `tests/integration/trelloCompat/members.test.ts`
  - `tests/integration/trelloCompat/organizations.test.ts`

## Technical Debt
- Trello member notifications/actions endpoints are currently placeholder empty arrays pending sprint-148 action/event compatibility work.
- Trello compat role/access helpers remain duplicated across resource routers and should be centralized in a shared module.

## What Should Be Done Next
- Continue with sprint-148 actions/search/custom-fields implementation and replace placeholder member action/notification responses with mapped activity/notification records.
- Consolidate Trello compat permission and role mapping utilities into shared helpers to reduce repeated logic across routers.
