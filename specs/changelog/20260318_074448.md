# Changelog — 2026-03-18T07:44:48Z

## Update
- `server/extensions/board/types.ts`: Added `GuestType` union type (`'VIEWER' | 'MEMBER'`) and `BoardGuestAccess` interface with the new `guest_type` field alongside existing fields.

## New
- `db/migrations/0085_guest_role_type.ts`: Non-destructive Knex migration that adds a `guest_type` enum column (`VIEWER | MEMBER`, `NOT NULL`, default `VIEWER`) to `board_guest_access`. All existing guest rows implicitly migrate to `VIEWER`.
- `server/extensions/board/mods/guestPermissions.ts`: Stateless permission helpers — `guestCanWrite(guestType)` returns `true` only for `MEMBER`, and `guestDeniedError(guestType)` returns the correct 403 error name (`guest-viewer-insufficient-permissions` or `guest-role-no-org-access`).
- `specs/tests/sprint-89-guest-role-split.md`: 17 Playwright MCP test scenarios covering migration correctness, VIEWER/MEMBER permission enforcement at API and UI layers, guest invite defaults, type update, and list response shape.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 2: Extend `boardVisibility.ts` middleware to attach `req.guestType`; update invite, list, and add update guest endpoints.
