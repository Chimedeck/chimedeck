## Update
- Updated `server/index.ts` to mount the Trello compat router after all `/api/*` routes, ensuring no impact on existing APIs.
- Updated `server/mods/flags/index.ts` and `server/mods/flags/defaults.ts` to add `TRELLO_COMPAT_ENABLED` flag and helper.
- Updated `.env.example` to include `TRELLO_COMPAT_ENABLED=false`.

## New
- Scaffolded Trello compat extension under `server/extensions/trelloCompat/`:
  - `index.ts`: Entry point and router mount logic.
  - `api/index.ts`: Main API router with `/members/me` stub.
  - `middlewares/trelloAuth.ts`: Middleware for Trello-style token auth.
  - `types/trello.ts`: Trello-compatible type definitions.
  - `serializers/member.ts`, `serializers/label.ts`, `serializers/position.ts`: Serializers for Trello shapes.
  - `common/errors.ts`: Trello error helpers.
- Added feature flag gating for all `/trello/1/*` endpoints.
- Added tests:
  - `tests/integration/trelloCompat/auth.test.ts` (auth, flag, error shape, router isolation)
  - `tests/unit/server/extensions/trelloCompat/serializers.test.ts` (member/position serialization)

## Technical Debt
- Cannot fully test `/trello/1/members/me` with valid/invalid tokens due to missing/invalid test credentials. Auth flows are blocked until a working test user is available.
- Pre-existing typecheck errors outside this iteration remain unresolved.

## What Should Be Done Next
- Provision a valid test user or DB seed/reset step to enable end-to-end Trello compat auth testing.
- Implement Trello boards endpoints and serializers for next iteration.
- Expand test coverage for Trello compat flows once credentials are available.
- Monitor for any side effects on `/api/*` routes as new compat endpoints are added.