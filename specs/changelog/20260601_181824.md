## Update
- Verified the subscription feature-gate wiring already in place for workspace-, board-, and card-scoped routes.
- Confirmed gated endpoints return the expected `402 feature-not-in-plan` envelope with `requiredTier` and `upgradeUrl`.
- Re-tested live API coverage for allowed and blocked routes, including workspace-scoped bypass behavior.

## New
- No new code was added in this iteration.
- Added this changelog entry to capture the current implementation state and validation results.

## Technical Debt
- Card-scoped custom-field access is still broken because `cards.board_id` is missing, causing a `500` on the live path.
- The blocked UI flow did not surface a distinct upgrade modal; the API response is still the primary user-facing signal.
- Subscription usage and entitlement logic still rely on coarse aggregation and tier-alignment assumptions from earlier iterations.

## What Should Be Done Next
- Fix the card-scoped lookup so custom-field routes can resolve the parent board cleanly.
- Add/verify the upgrade modal or billing redirect flow for `402 feature-not-in-plan` responses.
- Continue the remaining subscription roadmap items: member/guest/storage caps, shared-workspace rate limiting, and billing UI/navigation.
