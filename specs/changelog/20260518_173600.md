## Update
- Implemented full Trello checklist compatibility routes at `/trello/1/checklists/*`, including checklist CRUD, field-level reads/updates, checkItem create/read/delete, checklist copy via `idChecklistSource`, and board/cards sub-resources.
- Implemented full Trello label compatibility routes at `/trello/1/labels/*`, including create/read/update/delete plus field-level update endpoints for `name` and `color`.
- Wired checklist and label routers into the main Trello compatibility router.

## New
- Added checklist API module: `server/extensions/trelloCompat/api/checklists/index.ts`.
- Added label API module: `server/extensions/trelloCompat/api/labels/index.ts`.
- Added checklist and label test suites:
  - `server/extensions/trelloCompat/tests/checklists.test.ts`
  - `server/extensions/trelloCompat/tests/labels.test.ts`
- Extended Trello-specific error helpers with checklist/label not-found responses.

## Technical Debt
- Checklist and label short-ID resolution remains deferred; current routes resolve by canonical IDs only.
- Trello compat permission helpers are duplicated across resource routers and should be centralized in a shared module.
- Full-repository typecheck and lint still fail due unrelated pre-existing issues outside Trello compat scope.

## What Should Be Done Next
- Add shared Trello compat auth/permission utilities to remove repeated role-access logic in boards/cards/lists/checklists/labels routers.
- Introduce checklist/label short-ID resolution if parity requirements for these entities become mandatory.
- Continue with Iteration 6 endpoints (`/members/*`, `/organizations/*`) using the same Trello error/permission conventions.
