## Update
- Added new server routes in `server/index.ts` to serve API documentation assets:
  - `GET /api-docs`
  - `GET /api-docs/native.yaml`
  - `GET /api-docs/trello.yaml`
- Confirmed docs routes are reachable on local server (`200` for all three endpoints).

## New
- Added interactive API docs page at `docs/api/index.html` using Swagger UI with two tabs:
  - Native API
  - Trello Compatible Adapter API
- Added OpenAPI spec for native token-based API at `docs/api/native-openapi.yaml`.
- Added OpenAPI spec for Trello adapter endpoints at `docs/api/trello-openapi.yaml`.
- Both specs include security schemes and examples so users can test directly from docs via Authorize + Try it out.

## Technical Debt
- Native OpenAPI spec is endpoint-complete at a high level but some extension-specific request/response payloads still use generic schemas (`additionalProperties`) and can be tightened over time.
- Swagger UI assets are loaded from CDN; if offline/self-hosted docs are required, vendor the bundle into project assets.

## What Should Be Done Next
- Expand Native API schema coverage endpoint-by-endpoint with strict typed request/response models.
- Add example payloads for more complex endpoints (automation, plugins, webhooks) to improve testability.
- Optionally add API docs link in README and frontend navigation.
