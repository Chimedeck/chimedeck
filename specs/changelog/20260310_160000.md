# Changelog — 2026-03-10T160000

## Update

- `src/extensions/CalendarView/CalendarView.tsx`: Enabled the month/week mode toggle (previously disabled), extracted drag-to-reschedule logic into `useCalendarDrag`, added week navigation state and handlers, wires `CalendarWeekGrid` when mode is `'week'`.
- `src/extensions/CalendarView/types.ts`: Added `CalendarWeekGridProps` interface.
- `tests/e2e/calendar-view.spec.ts`: Extended with tests for weekly view toggle, week navigation, card display in week view, drag-to-reschedule (success path), and PATCH failure revert + toast.

## New

- `src/extensions/CalendarView/useCalendarDrag.ts`: Reusable hook encapsulating drag-to-reschedule logic — optimistic Redux update via `boardSliceActions.optimisticUpdateCardField`, PATCH call to `/cards/:id`, revert + error toast on failure.
- `src/extensions/CalendarView/CalendarWeekGrid.tsx`: 7-column week view component. Shows Sun–Sat columns with date headers; prev/next navigation shifts by 7 days; renders `CalendarDayCell` per day; supports drag-and-drop via `onCardDrop`.

## Technical Debt

- `CalendarMonthGrid.tsx:90` has a pre-existing `exactOptionalPropertyTypes` TS error (passing optional `onCardDrop` directly rather than conditionally spreading). Not introduced by this iteration.

## What Should Be Done Next

- Iteration 6: Sprint 54 — Timeline View swimlanes + date axis header.
