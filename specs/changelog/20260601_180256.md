## Update
- Confirmed the subscription entitlement API returns the expected workspace payload shape with `entitlements` and `usage`.
- Verified the existing feature-gate and limit-guard flows still return the correct 402 error envelopes for plan-locked and over-cap create requests.

## New
- Added browser-level validation for the subscriptions-disabled fallback path, ensuring `SUBSCRIPTIONS_ENABLED=false` resolves workspace entitlements to `unlimited`.
- Captured live end-to-end coverage for the entitlements endpoint, plan gating, quota gating, and an allowed create flow.

## Technical Debt
- Entitlement and usage resolution still depend on subscription rows and stored tier IDs staying aligned with the canonical tier map.
- Usage aggregation remains workspace-wide/direct-count based, so some quota signals are still coarse-grained.
- The unlimited sentinel is still represented as a string value.

## What Should Be Done Next
- Add dedicated tests for the entitlement resolver, usage aggregation, and subscriptions-disabled fallback path.
- Tighten normalization for stale or missing subscription rows so entitlement resolution is more defensive.
- Continue the remaining roadmap work for member/guest/storage caps, shared rate limiting, billing UI, and the global upgrade modal.
