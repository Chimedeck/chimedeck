# Changelog — 2026-03-23T10:39:13Z

## Update
- Wired `dispatchDirectCardNotification` into `update.ts`, `delete.ts`, and `archive.ts` endpoints to fire `card_updated`, `card_deleted`, and `card_archived` events, respectively.
- Ensured notifications are dispatched at the correct lifecycle points (after update, before delete, after archive toggle).
- Fixed a type issue in `update.ts` by extracting the card title before casting.
- Verified no new TypeScript errors were introduced (only pre-existing unrelated errors remain).

## New
- No new features or files were added in this iteration. All changes were integrations of existing notification dispatch logic.

## Technical Debt
- Playwright MCP tests for card notification flows do not exist and cannot run due to Bun/Node incompatibility.
- No e2e Playwright coverage for card update/delete/archive/notification flows.
- Pre-existing type errors (`amount`/`currency`) remain in card files (unrelated to this iteration).

## What Should Be Done Next
- Create Playwright tests for card update, delete, archive, and notification dispatch flows.
- Resolve Bun/Node incompatibility to enable Playwright test execution.
- Add e2e Playwright coverage for notification scenarios.
- Address pre-existing type errors in card files if possible.
