## Update
- **WS cache invalidation wired** — `broadcastStateTransitionUpdated` now calls `invalidatePhaseCacheFromStateTransitionEvent` before publishing WebSocket events, so subscribers read fresh phase data after graph saves.
- **Feature flag added** — `AGENTIC_WORKFLOW_ENABLED` (default `false`) exposed across all layers: server runtime env getter, hardcoded fallback, `GET /api/v1/flags` endpoint, client Redux slice with selector and fetcher, and `.env.example` documentation.
- **Graph editor wired** — `ColumnPhaseEditor` is now accessible from the graph canvas: `useGraphEditor` exports `setNodeData` for atomic node data patches and derives `selectedColumnNode` from multi-selection; `GraphCanvas` renders the inspector panel when a single column node is selected (mirrors `EdgeInspector` pattern) with close on pane click; `index.tsx` wires props through.
- **Translations** — added `columnInspectorTitle` and `columnInspectorClose` entries to `en.json`.

## New
- `src/extensions/StateTransitions/components/GraphEditor/__tests__/ColumnPhaseEditor-wired.test.tsx` — 5 new integration tests (vitest + jsdom): inspector opens on column click, phase toggle updates node data, inspector closes on pane click, inspector closes on Escape, EdgeInspector remains functional alongside column inspector.
- `AGENTIC_WORKFLOW_ENABLED` flag added to `server/config/featureFlags.ts`, `server/mods/flags/defaults.ts`, `server/index.ts`, `src/slices/featureFlagsSlice.ts`, and `.env.example`.

## Technical Debt
- EdgeInspector could not be tested via direct edge click due to overlapping nodes in the test board (edges rendered at 9.5×3px); programmatic selection via React Flow internal API was used for verification. This is a test harness limitation, not a product bug.
- `agenticWorkflowEnabled` flag is defined but not yet used to gate the column inspector in the UI — gating logic deferred to Sprint 173 when the trigger engine is integrated.

## What Should Be Done Next
- Sprint 173: Metadata Trigger Engine — DB migrations for `card_phase_trigger_runs` and `card_phase_trigger_attempts`, move-event evaluator with phase→trigger mapping, tier-aware gating, async worker with idempotent dispatch, activity events, and GET timeline endpoint.
- Wire `agenticWorkflowEnabled` flag to gate `ColumnPhaseEditor` rendering once the trigger engine is ready for production use.
- Consider enlarging the test board graph so Playwright can reliably click edges for EdgeInspector regression tests in CI.
