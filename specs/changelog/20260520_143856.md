## Update
- Simplified orthogonal state-transition edge routing to a fixed clean 3-segment shape (max two corners), removing zig-zag/stair-step routing behavior.
- Updated orthogonal handle interaction so dragging adjusts only route depth (vertical offset of the middle horizontal segment).
- Disabled waypoint/segment-based orthogonal deformation in renderer logic to keep orthogonal connectors clean and deterministic.

## New
- Added a changelog entry for the orthogonal routing simplification and interaction behavior change.

## Technical Debt
- Orthogonal routing now prioritizes visual simplicity over advanced route expressiveness; custom multi-corner waypoints are not used in this mode.

## What Should Be Done Next
- Add an optional advanced mode toggle if multi-corner/manual waypoint editing is needed for specific boards.
- Add UI regression tests validating orthogonal path shape remains at max three segments after edits and reload.
