## Update
- Standardized all `<input>`, `<textarea>`, and `<select>` elements across `src/` to use the canonical semantic token class pattern: `bg-bg-overlay text-base placeholder:text-subtle border border-border focus:outline-none focus:ring-2 focus:ring-primary`.
- Replaced all hardcoded `bg-gray-*`, `bg-white`, `bg-slate-*`, `text-gray-*`, `text-slate-*`, `placeholder-gray-*`, `placeholder-slate-*`, `placeholder:text-muted`, `border-gray-*`, `border-slate-*`, `focus:ring-indigo-500`, and `focus:ring-blue-500` classes on form elements with semantic tokens.
- Removed all redundant `dark:` variants from input/form elements.
- Updated `.prose` containers in `CardDescriptionTiptap.tsx` and `CardDescription.tsx` to remove hardcoded `text-gray-900/700` and `bg-gray-50`.
- Added themed scrollbar CSS to `src/index.css` using `::-webkit-scrollbar` and CSS custom properties.

## New
- Added `specs/tests/sprint-120-inputs-and-prose.md` Playwright MCP test spec covering input placeholder color, input backgrounds, prose-invert rendering, scrollbar theming, and theme-toggle consistency.

## Technical Debt
- Playwright `.md` test spec is not executable; must be implemented as `.spec.ts` for automation.
- Some List components retain intentional `[theme-exception]` hardcoded classes pending design decision.
- Pre-existing unrelated TypeScript errors remain unresolved.

## What Should Be Done Next
- Implement the new Playwright test scenarios as `.spec.ts` files for automated verification.
- Audit for any missed hardcoded color classes in `src/extensions/` and migrate as needed.
- Standardize error/success/disabled input states and perform a full text/contrast audit (deferred to next iteration).
- Finalize design decisions on List component surface tokens and remove `[theme-exception]` tags as appropriate.