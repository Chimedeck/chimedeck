## Update
- Fixed card description edit-mode data loss when existing descriptions include image content.
- Reworked description editor content loading to normalize persisted description data into HTML before injecting into TipTap.
- Prevented non-edit synchronization passes from mutating draft state while the editor is read-only.

## New
- Added safe content loader utilities in the description editor:
  - HTML detection for stored description bodies.
  - Markdown-to-HTML normalization fallback before editor setContent.
- Improved placeholder stripping fallback so empty image labels no longer collapse to blank content when attachment placeholders are unresolved.

## Technical Debt
- CardDescriptionTiptap still has high cognitive complexity and should be split into smaller hooks/helpers.
- Local linting remains partially blocked by existing tooling/dependency constraints in the workspace.

## What Should Be Done Next
- Add an automated integration test for this regression: existing description with image -> click edit -> content remains visible in rich and markdown modes.
- Add coverage for mixed legacy formats (raw HTML description + markdown + attachment placeholders).
- Refactor CardDescriptionTiptap into focused modules to simplify maintenance and reduce regressions.
