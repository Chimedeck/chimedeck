## Update
- Extended `PluginModal.tsx` to accept `boardPlugin` and `boardId`, rendering the allowed domains panel for plugins with `whitelistedDomains`.
- Updated `PluginDashboardPage.tsx` to pass `boardPlugin` and `boardId` to the settings modal, enabling domain management UI.
- Enhanced `usePluginBridge.ts` to enforce allowed domain checks for plugin modals and popups, rejecting blocked origins and returning domain errors.

## New
- Added `PluginAllowedDomainsPanel.tsx`, a React component for board admins to manage allowed domains for plugins, with checkbox UI and PATCH API integration.

## Technical Debt
- Modal height resizing and bridge for plugin settings are deferred.
- E2E Playwright tests for allowed domains flows are not yet implemented.
- UI/UX polish for config editing and error states is pending.

## What Should Be Done Next
- Implement backend enforcement of domain checks and security/permissions.
- Build board admin UI for allowed domains and enhance plugin registry.
- Add E2E Playwright tests for allowed domains flows.
- Address UI/UX polish and modal resizing.
- Consider plugin_auth_tokens and robust whitelist enforcement for security.