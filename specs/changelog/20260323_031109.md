# Changelog — 2026-03-23T03:11:09Z

## Update
- `EditProfilePage.tsx`: Refactored to a two-tab layout (Profile / Notifications) driven by the `?tab=` URL query param via `useSearchParams`. Defaults to `profile` tab if param is missing or invalid. Invalid tab values silently fall back to `profile`.

## New
- `en.json`: Added `UserProfile.tabProfile` ("Profile") and `UserProfile.tabNotifications` ("Notifications") translation keys for the new tab bar.

## Technical Debt
- Removed the standalone `<h2>` section headings for Notifications from `EditProfilePage`; they will be added inside `GlobalNotificationToggle` as a self-contained heading in the next iteration.

## What Should Be Done Next
- Iteration 2: Add remaining translation keys (`UserProfile.notificationsSubheading`, `UserProfile.notificationsMasterNote`), add self-contained `<h2>` heading inside `GlobalNotificationToggle.tsx`, and update the email footer URL to use `?tab=notifications` deep-link.
