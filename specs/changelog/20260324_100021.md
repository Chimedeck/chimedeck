## Update
- Modified `package.json` to add four npm scripts for unified test execution: `test:unit`, `test:e2e`, `test:mcp`, and `test:all`.

## New
- Added `test:unit` script: runs Bun integration tests (`bun test tests/integration`).
- Added `test:e2e` script: runs Playwright e2e tests (`playwright test --project=e2e`).
- Added `test:mcp` script: runs all MCP scenario tests (`bash run-all-tests.sh`).
- Added `test:all` script: runs all test modes in sequence (`bash run-all-tests-full.sh`).

## Technical Debt
- `test:mcp` and `test:all` reference shell scripts that will be created in the next iteration.

## What Should Be Done Next
- Implement `run-all-tests-full.sh` to support the `test:all` script.
- Ensure `run-all-tests.sh` exists and is up to date for `test:mcp`.
- Review and update documentation to reflect new test scripts.
- Consider adding CI integration for the new test scripts.