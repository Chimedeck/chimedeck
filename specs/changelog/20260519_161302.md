## Update
- Registered the new POST /api/v1/boards/:boardId/state-transitions/copy endpoint in the stateTransitions API router.
- Updated API router wiring to dispatch copy requests to the new handler.

## New
- Implemented server/extensions/stateTransitions/api/copy.ts: Handles copying state transition graphs between boards with permission checks, name matching, and error handling per spec.
- Added server/extensions/stateTransitions/api/__tests__/copy.test.ts: Comprehensive tests for copy scenarios, permission errors, and missing source transitions.

## Technical Debt
None

## What Should Be Done Next
- UI integration: Add modal, RTK Query hook, and banner for copy action.
- Implement copyEnabled/skippedNodes extension logic if required by UI.
- Clarify/handle Unicode normalization for name matching if needed.
- Decide and enforce policy for copying to self and duplicate column names.
- Continue Playwright UI test flows for state transition copy.
