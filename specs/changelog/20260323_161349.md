## Update
- Updated `cli/index.ts` to register and dispatch the new `set-price` and `invite` commands.
- Ensured all CLI commands validate required flags, support `--help` and `--json`, and exit with clear error messages.

## New
- Added `cli/commands/setPrice.ts`: Implements the `set-price` command for PATCHing card price (`/api/v1/cards/:cardId/money`) with flags for card, amount, currency, label, and clear.
- Added `cli/commands/invite.ts`: Implements the `invite` command for POSTing a new board member (`/api/v1/boards/:boardId/members`) with flags for board, email, and role.

## Technical Debt
- CLI and Playwright integration tests are blocked by missing valid API token (HORIFLOW_TOKEN).
- Playwright `.md` tests are not yet enabled or converted for automated runs.
- Some deferred CLI/server changes and TypeScript errors remain elsewhere in the codebase.

## What Should Be Done Next
- Obtain or generate a valid API token and re-run CLI and Playwright tests.
- Enable or convert Playwright `.md` tests for automated evaluation.
- Add CLI integration tests and command-specific `--help` output.
- Improve CLI error handling and address deferred features from previous iterations.