## Update
- Added 'card_updated', 'card_deleted', and 'card_archived' to the `NOTIFICATION_TYPES` constant in `server/extensions/notifications/mods/preferenceGuard.ts`.
- Extended notification type safety and preference guard coverage for new card activity types.
- Updated `boardActivityDispatch.ts` to support direct dispatch of these new notification types with type-safe payloads.

## New
- Introduced `DirectCardNotificationPayload` union type for direct card notification payloads in `boardActivityDispatch.ts`.
- Added `dispatchDirectCardNotification()` function to send typed notifications to board members, respecting all preference guards.

## Technical Debt
None

## What Should Be Done Next
- Integrate `dispatchDirectCardNotification()` into card-related endpoints (e.g., update, delete, archive) to trigger notifications.
- Add/extend Playwright MCP tests to verify notification delivery for the new types.
- Review and update documentation for notification extensibility and usage patterns if needed.
