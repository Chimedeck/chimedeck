## Update
- Updated `server/extensions/comment/api/create.ts` to support idempotency: accepts `idempotency_key` and returns existing comment for duplicate keys per user.
- Rewrote `server/extensions/card/api/patch.ts` to implement idempotent `PATCH /api/v1/cards/:id/description` with offline replay metadata handling.
- Updated `server/extensions/card/api/index.ts` to route description PATCH requests to the new handler.

## New
- Added migration `db/migrations/0084_comment_idempotency_key.ts` to add nullable `idempotency_key` to `comments` table with a `UNIQUE(user_id, idempotency_key)` constraint.
- Created `specs/tests/offline-replay-save-and-comment.md` with 16 Playwright MCP scenarios for comment/description idempotency and offline replay.

## Technical Debt
- Playwright offline replay/draft recovery scenarios fail due to `indexedDB is not defined` in the test environment; offline flows are untested until this is resolved.
- No client-side offline draft storage or reconciliation logic yet.

## What Should Be Done Next
- Fix or mock IndexedDB for Playwright tests to enable offline scenario coverage.
- Implement client-side offline draft storage, API integration, and reconciliation logic.
- Integrate offline replay logic into CardDescriptionTiptap and CommentEditor.
- Add cross-device draft continuity and conflict resolution handling.