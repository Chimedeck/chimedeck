# Changelog — 20260303_120001

## Update

- `server/extensions/auth/api/register.ts` — now generates a verification token and sends a verification email when `EMAIL_VERIFICATION_ENABLED` is on; returns `{ requiresVerification: true }` (no JWT) instead of session tokens
- `server/extensions/auth/api/login.ts` — now returns 403 `email-not-verified` when `EMAIL_VERIFICATION_ENABLED` is on and the user hasn't verified
- `server/mods/flags/defaults.ts` — added `EMAIL_VERIFICATION_ENABLED: false` and `SES_ENABLED: false` defaults
- `src/extensions/Auth/duck/authDuck.ts` — added `pending-verification` status, `pendingEmail` state field, and updated `signupThunk` to handle `requiresVerification` response
- `src/extensions/Auth/api/auth.ts` — added `verifyEmail` and `resendVerification` API methods
- `src/extensions/Auth/containers/SignupPage/SignupPage.tsx` — shows `VerificationPending` banner after signup when verification is required
- `src/routing/index.tsx` — added `/verify-email` public route
- `src/store/index.ts` — registered `verifyEmail` reducer
- `.env.example` — added `FLAG_EMAIL_VERIFICATION_ENABLED`, `FLAG_SES_ENABLED`, `SES_REGION`, `SES_FROM_ADDRESS`

## New

- `db/migrations/0014_email_verification.ts` — adds `email_verified`, `verification_token`, `verification_token_expires_at` columns; backfills existing users as verified
- `server/extensions/email/index.ts` — `send()` routes email via SES or console based on `SES_ENABLED` flag
- `server/extensions/email/mods/ses.ts` — AWS SES v3 SDK send wrapper
- `server/extensions/email/mods/console.ts` — dev fallback; logs email to stdout
- `server/extensions/email/config/index.ts` — centralises SES env vars
- `server/extensions/email/templates/verificationEmail.ts` — plain-text + HTML email body builder
- `server/extensions/auth/api/verifyEmail.ts` — `GET /api/v1/auth/verify-email?token=` handler
- `server/extensions/auth/api/resendVerification.ts` — `POST /api/v1/auth/resend-verification` handler, rate-limited to 3/hour per user
- `server/middlewares/requireVerified.ts` — middleware that 403s unverified users when flag is on
- `src/extensions/Auth/containers/VerifyEmailPage/VerifyEmailPage.tsx` — page to handle verification link
- `src/extensions/Auth/containers/VerifyEmailPage/VerifyEmailPage.duck.ts` — Redux state for verification page
- `src/extensions/Auth/components/VerificationPending.tsx` — banner shown after signup when verification is required
- `src/extensions/Auth/components/ResendVerificationButton.tsx` — button to trigger resend
- `specs/tests/email-verification.md` — E2E test spec
- `specs/tests/email-verification-flag.md` — unit/integration test spec

## Technical Debt

- `server/middlewares/requireVerified.ts` is wired but not yet applied to private route handlers; each private router must call it after `authenticate`. A future task should apply it globally in `server/index.ts`.
- The resend rate limit uses the in-process `memCache`; under horizontal scale this per-process counter can be bypassed. Switch to the Redis `cache` provider when `RATE_LIMIT_ENABLED` is true.

## What Should Be Done Next

- Apply `requireVerified` middleware to all private routers (workspace, board, list, card, etc.) in `server/index.ts`.
- Add Playwright E2E test for the full email verification flow.
- Implement Sprint 24 — Profile Settings: Avatar & Nickname.
