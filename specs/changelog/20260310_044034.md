# Changelog — 20260310_044034

## Update
- `server/mods/events/write.ts` — published WS message now includes a `version` field (integer equal to `sequence`) alongside the existing `sequence` string, satisfying §8 of the requirements spec.
- `server/extensions/realtime/api/events.ts` — REST events endpoint now includes `version: number` in each serialised event row.
- `server/extensions/board/api/guests/index.ts` (`handleInviteGuest`) — emits a `member_joined` event with `scope:'board'` after successfully granting guest access to a board. Also fixed a stale `{ name, data }` error shape to `{ error: { code, message } }`.

## New
- `server/mods/events/index.ts` — public entry point that re-exports `writeEvent`, `readEventsSince`, `WriteEventInput`, and `WrittenEvent`; consumers now import from `server/mods/events/index` instead of individual files.
- `server/extensions/workspace/api/invite/accept.ts` — emits a `member_joined` event with `scope:'workspace'` (fire-and-forget) after a user successfully accepts a workspace invite.
- `server/extensions/workspace/api/members/add.ts` — emits a `member_joined` event with `scope:'workspace'` after an admin directly adds a user to a workspace.
- `tests/e2e/member-joined-event.spec.ts` — Playwright E2E tests that verify board guest invite emits a `member_joined` event with a `version` field, and that all board events include the `version` field.

## Technical Debt
- Workspace-scoped `member_joined` events are stored in the DB but not broadcast over a pub/sub channel (no `workspace:{id}` channel exists yet). Clients can poll `GET /api/v1/boards/:id/events` for board-scoped events but have no real-time channel for workspace membership changes.

## What Should Be Done Next
- Add a `workspace:{id}` pub/sub channel so workspace-scoped `member_joined` events are delivered in real time to connected clients.
- Iteration 5: Sprint 51 — access token TTL change, WS session termination on logout.
