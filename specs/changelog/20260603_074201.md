# Iteration 1: Board Chat Header Integration (Sprint 164)

## Update
- Added Board Chat header entry point with feature gate `BOARD_CHAT_ENABLED`
- Button only appears for non-guest members when feature flag is enabled
- Follows existing Health Check pattern for feature flag configuration

## New
- Created `BoardChatButton` component in `src/extensions/BoardChat/components/`
- Created `boardChatConfig.ts` with centralized `BOARD_CHAT_ENABLED` flag
- Created BoardChat extension entry barrel at `src/extensions/BoardChat/index.ts`
- Added `onOpenBoardChat` prop to `BoardHeader` component
- Threaded board chat handler through `BoardPage` container
- Added comprehensive test coverage for visibility matrix and wiring

## Technical Debt
- Board Chat drawer, history, and persistence are deferred to iteration 2+
- No actual chat functionality yet; only header button wired

## What Should Be Done Next
- Iteration 2: Implement right drawer shell with loading/empty/error states
- Iteration 3: Add permission model and persistence layer
- Iteration 4: Implement sidebar guest controls
