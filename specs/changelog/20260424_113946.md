# Update
- Fixed card archive/unarchive activity recording so the mutation now persists an activity row reliably and publishes it to realtime activity streams.
- Fixed direct card notifications (`card_archived`, `card_updated`, `card_deleted`) to always write a non-null `source_id`, matching notifications schema constraints.

# New
- Archive API now publishes the written archive/unarchive activity via realtime publisher so open card/board activity views can update without relying on manual refresh.

# Technical Debt
- Server notification dispatch modules still contain broad unsafe-type lint debt, which makes file-level eslint noisy for targeted behavioral changes.

# What Should Be Done Next
- Add integration coverage for archive flow asserting: (1) `activities` row exists with `card_archived`/`card_unarchived`, (2) board activities endpoint includes it, and (3) notifications row is inserted successfully.
- Consider migrating direct card notifications to use a dedicated source identifier strategy (event ID vs card ID) across all direct types for clearer traceability.
