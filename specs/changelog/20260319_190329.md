## Update
- Replaced all hardcoded English UI strings in `src/common/components/` and `src/layout/` with translation lookups from `src/common/translations/en.json`.
- Updated `ErrorBoundary.tsx`, `Toast.tsx`, `ToastRegion.tsx`, `Spinner.tsx`, `ThemeToggle.tsx`, `MentionSuggestions.tsx`, `CommandPalette.tsx`, `Sidebar.tsx`, `TopBar.tsx`, and `AppShell.tsx` to use translation keys for all user-facing copy and aria-labels.
- Ensured all aria-labels and accessibility text are sourced from translations.

## New
- Created `src/common/translations/en.json` with 50+ translation keys for common and layout UI.
- Added `specs/tests/sprint-94-common-layout-i18n.md` Playwright MCP test plan covering all common/layout i18n scenarios.
- Added this changelog: `specs/changelog/20260319_190329.md`.

## Technical Debt
- Playwright config does not currently exclude `.md` files, causing test discovery errors.
- Some pre-existing TypeScript errors remain (unrelated to this iteration).
- Brand name `HoriFlow` intentionally left untranslated.

## What Should Be Done Next
- Update `playwright.config.ts` to exclude `.md` files from test discovery.
- Ensure correct Playwright test project/tag is used for MCP tests.
- Re-run Playwright MCP tests for sprint-94-common-layout-i18n.
- Defer cleanup of hardcoded strings in features/extensions outside Sprint 93–94 scope to future iterations.