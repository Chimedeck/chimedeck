## Update

- **`server/extensions/stateTransitions/common/types.ts`** — Added `WorkflowPhase` string literal union (NEW_DRAFT, REFINED_PENDING_REVIEW, SYNC_DOCUMENT, READY_FOR_DEV, GENERATE_SPRINT, UPDATE_AS_BUILT), `PhaseConfig` interface (`serviceTierOverride`, `autoRun`, `requiresHumanApproval`), and both as optional fields on `StateTransitionNode`.
- **`server/extensions/stateTransitions/common/validator.ts`** — Integrated `validateNodePhases` into `validateGraphShape` — rejects unknown phase enum values (400), duplicate phases (400), malformed `phaseConfig` types. Legacy graphs without phases pass unchanged.
- **`server/extensions/stateTransitions/common/serializer.ts`** — `serializeGraph` preserves `workflowPhases` and `phaseConfig` on nodes when present.
- **`src/extensions/StateTransitions/api.ts`** — Added `WorkflowPhase` union type and `PhaseConfig` interface to client-side API types.
- **`src/extensions/StateTransitions/components/GraphEditor/ColumnNode.tsx`** — Renders `PhaseChip` pills below the column label when `data.workflowPhases` is non-empty.
- **`src/extensions/StateTransitions/components/GraphEditor/useGraphEditor.ts`** — Fixed bug where `toReactFlowNodes` and `toGraphNodes` did not carry `workflowPhases`/`phaseConfig` between the API response and React Flow node data. Added explicit types to `GraphEditorNodeData`.

## New

- **`server/extensions/stateTransitions/common/config/workflowPhases.ts`** — `VALID_WORKFLOW_PHASES` constant array and `WORKFLOW_PHASE_LABELS` display-name map.
- **`server/extensions/stateTransitions/mods/phaseResolver/index.ts`** — Cached resolver module (60s TTL, Map-based cache): `resolveColumnWorkflowPhases({ boardId, listId })` returns phases for a column, `resolvePhaseConfig` returns per-phase overrides. Exports `invalidatePhaseCacheForBoard` for WS event wiring.
- **`src/extensions/StateTransitions/components/PhaseChip.tsx`** — Coloured pill component: blue (NEW_DRAFT), amber (REFINED_PENDING_REVIEW), green (SYNC_DOCUMENT), purple (READY_FOR_DEV), orange (GENERATE_SPRINT), teal (UPDATE_AS_BUILT).
- **`src/extensions/StateTransitions/components/ColumnPhaseEditor.tsx`** — Multi-select and toggle editor for assigning workflow phases and phaseConfig to a column node (not yet wired into the graph editor column edit panel).
- **`server/extensions/stateTransitions/common/__tests__/workflowPhases.test.ts`** — 12 unit tests: valid phases accepted, unknown enum rejected, duplicates rejected, malformed config rejected, absent phases (legacy compat) passes, edge cases for empty arrays.
- **`server/extensions/stateTransitions/mods/phaseResolver/__tests__/index.test.ts`** — 5 unit tests: resolver returns phases for configured list, returns empty array for unconfigured list, returns config overrides, cache invalidation clears entries, miss returns undefined for unused board.
- **`src/extensions/StateTransitions/components/__tests__/PhaseChip.test.tsx`** — Renders correct label text for each of the 6 `WorkflowPhase` enum values.

## Technical Debt

- `ColumnPhaseEditor` is built but not wired into the graph editor's column edit panel — the edit panel component needs a slot for this section.
- `invalidatePhaseCacheForStateTransitionEvent` is exported but not called from the WS broadcast handler in `api/put.ts` — the cache will only expire by TTL until wired.
- Phase data is stored in the `board_state_transitions.graph_data` JSONB column (no migration needed), but if phase metadata grows significantly, a dedicated columns table migration should be considered.

## What Should Be Done Next

1. Wire `ColumnPhaseEditor` into the graph editor's column edit panel so users can assign phases through the UI.
2. Wire `invalidatePhaseCacheForBoard` into the `broadcastStateTransitionUpdated` WS handler in `api/put.ts`.
3. **Sprint 173** — Metadata Trigger Engine: integrate `resolveColumnWorkflowPhases` into card-move handlers to fire automation actions (autoRun, human approval) based on the column's phase metadata.
