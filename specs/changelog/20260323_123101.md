## Update
- `server/extensions/card/api/index.ts`: Wired new `handleCreateCardComment` handler for `POST /api/v1/cards/:id/comments` (takes precedence over the richer internal comment router due to routing order).
- `playwright.config.ts` already includes both `tests/e2e` and `specs/tests` project directories — no change needed.

## New
- `server/extensions/card/api/comments.ts`: `POST /api/v1/cards/:id/comments` external API handler. Accepts `{ text: string }`, creates a comment row, dispatches `comment_added` event and activity record, returns slim shape `{ data: { id, cardId, userId, text, createdAt } }` (HTTP 201). Works with both JWT and `hf_` API token auth.
- `specs/tests/sprint-103-card-comments.md`: Playwright MCP test covering happy path (201 + correct response shape), validation errors (missing/empty text → 400), invalid-token 401, and UI verification.

## Technical Debt
- Card comments external endpoint does not fan out mention notifications or WS broadcasts (unlike `comment/api/create.ts`). Intentional for the minimal API surface; full parity can be added in a follow-up.
- DB connection pool exhaustion ("too many clients already") observed on dev server prevents live smoke testing; pre-existing infrastructure issue.

## What Should Be Done Next
- Iteration 9: Sprint 103 — API Reference Doc + Endpoint Verification: audit all 6 operations with `hf_` bearer tokens, fix any issues found, and write `docs/api-reference.md`.
