## Update
- Extended `server/extensions/auth/middlewares/authentication.ts` to accept `hf_` Bearer tokens as an alternative to RS256 JWT.
- JWT path unchanged; `hf_` tokens trigger a DB lookup by SHA-256 hash, with deny-first checks for revoked and expired tokens.
- `last_used_at` is updated asynchronously on every successful API token authentication without blocking the request.

## New
- `specs/tests/api-token-auth.md` — Playwright MCP test covering: valid hf_ token auth, revoked token rejection, expired token rejection, cross-user token scope.

## Technical Debt
None.

## What Should Be Done Next
- Iteration 4: Implement the ApiToken RTK Query slice (`src/extensions/ApiToken/apiToken.slice.ts`) and translations, register in reducers.ts (Sprint 102).
- Iteration 5: Build ApiTokenPage.tsx and associated modals (GenerateTokenModal, TokenCreatedModal, RevokeTokenDialog).
- Verify `playwright.config.ts` includes `tests/e2e/` so all E2E specs are discovered (identified in iteration 3 analysis).
