## Update
- Fixed `playwright.config.ts` to include both `specs/tests` and `tests/e2e/` as test directories using the `projects` array, so all E2E `.spec.ts` files are discovered and executed by the Playwright runner.
- Verified all E2E tests in `tests/e2e/` (including `notification-type-prefs.spec.ts`) are now listed and runnable via `npx playwright test`.

## New
None.

## Technical Debt
None.

## What Should Be Done Next
- Monitor E2E test stability under load; DB "too many clients" errors can occur in shared dev environments and may require connection pooling tuning (e.g. pg pool `max` setting).
- Consider adding a CI step that runs `npx playwright test --reporter=line` to catch regressions automatically.
