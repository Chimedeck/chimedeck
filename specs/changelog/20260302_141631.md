## Update
- Updated `server/extensions/realtime/api/presence.ts` to return full User objects (including `avatar_url`).
- Modified `server/extensions/realtime/mods/rooms/subscribe.ts` and `unsubscribe.ts` to broadcast `presence_update` events on join/leave.
- Added `SEARCH_ENABLED` flag to `server/config/env.ts` and wired new routers in `server/index.ts`.

## New
- Migration `db/migrations/0012_search_vectors.ts`: Adds `tsvector` columns, triggers, GIN indexes, and backfill for boards/cards.
- `server/extensions/search/`: Implements full-text search API (`/api/v1/workspaces/:id/search`), query builder, ranking, and RBAC.
- `server/extensions/presence/`: Presence API, WebSocket event publisher, expiry job, and router.
- `src/extensions/Search/`: Search modal (Cmd+K), input, result item, hook, API logic, and translations.
- Unit tests: `buildQuery.test.ts` (search query sanitization) and `expiryJob.test.ts` (presence expiry logic).

## Technical Debt
- No Playwright config or E2E tests for search/presence flows; only unit/integration tests exist.
- Redis adapter tests skipped unless Redis is running.

## What Should Be Done Next
- Add Playwright config and E2E tests for search modal and presence flows.
- Implement advanced feature flag logic, Redis integration, and RBAC middleware for deferred items.
- Polish UI/UX for comments/activity and improve S3/virus scan error handling.
- Monitor distributed queue and presence expiry reliability across cache adapters.