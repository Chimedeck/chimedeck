## Update
- Hardened State Transitions server handlers to use consistent `{ name, data }` error envelopes across GET/PUT/COPY paths.
- Added defensive graph handling in GET/PUT/COPY for malformed persisted transition graphs (repair/fallback on GET/PUT and explicit validation failure on COPY).
- Refined enforcement and UI guard/popup logic to normalize duplicate allowed transitions and stabilize allowed-state rendering.

## New
- Added targeted test coverage under `tests/stateTransitions/`:
  - `edgeCases.spec.ts` for graph validation, default graph generation, guard fail-open/fail-closed behavior, and error envelope shape.
  - `permissions.spec.ts` for admin/owner permission gates on PUT/COPY and forbidden-action coverage.
  - `uiFlows.spec.ts` for popup text/allowed-states normalization, race-condition 422 parsing, and locked-column indicator derivation.
- Updated root `playwright.config.ts` test matching to auto-discover both `*.spec.*` and `*.test.*` within the e2e project.

## Technical Debt
- Repository-wide lint remains blocked by pre-existing ESLint parser project-include errors in `cli/*` files.
- Repository-wide typecheck remains blocked by pre-existing unrelated TypeScript errors outside State Transitions scope.

## What Should Be Done Next
- Resolve global lint/typecheck baseline issues so full CI gates can run green without module scoping workarounds.
- Add full browser-level State Transitions E2E coverage once login/test-auth automation is fully reliable in Playwright flows.
