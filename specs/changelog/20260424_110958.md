# Update
- Mention notifications now carry and render the message text where the user was tagged.
- Persisted notification listing now resolves mention preview text from both comment mentions and card-description mentions.

# New
- Added optional `sourceText` propagation through mention notification creation so realtime mention payloads include preview content.
- Mention notifications created from card description edits now expose description text as preview in the notification list.

# Technical Debt
- `handleListNotifications` remains above current cognitive-complexity target in static analysis; no functional regression introduced in this change.

# What Should Be Done Next
- Add an integration test asserting mention notifications include preview text for both comment and card description mention sources.
- Add a UI test covering realtime mention rendering so preview appears immediately without requiring refresh.
