## Update
- Refactored `src/extensions/Board/api.ts` to add `BoardDeleteConflictError` typing for 409 responses.
- Updated `BoardListPage.duck.ts` to support optimistic board deletion with rollback on failure, including new `deleteSnapshot` state and extraReducers for the new thunk.
- Modified `BoardListPage.tsx` to dispatch `deleteBoardOptimisticThunk` instead of the legacy thunk.
- Expanded Playwright MCP scenarios in `specs/tests/board-delete-auto-refresh.md` for immediate removal and rollback.

## New
- Added `src/extensions/Board/slices/boardsSlice.ts` exporting `deleteBoardOptimisticThunk` for optimistic board deletion.
- Implemented full BDAR-UI-01 and BDAR-UI-02 test scenarios for board delete auto-refresh and rollback.

## Technical Debt
- Playwright tests cannot run under Bun due to ESM loader incompatibility; Playwright must be run with Node.js.
- Pre-existing type errors in `BoardListPage.duck.ts` unrelated to this iteration remain.

## What Should Be Done Next
- Update Playwright test command to use Node.js instead of Bun.
- Implement client-side redirect/toast on current-board delete and real-time UI updates (deferred to future iterations).
- Address middleware refactoring and logging improvements.
- Add full Playwright UI and real-time tests for board deletion flows.