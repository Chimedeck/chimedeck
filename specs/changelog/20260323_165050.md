## Update
- Refactored `server/extensions/mcp/index.ts` to use a new `registerMcpTools` helper for tool registration, reducing duplication and improving modularity.

## New
- Added `server/extensions/mcp/registerTools.ts`, exporting `registerMcpTools(server)` to register all MCP tools in one place.
- Created `server/extensions/mcp/http/sessions.ts`, implementing an in-memory session store with 30-minute TTL eviction for future HTTP transport support.

## Technical Debt
- None.

## What Should Be Done Next
- Free up port 3000 and reduce database connections to allow Playwright MCP tests to run.
- Implement HTTP transport for MCP using the new session store.
- Integrate Playwright .md tests for MCP HTTP endpoints once infra is fixed.
- Complete deferred error/edge case tests for MCP tools.
- Migrate to uuidv7 for session IDs and tool usage.
