## Update
- Modified `src/extensions/Card/slices/cardDetailSlice.ts` to add `ActivityData` type, `activities` field, and support for loading activities via `?include=activities`.
- Updated `src/extensions/Card/components/CardModal.tsx` to use the new `ActivityFeed` component and accept an `activities` prop.
- Updated `src/extensions/Card/containers/CardModal/index.tsx` to read activities from state and pass them to `CardModal`.

## New
- Added `src/extensions/Card/config/activityEventsConfig.ts`: client-side config listing visible activity event types.
- Added `src/extensions/Card/config/activityEventLabels.ts`: maps event types to labels and colors for UI.
- Added `src/extensions/Card/containers/CardModal/ActivityFeed.tsx`: unified feed component rendering comments and system events inline.

## Technical Debt
- No Playwright MCP tests for the Card modal activity feed UI were found or executed; UI test coverage is missing.
- Pre-existing unrelated TypeScript errors remain in the codebase.

## What Should Be Done Next
- Implement Playwright UI tests for the Card modal activity feed, covering event rendering, sorting, and edge cases.
- Address technical debt: add middleware/rate-limiting, handle legacy comments with `event_type = null`, and resolve unrelated TypeScript errors.
- Continue with client-side config and UI improvements as planned for the next iteration.