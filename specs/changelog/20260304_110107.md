## Update
- Modified `src/extensions/Card/components/CardItem.tsx` to render `CardPaymentButtons` when `monetizationType` is `'pay-to-paid'`, the card has an amount, and the list name matches the predicate.
- Ensured correct prop threading and amount conversion for payment buttons.
- Installed `@stripe/stripe-js` and `@stripe/react-stripe-js` for Stripe integration.

## New
- Added `src/config/index.ts` to expose `STRIPE_PUBLIC_KEY` from `VITE_STRIPE_PUBLIC_KEY`.
- Created `src/extensions/Board/config/stripePaymentButtonsConfig.ts` with `PAYMENT_BUTTONS` config.
- Added `src/extensions/Payment/config/stripeClient.ts` as a singleton Stripe.js loader.
- Implemented `src/extensions/Payment/components/StripePaymentModal.tsx` for Stripe Embedded Checkout.
- Added `src/extensions/Payment/components/CardPaymentButtons.tsx` to render payment buttons and manage modal state.

## Technical Debt
- No Playwright/e2e tests for payment UI or endpoints.
- RBAC and event sourcing for payment not fully reviewed.
- Some API verification deferred.

## What Should Be Done Next
- Implement and verify Playwright/e2e tests for payment UI and endpoints.
- Complete RBAC/event sourcing review for payment flows.
- Implement client-side Stripe UI for additional flows if needed.
- Document payment architecture and update related specs.