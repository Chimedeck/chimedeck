# Changelog — 20260310_070401

## Update
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx` — mounted `BoardViewSwitcher` above the `BoardCanvas`. The Kanban canvas renders when `activeView === 'KANBAN'`; other views show a "coming soon" placeholder until implemented in later iterations.
- `src/reducers.ts` — registered `viewPreferenceReducer` from the new slice.
- `src/store/index.ts` — added `viewPreference` key wired to `viewPreferenceReducer`.

## New
- `src/extensions/BoardViewSwitcher/` — new feature extension implementing Sprint 52 view-switcher UI:
  - `types.ts` — `ViewType`, `ViewPreference`, `ViewPreferenceState`
  - `constants.ts` — `VIEW_TYPES`, `DEFAULT_VIEW`
  - `api.ts` — `getViewPreference` / `putViewPreference` using `apiClient`
  - `viewPreference.slice.ts` — Redux slice with `fetchViewPreference` + `saveViewPreference` thunks and `setActiveView` optimistic action
  - `hooks.ts` — `useViewPreference({ boardId })` encapsulating load-on-mount + switch logic
  - `BoardViewTab.tsx` — single accessible tab button with icon + label
  - `BoardViewSwitcher.tsx` — `role="tablist"` bar rendering all four view tabs
  - `icons.tsx` — inline SVG icons for KANBAN, TABLE, CALENDAR, TIMELINE
  - `utils.ts` — `normaliseViewType` guard for unknown server values
  - `translations/en.json` — i18n strings for tab labels
  - `index.ts` — public re-exports
  - `README.md` — extension documentation
  - `__tests__/viewPreference.slice.spec.ts` — 10 unit tests (all passing)
  - `__tests__/integration.playwright.ts` — Playwright E2E test (tabs render, PUT fires, preference persists on reload)

## Technical Debt
- Non-Kanban views (TABLE, CALENDAR, TIMELINE) render a placeholder until Iterations 3–7 implement them.

## What Should Be Done Next
- Iteration 3: Implement `src/extensions/TableView/` (Sprint 52 Table View component).
