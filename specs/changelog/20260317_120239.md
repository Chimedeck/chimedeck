## Update
- Replaced the inline overflow dropdown in `OneLineToolbar.tsx` with the new `<CommandMenu>` component, providing a unified command menu for both `CardDescriptionTiptap` and `CommentEditor`.
- Updated `specs/tests/rich-text-toolbar-single-line-overflow.md` to include detailed scenario descriptions for + menu search, filter, and command execution in both editors.

## New
- Added `src/extensions/Card/components/CommandMenu.tsx`: a shared, keyboard-accessible command menu with search/filter, 5 commands (Mention, Emoji, Code snippet, Quote, Numbered list), and empty state handling.
- Integrated the new command menu into both card description and comment editors via the shared toolbar.

## Technical Debt
- Playwright test specs exist only as markdown scenario files; no executable Playwright test files or config present, so tests do not run.
- Accessibility and responsive layout improvements for the command menu are still pending.

## What Should Be Done Next
- Scaffold and implement Playwright test files for the described rich-text-toolbar + menu scenarios.
- Address accessibility and responsive layout for the command menu.
- Proceed to implement inline attachment feedback and upload preview in the next iteration.
