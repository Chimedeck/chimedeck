## Update
- Implemented Sprint 145 list compatibility routes in `server/extensions/trelloCompat/api/lists/index.ts` for Trello `/trello/1/lists/*` parity across create/read/update, field-level reads/updates, board/cards includes, and bulk card operations.
- Added full list `pos` handling (`top`, `bottom`, numeric) for both create and update/move scenarios using fractional index insertion.

## New
- Added list endpoints:
  - `POST /trello/1/lists`
  - `GET|PUT /trello/1/lists/{id}`
  - `GET|PUT /trello/1/lists/{id}/{field}`
  - `PUT /trello/1/lists/{id}/closed`
  - `PUT /trello/1/lists/{id}/idBoard`
  - `GET /trello/1/lists/{id}/cards`
  - `GET /trello/1/lists/{id}/board`
  - `POST /trello/1/lists/{id}/archiveAllCards`
  - `POST /trello/1/lists/{id}/moveAllCards`
- Added integration coverage at `tests/integration/trelloCompat/lists.test.ts` for list CRUD behaviors, field-level reads, pos edge cases, and archive/move bulk operations.

## Technical Debt
- `GET /trello/1/lists/{id}/actions` currently returns an empty placeholder response and still needs event/activity mapping when action compatibility is finalized.

## What Should Be Done Next
- In Iteration 5, implement checklist and label endpoints (including checklist copy and label field updates) and wire short-ID parity where required by the sprint plan.
