## Update
- Updated `vite.config.ts` to add `@sentry/vite-plugin` for source map upload, gated by deploy env vars; always generates source maps and removes them after upload.
- Modified `package.json` to add a `build:deploy` script that sets `SENTRY_RELEASE` from git SHA and runs the deploy build.
- Expanded `README.md` with Sentry source map upload instructions, required env vars, and clarified that local/dev builds skip upload.
- Verified `sentryClient.ts` and `sentry.ts` already unify release/environment tagging via config—no changes needed.

## New
- Added conditional Sentry source map upload pipeline for deploy builds using `@sentry/vite-plugin`.
- Introduced `build:deploy` script for consistent release tagging and source map upload.
- Documented Sentry deploy process and environment variables in the README.

## Technical Debt
- No automated tests for Sentry event capture or source map upload.
- Privacy/PII redaction for error payloads and uploaded source maps is not fully implemented.
- No endpoint for deliberate error triggering for integration tests.

## What Should Be Done Next
- Implement privacy hardening/redaction for error payloads and source maps.
- Add automated Sentry event verification tests (server/client).
- Provide an endpoint for deliberate error triggering to support integration testing.
- Reconcile sprint plan index and ensure all deferred items are tracked.