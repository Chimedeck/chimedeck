# Changelog — 20260318_023318

## Update
- `src/extensions/Board/components/BoardHeader.tsx` — added `searchToken` and `onSearchResultSelect` props; renders `BoardSearchBar` when a token is present.
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx` — passes `searchToken` (from `accessToken`) to `BoardHeader`.
- `src/extensions/Search/api.ts` — added `BoardSearchResult`, `BoardSearchResponse` interfaces and `searchBoard()` client function for `GET /api/v1/boards/:boardId/search`.
- `specs/tests/board-scoped-search.md` — replaced placeholder UI section with 10 Playwright MCP UI scenarios (UI-01 through UI-10); updated AC checklist.

## New
- `src/extensions/Board/components/BoardSearchBar.tsx` — inline board-scoped search bar component with:
  - 300 ms debounce
  - Min 2-character guard (no request fired below threshold)
  - Escape-to-clear (clears input, closes panel, returns focus)
  - Clear (×) button
  - Results dropdown panel with card/list icons and type labels
  - Empty-query / no-results state (`data-testid="board-search-no-results"`)
  - Click-outside to close
  - Full ARIA attributes (`role="searchbox"`, `aria-label`, `aria-controls`, `aria-expanded`, `role="listbox"`, `role="option"`)

## Technical Debt
- `onSearchResultSelect` in `BoardHeader` / `BoardPage` is not yet wired to open the card modal — this is deferred to Iteration 3.

## What Should Be Done Next
- Iteration 3: Wire card result click to open `CardModal`, implement URL `?boardSearch=` query param restore on refresh, and reset search state on board navigation.
