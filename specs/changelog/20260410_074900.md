# Changelog — 20260410_074900

## Update

- `server/extensions/comment/api/list.ts`: Filtered top-level comments only (`.whereNull('parent_id')`); added `reply_count` via inline subquery; added `parent_id` to select.
- `server/extensions/comment/api/create.ts`: Accepts `parent_id` in request body; enforces max-depth-one (returns `reply-depth-exceeded` for replies to replies); inserts `parent_id`; emits `comment_reply_added` WS event for replies (vs `comment_added` for top-level); fixed `commentPreview` variable ordering.
- `server/extensions/comment/api/index.ts`: Mounted `repliesRouter` under `GET /api/v1/comments/:commentId/replies`.

## New

- `db/migrations/0105_comment_replies.ts`: Adds nullable `parent_id` FK (→ `comments.id`, `ON DELETE CASCADE`) and index `idx_comments_parent_id`.
- `server/extensions/comment/api/replies/get.ts`: `GET /api/v1/comments/:commentId/replies` — returns direct replies with auth + board-membership guard, reactions included.
- `server/extensions/comment/api/replies/index.ts`: Sub-router for the replies namespace.
- `tests/integration/comment/commentReplies.test.ts`: 17 unit-level tests covering top-level filtering, reply_count, fetch replies, depth guard, cascade delete, auth guard.

## Technical Debt

- The `reply_count` subquery uses a Postgres cast (`::int`). If the DB is ever swapped this may need adjustment.

## What Should Be Done Next

- Sprint 132: UI for threaded replies — `CommentReplyThread` component, Reply button, `getReplies`/`postReply` API helpers, WS sync.
