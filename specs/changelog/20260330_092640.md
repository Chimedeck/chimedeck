## Update
- Fixed Backlog adjacent drag swap behavior in Kanban: dragging card #1 onto card #2 lower half now correctly swaps to position #2 without requiring hover over card #3.
- Improved drop-target resolution by applying final placement from `DragEnd` geometry/target (`over`) even when `onDragOver` intermediate updates are missed.

## New
- Added pointer-aware before/after insertion handling for same-list card hover in `BoardCanvas`.
- Added drag-end fallback placement computation that derives `toListId`, `newIndex`, and `afterCardId` from the drop target for reliable commit.

## Technical Debt
- `BoardCanvas` still has high cognitive-complexity lint warnings in drag handlers and should be split into focused helper functions.

## What Should Be Done Next
- Add e2e regression test: swap first and second cards in same list by dropping in lower half of card #2 (without touching card #3).
- Refactor `handleDragOver` and `handleDragEnd` into smaller pure helpers to satisfy complexity checks and make drag behavior easier to reason about.
