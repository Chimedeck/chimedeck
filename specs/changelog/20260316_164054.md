## Update
- Refactored `CardModal.tsx` to remove the fixed right sidebar (Members, Labels, Dates, Value, Actions sections).
- Integrated new `CardModalBottomBar` at the bottom of the modal.
- Added `activityVisible` state and togglable activity column in `CardModal.tsx`.
- Moved `CardValue` and `CardDetailPluginBadges` into an inline flex row in the main column.
- Cleaned up unused imports and sidebar logic in `CardModal.tsx`.

## New
- Created `CardModalBottomBar.tsx`: sticky bottom bar with power-ups, automations, actions dropdown, and activity toggle.
- Added Playwright test spec: `specs/tests/card-modal-bottom-bar.md`.

## Technical Debt
- Playwright tests did not run due to dev server permission issues and possible test path mismatch.
- No direct reference for CardModalBottomBar in sample-project; dropdown/action menu structure adapted from other Card components.

## What Should Be Done Next
- Fix dev server permissions and Playwright test path to enable test execution.
- Refactor CardActionMenu to pure dropdown and remove sidebar wrapper if still present.
- Continue CardModal restructuring and badge integration in next iteration.
- Remove `CardSidebarSection.tsx` if confirmed unused.