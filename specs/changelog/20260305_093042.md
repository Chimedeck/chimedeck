## Update
- Added `pluginDashboardReducer` to `src/reducers.ts` and store registration in `src/store/index.ts`.
- Registered plugin routes in `src/extensions.ts`.
- Updated `src/extensions/Board/containers/BoardSettings/BoardSettings.tsx` to add a Plugins tab for board admins.
- Modified `.gitignore` to track `src/extensions/Plugins/` by changing `plugins/` to `/plugins/`.

## New
- Created `src/extensions/Plugins/` feature subtree with:
  - `config/pluginsConfig.ts`, `api.ts`, `reducers.ts`, `routes.ts`, `README.md`
  - Components: `components/PluginCapabilityChips.tsx`, `PluginCard.tsx`, `PluginList.tsx`
  - Redux duck: `containers/PluginDashboardPage/PluginDashboardPage.duck.ts`
  - Page shell: `containers/PluginDashboardPage/PluginDashboardPage.tsx`
  - Hook: `hooks/useBoardPlugins.ts`
- Plugin Dashboard UI at `/boards/:boardId/settings/plugins` with optimistic enable/disable, capability chips, and admin-only access.

## Technical Debt
- No server endpoints, SDK, or iframe host/bridge implemented yet; UI/API are stubs.
- `plugin_auth_tokens` table is present but unused.
- No automated Playwright or MCP evaluator tests for plugin flows; only a manual test plan exists.

## What Should Be Done Next
- Implement server-side plugin APIs and board admin checks.
- Build the jhInstance SDK and iframe host/bridge for plugin runtime.
- Add Redux/API wrappers for plugin enable/disable flows.
- Complete UI injection points and modal/popup flows per spec.
- Write automated Playwright/MCP tests for plugin dashboard and SDK flows.