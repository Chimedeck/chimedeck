## Update
- Registered global notification preference endpoints in server API index.
- Updated EditProfilePage to include a "Notifications" section with global toggle.
- Added new translation keys for notification settings UI.

## New
- `server/extensions/notifications/api/globalPreference/get.ts`: GET endpoint for user global notification settings.
- `server/extensions/notifications/api/globalPreference/update.ts`: PATCH endpoint to update user global notification settings.
- `server/extensions/notifications/api/globalPreference/index.ts`: Router wiring for global preference endpoints.
- `src/extensions/UserProfile/containers/EditProfilePage/GlobalNotificationToggle.tsx`: Client toggle component.
- `specs/tests/global-notification-setting.md`: Playwright MCP test spec for global notification toggle.
- `specs/tests/board-notification-preference.md`: Playwright MCP test spec for per-board toggle.

## Technical Debt
- Playwright MCP evaluator script is missing; tests for markdown specs cannot be executed until installed.
- UI placement for "Notifications" section may need refinement based on user feedback.

## What Should Be Done Next
- Install or provide Playwright MCP evaluator to enable test execution for markdown specs.
- Rerun MCP tests for global and board notification toggles and address any failures.
- Refine UI/UX for notification settings as needed.
- Defer advanced notification types and board-specific preferences to future iterations.