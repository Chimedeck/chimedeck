# Changelog — 2026-03-23T10:53:18Z

## Update
- `src/layout/Sidebar.tsx`: Added `CommandLineIcon` import and a new `NavItem` for "MCP Docs" (route `/developer/mcp`) immediately after the "Plugin Docs" NavItem.
- `src/extensions/Workspace/components/Sidebar.tsx`: Added `CommandLineIcon` import, added `commonTranslations` import from `~/common/translations/en.json`, updated "Plugin Docs" to use the translation key, and added a new NavLink for "MCP Docs" (route `/developer/mcp`) immediately after "Plugin Docs".

## New
- `src/routing/index.tsx`: Added lazy import for `McpDocsPage` and a `<Route path="/developer/mcp">` inside the `AppShell` private route group, mirroring the `PluginDocsPage` pattern.
- `src/common/translations/en.json`: Added translation key `"Sidebar.mcpDocsLabel": "MCP Docs"`.

## Technical Debt
- The Workspace `Sidebar.tsx` previously hardcoded the "Plugin Docs" label string; this iteration migrated it to use the `commonTranslations` key for consistency.

## What Should Be Done Next
- Run Playwright MCP browser tests to verify: "MCP Docs" NavItem appears in sidebar, clicking it navigates to `/developer/mcp`, all 6 sections and 9 tools render correctly on `McpDocsPage`.
