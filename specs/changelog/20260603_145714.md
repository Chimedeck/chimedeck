# Sprint 164 - Board Chat Iteration 2: Right Drawer Shell + History States

## Update
- Extended Board Chat feature with right-side drawer component (BoardChatDrawer)
- Mounted drawer in BoardPage, wired open/close state to header button
- Drawer automatically closes on Escape key, backdrop click, and board navigation

## New
- **BoardChatDrawer.tsx** — Right-side slide-in drawer with loading/empty/error states
  - Sticky composer footer (3-row textarea + Send button)
  - Scrollable history area with message list display
  - Close button and backdrop dismiss handling
- **useBoardChatHistory.ts** — Stub hook for history query (Sprint 165 will wire backend)
  - Returns loading/empty/error/loaded states
  - Provides ChatMessage and UseBoardChatHistoryResult types
- **BoardChatDrawer.test.tsx** — Component test suite covering:
  - Drawer mount/unmount and header/footer rendering
  - History state rendering (loading, empty, error, loaded)
  - Close behavior (close button, backdrop click, Escape key)
  - Composer text input and send button state
- Updated **BoardPage.tsx** to mount drawer and handle lifecycle:
  - Conditional drawer mount based on boardChatOpen state
  - Close drawer on board navigation change
  - Spread operator pattern for optional onOpenBoardChat callback

## Technical Debt
- Test environment setup needed (vitest/jsdom not configured; affects all react-testing-library tests in repo)
- Send button currently non-functional (awaits Sprint 165 backend API wire-up)
- Message list design could support infinite scroll pagination (future optimization)

## What Should Be Done Next
- **Sprint 165**: Wire real backend API endpoint for message history (GET /api/v1/boards/:boardId/chat/history)
- **Sprint 165**: Implement message send path (POST /api/v1/boards/:boardId/chat/messages) with user auth
- **Sprint 165**: Add permission model for board chat access (ADMIN/OWNER manage ALLOW_GUEST_TO_VIEW/USE)
