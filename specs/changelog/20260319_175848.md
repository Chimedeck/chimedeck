## Update
- Migrated all UserProfile and AdminInvite UI strings to per-feature `translations/en.json` files.
- Replaced hardcoded English copy in `EditProfilePage.tsx`, `InviteExternalUserModal.tsx`, and `CredentialSheet.tsx` with translation-driven values.
- Ensured translation keys cover all modal, button, error, and aria-label strings.

## New
- Created `src/extensions/UserProfile/translations/en.json` (11 keys).
- Created `src/extensions/AdminInvite/translations/en.json` (35 keys).
- Added Playwright MCP test plan: `specs/tests/sprint-94-userprofile-admininvite-i18n.md`.
- Changelog: `specs/changelog/20260319_105000.md`.

## Technical Debt
- Some translation keys are pre-defined but not yet wired to UI (e.g., empty states, Quarter zoom).
- Locale-aware date formatting deferred.
- Playwright MCP test script missing from package.json; tests not executed.

## What Should Be Done Next
- Add Playwright MCP test script to package.json and rerun tests.
- Continue i18n migration for TableView, Mention, Notification, BoardViews, Common/Layout, Realtime, OfflineDrafts.
- Wire remaining translation keys to UI components.
- Implement locale-aware date formatting.
- Audit for duplicate translation keys across features.