## Update
- Registered `set_card_price` and `invite_to_board` tools in `server/extensions/mcp/index.ts`.
- Removed obsolete TODO comment from MCP tool registration.

## New
- Added `server/extensions/mcp/tools/setCardPrice.ts`: Implements `set_card_price` tool (PATCH /api/v1/cards/:cardId/money) with Zod schema, supports `amount: number | null`.
- Added `server/extensions/mcp/tools/inviteToBoard.ts`: Implements `invite_to_board` tool (POST /api/v1/boards/:boardId/members), returns structured error on permission issues.
- Added Playwright MCP markdown tests:
  - `setCardPrice.md` (T5)
  - `setCardPriceClear.md` (T6)
  - `inviteToBoard.md` (T7)
  - `inviteToBoardNoPermission.md` (T8)

## Technical Debt
- Playwright is not configured to recognize `.md` test files; new tests are not executed by default.
- Playwright test infra issues persist; `.md` tests require config update or conversion to `.spec.ts`.

## What Should Be Done Next
- Update Playwright config to include `.md` files or convert tests to `.spec.ts` format so new MCP tests run.
- Address Playwright infra issues to enable full test runs.
- Continue MCP CLI implementation (no CLI code exists yet).
- Monitor for outstanding UI/theme/TS errors and resolve as needed.