# Changelog - 2026-06-03 14:05:00

## Update

- Locked GitHub repository fetch authentication to GitHub App installation token in specs workflow docs.
- Locked commit identity to app bot alias for repository writes.
- Added performance strategy to avoid full-tree fetch/save round trips.

## New

- Updated `specs/sprints/sprint-169.md` with installation-token fetch requirements and manifest-first/lazy-file load strategy.
- Updated `specs/sprints/sprint-170.md` with delta-save, debounce, ETag/sha precondition guards, and bot-alias commit requirements.
- Updated Sprint 169/170 summary lines in `specs/sprints/sprint-plan.md` to reflect auth and performance decisions.
- Updated implementation decisions in `DESIGN.md` to include installation-token fetch, bot alias commit, and incremental transport pipeline.

## Technical Debt

- Installation token refresh/retry policy and expiration-handling edge cases are not yet broken out into dedicated error-state UX docs.
- Conflict-resolution UX for stale ETag/sha save attempts is defined at requirement level but not yet screen-level detailed.

## What Should Be Done Next

- Add detailed API schemas for manifest payload, file payload with ETag, and stale-write conflict responses.
- Add load/save performance SLO targets (for example p95 manifest load and p95 save latency) to sprint acceptance notes.
- Add E2E tests covering token expiration retry and stale-write conflict resolution flow.
