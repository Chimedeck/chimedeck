# Changelog — 2026-03-10T081903Z

## Update
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx`: Replaced the "coming soon" placeholder for the TIMELINE view with a full `<TimelineView>` mount (passes `cards`, `lists`, `onCardClick`, `addToast`).

## New
- `src/extensions/TimelineView/types.ts`: TypeScript types — `ZoomLevel`, `TimelineViewProps`, `Swimlane`, `TimelineHeaderProps`, `TimelineRowProps`, `TimelineZoomControlProps`.
- `src/extensions/TimelineView/TimelineZoomControl.tsx`: Day / Week / Month zoom toggle group with `aria-pressed` attributes and `data-testid` selectors.
- `src/extensions/TimelineView/TimelineHeader.tsx`: Horizontal date axis that adapts to zoom level (day → per-day columns; week → per-week columns; month → per-month columns). Today's column highlighted. Sticky sidebar placeholder aligned with swimlane labels.
- `src/extensions/TimelineView/TimelineRow.tsx`: One swimlane row per list. Sticky label shows list title and scheduled-card count. Bar area placeholder (bars implemented in Iteration 7). Unscheduled cards (missing `start_date` or `due_date`) rendered as chips below the bar area.
- `src/extensions/TimelineView/TimelineView.tsx`: Root container. Toolbar with Today button (scrolls axis to centre today) and `TimelineZoomControl`. Single `overflow-x: auto` scroll canvas containing `TimelineHeader` and `TimelineRow` list. Auto-scrolls to today on mount and zoom change.
- `src/extensions/TimelineView/README.md`: Documents structure, props, swimlane logic, zoom levels, Today button, and all `data-testid` selectors.
- `tests/e2e/timeline-view.spec.ts`: 8 Playwright E2E tests covering: view switch, swimlanes per list, scheduled bar area, unscheduled chips, Today button, zoom controls, chip click opening modal.

## Technical Debt
- Bar rendering (coloured bars spanning `start_date` → `due_date`) is deferred to Sprint 54 Iteration 7 (`TimelineBar.tsx`). The `timeline-bar-area-{listId}` div is present but empty.
- Drag/resize interactions and `useTimelineDrag.ts` are also deferred to Iteration 7.

## What Should Be Done Next
- Implement `TimelineBar.tsx` — coloured bar per scheduled card, positioned by `start_date`/`due_date` within the day-pixel grid.
- Implement `useTimelineDrag.ts` — drag left handle (update `start_date`), drag right handle (update `due_date`), drag body (shift both). Optimistic PATCH with revert + toast on error.
- Add Playwright tests for bar rendering and drag interactions (Sprint 54 Iteration 7).
