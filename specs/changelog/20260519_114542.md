## Update
- Updated `server/extensions/trelloCompat/serializers/customField.ts` to harden Trello contract normalization for CustomFields: `fieldGroup=id`, deterministic boolean coercion for `display.cardFront`, numeric `pos`, option-level numeric `pos`, and safe date normalization for custom field item values.
- Updated `server/extensions/trelloCompat/serializers/card.ts` to canonicalize embedded `customFieldItems` value payloads so list/date/number/checkbox/text items keep one Trello-typed value shape.
- Updated `server/extensions/trelloCompat/api/index.ts` to route all trelloCompat adapter execution through a centralized error-envelope handler and to return adapter-disabled responses via shared Trello error envelope helpers.
- Updated `specs/trello-adapter-normalization/matrix.md` with final Sprint 152 status tracking, including CustomFields and adapter error envelope normalization.

## New
- Added `server/extensions/trelloCompat/middlewares/errorHandler.ts` to guarantee adapter-level unexpected errors are normalized to Trello-style `{ message, error }` responses.
- Added `server/extensions/trelloCompat/tests/unit/server/extensions/trelloCompat/customField.contract.test.ts` with contract coverage for CustomField payload keys, option shape, card custom field item typed values, and adapter error envelope behavior.

## Technical Debt
- Full adapter-wide integration testing for runtime error paths still depends on broader router integration harness coverage; current coverage is serializer- and envelope-focused unit/contract tests.
- Some non-Sprint-152 adapter endpoints still have known field-projection parity gaps tracked in the normalization matrix.

## What Should Be Done Next
- Run full adapter endpoint parity pass against Trello docs for any remaining `PARTIAL`/`MISMATCH` rows in the normalization matrix outside Sprint 152 scope.
- Expand endpoint-level integration tests to validate error envelope behavior under thrown runtime errors across all trelloCompat routers.
