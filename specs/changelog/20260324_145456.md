## Update
- Registered the new `GET /api/v1/boards/:boardId/plugins/available` route in `server/extensions/plugins/api/index.ts`, ensuring it matches before generic plugin-segment handlers.

## New
- Added `server/extensions/plugins/api/board-plugins/available.ts` implementing the `/available` endpoint for board plugin discovery.
- Endpoint supports board member auth, q/category filters, and correct exclusion of already-enabled plugins.

## Technical Debt
- No Playwright or integration tests currently cover the new `/available` endpoint or its user flows.

## What Should Be Done Next
- Add Playwright tests for `/available` endpoint: board member access, filtering, exclusion logic, error cases.
- Proceed with UI changes for PluginDashboardPage and scenario test for board plugin discovery/enable flows.
- Implement optimistic update rollback/toast for plugin deactivation if not already done.