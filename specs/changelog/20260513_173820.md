# Update
- Refined the edit-mode link configuration modal to a compact horizontal toolbar layout to match the latest mockup.
- Replaced stacked option blocks with icon toggles and inline actions.

# New
- Added left-side compact display-mode toggle group in the link toolbar modal.
- Added inline "Edit link" text action in the toolbar row, followed by open/copy/delete icon actions.
- Updated link edit form popover anchor so it opens directly under the toolbar.

# Technical Debt
- Toolbar spacing and icon selection are currently hard-coded in component JSX; consider extracting to a dedicated link-toolbar component for reuse and easier visual iteration.

# What Should Be Done Next
- Add visual regression coverage for the link toolbar in edit mode.
- Validate icon semantics/accessibility labels with design/product review for final parity with mockup.
