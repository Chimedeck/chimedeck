## Update
- Sidebar (`src/layout/Sidebar.tsx`) now hides the Billing tab when `SUBSCRIPTIONS_ENABLED=false`. The flag is plumbed through `server/config/featureFlags.ts` → `/api/v1/flags` → `featureFlagsSlice` → `selectSubscriptionsEnabled`.

## New
- Added `SUBSCRIPTIONS_ENABLED` getter to `server/config/featureFlags.ts` (mirrors the existing `STATE_TRANSITIONS_ENABLED` getter).
- Added `subscriptionsEnabled` field to `FeatureFlagsState`, initial state, fulfilled handler, and `selectSubscriptionsEnabled` selector in `src/slices/featureFlagsSlice.ts`.
- Added `subscriptionsEnabled` to the `/api/v1/flags` response payload in `server/index.ts`.

## Technical Debt
- None — no new TypeScript errors introduced; pre-existing `featureFlagsSlice.ts` payload type errors (lines ~102-111) remain unchanged.

## What Should Be Done Next
- Audit other subscription-aware UI surfaces (workspace settings, upgrade modals, `usePlanGate`) and gate them behind the same flag for consistency.
- Consider adding a test in `tests/integration/subscriptionApi.test.ts` (or a new Sidebar UI test) that asserts the Billing tab is hidden when the flag is false.
