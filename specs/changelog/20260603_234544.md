## Update
- Tightened the SpecsWorkspacePage save/commit state machine so dirty state, pending-commit paths, and editor content stay aligned across autosave, reload, and partial-commit flows.
- Made specs file updates enforce an explicit `If-Match` precondition for existing markdown files, while preserving stale-tag handling and markdown-only validation.
- Expanded client and server coverage for autosave-to-commit handoff, stale precondition recovery, partial commit reset behavior, and missing-precondition errors.

## New
- Added explicit missing-precondition handling for specs file saves.
- Added tests covering commit-button lockout during save and partial commit behavior in the documentation workspace.

## Technical Debt
- The docs workspace still relies on the backend manifest and mocked request surface for browser verification in this checkout.
- Broader repository typecheck/test noise remains outside this iteration's touched files.

## What Should Be Done Next
- Harden the manifest-backed docs workflow end to end in the real backend environment.
- Clean up the remaining baseline typecheck/test issues so the docs flow can be verified without shims or mocks.
