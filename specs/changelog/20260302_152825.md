## Update
- Added `workspaceShellReducer` to `src/reducers.ts` and registered it in `src/store/index.ts`.
- Updated `src/routing/index.tsx` to wrap private routes in `AppShell` and route `/workspaces` to the new `WorkspaceListPage`.
- Extended `src/extensions/Workspace/routes.ts` with a route for the workspace list page.
- Expanded `src/extensions/Workspace/translations/en.json` with Sidebar, WorkspaceListPage, and CreateWorkspaceModal strings.

## New
- `src/extensions/Workspace/duck/workspaceDuck.ts`: Redux slice for workspaces, including thunks for fetching and creating workspaces and active workspace selection.
- `src/extensions/Workspace/components/Sidebar.tsx`: Persistent sidebar with workspace navigation, switcher, and user menu.
- `src/extensions/Workspace/components/CreateWorkspaceModal.tsx`: Modal dialog for creating new workspaces.
- `src/extensions/Workspace/containers/WorkspaceListPage/WorkspaceListPage.tsx`: Workspace cards grid with empty state and navigation.
- `src/layout/AppShell.tsx`: Layout wrapper for sidebar and main content.
- `src/extensions/Workspace/README.md`: Documentation for the Workspace extension structure and usage.
- `src/extensions/Workspace/__tests__/WorkspaceShell.test.tsx`: 8 unit tests for sidebar and shell (all passing).
- `src/extensions/Workspace/__tests__/WorkspaceModal.test.tsx`: 4 unit tests for workspace modal (all passing).

## Technical Debt
- None introduced in this iteration. Pre-existing issues (e.g., duplicate slices, unverified Playwright tests) remain.

## What Should Be Done Next
- Implement Board view (Kanban), Card modal, and real-time sync in future sprints.
- Add Playwright test specs and config for automated UI flow coverage.
- Address technical debt from previous sprints (duplicate slices, error mapping, Playwright test verification).
- Refine empty state UI, modal accessibility, and error handling for workspace/board flows.
- Ensure i18n coverage for all new UI strings.