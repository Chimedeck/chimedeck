## Update
- Fixed Custom Fields dynamic columns staying at 1 column after load.
- ResizeObserver now re-attaches after async field/value loading so container width is measured when the grid exists.

## New
- Dynamic column transitions (1/2/3) now work after initial data fetch instead of locking to initial mount state.

## Technical Debt
- Responsive column logic still lives directly in component effect; extracting a shared `useResponsiveColumns` hook would simplify reuse.

## What Should Be Done Next
- Add an integration test that mounts with loading state first, resolves data later, and asserts column count updates from container width.
