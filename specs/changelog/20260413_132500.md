# Changelog — 20260413_132500

## Update
- `src/extensions/DesignSystem/components/CodeSnippet.tsx` — replaced stub with a fully functional copy-to-clipboard button using `navigator.clipboard`; shows a `CheckIcon` + "Copied!" label for 2 s then resets; `aria-live="polite"` region announces copy success to screen readers; button has descriptive `aria-label` and visible focus ring.
- `src/extensions/DesignSystem/DesignSystemPage.tsx` — replaced inline `<nav>` with the new `<StickyNav>` component; added a mobile `<select>` jump-to-section control (visible on `< lg`) with a `sr-only` label; reduced prose `mb` before the mobile nav to keep layout tight.
- `src/extensions/DesignSystem/components/index.ts` — added `StickyNav` export.

## New
- `src/extensions/DesignSystem/components/StickyNav.tsx` — sticky sidebar nav extracted into its own component; uses `IntersectionObserver` to track which section is currently visible and highlights the matching nav link with `aria-current="location"` and bold weight; disconnects all observers on unmount; keyboard-focusable links with visible focus rings.

## Technical Debt
- No new technical debt introduced.

## What Should Be Done Next
- Add automated Playwright test for `/design-system` anchor navigation and clipboard copy (requires HTTPS or `localhost` for clipboard API).
- Consider adding syntax highlighting (e.g. `shiki` or `highlight.js`) to `CodeSnippet` in a future iteration.
