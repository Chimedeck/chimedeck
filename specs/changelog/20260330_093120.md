## Update
- Re-inspected Backlog adjacent swap issue and fixed the remaining edge case where dropping card #1 on card #2 (without crossing card #3) could still no-op.

## New
- Added deterministic same-list drop rule in Kanban drag logic: dropping onto a card now inserts after target when dragging downward, and before target when dragging upward.
- Kept pointer/geometry-based fallback for cross-list and non-deterministic paths.

## Technical Debt
- `BoardCanvas` drag handlers remain complex and should be split into helper utilities to satisfy static complexity rules.

## What Should Be Done Next
- Add an e2e regression for adjacent same-list swaps with explicit constraint: drop card #1 onto card #2 lower half and assert order changes.
