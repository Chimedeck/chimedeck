## Update
- Migrated all remaining hardcoded Tailwind color classes (`bg-white`, `bg-gray-*`, `text-gray-*`, `text-red-*`, `text-green-*`, `border-gray-*`) to semantic token classes across 169 files in `src/`.
- Removed stale `dark:` variants where semantic tokens now handle both themes.
- Updated CTA buttons to use `bg-primary`/`hover:bg-primary-hover`.
- Fixed loading screen and notification badge color usage to use semantic tokens.
- Corrected List header hover state to use semantic tokens instead of `hover:text-black`/`dark:hover:text-white`.

## New
- Expanded `specs/tests/sprint-120-contrast-audit.md` with 10 Playwright MCP test suites covering login, signup, workspace, board, card modal, theme toggling, notifications, profile/settings, automation, and plugin panels, plus shell grep verification commands.

## Technical Debt
- Playwright MCP tests for contrast audit did not run as expected due to route access issues; no screenshots or pass/fail results generated for required flows.
- Pre-existing TypeScript errors remain on the server side, unrelated to this migration.

## What Should Be Done Next
- Investigate and resolve why Playwright MCP tests are not discovering or running the correct scenarios; ensure the dev server exposes `/login`, `/board/:id`, and other required routes.
- Re-run the full contrast audit test suite and verify screenshots and pass/fail results are generated.
- Confirm all semantic tokens exist for both light and dark themes; add any missing tokens if needed.
- Document any remaining color usage exceptions pending design input.