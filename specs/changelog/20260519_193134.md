## Update
- Aligned stateTransitions server module to Iteration 2 contract (GET/PUT endpoints, feature flag enforcement, validation, and error handling).
- Confirmed all core files (`api/index.ts`, `api/get.ts`, `api/put.ts`, `common/serializer.ts`, `common/validator.ts`, `common/types.ts`, `config/featureFlags.ts`) match the approved API and validation requirements.

## New
- Added `tests/api/stateTransitions/get.test.ts` and `tests/api/stateTransitions/put.test.ts` to cover:
  - Default graph lazy-creation on GET
  - Successful PUT update
  - 422 error for unknown list IDs
  - 501 error when feature flag is disabled

## Technical Debt
None

## What Should Be Done Next
- Implement rules endpoint, WebSocket broadcast, and list sync hooks for state transitions (Iteration 3+).
- Clarify login form selectors or UI flow for browser-based authenticated API/PUT tests.
- Monitor for edge cases in concurrent PUTs and empty board lists.
