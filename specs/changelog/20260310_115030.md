## Update
- `server/config/env.ts`: Added `ACCESS_TOKEN_TTL_SECONDS` (default 86400) for configurable access token expiry.
- `server/extensions/auth/common/config/jwt.ts`: `accessTokenTtlSeconds` now reads from env var instead of hardcoded value.
- `server/extensions/auth/api/logout.ts`: Publishes `session_revoked` event to pubsub after logout.
- `server/extensions/auth/api/resetPassword.ts`: Publishes `session_revoked` event after password reset.
- `server/extensions/realtime/api/index.ts`: Integrates session revocation subscription and cleanup on WebSocket open/close.

## New
- `server/extensions/realtime/sessionRevocation.ts`: Helper to subscribe to session revocation events and close all user WebSockets with code 4001.

## Technical Debt
- No Playwright E2E tests for session revocation, token expiry, or multi-device flows.
- Playwright/Bun test runner incompatibility persists.
- Some uuidv4 usage remains in db/seeds/.

## What Should Be Done Next
- Scaffold Playwright E2E tests for session revocation, token expiry, and multi-device scenarios.
- Complete uuidv4 to uuidv7 migration.
- Fix Playwright/Bun runner issues.
- Expand E2E coverage and address technical debt in board visibility and auth flows.