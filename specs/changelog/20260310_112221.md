## Update
- Refactored all board GET handlers (`get.ts`, `members.ts`, `archived-cards.ts`, `labels.ts`, `boardActivity.ts`, `comments.ts`) to use new board visibility middleware, replacing manual auth/board loading logic.
- Updated `src/extensions/Board/api.ts` to support PATCHing board visibility and added `BoardVisibility` type.
- Integrated visibility selector into `BoardSettings.tsx` with optimistic UI update and rollback on failure.

## New
- Added `server/middlewares/boardVisibility.ts` middleware to enforce board visibility rules (PRIVATE/WORKSPACE/PUBLIC) on all board GET endpoints.
- Created `VisibilitySelector.tsx` React component for selecting board visibility.
- Added `tests/integration/board/boardVisibility.test.ts` (unit/integration) and `tests/e2e/board-visibility.spec.ts` (Playwright E2E) for board visibility access and UI.

## Technical Debt
- Board visibility middleware returns 401 Unauthorized instead of 403 Forbidden or 200 OK in some cases (see E2E failures).
- Playwright E2E: 3/4 tests fail due to incorrect status codes for PUBLIC/PRIVATE board access.
- UI/UX for visibility selector is basic; no role-based disabling or polish.

## What Should Be Done Next
- Fix board visibility middleware to return correct status codes (200 for PUBLIC, 403 for forbidden, 401 for unauthenticated).
- Polish UI: disable selector for non-admins, improve error messaging.
- Expand E2E coverage for edge cases (WORKSPACE visibility, role restrictions).
- Migrate remaining uuidv4 usage, address TypeScript errors, and add optimistic UI rollback for PATCH failures.
