# Update

- Implemented Iteration 8 edge authoring in the State Transitions graph editor: connect-to-create edges, custom edge rendering, inline edge inspector, and edge deletion.
- Added self-connection rejection feedback (node shake) and duplicate directional edge prevention in client-side edge creation.
- Extended server-side state transition graph validation to reject self-loop edges and duplicate directional edges.

# New

- Added `TransitionEdge.tsx`, `EdgeActionLabel.tsx`, `EdgeDeleteButton.tsx`, and `EdgeInspector.tsx` for ReactFlow edge UI.
- Added `src/extensions/StateTransitions/config/actionTypes.ts` as the action type extensibility stub and wired it into edge styling/labels/defaults.
- Added new translation keys for edge action labels, inspector controls, and edge delete actions.
- Added validator tests for self-referencing and duplicate directional edges.

# Technical Debt

- The graph editor currently uses static edge action defaults (from local config) and no toolbar-selected action source yet.
- Edge inspector currently anchors as a fixed inline panel; contextual positioning near the clicked edge can be improved in a follow-up.

# What Should Be Done Next

- Implement Iteration 9 toolbar integration so new edges use the currently selected action type source-of-truth.
- Add UI-level tests for edge creation/inspector/delete workflows and client persistence roundtrip assertions.
