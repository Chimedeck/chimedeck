## Update
- Fixed stale Kanban ordering after card drag commits by applying the authoritative move response from the server back into board state immediately after `moveCard` succeeds.

## New
- `BoardPage` now dispatches `remoteCardMove` with `{ id, list_id, position }` from the move API response, using `fromListId` from the drag context.

## Technical Debt
- Drag precision in automated browser replay is still sensitive to exact drop coordinates for "3rd" vs "4th" placement in dense columns.

## What Should Be Done Next
- Add an e2e drag regression that asserts UI order equals backend order after moving a card from one column to another and then reordering within the same target column.
