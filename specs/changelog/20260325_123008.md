## Update
- Updated `server/extensions/users/api/index.ts` to register the new avatar proxy endpoint.
- Modified `server/extensions/users/api/me.ts` and `server/extensions/users/api/profile/get.ts` to return `avatar_url` as a stable proxy path (`/api/v1/users/:id/avatar`) instead of a raw S3 URL.
- Updated `server/extensions/comment/api/list.ts` to return the proxied avatar path for each comment author.
- Enhanced `server/common/avatar/resolveAvatarUrl.ts` with a `buildAvatarProxyUrl` helper for consistent proxy URL generation.

## New
- Added `server/extensions/users/api/avatar/proxy.ts`: Implements `GET /api/v1/users/:id/avatar` to proxy (302-redirect) or stream user avatars, requiring authentication.
- Created `tests/e2e/sprint-117-avatar-proxy.md`: Playwright MCP test scenarios for avatar proxy API and UI behavior.

## Technical Debt
- Playwright `.md` test is not auto-executed due to config; needs conversion to `.spec.ts` for CI coverage.
- Some legacy endpoints may still expose raw S3 URLs; full audit deferred.

## What Should Be Done Next
- Convert `sprint-117-avatar-proxy.md` to a Playwright `.spec.ts` file for automated test runs.
- Audit all endpoints for raw S3 URL exposure and migrate to proxy paths.
- Decide on streaming vs. 302-redirect for avatar proxy (currently 302).
- Expand tests to cover additional edge cases and legacy flows if needed.
