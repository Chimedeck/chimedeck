# Changelog — 2026-03-23T04:02:04Z

## Update

- **NotificationItem.tsx**: Changed `card_commented` icon from `ChatBubbleLeftEllipsisIcon` to `ChatBubbleLeftIcon` and accent colour from `text-amber-400` to `text-indigo-400`, aligning with Sprint 98 spec (ChatBubbleLeftIcon + indigo accent).
- **mapActivityToNotification.ts**: Added `card_commented` to `ActivityAction` union, `ACTION_TO_NOTIFICATION_TYPE` map, `ActivityPayload` (added `commentPreview` and `commentId` fields), and `buildCopy` switch case.

## New

- `card_commented` is now fully handled in the client-side activity mapper with an optional `commentPreview` in the copy.

## Technical Debt

- None introduced.

## What Should Be Done Next

- Run end-to-end Playwright tests to confirm notification bell increments and correct copy for `card_commented`.
- Verify navigation on notification click routes to the correct card.
