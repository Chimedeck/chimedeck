## Update
- Modified `CardItem.tsx` to mount `CardPluginBadges` and `CardPluginButtons` after member avatars, enabling plugin badge and button injection on card tiles.
- Modified `CardModal.tsx` to mount `CardDetailPluginBadges` in the sidebar and `CardPluginSection` in the main column, supporting plugin UI injection in the card detail modal.
- Verified that the Plugins tab in BoardSettings was already present from a previous iteration.

## New
- Added `src/extensions/Plugins/uiInjections/CardPluginBadges.tsx`: Renders plugin-provided badges on card tiles using the plugin bridge.
- Added `src/extensions/Plugins/uiInjections/CardPluginButtons.tsx`: Renders plugin-provided buttons on card tiles and wires click events to the plugin bridge.
- Added `src/extensions/Plugins/uiInjections/CardDetailPluginBadges.tsx`: Renders plugin-provided badges in the card detail modal sidebar.
- Added `src/extensions/Plugins/uiInjections/CardPluginSection.tsx`: Renders plugin-provided section panels in the card detail modal main column.

## Technical Debt
- Playwright tests for plugin UI injection flows could not be executed due to an ESM loader error with Bun; no automated UI tests were run for this iteration.
- Some plugin-to-modal wiring and error handling are deferred to the next iteration.

## What Should Be Done Next
- Resolve Playwright ESM loader error to enable automated UI testing for plugin flows.
- Implement PluginPopup, PluginModal, show-settings, and complete end-to-end plugin UI wiring (deferred to Iteration 10).
- Add robust error handling and loading states for plugin UI injections.
- Finalize documentation and add missing tests for plugin UI injection points.