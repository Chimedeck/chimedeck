# Changelog — 20260313_112056

## Update
- Added `BoardMembersPanel` component at `src/extensions/Board/components/BoardMembersPanel/index.tsx`.
- Wired it to `boardMembersSlice` (RTK Query) for real-time CRUD: GET members on mount, POST to add, PATCH to change role, DELETE to remove.
- Guest filter applied to workspace member candidates so GUEST workspace members are excluded from the typeahead.

## New
- `src/extensions/Board/components/BoardMembersPanel/index.tsx` — main panel with backdrop, member list, and add-member section (admin-only).
- `src/extensions/Board/components/BoardMembersPanel/MemberRow.tsx` — single member row: avatar initials, name/email, role selector (admin-only), remove button with last-admin guard.
- `src/extensions/Board/components/BoardMembersPanel/AddMemberInput.tsx` — typeahead input filtering workspace members by name/email; excludes already-added members and GUESTs; role selector defaults to MEMBER.
- `specs/tests/sprint-79-iteration-5-board-members-panel.md` — Playwright MCP step-by-step test covering: add member, change role, remove member, last-admin guard, non-admin read-only view, typeahead exclusions.

## Technical Debt
- `BoardMembersPanel` currently requires workspace members to already be loaded in the Redux store (`WorkspacePage` duck). A future iteration should expose a standalone RTK Query endpoint for workspace members to avoid this coupling.
- The panel is not yet wired to a trigger in `BoardHeader` — that's deferred to Iteration 6.

## What Should Be Done Next
- Iteration 6: Wire `BoardMembersPanel` open trigger in `BoardHeader` via a member avatar stack; add visibility badge on workspace board grid cards.
