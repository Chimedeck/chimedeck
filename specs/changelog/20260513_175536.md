# Update
- Fixed link mode switching to re-anchor toolbar state using current editor selection after a mode change.
- Improved link rectangle refresh fallback when original anchor nodes are replaced by editor transactions.

# New
- Added selection-based active-link resolver to avoid stale DOM anchor references.
- Mode toggles now recompute active link metadata (mode/text/href/rect) from live editor state immediately after updates.

# Technical Debt
- The link toolbar still depends on runtime DOM coordinates and may benefit from extraction into a dedicated positioning hook with unit tests.

# What Should Be Done Next
- Add integration tests for mode 2 -> mode 1 switching that assert:
  - active mode button state updates correctly
  - toolbar rectangle does not intersect link rectangle
- Add regression test for replacing a link node and preserving toolbar anchor targeting.
