# 20260422_162500

## Update

- Restored archived-card label visibility in the Board Archived Cards tab.

## New

- Extended archived cards API payload to include `labels` for each archived card:
  - `server/extensions/board/api/archived-cards.ts`
- Extended client archived card type with labels:
  - `src/extensions/BoardViews/types.ts`
- Rendered label chips in archived card rows:
  - `src/extensions/BoardViews/BoardArchivedCardsPanel.tsx`

## Technical Debt

- Archived card row rendering now has custom inline chip rendering; this could be extracted into a shared compact label-chip component if reused elsewhere.

## What Should Be Done Next

- Add API test coverage for archived card labels in `GET /api/v1/boards/:id/archived-cards`.
- Add UI test verifying labels render in archived card rows.
