# Changelog — 2026-03-23T17:05:00Z

## Update
- `server/extensions/mcp/README.md`: Added a **Remote HTTP Transport** section documenting the `/api/mcp` endpoint, `Authorization` header usage, session lifecycle, curl examples for initialize / tool-call / SSE / terminate, error response table, and a stdio-vs-HTTP comparison table.

## New
- Remote HTTP section in MCP README covers endpoint URL, authentication, session TTL, and all four HTTP verbs (POST init, POST tool call, GET SSE, DELETE terminate).

## Technical Debt
- None introduced.

## What Should Be Done Next
- Add advanced troubleshooting notes (e.g., proxy/TLS configuration for production deployments) if users report confusion.
- Consider documenting rate-limiting and concurrent session limits once those features are implemented.
