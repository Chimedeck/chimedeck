## Update
- Switched the test runner configuration to use Vitest with a jsdom environment for React component suites.
- Updated the test setup so DOM globals and Testing Library matchers are available during tests.
- Adjusted package scripts to run the Vitest entrypoint instead of `bun test` for these suites.

## New
- Added `vitest` and `jsdom` as dev dependencies.
- Added `vitest.config.ts` and `tests/setup.ts` to centralize browser-like test setup.

## Technical Debt
- Some existing React test failures are still unrelated to the DOM harness and need separate cleanup.

## What Should Be Done Next
- Stabilize the remaining failing Vitest suites.
- Remove any now-unnecessary Bun test configuration if it is no longer used anywhere else.
