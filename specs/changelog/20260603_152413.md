# Update
- Added board chat permission persistence and API wiring.
- Normalized chat permission toggles so `use=true` implies `view=true`, and `view=false` clears `use`.
- Mounted the new chat-permissions routes in the board API.
- Added coverage for default reads, persisted reads, access control, and toggle normalization.

## New
- New `board_chat_permissions` migration with backfill for existing boards.
- New shared board chat permission types and normalization helper.
- New `GET /api/v1/boards/:boardId/chat-permissions` endpoint.
- New `PATCH /api/v1/boards/:boardId/chat-permissions` endpoint for OWNER/ADMIN updates.

## Technical Debt
- `GET` access policy is still broad and may need tightening if the permission surface expands.
- Live DB bootstrap was required because the migration set was not fully applied in the running environment.

## What Should Be Done Next
- Add guest-side read/send enforcement for board chat.
- Wire the sidebar guest controls to the new permissions API.
- Continue the remaining sprint work for history, search, assist, and repository/spec integration.
