## Update
- Hardened `serializeCard` in `server/extensions/trelloCompat/serializers/card.ts` to emit all required Trello card fields, including badges, cover, id arrays, nodeId, numeric pos, shortLink/shortUrl/url, start, and dueReminder, with correct types and defaults.
- Updated normalization logic to ensure badges and cover sub-keys are always present and Trello-compatible.
- Updated `specs/trello-adapter-normalization/matrix.md` with Iteration 5 endpoint statuses for Cards (PASS/MISMATCH/PARTIAL).

## New
- Added `server/extensions/trelloCompat/tests/unit/server/extensions/trelloCompat/cards.contract.test.ts` with contract tests for card normalization, covering required fields, nested keys, edge cases, and type invariants.

## Technical Debt
- Field projection endpoints (`GET /cards/{id}/{field}`) remain MISMATCH; not all field/nested resource normalization is complete.
- UI login is broken; all UI flows are blocked by authentication errors.
- Console errors present on all tested API/UI routes.

## What Should Be Done Next
- Fix login/authentication for UI to unblock end-to-end flows.
- Complete field projection and nested resource normalization for full Trello compatibility.
- Address console errors in API and UI routes.
- Re-verify normalization if checklist or matrix requirements change.