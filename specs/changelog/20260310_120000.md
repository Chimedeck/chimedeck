# Changelog — 2026-03-10 12:00:00

## Update
- Replaced all `uuidv4()` entity ID calls in `server/extensions/` with `generateId()` from the new central helper `server/common/uuid.ts`.

## New
- `server/common/uuid.ts`: Central UUID helper exporting `generateId()` which uses `uuidv7()` for time-ordered, k-sortable entity IDs.

## Technical Debt
- `db/seeds/` and any remaining files outside `server/extensions/` / `server/mods/` still use uuidv4 — will be addressed in Iteration 4.

## What Should Be Done Next
- Replace `uuidv4` in `db/seeds/` and any other remaining files across the codebase (Iteration 4).
