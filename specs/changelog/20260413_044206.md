# Changelog — 2026-04-13T04:42:06Z

## Update
- `src/common/components/Button.tsx`: Now uses `cn` (tailwind-merge) internally so consumer `className` props reliably override variant/size padding utilities.
- `src/common/components/IconButton.tsx`: Same tailwind-merge adoption as Button.

## New
- All raw `<button>` elements in the five Iteration 5 target files have been replaced with `Button` or `IconButton` components:
  - `src/layout/TopBar.tsx`: hamburger toggle → `IconButton` (aria-label preserved).
  - `src/layout/Sidebar.tsx`: collapse toggle → `IconButton`; workspace switcher, user-menu trigger, workspace list items, new-workspace, create-first, logout → `Button variant="ghost"`.
  - `src/extensions/Workspace/components/Sidebar.tsx`: all nav/action buttons → `Button variant="ghost"`.
  - `src/extensions/Workspace/containers/WorkspaceDashboard/index.tsx`: starred filter → `Button variant="ghost"` (aria-pressed retained); Create Board → `Button variant="primary"`.
  - `src/extensions/Workspace/containers/WorkspacePage/WorkspacePage.tsx`: Delete workspace → `Button variant="link"`; Invite Member → `Button variant="primary"`.

## Technical Debt
- Several pre-existing ESLint `no-confusing-void-expression` and `no-floating-promises` violations remain in the Workspace Sidebar and Dashboard files; they pre-date this iteration and are out of scope.
- The `onNavigate?: () => void` type in `NavItemProps`/`NavButtonProps` in `layout/Sidebar.tsx` is incompatible with `exactOptionalPropertyTypes`; this is a pre-existing type error.

## What Should Be Done Next
- Iteration 6: Migrate remaining raw buttons in List/Timeline/Table/Notifications/Auth/ErrorBoundary files.
