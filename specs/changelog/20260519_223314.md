## Update
- Normalized error handling in `server/extensions/stateTransitions/api/put.ts` to return `{ name: 'board-not-found', data: { message: 'Board not found' } }` with 404 for missing boards, matching API spec.
- Updated test mocks in `put.test.ts` to simulate missing-board behavior accurately.

## New
- Added test coverage in `get.test.ts` for GET `/api/v1/boards/:boardId/state-transitions` returning 404 for non-existent boards.
- Added tests in `put.test.ts` for malformed JSON (400, `bad-request`) and non-existent board (404, `board-not-found`).

## Technical Debt
None

## What Should Be Done Next
- Implement Playwright API tests for GET/PUT flows (with payloads/headers) to fully verify endpoints.
- Proceed with UI integration and enforcement logic for state transitions.
- Continue with further sprints as per the plan (e.g., UI, enforcement, advanced validation).