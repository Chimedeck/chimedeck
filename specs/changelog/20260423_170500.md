## Update
- Fixed card-modal attachment visibility after editor-driven uploads (including clipboard paste) so the Attachments panel updates immediately without page reload.
- Added a refresh signal path from comment editor attachment changes to AttachmentPanel reload logic.
- Synced card-level attachment counts when editor-side attachment state changes.

## New
- Added optional `refreshSignal` prop to AttachmentPanel to support external refresh triggers.
- Added optional `onAttachmentsChange` callback to ActivityFeed and CommentEditor to propagate editor attachment mutations upward.

## Technical Debt
- Card modal currently coordinates attachment state across siblings with prop callbacks; a shared attachment store or RTK slice would simplify this flow and reduce coupling.

## What Should Be Done Next
- Add a Playwright regression test for: paste image in comment editor -> verify attachment appears in Attachments panel without reload.
- Add a second regression test for drag/drop and toolbar file-input uploads to confirm consistent immediate panel updates.
