## Update
- No changes to existing code or features; only new database tables added.

## New
- Added migration 0086_board_notification_preferences.ts: creates board_notification_preferences table (id, user_id→users, board_id→boards, notifications_enabled boolean default true, updated_at, UNIQUE(user_id, board_id)).
- Added migration 0087_user_notification_settings.ts: creates user_notification_settings table (user_id PK→users, global_notifications_enabled boolean default true, updated_at).

## Technical Debt
None

## What Should Be Done Next
- Implement API endpoints for board and user notification preferences.
- Update notification dispatch logic to use new tables.
- Add client UI toggles for notification settings.
- Add tests for new flows and edge cases.