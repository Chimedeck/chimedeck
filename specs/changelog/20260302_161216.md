## Update
- Rebuilt `CardModal.tsx` as a Radix Dialog, now URL-driven via `?card=:id`.
- Updated `boardSlice.ts` to add `updateCard` and `removeCard` reducers for card mutations.
- Modified `BoardPage.tsx` to handle card click, sync modal state to URL, and render CardModalContainer.
- Registered `cardDetailReducer` in `reducers.ts` and `store/index.ts`.

## New
- Added `src/extensions/Card/slices/cardDetailSlice.ts`: Redux slice for modal/card detail state, optimistic actions, rollback.
- Added `src/extensions/Card/api/cardDetail.ts`: API wrappers for card, checklist, label, member actions.
- Added `src/extensions/Card/containers/CardModal/index.tsx`: Modal container, Redux-connected, orchestrates modal flows.
- Added components: `CardTitle.tsx`, `CardDescription.tsx`, `CardChecklist.tsx`, `CardLabels.tsx`, `CardMembers.tsx`, `CardDueDate.tsx`, `CardActionMenu.tsx`, `CardSidebarSection.tsx`.
- Created Playwright test specs: `specs/tests/card-detail-modal.md`, `card-checklist.md`, `card-labels.md`.

## Technical Debt
- Playwright MCP evaluator tests not run (missing project setup).
- Dev server failed to start (port conflict/misconfiguration).
- No user flows tested; no screenshots available.

## What Should Be Done Next
- Resolve dev server port issue and MCP evaluator setup; rerun Playwright tests for modal flows.
- Review/complete supporting components (pickers, chips, avatars) for edge cases and accessibility.
- Ensure all modal actions are fully optimistic with rollback on error.
- Add backend endpoints if any modal actions are stubbed or missing.
- Improve mobile responsiveness and error handling in modal components.