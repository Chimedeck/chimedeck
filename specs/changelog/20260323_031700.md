# Sprint 96 — Iteration 2: Translations, GlobalNotificationToggle heading, email footer URL

## Update
- `server/extensions/notifications/mods/emailTemplates/shared.ts`: Changed `PREFERENCES_URL` from `#notifications` hash fragment to `?tab=notifications` query param to align with the new tab-driven routing in EditProfilePage.

## New
- `src/extensions/UserProfile/translations/en.json`: Added keys `UserProfile.notificationsSubheading`, `UserProfile.notificationsMasterNote`, and `GlobalNotificationToggle.heading`. Removed obsolete `UserProfile.notificationPreferences` and `UserProfile.notifications` standalone section heading keys.
- `src/extensions/UserProfile/containers/EditProfilePage/GlobalNotificationToggle.tsx`: Added self-contained `<h2>` heading ("Master Notifications") using the new `GlobalNotificationToggle.heading` translation key.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 3: DB migration to extend CHECK constraints for new notification types (card_updated, card_deleted, card_archived).
