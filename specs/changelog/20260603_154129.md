# Update
- Implemented Sprint 165 iteration 4 guest-chat permission UX in `BoardChatDrawer` with lock states, deny states, and optimistic toggle updates with rollback.
- Wired `BoardPage` to pass guest/admin context into the chat drawer so permission controls and composer/history behavior are role-aware.
- Added server-side guest chat enforcement middleware and expanded permission helpers for deny-by-default view/send checks.

## New
- `src/extensions/BoardChat/api.ts` — typed GET/PATCH helpers for board chat permissions.
- `server/extensions/board/middlewares/chatPermissions.ts` — guest history/send guard middleware with typed 401/403 responses.
- `server/extensions/board/middlewares/__tests__/chatPermissions.test.ts` — permission matrix coverage for defaults, coupling, and denied names.

## Technical Debt
- Existing frontend test runtime still lacks DOM test harness support under `bun test`, so React `*.test.tsx` suites remain environment-blocked.
- Repository-wide `lint` and `typecheck` are currently failing on unrelated pre-existing files outside this iteration scope.

## What Should Be Done Next
- Iteration 5 (Sprint 166): implement board chat message storage/write path (`board_chat_messages` + embedding retry flow).
- Integrate the new chat permission middleware into upcoming history/send endpoints as those handlers are introduced.
