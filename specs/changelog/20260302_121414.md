## Update
- Mounted the new List router in `server/index.ts`.
- Updated `server/extensions/board/api/get.ts` to include active lists in the `includes.lists` field of board GET responses.
- Extended `BoardPage.tsx` and its duck to render and manage lists as columns, including drag-and-drop and list CRUD actions.

## New
- Added `db/migrations/0005_list.ts` for the `lists` table (id, board_id, title, position, archived).
- Implemented `server/extensions/list/` with:
  - Fractional indexing logic (`mods/fractional/`)
  - REST API endpoints: create, list, update, archive, delete, reorder
  - API router export
- Created `src/extensions/List/` with:
  - React components: `ListColumn`, `ListHeader`, `AddListButton`
  - Drag-and-drop container and `useListReorder` hook
  - Client API module and translations
- All endpoints follow required error envelope and RBAC protection.

## Technical Debt
- None

## What Should Be Done Next
- Add Playwright E2E tests for list flows: create, reorder, archive, delete, and drag-drop (including keyboard accessibility).
- Implement integration tests for edge cases (concurrent insert, position rebalance, archived list/card restrictions).
- Monitor for any issues with fractional indexing under high concurrency.
- Replace memCache with Redis for invite validation as planned in Sprint 09.
