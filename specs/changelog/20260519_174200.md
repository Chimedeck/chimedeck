## Update
- Added real-time `state_transition_updated` subscription in graph editor sync flow, including actor echo suppression (`actor_id` guard), remote cache invalidation, and remote update propagation into the editor.
- Added remote merge application in graph editor state to reconcile incoming graphs and clear client undo history on remote merges.
- Added live editor presence count wiring to the graph editor header and rendered the badge only for 2+ active editors.

## New
- Added `invalidateStateTransitionsBoardCache` helper in `src/extensions/StateTransitions/api.ts` for RTK Query tag invalidation from realtime listeners.
- Added merge/presence helper exports and focused tests under `src/extensions/StateTransitions/components/GraphEditor/__tests__/` for:
  - remote merge strategy,
  - echo suppression behavior,
  - presence badge count formatting,
  - undo-reset contract on remote merge.

## Technical Debt
- Presence count currently uses existing board-level presence events; editor-specific presence channels/avatars remain deferred by plan.

## What Should Be Done Next
- Iteration 11: implement Kanban DnD enforcement UI (`useStateTransitionGuard`, forbidden drop feedback, lock indicators, rollback popup on server 422).
