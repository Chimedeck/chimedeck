## Update
No changes made to existing code or features in this iteration.

## New
- Added six new MCP scenario markdown files to `specs/tests/`:
  - `mcp-http-session.md`: Session lifecycle, tool calls, and stale/missing ID handling.
  - `plugin-install.md`: Plugin install/uninstall, duplicate/unknown slug, and admin checks.
  - `board-star-follow.md`: Star/unstar and follow/unfollow board scenarios with access control.
  - `card-start-date.md`: Set/clear card start date, enforce start-before-due constraint.
  - `card-short-url.md`: Short URL in responses, resolves via short code, remains stable after edits.
  - `admin-user-list.md`: Admin-only paginated user list, filters/search, 403 for non-admins.
- All new files are recognized and processed by the test runner without format errors.

## Technical Debt
None

## What Should Be Done Next
- Fix missing `.vscode/mcp.json` config to enable MCP scenario test execution.
- Review and address unrelated Playwright e2e test failures (timeouts, missing elements).
- Refine and expand scenario coverage as needed after config and infra issues are resolved.
- Re-run and verify all MCP and Playwright tests once infrastructure is fixed.