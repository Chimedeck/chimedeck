# Changelog — 2026-03-13T08:34:45Z

## Update

- `src/layout/AppShell.tsx` — replaced hardcoded dark-only classes (`bg-slate-950`, `border-slate-800`, `bg-slate-900`, `text-slate-400`, `text-white`) with dual-mode light-base + `dark:` variant pairs throughout the shell wrapper, mobile topbar, and desktop topbar.
- `src/extensions/Workspace/components/Sidebar.tsx` — full audit and replacement of all dark-only Tailwind classes. Every surface (background, borders, text, hover states, active nav items, workspace switcher dropdown, user menu dropdown, kbd shortcut badge, empty-state create button) now has a light-mode base class plus its `dark:` override.
- `src/extensions/Board/containers/BoardListPage/BoardListPage.tsx` — updated heading, loading/empty state text, and filter chip classes to use dual-mode variants.
- `src/extensions/Board/components/BoardCard.tsx` — added `dark:` overrides to the existing light-mode gray card surface so it renders correctly in dark mode (dark border, dark background, dark title text, dark star button states).
- `src/extensions/Workspace/containers/WorkspaceListPage/WorkspaceListPage.tsx` — updated all heading, card tile, icon, body text, and action button classes to dual-mode.
- `src/extensions/Workspace/containers/WorkspacePage/WorkspacePage.tsx` — updated heading and section title classes to dual-mode.

## New

_No new files._

## Technical Debt

_None introduced._

## What Should Be Done Next

Iteration 5: Audit and fix hardcoded dark-only Tailwind classes in BoardView, ListColumn, CardItem, CardDetailModal, CommandPalette, Skeleton, EmptyState, and NotificationPanel.
