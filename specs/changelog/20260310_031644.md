# Changelog 2026-03-10T03:16:44Z

## Update
- Verified `server/common/uuid.ts` correctly exports `generateId()` using `v7 as uuidv7` from the `uuid` package — no changes needed.
- Confirmed `db/seeds/trello-import.ts` does not use `uuidv4`; seed file relies on Trello's existing member/board IDs and does not generate new UUIDs directly.

## New
- Sprint 47 UUID v7 migration is now fully complete: zero `uuidv4` references remain anywhere in the codebase outside of `node_modules`, `dist`, and the read-only `sample-project/` reference directory.

## Technical Debt
- One pre-existing test failure exists (`Auth store > initialises with unauthenticated status`) — expects `'unauthenticated'` but initial Redux auth state is `'idle'`. This is unrelated to UUID changes and predates this sprint.

## What Should Be Done Next
- Sprint 48: Add `board_stars` and `board_followers` DB tables (migration 0029), implement star/follow API endpoints, and build the BoardViews UI panels (activity, comments, archived cards).
