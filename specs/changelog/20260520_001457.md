## Update
- Renamed the primary list-label sync helper in state transitions from `updateNodeLabels` to `renameNodeLabel` and wired list-rename hook usage to the new helper name.
- Kept backward-compatible exports (`updateNodeLabels`, `syncNodeLabels`) so existing call sites and tests outside this iteration remain stable.

## New
- Added PUT API test coverage in `server/extensions/stateTransitions/api/__tests__/put.test.ts` for:
  - auto-syncing renamed list labels before save
  - stripping deleted-list nodes and dangling edges before save

## Technical Debt
- State transition API test suite still contains overlapping sync coverage across multiple files (`put.test.ts`, `put_list_sync.test.ts`, `put_sync_hooks.test.ts`), which could be consolidated later.

## What Should Be Done Next
- Proceed with Iteration 4 (`/copy` endpoint) and keep using the same sync helper contract (`renameNodeLabel`, `stripDeletedNodes`) to avoid divergence.
