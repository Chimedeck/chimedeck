## Update
- Refactored `CommentEditor.tsx` to integrate offline draft support using the new `useOfflineCommentDraft` hook.
- Added draft status footer and recovery banner to the comment editor, mirroring the card description editor's UX.
- Updated submit logic to handle offline/online transitions and clear drafts after successful submit.

## New
- Added `src/extensions/OfflineDrafts/hooks/useOfflineCommentDraft.ts` for comment draft persistence, background sync, and recovery.
- Created `specs/tests/offline-comment-draft-recovery.md` with 19 Playwright MCP scenarios for offline comment draft flows.
- Added `specs/tests/offline-comment-draft-recovery.test.ts` as a Playwright test for IndexedDB draft save/recovery.
- Added minimal `playwright.config.ts` to enable Playwright test discovery in `specs/tests/`.

## Technical Debt
- Playwright test only covers a subset of Markdown scenarios; full coverage is pending.
- Test login URL and dev server routing require further adjustment for full E2E automation.

## What Should Be Done Next
- Expand Playwright tests to cover all scenarios in the Markdown spec.
- Fix test login navigation and ensure dev server serves correct routes for E2E tests.
- Apply offline draft pattern to other editors (e.g., CardDescriptionTiptap) as planned for next iteration.
- Implement the real overflow menu for the single-line toolbar in CardDescriptionTiptap and CommentEditor.