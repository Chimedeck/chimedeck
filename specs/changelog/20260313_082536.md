# Changelog — 20260313_082536

## Update
- `InviteExternalUserModal.tsx`: Added "Mark email as verified" checkbox (default: checked). Checkbox is hidden when `EMAIL_VERIFICATION_ENABLED` feature flag is false. The `autoVerifyEmail` value is forwarded to the API when the flag is enabled.
- `CredentialSheet.tsx`: Added email verification status label ("Email verified" / "Email not verified") based on `email_verified_at` returned in the API response.
- `adminInvite.slice.ts`: Extended state and `setInviteCredentials` action to persist `emailVerifiedAt`.
- `types.ts`: Extended `AdminCreateUserRequest` to include `autoVerifyEmail?: boolean`; extended `AdminCreateUserResponse.data` to include `email_verified_at`; extended `AdminInviteState` to include `emailVerifiedAt`.
- `featureFlagsSlice.ts`: Added `emailVerificationEnabled` flag to state, fetch type, initial state, fulfilled handler, and selector (`selectEmailVerificationEnabled`).
- `server/index.ts`: Exposed `emailVerificationEnabled` from the `EMAIL_VERIFICATION_ENABLED` platform flag in the `/api/v1/flags` response.

## New
- `specs/tests/admin-auto-verify-email-ui.md`: Playwright MCP test spec covering modal checkbox default state, submission with checkbox checked/unchecked, credential sheet status display, and checkbox visibility under the feature flag.

## Technical Debt
- `featureFlagsSlice.ts` lines 65-70 have pre-existing TypeScript type errors (`action.payload.X` vs `action.payload.data.X`) from before this iteration; line 70 follows the same pattern — no new TS errors introduced in AdminInvite files.

## What Should Be Done Next
- Iteration 3: Implement Sprint 75 — Theme infrastructure (config, CSS vars, init, useTheme, ThemeToggle).
