# Changelog — 20260330_045808

## Update
- Refactored `emailChangeConfirmation.ts` to use `renderTemplate` and removed inline HTML template literal. Function is now `async`.
- Refactored `passwordResetEmail.ts` to use `renderTemplate` and removed inline HTML template literal. Function is now `async`.
- Updated `forgotPassword.ts` to `await buildPasswordResetEmail(...)`.
- Updated `changeEmail.ts` to `await buildEmailChangeConfirmation(...)`.

## New
- `server/extensions/email/templates/html/emailChangeConfirmation.html` — Handlebars template for email-change confirmation emails (`{{newEmail}}`, `{{confirmUrl}}`, `{{expiresIn}}`).
- `server/extensions/email/templates/html/passwordResetEmail.html` — Handlebars template for password reset emails (`{{resetUrl}}`, `{{expiresIn}}`).
- `server/extensions/email/templates/README.md` — Documents the email template pattern: one `.html` per email, Handlebars `{{var}}` syntax, `renderTemplate` usage, and migration rationale.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Sprint 121 is complete. All email templates under `server/extensions/email/templates/` now use the centralised Handlebars pattern with external `.html` files.
- If new emails are added, follow the pattern documented in `README.md`.
- Consider adding Handlebars template lint/validation in CI.
