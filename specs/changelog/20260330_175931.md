# Changelog 20260330_175931

## Update

- Added Sprint 123 planning spec for Sentry rollout across both runtime tiers: React client and Bun server.
- Updated sprint index in `specs/sprints/sprint-plan.md` to include Sprint 123 in the sprint overview table.

## New

- Added `specs/sprints/sprint-123.md` with implementation scope covering:
  - client SDK bootstrapping and ErrorBoundary capture
  - server SDK initialization and API exception capture
  - unified release/environment tagging
  - source map upload pipeline
  - privacy filtering and independent tier-level enable flags
- Added feature-flag coverage entries in the sprint plan for `SENTRY_CLIENT_ENABLED` and `SENTRY_SERVER_ENABLED`.

## Technical Debt

- Sprint 122 exists as a dedicated sprint file but is still not indexed in `specs/sprints/sprint-plan.md`; this should be reconciled in a separate housekeeping update to keep the plan fully consistent.

## What Should Be Done Next

- Implement Sprint 123 in code with minimal, centralized integration points (`src/main.tsx`, `server/index.ts`, and config modules).
- Add `.env.example` entries and deployment pipeline steps for Sentry release/source map upload.
- Validate with one controlled client error and one controlled server error in staging before production enablement.
