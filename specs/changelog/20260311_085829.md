## Update
- Added `@opentelemetry/*` dependencies to `package.json` and updated `bun.lock`.
- Updated `server/config/env.ts` to include `OTEL_EXPORTER_OTLP_ENDPOINT` and `OTEL_SERVICE_NAME` with safe defaults and documentation.
- Fixed `server/mods/observability/tracer.ts` to use the new `NodeTracerProvider` API (spanProcessors in constructor).
- Modified `server/index.ts` to call `initObservability()` at server startup, gated by `OTEL_ENABLED`.

## New
- Created `server/mods/observability/index.ts` to centralize OTel initialisation (`initObservability`).
- OTel tracer and metrics initialisation now fully gated by `OTEL_ENABLED` env var.

## Technical Debt
- None

## What Should Be Done Next
- Add E2E tests to verify OTel traces/metrics are exported when enabled.
- Implement IndexedDB persistence for offline mutation queue.
- Expand OTel instrumentation coverage and error handling.
- Review and update documentation for observability setup.