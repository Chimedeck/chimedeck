## Update
- Hardened `GET /api/v1/boards/:boardId/state-transitions` to reconcile persisted graph data with current board lists before returning data.
- GET now self-repairs stale transition graphs by stripping deleted-list nodes/edges and refreshing renamed list labels.

## New
- Added regression coverage for GET repair behavior after list rename/delete drift in `server/extensions/stateTransitions/api/__tests__/get.test.ts`.

## Technical Debt
- Repository-wide lint/typecheck baseline still contains pre-existing failures outside this sprint scope.

## What Should Be Done Next
- Continue Iteration 4 (`copy` endpoint) from the approved roadmap and keep sync semantics aligned across GET/PUT/copy flows.
