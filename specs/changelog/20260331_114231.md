## Update
- No changes to existing modules; all updates in this iteration are additive.

## New
- Added `terraform/modules/asg-slot/` module:
  - `main.tf`: Launch Template, ASG, Target Group for a single slot.
  - `variables.tf`: All required input variables for slot configuration.
  - `outputs.tf`: Outputs for ASG and Target Group ARNs.
- Added `terraform/modules/ec2-asg-bluegreen/` module:
  - `main.tf`: Composes two `asg-slot` modules, sets up ALB with weighted routing, health gate, and precondition on weights.
  - `variables.tf`: Inputs for blue/green weights, slot configs, health gate, ACM cert, etc.
  - `outputs.tf`: Outputs for ALB, target groups, and ASG names.
  - `README.md`: Usage, architecture, slot switching, and variable/output documentation.

## Technical Debt
None

## What Should Be Done Next
- Integrate both modules into environment-level compositions.
- Implement and test slot switching workflows in staging.
- Add automated tests for health gate and precondition logic.
- Review and refine documentation as needed after integration.