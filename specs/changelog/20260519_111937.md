## Update
- Refactored `label.ts` to emit canonical label shape (`id`, `idBoard`, `name`, `color`) for all contexts.
- Updated `card.ts`, `action.ts`, and `board.ts` serializers to ensure embedded label objects use the canonical label serializer.
- Tightened `toCardLabelIds` to only extract valid string IDs.
- Expanded contract tests in `boards.contract.test.ts` and `cards.contract.test.ts` to assert embedded label shape consistency.
- Updated normalization matrix for Sprint 151 endpoints with new audit results.

## New
- Added `labels.contract.test.ts` to assert standalone label endpoint contract.
- Added `actions.contract.test.ts` to assert embedded label shape in action payloads.

## Technical Debt
- No cross-serializer audit for embedded label consistency prior to this iteration; now partially addressed.
- Some legacy code paths may still emit ad-hoc label shapes; flagged for future refactor.

## What Should Be Done Next
- Complete audit and refactor of any remaining legacy code emitting non-canonical label shapes.
- Expand contract tests to cover all embedded label contexts, including edge cases.
- Ensure test credentials and dev server state allow Playwright UI/API flow tests to run successfully.
- Continue updating normalization matrix as further endpoints are audited.