## Update
- Fixed `server/extensions/mcp/http/sessions.ts` to use `WebStandardStreamableHTTPServerTransport` instead of the Node.js transport, aligning with Bun's web-standard API.
- Modified `server/index.ts` to import and wire in the new `mcpHttpHandler` before the 404 fallback, ensuring correct routing and authentication enforcement for MCP HTTP endpoints.

## New
- Added `server/extensions/mcp/http/index.ts` implementing `mcpHttpHandler` with POST (initialize), GET (SSE), and DELETE (terminate) endpoints for MCP HTTP transport, enforcing authentication and session ownership.
- Created `specs/tests/sprint-106-mcp-http-init.spec.ts`, a Playwright test covering unauthenticated POST (401), valid POST (200 + session header), DELETE (204), and session hijacking (403).

## Technical Debt
- None

## What Should Be Done Next
- Investigate and resolve why the dev server process does not persist for Playwright test automation (likely process management or SIGHUP issue).
- Ensure the dev server stays running to enable automated Playwright MCP HTTP tests.
- Re-run Playwright tests after server persistence is fixed to verify MCP HTTP handler functionality.
- Continue with further MCP HTTP endpoint enhancements and broader test coverage as needed.
