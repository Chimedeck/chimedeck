## Update
- Added `money_label` (varchar 100, nullable) column to `cards` table via migration `0094_card_money_label.ts`.
- Implemented new `PATCH /api/v1/cards/:id/money` endpoint in `server/extensions/card/api/money.ts` and mounted it in the card API router.
- Endpoint supports both JWT and `hf_` API token authentication using the existing `authenticate` middleware.
- Added Playwright MCP test spec for card money endpoint covering happy path, partial updates, null clearing, and validation errors.

## New
- `db/migrations/0094_card_money_label.ts`: Migration for `money_label` column on `cards` table.
- `server/extensions/card/api/money.ts`: New handler for updating card money fields.
- `specs/tests/sprint-103-card-money.md`: Playwright MCP test spec for the new endpoint.

## Technical Debt
None.

## What Should Be Done Next
- Convert `.md` Playwright test spec to `.spec.ts` or `.spec.js` so it is discovered and executed by Playwright.
- Re-run Playwright E2E tests to confirm all tests are now discovered and executed.
- Continue with any remaining client-side polish, additional translations, or deferred integration tests for notification preferences.
- Proceed to Sprint 101 (API Token Infrastructure) as planned.
