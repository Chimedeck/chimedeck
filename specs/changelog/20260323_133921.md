## Update
- Updated `playwright.config.ts` to set `testDir` to `./tests/e2e`, ensuring E2E tests are discovered and run.
- Verified all supporting infrastructure for notification preferences is present and functional.

## New
- Added `db/migrations/0093_api_tokens.ts` to create the `api_tokens` table with all required columns and indexes.
- Added `server/extensions/apiToken/mods/generate.ts` with a `generateApiToken()` helper that returns a raw token, hash, and prefix. Unit-tested for format correctness.

## Technical Debt
None.

## What Should Be Done Next
- Continue with any remaining client-side polish, additional translations, or deferred integration tests for notification preferences.
- Ensure Playwright E2E tests are executed as part of CI and cover all relevant flows.
