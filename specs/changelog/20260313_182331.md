## Update
- Integrated Board Members Panel into Board extension, leveraging boardMembersSlice and workspace member selectors.
- Enforced last-admin guard and admin-only permissions in member management UI.

## New
- Added `src/extensions/Board/components/BoardMembersPanel/index.tsx`: Main panel for viewing and managing board members.
- Added `MemberRow.tsx`: Renders each member with role selector and remove button, enforcing last-admin guard.
- Added `AddMemberInput.tsx`: Typeahead input for adding workspace members to the board.
- Created test spec: `specs/tests/sprint-79-iteration-5-board-members-panel.md` covering add, change role, remove, and guard scenarios.

## Technical Debt
- Playwright UI tests for Board Members Panel are not yet implemented; only the markdown test spec exists.
- Playwright cannot be run via Bun; Node.js must be used for test execution.

## What Should Be Done Next
- Implement Playwright test files for Board Members Panel as per the markdown spec.
- Ensure Playwright tests run under Node.js, not Bun, to resolve loader conflicts.
- Proceed to Sprint 80: guest invite flow, guest workspace view, client permission guards, and related UI/RTK slices.
- Defer Workspace Board Grid Visibility Badge and Board Header Avatar Stack to next iteration.