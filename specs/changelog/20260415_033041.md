# Changelog — 2026-04-15T03:30:41Z

## Update
- `WebhooksRegisterPage.tsx` — wired up Edit and Delete action buttons in the table rows; each button now opens the corresponding modal/dialog with the selected webhook passed as a prop.
- `translations/en.json` — added missing `EditWebhookModal.cancel` and `DeleteWebhookDialog.body` translation keys.

## New
- `EditWebhookModal.tsx` — modal for editing an existing webhook. Pre-populates label, endpoint URL, event-type checklist, and active/inactive toggle from the selected webhook. Shares the same validation rules as `RegisterWebhookModal` (https:// prefix, at least one event type).
- `DeleteWebhookDialog.tsx` — confirmation dialog that displays the webhook's label and calls `deleteWebhook(id)` on confirm. Calls `onDeleted` callback on success; silently stays open on error.
- `tests/unit/webhooks/EditWebhookModal.test.tsx` — 24 unit tests covering pre-fill logic, active toggle, URL/event validation, and update payload construction.
- `tests/unit/webhooks/DeleteWebhookDialog.test.tsx` — 8 unit tests covering label display, confirm/cancel flows, and error handling.
- `tests/integration/webhooks/ui-crud.test.ts` — 16 integration tests covering the complete edit and delete UI CRUD flows including state management, API payload correctness, and modal exclusivity.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 8: `SignatureVerificationSnippet` (collapsible panel with JS verification code) and completing `translations/en.json` with any remaining keys.
