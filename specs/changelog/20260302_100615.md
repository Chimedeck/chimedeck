## Update
- Updated README.md with setup, development, and Docker Compose instructions.
- Updated .gitignore to cover Bun, node_modules, env files, and build artifacts.

## New
- Initialized Bun + TypeScript project with strict mode (package.json, tsconfig.json).
- Added ESLint (flat config) and Prettier for linting/formatting.
- Added Dockerfile and docker-compose.yml (Postgres, MinIO, optional Redis via profile).
- Created .env.example covering both Redis and non-Redis modes.
- Scaffolded server skeleton: server/index.ts, config modules, logger, helmet, openapi, parser middleware.
- Implemented feature flag system (multi-provider: defaults, env, json, remote stubs) in server/mods/flags/.
- Added minimal Prisma schema and migration for DB bootstrapping.
- Added CI workflow (.github/workflows/ci.yml) for lint, typecheck, test, and Docker build.
- Added placeholder test for feature flags.

## Technical Debt
- Flagsmith and FeatBit providers are stubs (to be implemented in future sprints).
- Activity backfill and board_snapshots policy are stubbed per architecture spec.
- Docker Compose healthchecks are basic; may need refinement for production.

## What Should Be Done Next
- Implement application features per sprint-02 and beyond.
- Flesh out remote flag providers (Flagsmith, FeatBit) and board_snapshots policy.
- Expand test coverage and add integration tests for all endpoints.
- Review and refine Docker Compose healthchecks and CI for production readiness.
- Monitor for any architectural ambiguities as features are added.