# Changelog - 2026-06-03 20:42:45

## Update

- Added Sprint 170 save/commit sync wiring for the Documentation workspace: delta save requests now carry `If-Match`, dirty files autosave on a debounce, and commit flow stages pending specs files from the workspace footer.
- Extended the board router with `PUT /api/v1/boards/:boardId/github/specs/file` and `POST /api/v1/boards/:boardId/github/specs/commit`.

## New

- `server/extensions/board/mods/specs/write.ts`: specs-only markdown write helper with path validation, stale precondition checks, SHA/ETag calculation, and created/updated detection.
- `server/extensions/board/mods/specs/commit.ts`: git commit helper that stages specs markdown files, appends actor/board/bot footer metadata, and pushes when an installation token is available.
- `src/extensions/DeveloperDocs/containers/SpecsWorkspacePage/SpecsWorkspacePage.tsx`: autosave + reload + commit shell for the Documentation workspace, including conflict state and commit message footer.
- `server/extensions/board/api/github/specs/*`: route handlers for save/commit.
- `server/extensions/board/api/__tests__/specs.test.ts`: handler coverage for save success, stale precondition, markdown-only rejection, and commit pending/pushed responses.

## Technical Debt

- The repo still has unrelated baseline typecheck issues outside the Sprint 170 files touched here.
- The component test suite still depends on the repo’s existing React test environment setup.

## What Should Be Done Next

- Configure the React test environment so the workspace component tests can run under the project’s standard test command.
- Decide whether to surface a dedicated “Reload from server” banner in the workspace header for conflict recovery.
