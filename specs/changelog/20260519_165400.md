## Update
- Board Settings UI updated to include "Enforceable State Transitions" section, gated by feature flag and auto-hiding/auto-closing when disabled.
- Redux store and reducers extended to register StateTransitions API slice and feature flag.
- Server `/api/v1/flags` endpoint exposes `stateTransitionsEnabled` for client gating.

## New
- Added `src/extensions/StateTransitions/` feature module:
  - RTK Query API slice (`api.ts`) for get/put state transitions.
  - Board Settings entry (`StateTransitionsSettingsEntry.tsx`).
  - Full-screen overlay graph editor (`GraphEditor/index.tsx`, `GraphEditorHeader.tsx`, `GraphCanvas.tsx`).
  - Custom Kanban column node (`ColumnNode.tsx`).
  - State management hooks (`useGraphEditor.ts`, `useStateTransitionsSync.ts`).
  - English translations (`translations/en.json`).
- Installed `@xyflow/react` for ReactFlow canvas.
- Persisted node positions and enforce toggle via debounced API PUT.

## Technical Debt
- List sync hooks remain synchronous (should be async/event-driven).
- Some Trello-compat endpoints have partial field-projection/test coverage.
- UI: Node overlap not auto-resolved; edge creation/inspector and toolbar deferred.

## What Should Be Done Next
- Implement edge creation, edge inspector, toolbar, sticky notes, real-time sync, Kanban DnD enforcement, copy modal, and banner (see Iterations 8–12).
- Add empty state for boards with no lists.
- Improve error handling (toasts, disable editing on API failure).
- Refactor list sync hooks to be async/event-driven.