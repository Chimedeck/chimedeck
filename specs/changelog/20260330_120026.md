## Update
- Refactored `emailChangeConfirmation.ts` and `passwordResetEmail.ts` to use external Handlebars `.html` templates and the shared `renderTemplate` helper.
- Updated all call sites (`changeEmail.ts`, `forgotPassword.ts`) to `await` the now-async email builder functions.

## New
- Added `html/emailChangeConfirmation.html` and `html/passwordResetEmail.html` Handlebars templates with appropriate `{{...}}` placeholders.
- Created `README.md` in `server/extensions/email/templates/` documenting the new email template pattern and migration rationale.

## Technical Debt
None

## What Should Be Done Next
- If any additional email templates or call sites remain unmigrated, migrate them to the new Handlebars pattern in a future iteration.
- Consider adding tests for edge cases involving expired/invalid tokens and direct email content verification if feasible.
- Review for i18n/localisation needs in email templates.