## Update
- Updated `server/extensions/activity/mods/createActivityEvent.ts` to call `mapActivityToNotification` after each activity write alongside the existing `publishCardActivityEvent` call.
- Updated `server/extensions/notifications/mods/preferenceGuard.ts` to include `card_member_assigned` and `card_member_unassigned` in `NOTIFICATION_TYPES`.
- Updated `server/extensions/notifications/mods/emailDispatch.ts` to import and dispatch email templates for `card_member_assigned` and `card_member_unassigned`.
- Updated `src/extensions/Notifications/NotificationPreferences/types.ts` to add `card_member_assigned` and `card_member_unassigned` types with display labels.
- Updated `src/extensions/Notification/components/NotificationItem.tsx` to render icons, accents, and copy for `card_member_assigned` and `card_member_unassigned` notification types.
- Appended Section 10 (notification mapping and fan-out) to `specs/tests/card-activity-create-move-assign.md` with 10 Playwright MCP scenarios.

## New
- Added `server/extensions/activity/mods/mapActivityToNotification.ts` — server-side mapper that fans out workspace member notifications for `card_created`, `card_moved`, `card_member_assigned`, and `card_member_unassigned` activity events, respecting per-user notification preferences.
- Added `src/extensions/Notifications/mods/mapActivityToNotification.ts` — client-side utility that converts an activity event payload + actor name into a notification display object (copy, type, listTitle), returning `null` when the user has opted out via in-app preferences.
- Added `server/extensions/notifications/mods/emailTemplates/cardMemberAssigned.ts` — email template for member assignment notifications.
- Added `server/extensions/notifications/mods/emailTemplates/cardMemberUnassigned.ts` — email template for member unassignment notifications.

## Technical Debt
- `boardActivityDispatch.ts` and `mapActivityToNotification.ts` (activity-side) now handle overlapping event types (`card_created`, `card_moved`) via different input shapes. These could be unified behind a single notification pipeline in a future refactor.

## What Should Be Done Next
- Run Iteration 11: regression and acceptance hardening — retest all scenarios end-to-end, address edge cases, and finalize Playwright MCP runbook.
