# Update
- Extended avatar URL normalization to board, card, and notification payloads so AWS S3-backed avatars are returned as pre-signed URLs instead of raw object URLs.
- Kept localhost LocalStack/custom endpoint behavior intact by reusing the existing endpoint-aware resolver path.

# New
- Added `resolveAvatarUrlsInCollection` helper to normalize arrays of entities that include an `avatar_url` field with per-request URL caching.
- Applied normalization to:
  - board detail card-member aggregates
  - board member suggestions
  - card detail members
  - card list member aggregates
  - notifications list actor payload
  - realtime `notification_created` actor payload

# Technical Debt
- The aggregate SQL still selects `users.avatar_url` as raw source data and then normalizes post-query in application code.
- If these payloads grow, consider DB-level or service-level projection strategy to reduce repeated mapping logic.

# What Should Be Done Next
- Add integration tests for board/card/notification responses asserting avatar URLs are signed in AWS mode and remain direct in LocalStack mode.
- Add websocket-level tests for `notification_created` actor payload avatar URL behavior.
- Consider a typed shared response mapper for user-like objects (`id`, `name`, `avatar_url`) to enforce normalization centrally.
