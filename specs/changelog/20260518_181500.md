## Update
- Added Trello-compatible custom field router wiring in `server/extensions/trelloCompat/api/index.ts`.
- Extended `server/extensions/trelloCompat/api/cards/index.ts` to support custom field item upsert and typed custom field item serialization.
- Updated `server/extensions/trelloCompat/serializers/card.ts` and `server/extensions/trelloCompat/types/trello.ts` to include `customFieldItems` and Trello custom field item/option typings.
- Added Trello custom field specific errors in `server/extensions/trelloCompat/common/errors.ts`.

## New
- Added `server/extensions/trelloCompat/api/customFields/index.ts` implementing:
  - `POST /trello/1/customFields`
  - `GET /trello/1/customFields/{id}`
  - `PUT /trello/1/customFields/{id}`
  - `DELETE /trello/1/customFields/{id}`
  - `GET /trello/1/customFields/{id}/options`
  - `POST /trello/1/customFields/{id}/options`
  - `DELETE /trello/1/customFields/{id}/options/{id}`
- Added `server/extensions/trelloCompat/serializers/customField.ts` for Trello custom field and custom field item serializers.
- Added integration coverage in `tests/integration/trelloCompat/customFields.test.ts`.
- Extended card endpoint coverage in `server/extensions/trelloCompat/api/cards/__tests__/cards.test.ts` for:
  - `PUT /trello/1/cards/{idCard}/customField/{idCustomField}/item`
  - `GET /trello/1/cards/{id}/customFieldItems`

## Technical Debt
- Trello compatibility routers still duplicate board permission helpers (`getWorkspaceRole`, `canReadBoard`, `canMutateBoard`) across resources and should be consolidated into shared helpers.
- `lint` and `typecheck` continue to report pre-existing unrelated failures outside Trello compatibility code.

## What Should Be Done Next
- Add parity tests for number/date/checkbox/list custom field value upserts and invalid payload cases.
- Add field-level read endpoint parity for `/trello/1/customFields/{id}/{field}` if required by client compatibility tests.
- Continue consolidating shared Trello compat auth/permission helpers to reduce repeated logic.
