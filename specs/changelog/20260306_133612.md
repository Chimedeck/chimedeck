## Update
- Synced escrow plugin SDK runtime files to the current capability protocol used by the kanban host bridge.
- Updated `plugins/sharetribe-horizon-escrow/public/js/jh-instance.js` to emit `RESOLVE_CAPABILITY_RESPONSE` for `CAPABILITY_INVOKE` and support callback serialization for button handlers.
- Synced matching dist copies in `plugins/sharetribe-horizon-escrow/dist/web/client/js/jh-instance.js` and `plugins/sharetribe-horizon-escrow/dist/web/server/js/jh-instance.js`.

## New
- Escrow capability responses now include callback-safe payload encoding (`__callbackId`) and `BUTTON_CLICKED` handling, enabling `card-buttons` callbacks (e.g. modal open actions) to execute correctly.

## Technical Debt
- SDK runtime code exists in multiple locations (`public/sdk`, plugin `public/js`, and plugin `dist/**`), which can drift again if not synchronized by the build pipeline.

## What Should Be Done Next
- Consolidate SDK distribution so escrow plugin builds always consume `server/extensions/plugins/sdk/jh-instance.ts` output automatically.
- Add a small integration check that validates a plugin capability flow (`CAPABILITY_INVOKE` → `RESOLVE_CAPABILITY_RESPONSE`) for active board plugins.
- Rebuild/redeploy escrow plugin artifacts in environments that serve prebuilt `dist/` assets.
