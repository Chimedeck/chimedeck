# Changelog — 20260324_164000

## Update
- `src/extensions/HealthCheck/components/HealthCheckStatusDot.tsx` — new file: presentational traffic-light dot with hover tooltip and pulse animation for green/amber statuses; fully accessible (`role="img"`, `aria-label` tooltip text).
- `src/extensions/HealthCheck/components/HealthCheckRow.tsx` — new file: single row showing status dot, service name, URL (clickable link), response time, relative last-checked time, and a hover-revealed `TrashIcon` remove button with loading spinner while probing.
- `src/extensions/HealthCheck/components/HealthCheckEmptyState.tsx` — new file: empty state panel with `HeartIcon` illustration, heading, description, and "Add your first service" CTA button.
- `src/extensions/HealthCheck/components/HealthCheckCountdown.tsx` — new file: countdown timer for auto-refresh with `aria-live="polite"` region, visual progress bar, and "Paused" state (for when the Page Visibility API hides the tab).
- `src/extensions/HealthCheck/components/index.ts` — new barrel file re-exporting all four components.

## New
- `src/extensions/HealthCheck/components/__tests__/HealthCheckStatusDot.test.tsx` — smoke tests covering all status values and tooltip content.
- `src/extensions/HealthCheck/components/__tests__/HealthCheckRow.test.tsx` — render tests for probing spinner, response time formatting, and Timeout label.
- `src/extensions/HealthCheck/components/__tests__/HealthCheckEmptyState.test.tsx` — render + click handler tests.
- `src/extensions/HealthCheck/components/__tests__/HealthCheckCountdown.test.tsx` — tests for seconds/minutes formatting, zero-seconds state, paused state, and live-region attribute.

## Technical Debt
- Test files import `@testing-library/react` and `vitest` — these are flagged as missing by `tsconfig.client.json` (same pre-existing pattern as other test files excluded from the client tsconfig). Tests will run via the test runner config (vite/vitest) which resolves these correctly.

## What Should Be Done Next
- Iteration 8: Add Service Modal (`AddServiceModal.tsx`), `useHealthCheckAutoRefresh` hook, and `useHealthCheckProbe` hook.
