## Update
None.

## New
- Added five standalone MCP scenario files in `specs/tests/mcp/`:
  - `get_boards.md`: Workspace board listing scenarios
  - `get_board.md`: Board detail retrieval scenarios
  - `get_cards.md`: List cards in a list scenarios
  - `get_card.md`: Single card detail scenarios
  - `create_card.md`: Card creation and validation scenarios
- All files follow the scenario format (Title, Overview, Preconditions, numbered steps with **Assert** markers, expected API paths/responses).

## Technical Debt
- MCP scenario tests require `.vscode/mcp.json` to run; all tests currently fail for missing config, not scenario content.
- No new UI, API, or auth code was changed; only scenario markdown files were added.

## What Should Be Done Next
- Add or update `.vscode/mcp.json` to enable MCP scenario test execution.
- Review and refine scenario files for completeness and alignment with evolving requirements.
- Implement or update MCP tool endpoints as needed to pass new scenario tests.
- Address any test failures once MCP config is in place.