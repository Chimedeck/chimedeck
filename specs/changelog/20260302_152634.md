# Sprint 17 — Workspace Dashboard & Navigation Shell

## Update

- `src/routing/index.tsx` — private routes now wrapped in `AppShell` (sidebar layout); `WorkspacesPage` lazy import points to new `WorkspaceListPage` instead of `WorkspacePage`.
- `src/store/index.ts` — added `workspaceShell` reducer key.
- `src/reducers.ts` — exports `workspaceShellReducer` from workspace duck.
- `src/extensions/Workspace/routes.ts` — added `/workspaces` route pointing to `WorkspaceListPage`.
- `src/extensions/Workspace/translations/en.json` — added Sidebar, WorkspaceListPage, and CreateWorkspaceModal i18n strings.

## New

- `src/extensions/Workspace/duck/workspaceDuck.ts` — shared Redux slice: `workspaces[]`, `activeWorkspaceId`, `status`; thunks `fetchWorkspacesThunk` + `createWorkspaceThunk`; action `setActiveWorkspace`; selectors.
- `src/extensions/Workspace/components/Sidebar.tsx` — persistent sidebar: workspace switcher dropdown, nav links (Boards / Members / All Workspaces), user menu with logout, mobile-responsive (collapsible).
- `src/extensions/Workspace/components/CreateWorkspaceModal.tsx` — Radix Dialog for creating a workspace with name field, validation, and optimistic dispatch.
- `src/extensions/Workspace/containers/WorkspaceListPage/WorkspaceListPage.tsx` — card grid of all workspaces with empty state CTA.
- `src/layout/AppShell.tsx` — sidebar + `<Outlet />` layout wrapper for all private pages; fetches workspaces on mount; mobile hamburger drawer.
- `src/extensions/Workspace/README.md` — documents extension structure, Redux state shape, thunks, and routes.
- `src/extensions/Workspace/__tests__/WorkspaceShell.test.tsx` — 8 unit tests for workspace slice (fetch, switch, error states).
- `src/extensions/Workspace/__tests__/WorkspaceModal.test.tsx` — 4 unit tests for createWorkspaceThunk (pending/fulfilled/rejected).

## Technical Debt

- `WorkspacePage.tsx` still uses `LayoutSingleColumn` — should be updated to render plain page content once all page layouts are migrated to `AppShell`.
- `BoardListPage.tsx` still uses `LayoutSingleColumn` — same migration needed.
- `WorkspaceSwitcher.tsx` (standalone component) and the duck inside `WorkspacePage.duck.ts` still duplicate workspace-list state; these should be consolidated onto `workspaceDuck` in a future clean-up sprint.

## What Should Be Done Next

- Sprint 18: Board View (Kanban) — `BoardPage`, `ListColumn`, `CardTile`, drag-and-drop with `@dnd-kit`.
- Migrate `BoardListPage` and `WorkspacePage` to pure content components (remove `LayoutSingleColumn` from them since `AppShell` owns the chrome).
- Add `Header.tsx` breadcrumb bar inside `AppShell` once board navigation is in place.
