# Changelog — 2026-03-24T07:36:50Z

## Update
- `PluginRegistryPage.tsx`: Wired the "+ Register Plugin" button to open `RegisterPluginModal`. On successful submission the page transitions to `ApiKeyRevealModal` showing the one-time API key. Closing the reveal triggers a list refresh so the new plugin appears at the top.
- `PluginRegistryPage.duck.ts`: Added `addPluginThunk` which calls `registerPlugin` API and returns the full plugin payload (including the one-time `apiKey`) to the caller.

## New
- `addPluginThunk` in `PluginRegistryPage.duck.ts` — async thunk that wraps `POST /api/v1/plugins` for platform admins.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 2: Add `GET /api/v1/boards/:boardId/plugins/available` server endpoint.
- Iteration 3: Overhaul `PluginDashboardPage` with Enabled + Discover sections.
- Iteration 4: Write Playwright MCP scenario for board plugin discovery.
