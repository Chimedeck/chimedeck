# Changelog — 2026-03-23T03:25:00Z

## Update
- Rebuilt `notifications_type_check` and `notification_preferences_type_check` DB constraints to include the three new notification types.

## New
- `db/migrations/0091_notification_types_extend2.ts` — drops and recreates both CHECK constraints with the full 9-type list: `mention`, `card_created`, `card_moved`, `card_commented`, `card_member_assigned`, `card_member_unassigned`, `card_updated`, `card_deleted`, `card_archived`.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Update `NOTIFICATION_TYPES` constant in `server/extensions/notifications/common/types.ts` (or `preferenceGuard.ts`) and `boardActivityDispatch.ts` payload union to include the 3 new types (Iteration 4).
