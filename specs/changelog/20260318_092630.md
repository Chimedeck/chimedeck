## Update
- Updated `server/extensions/search/api/index.ts` to add a board-scoped search route (`GET /api/v1/boards/:boardId/search`).

## New
- Added `server/extensions/search/mods/queryBoardSearch.ts`: Board-constrained full-text query module for lists and non-archived cards.
- Added `server/extensions/search/api/getBoardSearch.ts`: Handler for board search endpoint, enforcing board access and delegating to query module.
- Added `specs/tests/board-scoped-search.md`: 13 scenario Markdown spec for board-scoped search API, including Sprint 84 acceptance checklist.

## Technical Debt
- Playwright test file for board-scoped search scenarios not yet implemented; only Markdown spec exists.

## What Should Be Done Next
- Convert `specs/tests/board-scoped-search.md` scenarios into a Playwright test file for automated testing.
- Run Playwright tests and report PASS/FAIL for each scenario.
- Address any test failures or gaps identified during implementation.