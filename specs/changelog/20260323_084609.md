## Update
- Wired `move-card` and `comment` commands into `cli/index.ts` dispatcher.

## New
- `cli/commands/moveCard.ts`: `move-card` command — PATCH `/api/v1/cards/:cardId/move`, flags `--card`, `--list`, `--position`. Prints confirmation on success; `--json` outputs raw JSON.
- `cli/commands/comment.ts`: `comment` command — POST `/api/v1/cards/:cardId/comments`, flags `--card`, `--text`. Prints confirmation on success; `--json` outputs raw JSON.

## Technical Debt
- None

## What Should Be Done Next
- Iteration 8: Implement `create-card` and `edit-description` commands.
