# Changelog — 20260312_032830

## Update
- None

## New
- `src/extensions/Automation/utils/scheduleSummary.ts` — pure functions `scheduleSummary` and `dueDateSummary` converting schedule/due-date configs to human-readable strings (e.g. "Every Monday at 09:00", "2 days before due date")
- `src/extensions/Automation/components/SchedulePanel/builders/ScheduledCommandBuilder.tsx` — 3-step modal for creating/editing SCHEDULED automations (step 1: frequency + day/time picker with live preview; step 2: ActionList (list/board-scoped); step 3: auto-generated name override + save)
- `src/extensions/Automation/components/SchedulePanel/builders/DueDateCommandBuilder.tsx` — 2-step modal for creating/editing DUE_DATE automations (step 1: before/on/after timing + offset value/unit; step 2: ActionList + name override + save)

## Technical Debt
- ActionList used in both builders does not yet filter to list/board-scoped vs card-scoped action types — the API would need a `scope` query param or the picker could filter client-side once categories are returned

## What Should Be Done Next
- Iteration 7: SchedulePanel list UI (index.tsx, ScheduleList, ScheduleItem, ScheduleEmptyState), quick-start templates, and Schedule tab activation in AutomationPanel
