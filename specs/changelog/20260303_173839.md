## Update

### Sprint 27 — Collapsible Label Chips on Card Tiles
- `server/extensions/board/api/get.ts`: Fixed `GET /boards/:id` to join `card_labels→labels` and `card_members→users`, returning `labels` and `members` arrays per card via PostgreSQL `json_agg`/`jsonb_build_object`/`FILTER (WHERE x IS NOT NULL)`. Previously cards were fetched without these fields causing the board view to show no chips.
- `server/extensions/card/api/list.ts`: Same fix applied to `GET /lists/:id/cards` — replaced SQLite `json_group_array` with correct PostgreSQL aggregation.
- `src/extensions/Card/api.ts`: Added `labels: Array<{id, name, color}>` and `members: Array<{id, email, name}>` to the `Card` interface.
- `src/extensions/Card/hooks/useCardLabelExpanded.ts`: New hook — persists collapsed/expanded state per board in `localStorage` under key `card-labels-expanded:<boardId>`.
- `src/extensions/Card/components/CardLabelChips.tsx`: New component — renders coloured bars (collapsed) or labelled pills (expanded); `e.stopPropagation()` prevents opening the card modal on click.
- `src/extensions/Card/components/CardItem.tsx`: Renders `<CardLabelChips>` above title when labels present; renders `<CardMemberAvatars>` below title when members present; reads `currentUser` from Redux auth slice; uses `apiClient` directly for `removeMember`.
- `src/extensions/List/containers/BoardPage/ListColumn.tsx`: Passes `labelsExpanded` and `onToggleLabels` down to each `<CardItem>`.
- `src/extensions/Board/components/BoardCanvas.tsx`: Accepts `boardId` prop, calls `useCardLabelExpanded(boardId)`, passes state to columns and drag overlay.
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx`: Passes `boardId` to `<BoardCanvas>`.

### Sprint 28 — Member Avatar Popover
- `src/extensions/Card/components/MemberAvatarPopover.tsx`: New component — fixed-positioned popover anchored to clicked avatar; shows "Edit profile info" (own avatar) or "Remove from card" (other's) with spinner; closes on Escape/click-outside.
- `src/extensions/Card/components/CardMemberAvatars.tsx`: Rewritten — each avatar is now a `<button>` that opens `<MemberAvatarPopover>`; overflow `+N` chip is a plain `<span>` (no popover).
- `src/extensions/Card/components/CardMembers.tsx`: Added `cardId` and `currentUserId` props; passes them through to `<CardMemberAvatars>`.
- `src/extensions/Card/components/CardModal.tsx`: Passes `cardId={card.id}` and `currentUserId` to `<CardMembers>`.
- `src/extensions/UserProfile/containers/EditProfilePage/EditProfilePage.tsx`: New stub page — "Edit Profile" heading with back button; full implementation deferred.
- `src/routing/index.tsx`: Added lazy import for `EditProfilePage` and `/profile/edit` route inside `PrivateRoute`.

## New
- `src/extensions/Card/hooks/useCardLabelExpanded.ts`
- `src/extensions/Card/components/CardLabelChips.tsx`
- `src/extensions/Card/components/MemberAvatarPopover.tsx`
- `src/extensions/UserProfile/containers/EditProfilePage/EditProfilePage.tsx`

## Technical Debt
- `MembersSection.tsx` (CardModal sections) was also updated in a prior agent-loop run; its `cardId` prop integration is complete but not covered by automated tests.
- `EditProfilePage` is a stub — full profile editing (name, nickname, avatar) is not yet implemented.

## What Should Be Done Next
- Implement full profile editing on `/profile/edit` (name, nickname, avatar upload).
- Add Playwright E2E tests for label chip toggle (persistence in localStorage) and member avatar popover flows.
- Wire real-time WS events for `card_member_removed` so the avatar disappears across connected clients without reload.