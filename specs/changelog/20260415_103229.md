## Update
- Updated `WebhooksRegisterPage.tsx` to integrate Edit and Delete modals/dialogs, wiring up edit/delete actions from the table and passing callbacks to child components.
- Added new translation keys to `src/extensions/Webhooks/translations/en.json` for edit and delete modal/dialog UI.

## New
- Added `EditWebhookModal.tsx`: Modal for editing a webhook, with pre-filled fields, event-type checklist, and active/inactive toggle.
- Added `DeleteWebhookDialog.tsx`: Confirmation dialog for deleting a webhook, displaying the label and handling confirm/cancel actions.
- Created `tests/unit/webhooks/EditWebhookModal.test.tsx`: Unit tests for edit modal pre-fill, toggle, validation, and update logic.
- Created `tests/unit/webhooks/DeleteWebhookDialog.test.tsx`: Unit tests for delete dialog label display, confirm/cancel, and error handling.
- Created `tests/integration/webhooks/ui-crud.test.ts`: Integration tests for edit and delete UI CRUD flows.

## Technical Debt
- None

## What Should Be Done Next
- Resolve login failure in Playwright tests by ensuring the dev server is running with a seeded database and valid test credentials as per `specs/tests/TEST_CREDENTIALS.md`.
- Implement SignatureVerificationSnippet and related translations for webhook signature display and copy functionality.
- Continue with backend and UI enhancements as outlined in Sprint 135/136 specs.