## Update
Added `SUBSCRIPTIONS_ENABLED=false` coverage to `server/middlewares/limitGuard.test.ts`, mocking env, DB, and tier lookup so workspace and resource guards short-circuit without tier evaluation.

## New
New test coverage for the subscription-disabled bypass path in the limit guard.

## Technical Debt
None

## What Should Be Done Next
Revisit the existing limit-enforcement roadmap items: member/guest/storage caps, shared workspace rate limiting, billing UI/nav, and the global upgrade modal.
