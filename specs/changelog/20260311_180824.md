## Update
- Modified `server/extensions/attachment/api/index.ts` to add and wire all four multipart S3 upload endpoints before existing single-file routes.

## New
- Implemented `server/extensions/attachment/api/multipart/start.ts`: Initiates S3 multipart upload, validates MIME/size, creates PENDING attachment row.
- Implemented `server/extensions/attachment/api/multipart/partUrl.ts`: Generates pre-signed `UploadPart` URL with partNumber validation and key-ownership check.
- Implemented `server/extensions/attachment/api/multipart/complete.ts`: Calls S3 `CompleteMultipartUpload`, enqueues virus scan, publishes WS event, returns persisted attachment.
- Implemented `server/extensions/attachment/api/multipart/abort.ts`: Best-effort S3 abort and deletes PENDING DB row to prevent orphans.
- Added `tests/integration/attachments/multipart.test.ts` with 12 passing validation tests for the multipart flow.

## Technical Debt
- Input sanitization, CSRF protection, and test automation for edge cases are deferred.
- Playwright E2E tests for multipart upload flows are missing and must be created.

## What Should Be Done Next
- Scaffold and implement Playwright E2E tests for multipart upload flows (happy path, abort, error, WS events).
- Address input sanitization, CSRF protection, and automate edge case testing.
- Monitor for S3 error handling and DB cleanup improvements as usage grows.