## Update
- Updated `featureFlagsSlice.ts` to add `notificationPreferencesEnabled` and `emailNotificationsEnabled` flags, including state, reducer, and selectors.
- Modified `server/index.ts` to expose both flags in the `/api/v1/flags` endpoint.
- Updated `EditProfilePage.tsx` to mount the new Notification Preferences section, gated by the feature flag.

## New
- Created `NotificationPreferencesPanel.tsx`: Accessible table-of-toggles UI for 4 notification types × 2 channels (In-App, Email), with native `button[role="switch"]` and Tailwind styling. Email toggles are disabled if SES/email is off. RTK Query powers fetch/update. Error toasts on API failure.
- Added `specs/tests/notification-preferences-ui.md`: Playwright MCP spec with 9 scenarios for UI, toggling, optimistic update, error rollback, gating, accessibility, and loading state.

## Technical Debt
- Playwright MCP test did not run: "No tests found." Playwright may not recognize `.md` scenario files or the test file path/format. No UI flows were tested this iteration.
- No end-to-end UI tests for notification preferences are running yet.

## What Should Be Done Next
- Fix Playwright configuration to recognize and run `.md` scenario files for notification preferences UI tests.
- Complete end-to-end UI and integration tests for notification preferences.
- Wire notification preference checks into all notification types and implement SES/email delivery logic.
- Confirm copy and icons for each notification type with design/product if needed.
- Optimize notification batching and DB writes in future iterations.