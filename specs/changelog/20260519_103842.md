## Update
- Normalized action serializer output in `server/extensions/trelloCompat/serializers/action.ts` to ensure all required Trello keys are present and object-safe.
- Added generic field projection handler to `server/extensions/trelloCompat/api/actions/index.ts` for `GET /actions/{id}/{field}`.
- Expanded fixtures and assertion helpers for edge cases and stricter shape validation.
- Updated contract and integration tests for full key coverage and field projection.
- Updated normalization tracking in `specs/trello-adapter-normalization/matrix.md`.

## New
- Added `server/extensions/trelloCompat/tests/unit/actions.contract.test.ts` for focused adapter contract coverage.
- Added new fixtures for missing-list and edge-case actions.
- Added changelog entry for this iteration.

## Technical Debt
- API endpoints for `/api/actions` and `/api/actions/{id}` return 404 Not Found; routes may not be exposed or server misconfigured.
- Field projection handler and some edge-case tests may require further review once endpoints are accessible.

## What Should Be Done Next
- Verify and fix dev server/API routing to expose `/api/actions` endpoints.
- Re-run contract and integration tests once endpoints are accessible.
- Review and expand field projection and error handling for unsupported fields as needed.
- Continue normalization for any remaining Trello endpoints or edge cases.