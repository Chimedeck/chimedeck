## Update
- Modified `boardActivityDispatch.ts` to fetch recipients from `board_members` instead of workspace memberships.
- Applied board and global notification preference guards per recipient, ensuring opt-out logic (missing row = enabled).
- Dispatch logic now fails open on DB errors, avoiding notification loss.

## New
- Added `boardPreferenceGuard.ts` to check per-board notification preferences.
- Added `globalPreferenceGuard.ts` to check global notification settings.

## Technical Debt
- No automated tests for new notification preference logic; only manual verification performed.
- No API endpoints or client UI toggles for notification preferences yet.

## What Should Be Done Next
- Add unit/integration tests for notification preference guards and dispatch logic.
- Implement API endpoints and client UI toggles for managing notification preferences.
- Retest notification flows with new tests and document results.