# Changelog - 2026-05-08 15:30:36

## Update
- Hardened `requireMemberOrBoardGuestMember` to reliably allow board guest members on write endpoints.
- The helper now:
  - accepts `req.guestType === MEMBER` from board visibility middleware when present
  - normalizes DB `guest_type` to uppercase before comparing to `MEMBER`

## New
- Added `guestType?: string` to `WorkspaceScopedRequest` for middleware interop.

## Technical Debt
- Guest write permission logic exists in multiple flows; long-term, centralize all guest subtype checks into a single guard utility to avoid drift.

## What Should Be Done Next
- Add integration tests for guest member list creation where `guest_type` casing differs (`MEMBER`, `member`).
- Add regression test that confirms VIEWER guests remain blocked.
