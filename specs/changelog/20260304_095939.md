## Update
- Added `?include=activities` support to `server/extensions/card/api/get.ts` and `server/extensions/board/api/get.ts` for sideloading filtered activities in API responses.
- Updated activity endpoints to filter events using a centralized allowlist (`VISIBLE_EVENT_TYPES`).

## New
- Created `server/extensions/activity/config/visibleEventTypes.ts` exporting `VISIBLE_EVENT_TYPES` for activity feed filtering.
- Implemented `server/extensions/activity/api/cardActivity.ts` and `boardActivity.ts` endpoints to return only allowed event types.

## Technical Debt
- Playwright/browser UI tests are missing; only Bun-based unit/integration tests exist and pass.
- No global middleware or rate-limiting added yet.

## What Should Be Done Next
- Add Playwright/browser UI tests for activity feed and sideloaded activities.
- Implement global middleware and rate-limiting as per architecture specs.
- Complete deferred profile editing and real-time profile propagation features.
- Address accessibility and keyboard navigation for new UI elements.