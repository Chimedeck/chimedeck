## Update

- Added board integrations backend support for GitHub Project URL settings.
- Mounted `GET` and `PATCH` routes at `/api/v1/boards/:id/settings/integrations`.
- Enforced ADMIN/OWNER writes, guest-safe reads, and audit/event emission for URL changes.

## New

- Added `boards.github_project_url` via migration `0121_board_github_project_url.ts`.
- Added URL normalization and audit reference/hash helpers in `server/extensions/board/mods/githubProjectUrl.ts`.
- Added integration API coverage for valid/invalid URLs, role checks, no-op updates, and audit payloads.
- Added `board_github_project_url_updated` to the activity visibility allowlist.

## Technical Debt

- None.

## What Should Be Done Next

- Wire the Board Settings UI `Github Project Url` field to the new integrations API.
- Add inline validation messaging and disabled/read-only states for unauthorized users.
