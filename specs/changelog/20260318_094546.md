## Update
- Enhanced `BoardSearchBar.tsx` with matched-text highlighting, `initialQuery` prop (for URL param restore), `onQueryChange` callback (for query param sync), and boardId-change state reset.
- Updated `BoardHeader.tsx` to thread new search props to `BoardSearchBar`.
- Modified `BoardPage.tsx` to handle search result selection (card modal open, list scroll), query param sync, and state reset on board switch.
- Added `id="board-list-${list.id}"` to `ListColumn.tsx` for DOM-based list scrolling.
- Expanded `specs/tests/board-scoped-search.md` with new UI scenarios and marked AC-4/5 as done.

## New
- Introduced `HighlightedTitle` sub-component for search result highlighting in `BoardSearchBar.tsx`.
- Added Playwright MCP test scenarios for result click/modal open, highlight, URL param restore, and board-switch reset to `specs/tests/board-scoped-search.md`.
- Created changelog: `specs/changelog/20260318_024325.md` (iteration 3).

## Technical Debt
- Playwright tests for board search flows are only specified in Markdown; no executable `.test.ts` files exist yet, so scenarios are not automated.
- Pre-existing type errors in `BoardPage.tsx` and `ListColumn.tsx` remain unresolved.

## What Should Be Done Next
- Scaffold Playwright `.test.ts` files for board search scenarios to enable automated UI/API testing.
- Address pre-existing type errors in `BoardPage.tsx` and `ListColumn.tsx`.
- Continue search UX polish, error handling, and advanced routing in future iterations as needed.
