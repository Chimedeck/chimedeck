## Update
- Modified `server/extensions/mcp/registerTools.ts` to import and register the new `registerSearchCards` and `registerSearchBoard` tools.

## New
- Added `server/extensions/mcp/tools/searchCards.ts`: Implements the `registerSearchCards` tool using zod validation and the apiClient pattern, wrapping `GET /api/v1/workspaces/:workspaceId/search`.
- Added `server/extensions/mcp/tools/searchBoard.ts`: Implements the `registerSearchBoard` tool using the same pattern, wrapping `GET /api/v1/boards/:boardId/search`.
- Added `server/extensions/mcp/tests/searchCards.md`: Playwright scenario test for the searchCards tool.
- Added `server/extensions/mcp/tests/searchBoard.md`: Playwright scenario test for the searchBoard tool.

## Technical Debt
- Playwright reports "No tests found" for the new scenario files, indicating a possible issue with test file format or Playwright configuration. No MCP flows were tested yet.

## What Should Be Done Next
- Fix Playwright test file format or configuration so that the new scenario tests are recognized and executed.
- Ensure MCP flows for searchCards and searchBoard are covered by automated tests.
- Continue with deferred items: board-level views, notification preferences, uuidv7 migration, and additional Playwright/MCP server tests.
