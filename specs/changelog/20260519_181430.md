## Update
- Added a dedicated API-level sync test file at `server/extensions/stateTransitions/api/__tests__/sync.test.ts` to cover list-delete node/edge stripping and list-rename label syncing behavior.
- Tightened PUT error handling by replacing ad-hoc inline error objects with a shared state-transition error helper for validation/update failures.
- Updated PUT websocket handling to surface a concrete `state-transition-broadcast-failed` API error instead of silently swallowing publish failures.

## New
- Added `StateTransitionListLike` shared type in `server/extensions/stateTransitions/common/types.ts` and reused it in sync helpers.
- Added `stateTransitionError()` and `StateTransitionErrorResponse` in `server/extensions/stateTransitions/common/errors.ts` for consistent error envelope generation.

## Technical Debt
- Repository-wide lint/typecheck baseline remains noisy outside StateTransitions scope (pre-existing parser/type errors in unrelated modules).

## What Should Be Done Next
- Consider consolidating duplicate sync-helper tests (`common/__tests__/sync.test.ts` and `api/__tests__/sync.test.ts`) once iteration-specific file placement requirements are no longer strict.
