## Update
- Added Sprint 166 board-chat semantic read path with a new board-scoped search endpoint and handler-level permission enforcement.
- Wired `POST /api/v1/boards/:boardId/chat/search` into the board router, reusing existing board access, workspace membership, and guest view checks.

## New
- `server/extensions/board/api/chatSearch/*` for the new semantic search API.
- `server/extensions/board/mods/chat/search/*` for query embedding + similarity ranking over persisted chat vectors.
- `server/extensions/board/api/__tests__/chatSearch.test.ts` and `server/extensions/board/mods/chat/search/__tests__/query.test.ts` for API and query behavior coverage.
- New board chat search result/types contracts in `server/extensions/board/types.ts`.

## Technical Debt
- Similarity ranking currently runs in application code over board-scoped vectors; this can be moved to a database-native vector index once pgvector is introduced.
- Search currently returns top scored hits only; cursor pagination for semantic retrieval can be added later if result sets grow.

## What Should Be Done Next
- Start Sprint 167 by adding the OpenAI-compatible adapter and board chat assist endpoint.
- Integrate semantic search controls in the board chat UI in the next iteration.
