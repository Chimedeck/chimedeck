## Update
- Confirmed `CardValue` (money badge) and `CardDetailPluginBadges` are rendered as an inline `flex-wrap` badge row in the left column of `CardModal.tsx`, below checklist and attachments — no sidebar placement.
- Confirmed `CardSidebarSection.tsx` is fully unused (zero imports across the entire codebase); file left in place but is dead code.
- Updated `specs/tests/card-detail-modal.md` with two new test steps (steps 8 and 9) verifying badge row placement in the left column and absence of any sidebar sections.

## New
- Created `src/extensions/Card/components/CardValueBadges.mcp.md`: comprehensive Playwright MCP test spec covering value badge inline placement, save/validation flows, no-sidebar assertion, and full Sprint-81 acceptance criteria (two-column layout, drag-resize persistence, activity sort order, inline description edit, bottom bar toggle, Actions dropdown, mobile layout).

## Technical Debt
- `CardSidebarSection.tsx` remains in the codebase unused. Safe to delete in a future housekeeping pass.

## What Should Be Done Next
- Run Playwright tests against the dev server to validate all Sprint-81 acceptance criteria end-to-end.
- Delete `CardSidebarSection.tsx` in a housekeeping iteration once confirmed no other branch references it.
- Consider extracting the badge row into a named `CardBadgeRow` sub-component if it grows beyond two items.
