# 20260422_151500

## Update

- Enabled notification settings visibility for board guests in board settings by treating board guests as board participants for notification controls.
- Extended notification eligibility defaults so board guests are treated the same as joined board members when no explicit board preference row exists.

## New

- Updated board settings wiring to pass board-participant eligibility:
  - `src/extensions/Board/containers/BoardPage/BoardPage.tsx`
  - `src/extensions/Board/containers/BoardSettings/BoardSettings.tsx`
- Updated board preference defaults to include guest access rows:
  - `server/extensions/notifications/mods/boardPreferenceGuard.ts`
  - `server/extensions/notifications/api/boardPreference/get.ts`
- Updated board activity notification recipient resolution to include both joined members and board guests (deduped by user id):
  - `server/extensions/notifications/mods/boardActivityDispatch.ts`

## Technical Debt

- `boardActivityDispatch.ts` still has pre-existing cognitive complexity warnings in `dispatchDirectCardNotification`; recipient-source extraction could be moved to a shared helper.
- `BoardPage.tsx` still has pre-existing nested ternary lint warnings unrelated to this change.

## What Should Be Done Next

- Add integration tests covering guest notification delivery for card events (`card_created`, `card_moved`, direct card/comment notifications).
- Add API tests validating board preference default resolution for:
  - board member,
  - board guest,
  - non-participant with board visibility access.
- Add a UI test to verify board settings shows user notification controls for guest users.
