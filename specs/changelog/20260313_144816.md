## Update
- Extended `server/mods/events/dispatch.ts` to call `handleBoardActivityNotification` after event persistence (fire-and-forget).
- Updated `server/extensions/comment/api/create.ts` to use `dispatchEvent` for comment events, ensuring board activity notifications are triggered.
- Enhanced `createNotifications.ts` to support `cardTitle` and `boardName` params and to dispatch mention emails after in-app notification creation. All callers updated.
- Fixed all usages of `board.name` to `board.title` for consistency with board entity.

## New
- Added `server/extensions/notifications/mods/boardActivityDispatch.ts` for per-member board activity email notifications.
- Created `tests/integration/notifications/emailNotifications.test.ts` with 13 passing tests for email notification flows and feature flag gating.
- Added `specs/tests/email-notifications.md` Playwright MCP spec with 9 end-to-end scenarios for email notifications.

## Technical Debt
- Playwright MCP spec is not yet automated; UI end-to-end coverage is pending.
- Board activity notification coverage is limited to email; in-app channel and notification list filtering are deferred.

## What Should Be Done Next
- Run Playwright MCP against the new spec to verify UI flows and document any failures.
- Implement in-app channel for board activity notifications.
- Add migration for notification type constraint and notification list filtering.
- Expand test automation for full event and channel coverage.