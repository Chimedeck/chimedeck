# Changelog — 2026-03-30 11:07:37

## Update
- `package.json`: Added `@sentry/react@10.46.0` and `@sentry/node@10.46.0` as runtime dependencies.
- `server/config/env.ts`: Added `SENTRY_SERVER_ENABLED`, `SENTRY_SERVER_DSN`, `SENTRY_ENV`, and `SENTRY_RELEASE` fields — all read via `Bun.env`, following the centralized config pattern.
- `src/config/index.ts`: Added `sentryEnabled`, `sentryDsn`, `sentryEnv`, and `sentryRelease` fields — all read via `import.meta.env` (Vite-exposed), following the centralized config pattern.
- `.env.example`: Added Sentry section with `SENTRY_SERVER_ENABLED`, `SENTRY_SERVER_DSN`, `VITE_SENTRY_CLIENT_ENABLED`, `VITE_SENTRY_CLIENT_DSN`, `SENTRY_ENV`, `SENTRY_RELEASE`, `VITE_SENTRY_ENV`, `VITE_SENTRY_RELEASE` — all default to disabled/empty so the app starts without Sentry in dev.
- `README.md`: Added Sentry environment variable table under the Production → Environment section.

## New
- Sentry SDK packages installed; config fields exposed for both client and server.

## Technical Debt
- No Sentry initialization code exists yet — deferred to Iteration 2 (client) and Iteration 4 (server).

## What Should Be Done Next
- Iteration 2: Initialize `@sentry/react` in `src/main.tsx` behind `config.sentryEnabled` and `config.sentryDsn` guards.
- Iteration 4: Initialize `@sentry/node` in `server/index.ts` behind `env.SENTRY_SERVER_ENABLED` and `env.SENTRY_SERVER_DSN` guards.
