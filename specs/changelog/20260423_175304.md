## Update
- Fixed a card detail modal render loop that could trigger React "Maximum update depth exceeded" during Dialog lifecycle updates.
- Stabilized attachment-related callback identities in CardModal so AttachmentPanel effects do not re-fire solely because parent functions were recreated.
- Added a no-op guard before dispatching board card attachment count updates from CardModalContainer.

## New
- Added repository memory note for this failure mode: callback identity + effect dependency loops in modal attachment syncing.

## Technical Debt
- AttachmentPanel still reloads server data whenever its `onCountChange` callback identity changes; current fix prevents loop-causing dispatch churn, but callback dependency sensitivity remains.

## What Should Be Done Next
- Consider narrowing AttachmentPanel effect dependencies by using stable event refs for optional callbacks where possible.
- Add a regression test that opens card modal, loads attachments, and verifies no repeated close/open or nested update errors.
