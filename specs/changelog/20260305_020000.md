# Changelog — 2026-03-05T02:00:00Z

## Update
- `server/index.ts` — mounted the `pluginsRouter` and added the `/sdk/jh-instance.js` static file route.

## New
- `server/extensions/plugins/config/index.ts` — central config for the plugin system (`appUrl`, `sdkOrigin`, `sdkServePath`, `sdkBundlePath`).
- `server/extensions/plugins/middlewares/board-admin-guard.ts` — reusable middleware that authenticates the request and verifies the caller holds OWNER or ADMIN role in the board's workspace; returns `403 { name: 'not-board-admin' }` otherwise.
- `server/extensions/plugins/api/index.ts` — plugin API router skeleton; routes for board plugins, plugin data, and plugin registry are stubbed and will be wired in subsequent iterations.

## Technical Debt
- Plugin sub-routers (board-plugins, plugin-data, registry) are stubs returning `null`; they will be implemented in iterations 3–5.
- SDK bundle does not yet exist; the `/sdk/jh-instance.js` route returns 404 until iteration 6 builds it.

## What Should Be Done Next
- Iteration 3: implement board plugin management endpoints (list, enable, disable).
