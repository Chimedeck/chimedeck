## Update
- Wired `move-card` and `comment` commands into the CLI dispatcher in `cli/index.ts`.
- Ensured both commands validate required flags, support `--json` output, and provide clear error messages.

## New
- Added `cli/commands/moveCard.ts`: Implements the `move-card` CLI command, calling the MCP API to move a card between lists/positions.
- Added `cli/commands/comment.ts`: Implements the `comment` CLI command, calling the MCP API to add a comment to a card.

## Technical Debt
- No Playwright or CLI integration tests exist for these commands; test runs found no matching tests.
- Error/edge case handling is basic; further refinement needed for network/API errors and flag precedence.

## What Should Be Done Next
- Implement Playwright and CLI integration tests for `move-card` and `comment` commands.
- Address Playwright config issues and ensure tests are discoverable and runnable.
- Continue CLI implementation for remaining MCP tools (create-card, edit-description, set-price, invite).
- Improve error handling and flag precedence logic in CLI commands.
