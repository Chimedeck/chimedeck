## Update
- Updated `GET /api/v1/boards/:boardId/state-transitions/rules` to return `422 state-transition-graph-invalid` when persisted `graph_data` is not a graph container (`nodes`, `edges`, `notes` arrays), instead of silently proceeding.
- Refactored state-transition validator internals to reuse a shared `hasGraphCollections(...)` guard.

## New
- Added API test coverage at `tests/server/extensions/stateTransitions/api/getRules.test.ts` for:
  - feature flag disabled (501)
  - empty-edge graph rules response
  - allowed/forbidden derivation
  - invalid persisted graph payload (422)
- Added sync helper tests at `tests/server/extensions/stateTransitions/common/sync.test.ts` for:
  - node+edge stripping on list deletion
  - node label updates on list rename

## Technical Debt
- Rules validation for persisted legacy graph variants is currently shallow at endpoint level (container shape); deeper structural reconciliation remains in the shared rules derivation layer.

## What Should Be Done Next
- Add a DB-level repair or migration path for malformed `board_state_transitions.graph_data` rows so runtime 422s are less likely in production.
