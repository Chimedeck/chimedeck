# Changelog — 2026-04-13T05:04:27Z

## Update
- Migrated remaining raw `<button>` elements in `RegisterPluginModal.tsx` (category tag removal) and `EditPluginModal.tsx` (category and domain tag removal) to `<IconButton>`, completing the Sprint 133 button migration sweep.

## New
- No new files or features added.

## Technical Debt
- Pre-existing lint errors in `vite.config.ts` (non-null assertions) and `featureFlagsSlice.ts` (type errors) remain unresolved — deferred as out of scope for this iteration.

## What Should Be Done Next
- Iteration 8: Sprint 134 — feature flag + route wiring for the Design System page.
