## Update
- Modified `server/extensions/mcp/index.ts` to register the new `move_card` and `write_comment` tools with the MCP server.

## New
- Added `server/extensions/mcp/tools/moveCard.ts`: Implements the `move_card` tool with Zod schema validation, PATCHes `/api/v1/cards/:cardId/move`, and returns structured errors.
- Added `server/extensions/mcp/tools/writeComment.ts`: Implements the `write_comment` tool with Zod schema validation, POSTs to `/api/v1/cards/:cardId/comments`, and returns structured errors.

## Technical Debt
- No Playwright tests for MCP tools found in the expected "mcp" project; test verification deferred.
- Deferred tests for error cases and edge conditions.
- API schema changes may require future schema updates.

## What Should Be Done Next
- Run Playwright tests in the correct project ("e2e" or "specs") to verify `move_card` and `write_comment` flows.
- Implement and register `create_card` and `edit_card_description` tools in the next iteration.
- Add/expand tests for error handling and edge cases.
- Monitor for API changes and update schemas as needed.