## Update
- Aligned `db/migrations/0118_state_transitions.ts` to match the approved schema: ensured correct columns, types, JSONB default, and index on `board_id`.
- Updated `server/config/featureFlags.ts` to export `STATE_TRANSITIONS_ENABLED` from `Bun.env` and verified correct flag reading.
- Confirmed TypeScript interfaces in `server/extensions/stateTransitions/common/types.ts` match the sprint spec.

## New
- Introduced the `board_state_transitions` table for state transition graphs per board, with enforced schema and index.
- Added centralized `STATE_TRANSITIONS_ENABLED` feature flag in config.
- Defined `StateTransitionGraph`, `StateTransitionNode`, `StateTransitionEdge`, and `StateTransitionNote` interfaces for state transitions.

## Technical Debt
None

## What Should Be Done Next
- Unblock and rerun `bun run db:migrate` to verify table, index, and default in a live Postgres environment.
- Add API endpoints and UI for state transitions in subsequent iterations.
- Implement tests for API and UI flows once exposed.
- Monitor for any spec changes in requirements or sprint docs.