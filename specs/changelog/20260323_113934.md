## Update
- Mounted new `boardTypePreferences` router in `server/extensions/notifications/api/index.ts` to handle board-level notification type preferences endpoints.

## New
- Implemented `GET /api/v1/boards/:boardId/notification-preferences/types` to return all 9 notification types with resolved value and source (`'board'|'user'|'default'`).
- Implemented `PATCH /api/v1/boards/:boardId/notification-preferences/types` to upsert a single board notification type preference for the user/board/type.
- Implemented `DELETE /api/v1/boards/:boardId/notification-preferences/types` to remove all board notification type preferences for the user+board.
- Added integration test `tests/integration/notifications/boardTypePreferences.test.ts` covering GET, PATCH, DELETE, and error/auth flows (6 tests, all passing).

## Technical Debt
None

## What Should Be Done Next
- Implement server logic for notification channel cascade (resolveNotificationChannels).
- Add email templates for new notification types and wire into dispatch.
- Add client RTK Query slice and UI for board-level notification type preferences.
- Integrate new UI into board settings and add translations.
- Write and verify Playwright MCP tests for client flows.
