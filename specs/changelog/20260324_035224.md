# Changelog — 2026-03-24T03:52:24Z

## Update
- Added two new integration test files under `tests/integration/`.

## New
- `tests/integration/apiToken/apiToken.test.ts`: 27 unit-level integration tests covering:
  - `generateApiToken` mod: `hf_` prefix, 67-char length, hex validation, unique generation, deterministic hashing
  - Authentication guard logic: hf_ dispatch, revoked token rejection, expiry handling
  - Token revocation: `revoked_at` lifecycle, ownership check
  - Token creation validation: name required, expiresAt ISO format

- `tests/integration/card/cardCRUD.test.ts`: 34 unit-level integration tests covering:
  - Card title validation: empty/missing/too-long rejection, valid acceptance
  - Fractional position logic: append, prepend, between-two-cards ordering invariants
  - Card move: `targetListId` required, cross-board prevention, position assignment in target list
  - Card archive: toggle behaviour, archived card excluded from queries, board-archived guard
  - `due_date` / `start_date`: ISO 8601 validation, null acceptance, `start_date ≤ due_date` invariant, `listDueCards` filter logic

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 12: Integration tests for `list` (listCRUD) and `workspace` (workspaceCRUD).
