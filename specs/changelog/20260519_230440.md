## Update
- Refactored `server/extensions/stateTransitions/api/getRules.ts` to validate and coerce legacy graph payloads, sync with active lists, persist when needed, and return normalized rules.
- Updated `validator.ts` to export `coerceLegacyGraphShape` for tolerant legacy graph parsing.
- Enhanced `put.test.ts` to assert WS broadcast and sync/strip logic on PUT.
- Improved `getRules.test.ts` for edge cases and sync persistence.

## New
- Added rules normalization and sync logic to `getRules` endpoint.
- Expanded test coverage for rules, sync, and WS event flows in `api/__tests__/getRules.test.ts` and `put.test.ts`.

## Technical Debt
- Error envelope formats remain inconsistent across modules; revisit for standardization.

## What Should Be Done Next
- Implement `/rules` copy endpoint, enforcement guard, and UI integration.
- Standardize error envelope formats across all modules.
- Resolve dev server port conflicts to enable Playwright MCP browser tests.
