# Changelog — 2026-03-17T12:17:10Z

## Update
- Threaded `cardId` prop through `CardModal.tsx`, `ActivityFeed.tsx`, and `CommentItem.tsx` to ensure attachment uploads are correctly scoped to the current card in all editors.
- Updated `CardDescriptionTiptap.tsx` and `CommentEditor.tsx` to accept and utilize `cardId` for attachment upload context.
- Expanded `specs/tests/rich-text-toolbar-single-line-overflow.md` with end-to-end Playwright MCP scenarios for verifying cardId threading and attachment upload flows.

## New
- Added three Playwright MCP test scenarios to validate attachment upload in both card description and comment editors, ensuring correct card association.

## Technical Debt
- Playwright E2E tests are not runnable due to Bun/Node.js ESM loader conflicts and missing test fixture files (e.g., sample.png, sample.pdf).
- No Node-based Playwright runner script is present; E2E test automation is currently blocked.

## What Should Be Done Next
- Add required Playwright test fixtures to `tests/e2e/attachments/fixtures/`.
- Create a Node-based Playwright test script or config to bypass Bun for E2E tests.
- Rerun Playwright tests to verify attachment upload flows.
- Resume deferred offline draft and replay features in the next iteration.
