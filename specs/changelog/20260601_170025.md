# Iteration 5 Retest: Entitlements + Usage API

## Update
- Fixed the entitlements usage aggregation to match the actual database schema.
- Removed unsupported attachment archive/deletion assumptions and qualified aggregate queries to avoid ambiguous joins.
- Confirmed the workspace entitlements endpoint now returns entitlements and usage for free, pro, disabled-subscription, and missing-subscription cases.

## New
- No new surface area in this retest pass.

## Technical Debt
- Usage aggregation is still workspace-wide and not user-scoped.
- The entitlements response currently relies on subscription rows being maintained externally.

## What Should Be Done Next
1. Add authorization and feature-gate middleware around entitlement consumption.
2. Replace the remaining workspace-wide usage heuristics with tighter entity-scoped counters if needed.
