## Update
- None. No changes to existing code or features; this iteration focused solely on documentation.

## New
- Added `tests/README.md` (129 lines) documenting the test directory structure:
  - Describes the three-directory layout: `integration/` (Bun `.test.ts`), `e2e/` (Playwright `.spec.ts`), and `load/` (plain `.js`).
  - Provides a file-type rules table for contributors.
  - Explains where to place new tests and how to run each suite (`bun run test:unit`, `test:e2e`, `test:mcp`, `test:all`).
  - Clarifies that scenario `.md` files for MCP live in `specs/tests/`, not in `tests/`.

## Technical Debt
None

## What Should Be Done Next
- Review and update test suite scripts as needed to ensure all documented commands remain accurate.
- Add more detailed examples or links to sample test files for each directory if contributors request clarification.
- Continue to align test documentation with evolving architecture and requirements specifications.
