## Update
- Completed all `/trello/1/lists/*` endpoints in the Trello compatibility layer, aligning with sprint-145 spec.
- Enhanced `pos` logic to support Trello-style `top`, `bottom`, and numeric positions using fractional ordering.
- Improved bulk archive and move logic for cards, including permission checks and error handling.
- Expanded integration test coverage for list endpoints and edge cases.

## New
- Added integration test suite: `tests/integration/trelloCompat/lists.test.ts` covering all list endpoints, field-level GETs, bulk operations, and error scenarios.
- Implemented endpoints for `archiveAllCards`, `moveAllCards`, and field-level GETs (e.g., `/lists/{id}/name`).

## Technical Debt
- Deferred short ID support for checklist, checklist-item, and label entities.
- Board sync and comment creation logic remain complex and flagged for future refactor.
- Some ambiguity persists in invitation context binding and tenant handling.

## What Should Be Done Next
- Extend short ID support to checklist, checklist-item, and label entities.
- Refactor board sync and comment creation logic for clarity and maintainability.
- Address invitation context and tenant ambiguity.
- Implement checklist and label endpoints per Trello spec in the next iteration.