## Update
- Registered `aiEditOrchestratorRouter` in `server/index.ts` after aiContextRouter, gated by `AI_EDIT_ENABLED` flag
- Added `AI_EDIT_ENABLED` env-var accessor to `server/config/featureFlags.ts`
- Added `AI_EDIT_ENABLED: false` default to `server/mods/flags/defaults.ts`
- Added `aiEditEnabled` selector and fulfillment handlers to `src/slices/featureFlagsSlice.ts`
- Added `FLAG_AI_EDIT_ENABLED=false` to `.env.example`

## New
- **DB migration `20260610_card_ai_edit_runs.ts`**: `card_ai_edit_runs` table — UUID PK, card FK, workspace FK, creator FK, status (REQUESTED→CONTEXT_GATHERED→FILE_SCOPE_PLANNED→FILES_CREATED→FILES_EDITED→COMMITTED|FAILED), snapshot FK, file_scope_plan (JSONB), error_message, timestamps
- **DB migration `20260610_card_ai_edit_steps.ts`**: `card_ai_edit_steps` table — UUID PK, run FK (CASCADE), step_name, status (PENDING|RUNNING|SUCCEEDED|FAILED), attempt counter, input/output/error JSONB, timestamps; UNIQUE on (run_id, step_name)
- **`server/extensions/aiEditOrchestrator/`**: New extension with config, types, state machine, persistence, API handler, and router
  - `common/config/index.ts` — flag key constant, allowed file paths, max retry attempts
  - `types.ts` — EditRunStatus, EditStepName, EditRun, EditStep, input/result interfaces
  - `mods/stateMachine/index.ts` — allowed-transitions map (REQUESTED→CONTEXT_GATHERED→…→COMMITTED, any→FAILED), rejects invalid transitions with `invalid-state-transition` error
  - `mods/persistence/index.ts` — create/get/update run and step operations with state-machine guard before persisting
  - `api/edit/index.ts` — POST /api/v1/cards/:cardId/ai/edit: auth, board access, workspace membership, creates run in REQUESTED, emits activity event
  - `api/index.ts` — router with feature-flag gate, cardId resolution, board visibility enforcement
- **3 test suites**: state machine (valid/invalid transitions), persistence (CRUD + state guards), API handler (201/400/401/403/404 responses)

## Technical Debt
- Persistence test DB mock still hits real PostgreSQL — mock interception needs restructuring to work before the real db module creates a connection
- API test import paths were corrected via sed but the fix may need re-verification after full test run
- File creation/editing/commit/resume pipeline (Iteration 11) not yet wired — only the POST /ai/edit endpoint creates runs in REQUESTED; the orchestrator does not call file scope planner or trigger execution steps yet
- Trigger engine DB migrations (`card_phase_trigger_runs` / `card_phase_trigger_attempts`) still pending and may block end-to-end wiring in Iteration 11

## What Should Be Done Next
- Wire the execution pipeline: context gather → file scope plan → files create → files edit → commit (Sprint 175 Iteration 11)
- Implement run status polling endpoint (GET /api/v1/cards/:cardId/ai/edit/status) so the client can display progress
- Add retry logic at the step level: on FAILED step, increment attempt counter and retry with backoff (up to maxAttempts from config)
- Fix persistence test DB mock so all 3 test suites pass cleanly
- Create client-side UI for triggering AI edits and displaying run progress/status
