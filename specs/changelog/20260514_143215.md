## Update
- Fixed Custom Fields layout responsiveness in card modal by switching from viewport breakpoints to container-width detection.
- Column count now adapts to the actual section width in the sidebar pane, not the browser width.

## New
- Custom Fields section now calculates columns with a hard cap of 3 and floor of 1:
  - narrow container: 1 column
  - medium container: 2 columns
  - wide container: 3 columns

## Technical Debt
- Container-width thresholds are currently hardcoded in component logic. If more responsive panels use this pattern, we should extract a shared hook.

## What Should Be Done Next
- Add a UI test that resizes the card modal/sidebar and validates custom fields column count transitions (1/2/3).
