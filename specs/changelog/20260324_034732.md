# Changelog — 20260324_034732

## Update
- No existing files were modified in this iteration.

## New
- `specs/tests/mcp-http-session.md` — MCP HTTP session lifecycle scenario: initialize session, call tool, terminate session, reject stale/missing session IDs.
- `specs/tests/plugin-install.md` — Plugin install scenario: list registry, install as admin, reject duplicate/non-admin/unknown slug, uninstall.
- `specs/tests/board-star-follow.md` — Board star and follow scenario: star/unstar board, follow/unfollow board, reject non-member and unauthenticated access.
- `specs/tests/card-start-date.md` — Card start date scenario: set start date, validate start-before-due constraint, clear start date, reject invalid formats.
- `specs/tests/card-short-url.md` — Card short URL scenario: short URL present in GET, resolves via short-code endpoint, stable across title updates, archived cards return 404/410.
- `specs/tests/admin-user-list.md` — Admin user list scenario: paginated list, filter by status/role, search by email, non-admin returns 403.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 11: Write integration tests for `apiToken` and `card` in `tests/integration/`.
