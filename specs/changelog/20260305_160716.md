## Update
- Extended plugin registration and update APIs to accept, validate, and persist `whitelistedDomains` (max 20, HTTPS-only).
- Modified plugin list API to always include `whitelisted_domains` in responses.
- Updated API router to mount new board-allowed-domains handler and route.

## New
- Migration `0023_plugin_whitelisted_domains.ts` adds `whitelisted_domains` JSONB column to `plugins` table.
- Utility `isValidHttpsOrigin.ts` for strict HTTPS origin validation.
- `PATCH /api/v1/boards/:boardId/plugins/:pluginId/allowed-domains` endpoint for board admins to set allowedDomains, enforcing plugin whitelist.
- Unit tests for domain validation and API edge cases in `whitelisted-domains.test.ts` (all passing).

## Technical Debt
- No Playwright or MCP evaluator E2E tests for plugin domain flows; only unit/integration tests exist.
- Some pre-existing TypeScript errors remain unrelated to these changes.

## What Should Be Done Next
- Implement Playwright MCP evaluator tests for plugin registration, update, and allowedDomains flows as per `specs/tests/plugins-sdk-mcp-evaluator.md`.
- Add UI for editing plugin whitelisted domains and board-level allowedDomains.
- Review and improve error handling and validation edge cases as needed.
- Monitor for backward compatibility issues with plugins lacking whitelistedDomains.