## Update
- Added the Board Chat header entry point to the board shell and threaded the open handler through `BoardPage` and `BoardHeader`.
- Introduced a central `BOARD_CHAT_ENABLED` client flag module aligned with the existing feature-flag pattern.
- Added visibility and wiring coverage for the Board Chat header control.

## New
- Added `src/extensions/BoardChat/components/BoardChatButton.tsx` for the header action button.
- Added `src/extensions/BoardChat/index.ts` as the feature barrel.
- Added board chat tests under `src/extensions/Board/components/__tests__/BoardHeader.boardChat.test.tsx` and `src/extensions/Board/containers/BoardPage/__tests__/BoardPage.boardChat.test.tsx`.

## Technical Debt
- The button renders, but the live UI does not yet mount a Board Chat drawer/panel after click.

## What Should Be Done Next
- Implement the Board Chat drawer shell and mount it from the header action.
- Add history/loading/empty/error states for the new panel.
- Complete guest/member ACL coverage against the live drawer behavior.
