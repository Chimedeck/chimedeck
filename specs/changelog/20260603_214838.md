## Update
- Fixed SpecsWorkspacePage state-rendering bug where `state.files` could be undefined during loading transitions
- Added defensive null-check in render method to prevent crashes
- Removed `api` from useEffect dependency arrays (api is a stable module-level export)

## New
- Added explicit `save/conflict-clear` action for transparent conflict recovery state transitions
- Implemented persistent conflict banner below editor header with error message and reload button
- Conflict state transitions now explicitly clear error fields when file is reloaded

## Technical Debt
- Test suite for SpecsWorkspacePage has timing issues with fake timers and jsdom Response mock
- Consider using more robust mock setup for fetch operations or switching to integration tests

## What Should Be Done Next
- Resolve hanging tests in SpecsWorkspacePage.test.tsx related to fake timer and fetch mock handling
- Consider adding test utilities for consistent async operation mocking across test suite
