# Changelog 20260521_123500

## Update

- Fixed API docs spec endpoints to resolve OpenAPI YAML from runtime-available locations instead of only `docs/api/*`.
- Added fallback lookup order for `/api-docs/native-openapi.yaml` and `/api-docs/trello-openapi.yaml`: `public/api-docs/*`, then `dist/api-docs/*`.

## New

- Introduced `serveOpenApiSpec` helper in `server/index.ts` to centralize spec file lookup and response headers.

## Technical Debt

- The API docs page still loads Swagger UI assets from `unpkg`; moving those assets to same-origin static files would remove CSP exceptions and external runtime dependency.

## What Should Be Done Next

- Verify `/developer/api-docs` in production for both tabs and ensure both spec requests return `200`.
- Optionally remove route-level CSP allowances for `https://unpkg.com` after hosting Swagger assets locally.
