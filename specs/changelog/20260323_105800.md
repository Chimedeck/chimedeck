## Update
- Removed `comment_added` from the notification events pipeline to prevent duplicate notifications.
- Extended `DirectCardNotificationPayload` to support `card_commented` with `commentId`, `commentPreview`, and `cardTitle`.
- Updated `dispatchDirectCardNotification` to store `commentId` as `source_id` and send notification emails for `card_commented` events.
- Updated `create.ts` to build a comment preview and trigger direct notification dispatch after comment creation.
- Clarified comments in `dispatch.ts` regarding event handling changes.

## New
- Direct notification dispatch for `card_commented` events, including email and in-app notifications, triggered immediately after comment creation.

## Technical Debt
- Playwright MCP and related tests could not be executed due to ESM loader and environment issues; test coverage for new notification flows is unverified.

## What Should Be Done Next
- Fix Playwright test runner configuration and ensure all notification/comment/card activity tests are runnable and passing.
- Add/expand tests to cover direct notification dispatch for comments.
- Monitor for regressions in notification delivery and event handling.