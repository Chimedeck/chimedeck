# Changelog 2026-03-05T09:12:10Z — Sprint 39 Iteration 2: Plugin Edit UI & Domain Whitelisting Types

## Update
- `src/extensions/Plugins/api.ts`: Added `whitelistedDomains?: string[]` to `Plugin` interface; added `config?: BoardPluginConfig` to `BoardPlugin`; added `UpdatePluginBody` interface and `updatePlugin` (PATCH) API function.
- `src/extensions/Plugins/components/PluginCard.tsx`: Added optional `onEdit?: (plugin: Plugin) => void` prop to both `EnableCardProps` and `DisableCardProps`; renders a pencil (✏️) button when `onEdit` is provided (platform admins only).
- `src/extensions/Plugins/components/PluginList.tsx`: Added `onEdit?: (plugin: Plugin) => void` prop and passes it through to both active and available `PluginCard` instances.
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.duck.ts`: Added `updateStatus` / `updateError` to state; added `updatePluginThunk` (PATCH `/api/v1/plugins/:pluginId`); added `clearUpdateState` reducer; added `selectUpdateStatus` / `selectUpdateError` selectors.
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.tsx`: Integrated `EditPluginModal`, `ToastRegion`, edit state/handlers; passes `onEdit` to `PluginList` for platform admins only; shows "Plugin updated." toast on success.

## New
- `src/extensions/Plugins/types.ts`: New file defining `BoardPluginConfig` interface with `allowedDomains?: string[] | null`.
- `src/extensions/Plugins/modals/EditPluginModal.tsx`: New modal form for platform admins to edit all plugin fields (name, description, connector URL, manifest URL, icon URL, author, emails, categories, whitelisted domains, visibility). Pre-fills from the selected `Plugin` object. Uses tag-input pattern for `whitelistedDomains`.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 3: Client Allowed Domains Panel in per-plugin board modal + iframe bridge enforcement (`usePluginBridge.ts`).
