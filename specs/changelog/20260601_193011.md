## Update
- Wired workspace-scoped READ/WRITE rate limiting into the request pipeline.
- Switched the limiter to use shared workspace buckets keyed by tier quotas.
- Preserved the legacy per-user/IP fallback when subscriptions are disabled.
- Kept unlimited-tier requests bypassing the limiter.

## New
- Added workspace-aware rate-limit behavior for read and write traffic.
- Added focused coverage for shared buckets, workspace separation, and fallback behavior.
- Added a request-context test to pin workspace resolution.

## Technical Debt
- None

## What Should Be Done Next
- Keep subscription tier mappings and stored subscription rows aligned.
- Extend coverage if new read/write routes need explicit classification.
