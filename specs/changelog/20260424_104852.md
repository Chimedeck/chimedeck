# Update
- Redesigned notification list items to a wrapped card-style layout aligned with the provided mockups.
- Added structured sections inside each notification item: card title capsule, board/list location line, and an activity wrapper with actor avatar, actor name, time, and action text.
- Kept unread visual emphasis while preserving existing click-to-open card navigation and per-item delete action.

# New
- Added interactive comment reactions directly in notification items for comment-backed notifications.
- Notification rows now support optimistic add/remove emoji reactions and call comment reaction endpoints.
- Enriched notification list API payload with:
  - comment_content (for inline comment preview)
  - comment_reactions (aggregated emoji/count/reactedByMe/reactors)
- Extended client notification types to include comment reaction summary payload fields.

# Technical Debt
- VS Code Problems panel currently reports stale unsafe-type diagnostics in NotificationItem despite file-level ESLint passing and no NotificationItem errors in typecheck output. This appears to be workspace-wide diagnostic noise from existing project errors.
- NotificationPanel visual shell (header/actions/background) remains close to current implementation; only item cards were redesigned.

# What Should Be Done Next
- Validate the new notification item visuals in both light and dark themes against production data density (long card titles, long board/list names, and long comments).
- Consider adding a focused Notification UI regression test in specs/tests/ for reaction interaction and keyboard/card navigation behavior.
- If desired, refine panel-level theming to fully match the dark mockup chrome (header separators, card shadows, and panel contrast).
