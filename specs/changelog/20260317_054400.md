# Changelog — 2026-03-17T05:44:00Z

## Update
- **CardDescriptionTiptap**: Integrated full offline draft lifecycle — debounced local IndexedDB save, background server sync, draft restore on card open (local-first then reconcile with server), offline save queuing, and footer status UI.
- **messageQueue**: Extended `QueuedMutation` interface with an optional `meta` field to carry offline draft context (`draftType`, `cardId`, `userId`, `workspaceId`) for post-replay cleanup.
- **offline-description-draft-recovery.md**: Expanded with 19 new Playwright MCP scenarios (35–53) covering all CardDescriptionTiptap offline draft flows.

## New
- `src/extensions/OfflineDrafts/hooks/useOfflineDescriptionDraft.ts`: Custom hook encapsulating all offline description draft logic — restore, debounced local save, background sync, offline save queuing via messageQueue, clear, retry, and discard.
- Draft status footer in `CardDescriptionTiptap` showing: "Saving draft…" / "Draft saved locally" / "Syncing draft…" / "Synced draft" / "Will sync when online" / "Sync failed — Retry | Discard".
- Draft recovery banner in view mode when an unsaved draft differs from saved description.

## Technical Debt
- The reconnect event (WebSocket open) does not yet automatically flush a pending background sync; users must type to trigger the next debounce cycle. Full reconnect-triggered flush is deferred to Iteration 12.
- Queued PATCH replay acknowledgement (clearing the draft on success) will be wired in the wsMiddleware replay handler in Iteration 12.

## What Should Be Done Next
- Iteration 11: CommentEditor offline draft integration (same pattern, `submit_pending` + idempotency key).
- Iteration 12: Cross-device continuity, replay conflict policy, and full offline QA sweep.
