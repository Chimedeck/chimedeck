## Update
- None. No changes to existing code; all additions are new modules for the Health Check feature.

## New
- Added `server/extensions/healthCheck/common/config/healthCheck.ts`: Centralized config for probe engine (enabled, timeoutMs, amberThresholdMs) using env module.
- Added `server/extensions/healthCheck/mods/probe/classify.ts`: Pure function to classify probe results (status, timing) into green/amber/red.
- Added `server/extensions/healthCheck/mods/probe/runProbe.ts`: HTTP(S) probe with DNS-based SSRF guard, 10s timeout, and 3xx→amber logic.
- Added `server/extensions/healthCheck/mods/probe/persistResult.ts`: Persists probe results to `board_health_check_results` table.
- Added `server/extensions/healthCheck/mods/probe/index.ts`: Composes probe pipeline (runProbe → persistResult).
- Added `tests/unit/server/extensions/healthCheck/mods/probe/classify.test.ts`: 18 unit tests for all classify() edge cases (status, timing, SSRF, threshold).

## Technical Debt
- None. All config, error handling, and SSRF logic follow architecture specs. No shortcuts taken.

## What Should Be Done Next
- Implement integration tests for the full probe pipeline (runProbe + persistResult).
- Finalize SSRF allow-list and document in config.
- Add error object shape tests for probe failures to match API conventions.
- Review DB schema for `board_health_check_results` for any migration edge cases.
- Begin wiring probe engine into health check API endpoint.