## Update
- **Session lifecycle**: Added `IDLE` status to `CardChatSessionStatus` union, `resumeSession` lifecycle function (IDLE/PAUSED→ACTIVE_REFINEMENT transitions, no-op for already-active, 409 for READY_FOR_REVIEW), and updated `findActiveSession` to match IDLE sessions.
- **API router**: Added `POST /api/v1/cards/:cardId/chat/session/resume` route between pause and refine — full auth, membership, validation, lifecycle delegation, and activity emission.
- **Client CardChatDrawer**: Added Resume button and `handleResume` callback for PAUSED/IDLE sessions; replaced the static "Session is paused" message with a clickable resume control.
- **Client api.ts**: Added `'IDLE'` to `CardChatSessionStatus` type and exported `resumeCardChatSession(cardId, sessionId)` helper.
- **CardModal container**: `handleChatStart` now checks for existing PAUSED/IDLE sessions via `getCardChatSession` and calls resume instead of always starting a new session.
- **Client RefinementStatusBadge**: Maps `IDLE` → `DRAFT` explicitly.
- **Test runner**: `run-cardchat-tests.sh` includes the new resume handler tests.

## New
- `db/migrations/0122_card_chat_idle_state.ts` — DROP/ADD CHECK constraint to add `'IDLE'` to `card_chat_sessions.status`.
- `server/extensions/cardChat/api/session/resume.ts` — handler with auth, membership, body validation, lifecycle delegate, fire-and-forget activity emission.
- `server/extensions/cardChat/api/__tests__/resume.test.ts` — 6 handler tests (success, 409 READY_FOR_REVIEW, 409 non-paused, 404, no sessionId, bad card).
- `server/extensions/cardChat/api/session/__tests__/resume.test.ts` — linked from runner.
- `specs/changelog/20260611_061500.md` — prior changelog from earlier in this iteration (superseded by this one).

## Technical Debt
- Bun `mock.module` cross-file contamination causes 46+ server tests to fail when run in bulk; they pass individually via `run-cardchat-tests.sh` isolation script. Root cause is module-scoped dependencies in `goalLoop.ts`, `lifecycle.ts`, `activities.ts`, `write.ts`, and `query.ts` — needs factory/constructor injection refactor.
- 6 client tests fail under Bun runner due to missing `@happy-dom/global-registrator` DOM polyfill; pass correctly under vitest.
- API server is unstable during development — crashes silently within seconds when started without `detach: true`, likely due to file-watcher reloads or background process conflicts.
- No `models/index.ts` per sprint spec — `types.ts` fills the same role adequately.

## What Should Be Done Next
- Refactor module-scoped dependencies in cardChat mods to factory injection to fix `mock.module` cross-contamination (affects `goalLoop.ts`, `lifecycle.ts`, `activities.ts`, `write.ts`, `query.ts`).
- Add `@happy-dom/global-registrator` to Bun test preload config so client tests pass natively.
- Audit whether `startSession` should create in `IDLE` instead of `ACTIVE_REFINEMENT` — current behavior (implicit IDLE via absent session row) works correctly but differs from sprint spec.
- Proceed to Sprint 172: Workflow Phase Metadata on Columns (stateTransition graph schema extension + diagram editor UI). Skeleton exists in `server/extensions/stateTransitions/mods/workflowPhases/` and `src/extensions/StateTransitionEditor/`.
