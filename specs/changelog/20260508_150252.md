# Changelog - 2026-05-08 15:02:52

## Update
- Updated notification grouping so contiguous `card_updated` and `card_commented` events on the same board are grouped into one notification stack.
- Preserved contiguous-only behavior: only adjacent events are grouped.

## New
- Added board-comment stack eligibility into the notification stack-key resolver (`board-update:<board_id>`).
- Enhanced grouped notification header copy for mixed board activity stacks:
  - Shows board headline
  - Shows summary count: `<updates> updates • <comments> comments`

## Technical Debt
- Mixed board-activity grouping currently includes only `card_updated` and `card_commented`; if future board-activity types should co-group, stack-key strategy should be centralized by type-family.

## What Should Be Done Next
- Add tests for contiguous board mixed stacks:
  - `card_updated + card_commented` same board, contiguous => grouped
  - same board but non-contiguous => not grouped
  - contiguous but different board => not grouped
- Optionally localize mixed-stack summary copy into translation keys.
