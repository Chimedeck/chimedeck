# Changelog — 2026-03-13T07:18:41Z

## Update
- `src/reducers.ts` — registered `notificationPreferencesApi` export
- `src/store/index.ts` — added `notificationPreferencesApi` reducer and middleware to `configureStore`

## New
- `src/extensions/Notifications/NotificationPreferences/types.ts` — `NotificationPreference`, `NotificationType`, `UpdatePreferencesBody`, `NOTIFICATION_TYPES`, `NOTIFICATION_TYPE_LABELS`
- `src/extensions/Notifications/NotificationPreferences/notificationPreferences.slice.ts` — RTK Query `createApi` slice with `getNotificationPreferences` (GET) and `updateNotificationPreferences` (PATCH) endpoints, `providesTags`/`invalidatesTags` for cache invalidation

## Technical Debt
- None

## What Should Be Done Next
- Iteration 5: Build `NotificationPreferencesPanel` component (Headless UI Switch, Tailwind) and mount it in `ProfileSettingsPage` behind the `NOTIFICATION_PREFERENCES_ENABLED` feature flag.
