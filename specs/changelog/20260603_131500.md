# Changelog - 2026-06-03 13:15:00

## Update

- Locked the markdown editor decision for specs workflow to TipTap Markdown mode.
- Clarified backend commit implementation in sprint docs to use a server-side Git service wrapper.

## New

- Updated `specs/sprints/sprint-170.md` with TipTap Markdown mode requirements, markdown round-trip acceptance criteria, and commit implementation contract.
- Updated `specs/sprints/sprint-plan.md` Sprint 170 summary text to explicitly reference TipTap Markdown mode and git-service commit pipeline.
- Updated `DESIGN.md` section 17.2 to include the TipTap decision and commit pipeline constraints.

## Technical Debt

- TipTap extension set is defined at capability level but exact package/version matrix is not yet pinned.
- Git service wrapper abstraction is specified, but retry policy and conflict-resolution UX are still open.

## What Should Be Done Next

- Pin exact TipTap extension list and markdown plugin/version compatibility in Sprint 170 implementation notes.
- Add API examples for commit response metadata and failure modes.
- Add E2E coverage for markdown round-trip fidelity and push-failure recovery UX.
