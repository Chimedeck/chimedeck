## Update
- Added 404 `board-not-found` guard to `GET` and `PUT` state-transitions endpoints when the board has no active lists.
- Updated tests to cover the new 404 behavior for both endpoints.

## New
- Added tests for GET/PUT `/api/v1/boards/:boardId/state-transitions` returning 404 when no active lists exist.
- Created this changelog entry documenting the changes.

## Technical Debt
- Error envelope formats remain inconsistent across modules (see prior changelogs).

## What Should Be Done Next
- Implement `/rules` endpoint for state transitions.
- Add list sync hooks and websocket broadcast for state transition updates.
- Standardize error envelope formats across all modules.
