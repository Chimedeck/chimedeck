## Update
- Added board background image rendering to `BoardPage.tsx` with a dark scrim for legibility.
- Updated `BoardCard.tsx` to display a background thumbnail in the workspace dashboard.
- Enhanced `SearchResultItem.tsx` to show a 32×20 px board background thumbnail in search results.
- Extended `Board` type and API to support background URLs and client upload/delete functions.
- Added Redux action for real-time board background updates via WS event handler.
- Integrated `BackgroundPicker` into board settings UI.

## New
- `BackgroundPicker.tsx`: UI for uploading/removing board backgrounds with preview and Redux integration.
- `specs/tests/board-background-ui.md`: Playwright-style test spec for board background UI flows (7 cases).

## Technical Debt
- Playwright UI tests are not runnable (missing config and test runner).
- Some components still need theme audit.
- TypeScript errors persist in unrelated files (pre-existing).
- In-app notification UI/filtering and CommandPalette search filtering deferred.

## What Should Be Done Next
- Bootstrap Playwright and convert markdown specs to runnable tests.
- Complete theme audit for all components.
- Implement in-app notification UI/filtering and CommandPalette search filtering.
- Address pre-existing TypeScript errors.
- Consider advanced background editing (cropping, repositioning) in future iterations.