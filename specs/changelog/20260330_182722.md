## Update
- Modified `server/index.ts` to import and call `initSentry()` at the very start of server startup, ensuring Sentry is initialized before any other logic.
- Confirmed `server/config/env.ts` already contains all required Sentry config keys; no changes needed.

## New
- Added `server/common/monitoring/sentry.ts`: Bun-compatible Sentry initialization module exporting `initSentry`, `captureError`, and `captureMessage`. Applies `SENTRY_SERVER_ENABLED` guard, validates DSN, sets `environment` and `release` tags, and defaults to `sendDefaultPii: false` for privacy.
- Created this changelog documenting the changes.

## Technical Debt
- No automated tests for Sentry server integration; manual verification required.
- Privacy hardening and error middleware integration deferred to next iteration.

## What Should Be Done Next
- Integrate Sentry into error middleware for global error capture.
- Harden privacy and redaction of error payloads.
- Add/extend automated tests for error capture and envelope shape.
- Document manual test checklist for Sentry server integration if automated tests remain unavailable.
