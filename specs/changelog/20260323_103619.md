# Changelog — 2026-03-23T10:36:19Z

## Update
- `server/extensions/mcp/registerTools.ts`: Imported and registered `registerGetCard` so the new `get_card` tool is available in every transport mode.
- `server/extensions/mcp/README.md`: Updated the Available Tools table to include `search_cards`, `search_board`, and `get_card`; added parameter documentation sections for all three tools.

## New
- `server/extensions/mcp/tools/getCard.ts`: Implements the `get_card` MCP tool (`GET /api/v1/cards/:cardId`). Follows the exact pattern of `moveCard.ts` — Zod schema, `apiCall` via `apiClient`, structured error/success response.
- `server/extensions/mcp/tests/getCard.md`: Playwright MCP scenario test (T8) for `get_card`, covering the success path and two error cases.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 3: Create `src/extensions/DeveloperDocs/containers/McpDocsPage/McpDocsPage.tsx` — a static docs page with sticky TOC and 6 sections documenting all 9 MCP tools.
