# 20260422_143000

## Update

- Added board/card activity logging when comment reactions are added or removed.
- Ensured idempotent reaction add requests do not emit duplicate board activity entries or duplicate reaction notifications.
- Exposed reaction activity actions in both server and client activity allowlists so they are visible in existing activity feeds.

## New

- Added `comment_reaction_added` and `comment_reaction_removed` activity writes in:
  - `server/extensions/comment/api/reactions/add.ts`
  - `server/extensions/comment/api/reactions/remove.ts`
- Added reaction actions to feed visibility configuration:
  - `server/extensions/activity/config/visibleEventTypes.ts`
  - `src/extensions/Card/config/activityEventsConfig.ts`
- Added reaction-specific activity labels and translation templates:
  - `src/extensions/Card/config/activityEventLabels.ts`
  - `src/extensions/Activity/translations/en.json`
  - `src/extensions/Activity/components/ActivityItem.tsx` (`emoji` interpolation support)

## Technical Debt

- Reaction notifications are still direct-only to the comment author; there is no email channel for comment reaction notifications yet.
- Reaction add/remove handlers still perform repeated board/card/list lookup logic that could be extracted into a shared helper with comment endpoints.

## What Should Be Done Next

- Add integration tests for reaction activity logging and idempotent reaction add behavior.
- Validate realtime + activity ordering under concurrent reactions from multiple users.
- Decide whether reaction removal should remain visible in board activity by default or be hidden behind a filter.
