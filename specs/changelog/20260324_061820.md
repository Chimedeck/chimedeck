# Changelog — 2026-03-24T06:18:20Z

## Update

- `src/extensions/Plugins/api.ts` — added `isActive` query param support to `fetchAvailablePlugins`; added `deletePlugin` (DELETE) and `reactivatePlugin` (PATCH `isActive: true`) wrappers.
- `src/extensions/Plugins/routes.ts` — added `/plugins` route pointing to new `PluginRegistryPage`.
- `src/extensions/Plugins/reducers.ts` — exported `pluginRegistryReducer` alongside existing `pluginDashboardReducer`.
- `src/reducers.ts` — added `pluginRegistryReducer` to global reducer registry.
- `src/store/index.ts` — registered `pluginRegistry` key in the Redux store.
- `src/routing/index.tsx` — lazy-loaded `PluginRegistryPage` and added `/plugins` route inside the authenticated `AppShell`.
- `src/layout/Sidebar.tsx` — imported `isPlatformAdmin`; added `isPlatformAdminUser` derivation; added "Administration" section heading + "Plugins" `NavItem` (to `/plugins`) visible only to platform admins.
- `src/common/translations/en.json` — added `Sidebar.administrationSection` and `Sidebar.pluginsLabel` keys.
- `src/extensions/Plugins/translations/en.json` — added `plugins.registry.*` translation keys for the new page.

## New

- `src/extensions/Plugins/containers/PluginRegistryPage/PluginRegistryPage.duck.ts` — Redux duck with `fetchPluginsThunk`, `deletePluginThunk`, `reactivatePluginThunk`; state includes search query, category, and status filter; selectors for all state fields.
- `src/extensions/Plugins/containers/PluginRegistryPage/PluginRegistryPage.tsx` — Page shell with admin guard (redirects non-admins to `/`), heading "Plugin Registry", "+ Register Plugin" button (placeholder, wired in Iteration 4), loading/empty/error states, and a basic plugin name list (table wired in Iteration 3).

## Technical Debt

- The "+ Register Plugin" button is `disabled` pending Iteration 4 (RegisterPluginModal wiring).
- The plugin list renders only name + authorEmail as a placeholder; `PluginRegistryTable` + `PluginRegistryRow` are deferred to Iteration 3.
- `PluginRegistryPage` uses the same `fetchAvailablePlugins` API endpoint as the board dashboard; the server-side `isActive` param may need verification.

## What Should Be Done Next

- Iteration 3: Build `PluginRegistryTable`, `PluginRegistryRow`, extend `PluginSearchBar` with status filter, wire search/filter to debounced dispatch.
- Iteration 4: Wire "+ Register Plugin" button to `RegisterPluginModal` + `ApiKeyRevealModal`, add `addPlugin` thunk to the registry duck.
