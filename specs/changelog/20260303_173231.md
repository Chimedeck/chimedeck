## Update
- Extended server card list endpoint (`server/extensions/card/api/list.ts`) to aggregate and return card labels as a JSON array per card.
- Updated Card interface in `src/extensions/Card/api.ts` to include an optional `labels` field.
- Modified `CardItem`, `ListColumn`, `BoardCanvas`, and `BoardPage` to support and propagate label expansion state and rendering.

## New
- Added `useCardLabelExpanded` hook (`src/extensions/Card/hooks/useCardLabelExpanded.ts`) for per-board label expansion state in localStorage.
- Created `CardLabelChips` component (`src/extensions/Card/components/CardLabelChips.tsx`) for collapsible label chip UI.
- Integrated label chips into card tiles and enabled toggling/collapse/expand functionality.

## Technical Debt
- Playwright E2E and accessibility tests for label chips and member avatars are deferred.
- Real-time profile propagation and global middleware/rate-limiting remain incomplete.
- Keyboard navigation and accessibility for label chips are postponed to the next iteration.

## What Should Be Done Next
- Implement Playwright E2E and accessibility tests for label chips and member avatars.
- Complete real-time profile propagation and global middleware/rate-limiting.
- Add keyboard navigation and accessibility improvements for label chips.
- Wire up member avatar popovers and ensure state persistence for expanded/collapsed labels.