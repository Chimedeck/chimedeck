## Update
- Updated `server/extensions/plugins/sdk/jh-instance.ts` so `t.get()` and `t.set()` auto-resolve `resourceId` and `boardId` from the current plugin context (`this.args[scope]?.id`, `this.args.board?.id`).
- Rebuilt `public/sdk/jh-instance.js` to include the new auto-resolution logic for SDK data calls.

## New
- No new features or files added in this iteration.

## Technical Debt
- Playwright/e2e and MCP integration tests for plugin SDK remain unimplemented due to Bun/ESM incompatibility.
- Manual verification only; automated regression coverage is still lacking for SDK data flows.

## What Should Be Done Next
- Enable and implement Playwright/MCP tests for plugin SDK context/data flows once Bun/ESM blockers are resolved.
- Refactor admin logic to use DB roles instead of env/email.
- Complete registry caching, SDK versioning, and plugin authentication.
- Audit error/context handling and build the admin dashboard UI.