## Update
- Migrated 8 shared component files in `src/components/` and `src/common/components/` from hardcoded Tailwind color classes to semantic design tokens (e.g., `bg-bg-base`, `text-base`, `border-border`).
- Updated all relevant usages in `Page.tsx`, `Input.tsx`, `ThemeToggle.tsx`, `Toast.tsx`, `CommandPalette.tsx`, `ErrorBoundary.tsx`, `MentionChip.tsx`, and `MentionSuggestions.tsx`.
- Added `// [theme-exception]` comment to intentional brand-tint highlight in `renderMentions.tsx`.

## New
- Added Playwright MCP test spec `specs/tests/sprint-119-components-light-mode.md` covering 9 scenarios for migrated components in light/dark mode.

## Technical Debt
- Playwright is not configured to run `.md` test specs; the new test is not executed by default.
- Pre-existing type errors remain in unrelated files.

## What Should Be Done Next
- Convert the `.md` Playwright test spec to a `.spec.ts` file for execution.
- Address pre-existing type errors in affected files.
- Continue with the next planned iteration.
