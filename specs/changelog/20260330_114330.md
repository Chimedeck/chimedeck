## Update
- Added `handlebars@4.7.9` (runtime) and `@types/handlebars@4.1.0` (dev) to `package.json` using `bun add`.

## New
- Created `server/extensions/email/templates/render.ts` helper: provides `renderTemplate({ templateName, data })` to load, compile, and cache Handlebars templates from the `html/` subfolder.

## Technical Debt
None

## What Should Be Done Next
- Extract verification and admin invite email HTML to `.html` files using Handlebars syntax.
- Update builder functions to use the new async `renderTemplate` helper.
- Update all call sites to await the new async builder functions.
- Add README for template conventions.