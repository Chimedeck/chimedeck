## Update
- Refactored `src/extensions/Card/components/CardDescriptionTiptap.tsx` to enforce a single-line toolbar layout (no wrapping).
- Updated toolbar to use icon-only primary controls with accessibility labels and titles.
- Added click-outside detection to close the overflow menu.

## New
- Introduced a `+` overflow button at the end of the toolbar, opening a dropdown menu for secondary controls (Numbered list, Quote, Code block).
- Created `specs/tests/rich-text-toolbar-single-line-overflow.md` with Playwright MCP scenario tests for toolbar layout, overflow, and accessibility.

## Technical Debt
- Playwright test file is not recognized as a valid test (wrong extension/format); tests not executed yet.
- No shared ToolbarOneLine component created—logic is inlined in CardDescriptionTiptap for now.

## What Should Be Done Next
- Convert the scenario markdown to a valid Playwright `.spec.ts` test file and re-run tests.
- Extract shared one-line toolbar logic to a reusable component for use in CommentEditor.
- Apply the same toolbar/overflow pattern to CommentEditor.
- Address any accessibility or responsive layout issues found during testing.