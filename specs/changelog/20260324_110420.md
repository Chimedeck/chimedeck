## Update
- No changes made to existing code or features; all work focused on new integration tests.

## New
- Added `tests/integration/apiToken/apiToken.test.ts` covering API token creation, validation, authentication, and revocation scenarios (27 tests).
- Added `tests/integration/card/cardCRUD.test.ts` covering card creation, movement, archiving, and date field logic (34 tests).
- Both new test suites follow project conventions for integration tests using bun:test.

## Technical Debt
- Playwright MCP/e2e UI tests for these flows are missing or not configured; only integration tests were run and verified.
- Some unrelated Playwright e2e failures and missing `.vscode/mcp.json` remain unresolved.

## What Should Be Done Next
- Implement or tag Playwright MCP/e2e UI tests for API token and card CRUD flows to ensure end-to-end coverage.
- Address outstanding Playwright e2e failures and missing `.vscode/mcp.json` to enable full scenario test execution.
- Review and expand integration test coverage for edge cases as needed.
- Monitor for any required API/model changes surfaced by new tests.