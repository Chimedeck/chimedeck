# Changelog — 2026-03-18

## Update
- Updated `server/middlewares/boardVisibility.ts` to attach `req.guestType` for GUEST users, defaulting to 'VIEWER' if missing.
- Modified `server/extensions/board/api/guests/create.ts` to accept and persist `guestType` in POST body, defaulting to 'VIEWER'.
- Enhanced `server/extensions/board/api/guests/index.ts` to include `guestType` in list responses and legacy invite handler.
- Registered PATCH guest route in `server/extensions/board/api/index.ts`.
- Appended new test scenarios to `specs/tests/sprint-89-guest-role-split.md` for middleware and guestType flows.

## New
- Added `server/extensions/board/api/guests/updateGuest.ts` implementing PATCH endpoint to update guestType with validation, error handling, and event logging.

## Technical Debt
- None

## What Should Be Done Next
- Implement client-side permission guard and update invite modal to support guestType selection.
- Expand tests for UI flows and edge cases.
- Review event logging for completeness and auditability.
