# Changelog — 20260324_043135

## Update
- Created `specs/tests/mcp/` directory to house MCP-tool-specific scenario files.

## New
- `specs/tests/mcp/get_boards.md` — MCP scenario for listing workspace boards; covers success, non-member rejection, unauthenticated rejection, invalid workspace ID, and empty workspace.
- `specs/tests/mcp/get_board.md` — MCP scenario for retrieving a single board with its lists and cards; covers success, board-not-found, PRIVATE board access denial, PUBLIC board open access, and auth enforcement.
- `specs/tests/mcp/get_cards.md` — MCP scenario for listing cards in a list; covers success, archived card exclusion, empty list, list-not-found, board-access-denied, and auth enforcement.
- `specs/tests/mcp/get_card.md` — MCP scenario for retrieving a single card's full detail; covers success, optional fields, card-not-found, board-access-denied, archived card retrieval, and auth enforcement.
- `specs/tests/mcp/create_card.md` — MCP scenario for creating a new card in a list; covers title-only creation, creation with description, post-creation list verification, missing-title validation error, invalid listId, VIEWER access denial, non-member rejection, and auth enforcement.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 18: Create the remaining four MCP scenario files (`move_card.md`, `add_comment.md`, `search_cards.md`, `get_members.md`) in `specs/tests/mcp/`.
