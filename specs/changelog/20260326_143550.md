## Update
- `src/components/Page.tsx`: Replaced `bg-gray-900 text-white` with semantic `bg-bg-base text-base`.
- `src/common/components/Input.tsx`: Replaced auth glass-card classes (`bg-white/5`, `border-white/20`, `text-white`, `text-gray-300`, `text-gray-500`, `text-red-400`, `border-red-500`, `ring-blue-500`) with semantic tokens (`bg-bg-overlay`, `border-border`, `text-base`, `text-muted`, `text-subtle`, `text-danger`, `border-danger`, `ring-primary`).
- `src/common/components/ThemeToggle.tsx`: Replaced `text-slate-500 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-200 hover:bg-slate-100 dark:hover:bg-slate-800` with `text-muted hover:text-base hover:bg-bg-overlay`; removed all `dark:` overrides.
- `src/common/components/Toast.tsx`: Replaced `bg-slate-800`, `border-slate-700`, `text-slate-200`, `text-slate-500`, `hover:text-slate-300` with `bg-bg-surface`, `border-border`, `text-base`, `text-subtle`, `hover:text-muted`.
- `src/common/components/CommandPalette.tsx`: Replaced all hardcoded `bg-white/gray/slate`, `text-gray/slate`, `border-gray/slate`, `dark:` overrides with semantic tokens (`bg-bg-surface`, `border-border`, `text-base`, `text-muted`, `text-subtle`, `text-danger`, `bg-primary`, `text-inverse`, `bg-bg-overlay`).
- `src/common/components/MentionInput/MentionChip.tsx`: Replaced `bg-slate-700`, `bg-indigo-600`, `text-white`, `text-slate-200`, `text-slate-400` with `bg-bg-overlay`, `bg-primary`, `text-inverse`, `text-base`, `text-muted`.
- `src/common/components/MentionInput/MentionSuggestions.tsx`: Replaced `bg-slate-800 border-slate-700` with `bg-bg-surface border-border`.
- `src/common/components/ErrorBoundary.tsx`: Replaced `text-red-400` with semantic `text-danger`.

## New
- `specs/tests/sprint-119-components-light-mode.md`: Playwright MCP test spec (9 scenarios) covering CommandPalette, Toast, Input, ThemeToggle, MentionInput dropdown, and Page wrapper in both light and dark modes.

## Technical Debt
- `src/common/components/MentionInput/renderMentions.tsx`: Mention chip `bg-indigo-900/50 text-indigo-300` kept intentionally and tagged `// [theme-exception]` — brand-tinted mention highlight has no direct semantic token equivalent yet.
- Pre-existing TypeScript errors in the project remain unrelated to this iteration.

## What Should Be Done Next
- Iteration 6: Audit and migrate `src/extensions/` for the same hardcoded colour patterns.
- Add a `--color-mention` CSS custom property to the design token set and replace the `renderMentions.tsx` indigo hardcode with it.
- Convert `specs/tests/sprint-119-components-light-mode.md` to a `.ts` Playwright test file for automated execution.
