# Sprint 31 — Card Money Badge UI

## Update
- `src/extensions/Card/api.ts`: Added `amount: string | null` and `currency: string | null` fields to the `Card` interface.
- `src/extensions/Card/api/cardDetail.ts`: Extended `patchCard` fields type to include `amount` and `currency`.
- `src/extensions/Card/slices/cardDetailSlice.ts`: Added `amount` and `currency` to `applyOptimisticCardUpdate` fields type.
- `src/extensions/Card/components/CardItem.tsx`: Added `CardMoneyBadge` to the card footer — shown when `card.amount` is non-null.
- `src/extensions/Card/components/CardModal.tsx`: Added `onMoneySave` prop and a "Value" sidebar section using `CardValue` component.
- `src/extensions/Card/containers/CardModal/index.tsx`: Added `handleMoneySave` handler with optimistic update + board sync, wired to `CardModal`.

## New
- `src/extensions/Card/components/CardMoneyBadge.tsx`: Green badge that formats and displays the card's amount/currency using `Intl.NumberFormat`.
- `src/extensions/Card/components/CardValue.tsx`: Editable amount + currency sidebar component with inline validation (positive number, 3-letter ISO code).

## Technical Debt
- None.

## What Should Be Done Next
- Add Playwright E2E test for the money badge and Value section in card modal.
