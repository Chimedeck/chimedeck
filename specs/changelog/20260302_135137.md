## Update
- BoardPage.tsx updated to wire in real-time sync, presence avatars, connection indicator, and drag abort on disconnect.
- Integration of Redux slices for List and Card entities with optimistic, confirm, and rollback reducers.

## New
- Singleton WebSocket client with reconnect/backoff: `src/extensions/Realtime/client/socket.ts`
- In-memory mutation queue with overflow handling: `src/extensions/Realtime/client/messageQueue.ts`
- React hook for WebSocket lifecycle: `src/extensions/Realtime/hooks/useWebSocket.ts`
- React hook for board event sync: `src/extensions/Realtime/hooks/useBoardSync.ts`
- Redux middleware for optimistic/confirm/rollback: `src/extensions/Realtime/middleware/wsMiddleware.ts`
- List slice with optimistic/remote event logic: `src/extensions/List/listSlice.ts`
- Card slice with optimistic/remote event logic: `src/extensions/Card/cardSlice.ts`
- Presence avatars component: `src/extensions/Realtime/components/PresenceAvatars.tsx`
- User cursor component: `src/extensions/Realtime/components/UserCursor.tsx`
- 17 new tests for real-time sync, queue, and middleware (all passing).

## Technical Debt
- Activity backfill and board snapshot policy remain stubbed/hardcoded.
- WebSocket broadcasts for list/card events are stubbed.
- Some API endpoints return empty arrays for extended fields until later sprints.
- CardModal/CardTile integration and advanced drag conflict handling deferred.
- Playwright MCP evaluator not configured; E2E MCP tests not run.

## What Should Be Done Next
- Complete frontend integration for real-time sync in CardModal/CardTile.
- Replace activity stubs and make board snapshot policy configurable.
- Implement WebSocket broadcasts for all list/card events.
- Add advanced drag conflict handling and user cursor UI if needed.
- Configure Playwright MCP evaluator and run E2E tests for real-time flows.
- Implement deferred auth extension and silent refresh logic.