# Changelog — 20260311_015900

## Update

- `server/config/env.ts`: Added `OTEL_EXPORTER_OTLP_ENDPOINT` (default `http://localhost:4318`) and `OTEL_SERVICE_NAME` (default `kanban-server`) environment variables.
- `server/mods/observability/tracer.ts`: Fixed `NodeTracerProvider` instantiation — new SDK version requires `spanProcessors` in constructor options rather than `addSpanProcessor()` call.

## New

- `server/mods/observability/index.ts`: Added `initObservability()` entry point that initialises both tracer and metrics providers concurrently.
- `server/index.ts`: Wired `initObservability()` call at server boot (before flag loading), so OTel is active from the first request.
- `package.json` / `bun.lock`: Added all required `@opentelemetry/*` packages: `sdk-node`, `api`, `sdk-trace-node`, `exporter-trace-otlp-http`, `instrumentation-http`, `sdk-metrics`, `exporter-prometheus`, `exporter-metrics-otlp-http`.

## Technical Debt

- None introduced.

## What Should Be Done Next

- Iteration 2: Persist the offline mutation queue to IndexedDB (`idb` package) so pending mutations survive page reload.
- Iteration 3: Add `realtime.conflicts` counter and `realtime.propagation_delay_ms` histogram; add `POST /api/v1/metrics/propagation` endpoint.
