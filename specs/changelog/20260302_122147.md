## Update
- Mounted card API router in `server/index.ts`.
- No changes to existing features; all card logic is new this iteration.

## New
- `db/migrations/0006_card.ts`: Migration for `cards` table (fractional `position`, `archived`, `due_date`, FKs, timestamps).
- `server/extensions/card/`: Full backend for card CRUD, move, archive, duplicate, with permission checks and router wiring.
- `src/extensions/Card/`: Client API, CardTile, CardModal, CreateCardForm, and English translations for card UI.

## Technical Debt
- No Playwright/integration tests for card flows yet.
- Some error handling and edge cases (e.g., fractional index collisions) may need further hardening.
- Dev server and Playwright tests could not be run due to permission issues; manual verification required.

## What Should Be Done Next
- Implement Playwright/integration tests for card CRUD/move/permissions.
- Extend card model with labels, assignees, due dates, and checklists (Sprint 08).
- Harden edge cases for move logic and error handling.
- Ensure dev server and test runner permissions are resolved for future iterations.