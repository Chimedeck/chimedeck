# Changelog — 2026-04-13T04:25:00Z

## Update
- **ApiTokenPage**: Replaced raw `<button>` (Revoke action) with `Button variant="ghost" size="sm"` carrying `!text-danger` overrides to maintain the subtle danger-colored inline style.
- **MemberList**: Replaced raw `<button>` (Remove member action) with `Button variant="link" size="sm" className="!text-danger"` preserving the link-style danger text with hover underline.
- **WorkspaceListPage**: Replaced three raw `<button>` elements — header "New Workspace" and empty-state "Create workspace" with `Button variant="primary" size="md"`; workspace card "Open" with `Button variant="secondary" size="sm"`. Added `Button` import.
- **AcceptInvitePage**: Replaced raw `<button>` (Accept Invitation) with `Button variant="primary" size="md"`. Added `Button` import.

## New
- None (migration only).

## Technical Debt
- `AcceptInvitePage` has two pre-existing type errors (`post` method missing on API stub, `contentClassName` prop mismatch) that are out of scope for this iteration.

## What Should Be Done Next
- Iteration 5: Migrate remaining workspace/layout raw buttons — WorkspacePage, WorkspaceDashboard, Sidebar, TopBar.
