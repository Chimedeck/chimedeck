## Update
- Updated `server/config/env.ts` to add `SUBSCRIPTIONS_ENABLED` and `SUBSCRIPTIONS_DEFAULT_UNLIMITED_TIER` flags for controlling subscription gating and fallback behavior.

## New
- Added `db/migrations/20260601_workspace_subscriptions.ts` to create the `workspace_subscriptions` table (workspaceId FK, tier/status enums, Stripe fields, timestamps).
- Added `server/extensions/subscription/common/types.ts` for subscription/tier/status types.
- Added `server/extensions/subscription/common/serializer.ts` for workspace subscription serialization.
- Added `server/extensions/subscription/common/subscriptionRepo.ts` implementing CRUD and `getCurrentTier` logic with flag-aware fallback.
- Added `tests/server/extensions/subscription/common/subscriptionRepo.test.ts` for repository and tier fallback tests.
- Added `tests/db/migrations/20260601_workspace_subscriptions.test.ts` for migration up/down and schema validation.

## Technical Debt
None

## What Should Be Done Next
- Implement API endpoints for managing workspace subscriptions.
- Integrate Stripe for billing and tier management.
- Add tier resolver logic and feature gating at the API/service layer.
- Extend tests to cover API and integration scenarios.
- Monitor for future migration filename conflicts and adjust as needed.