## Update
- Extended `CardWithResolvedCover` type to include `cover_aspect_ratio` ('16:9' | '1:1' | null) and `cover_is_gif` (boolean).
- Updated SQL query in `server/common/cards/cover.ts` to select `mime_type`, `width`, and `height` for cover attachments.
- Modified cover resolution logic: GIFs now use `/view` URL, aspect ratio is derived from dimensions, and all fallback paths include the new fields.

## New
- Added aspect ratio and GIF detection to cover resolution, propagating `cover_aspect_ratio` and `cover_is_gif` in all cover payloads.

## Technical Debt
None

## What Should Be Done Next
- Add/extend tests to verify aspect ratio and GIF logic in cover resolution.
- Confirm all downstream consumers of `CardWithResolvedCover` handle the new fields.
- Monitor for edge cases with missing or invalid attachment data.