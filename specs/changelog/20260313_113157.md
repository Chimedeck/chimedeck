# Changelog — 20260313_113157

## Sprint 79 Iteration 6: Workspace Board Grid Visibility Badge + Board Header Avatar Stack

### Update
- `src/extensions/Board/components/BoardCard.tsx` — added `VisibilityBadge` import and render; visibility badge now appears alongside the state chip in each board card's title row.
- `src/extensions/Board/components/BoardHeader.tsx` — replaced static `BoardMemberAvatars` with the new `MemberAvatarStack`; added `onOpenMembers` prop to the interface and destructuring.
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx` — wired `useGetBoardMembersQuery` to supply live `boardMembers` to the header; added `membersOpen` state; passes `onOpenMembers={() => setMembersOpen(true)}` to `BoardHeader`; renders `BoardMembersPanel` when `membersOpen` is true.

### New
- `src/extensions/Board/components/VisibilityBadge.tsx` — small chip component showing board visibility (PRIVATE 🔒 slate, WORKSPACE 👥 blue, PUBLIC 🌐 green) with accessible `aria-label`.
- `src/extensions/Board/components/MemberAvatarStack.tsx` — clickable wrapper around `BoardMemberAvatars`; shows "+ Add members" prompt when the board has no members; emits `onOpenMembers` on click.
- `specs/tests/sprint-79-iteration-6-board-ui.md` — Playwright MCP test covering visibility badge on board cards, avatar stack opening the members panel, overflow indicator, and empty-state prompt.

### Technical Debt
- `BoardHeader` `Member` interface (id/display_name/email) diverges from `BoardMember` (user_id/…); BoardPage maps user_id → id at the call site. A shared type would eliminate the mapping.

### What Should Be Done Next
- Iteration 7: Sprint 80 — Guest Invite Flow (Guests tab in Board Members Panel + RTK Query slice for board guests).
