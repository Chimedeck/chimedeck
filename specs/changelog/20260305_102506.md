## Update
- Updated `server/config/env.ts` to add support for the `PLATFORM_ADMIN_EMAILS` environment variable.
- Replaced membership-based admin checks in `server/extensions/plugins/api/registry/create.ts`, `update.ts`, and `delete.ts` with the new `platformAdminGuard` middleware.
- Improved error handling and validation in plugin registry endpoints (e.g., duplicate slug returns `plugin-slug-taken`, connectorUrl must start with `https://`).
- Fixed delete handler to return `{ data: {} }` instead of `{ data: null }`.

## New
- Added `server/config/platformAdmin.ts` to read and expose platform admin emails from environment variables.
- Added `server/middlewares/platformAdminGuard.ts` middleware to enforce platform admin access for plugin registry mutations.
- Added `PLATFORM_ADMIN_EMAILS` to `.env` and `.env.example` for configuration and onboarding.

## Technical Debt
- No Playwright or MCP evaluator tests exist for plugin registry admin, forbidden, or validation flows; automated test coverage is missing for these scenarios.
- Deferred robust error handling for plugin iframes and full end-to-end integration.
- Soft-deleted plugins (is_active=false) may not be excluded from GET results elsewhere (dependency for future work).

## What Should Be Done Next
- Add Playwright tests for plugin registry admin, forbidden, and validation scenarios (e.g., 403 forbidden, 422 invalid-connector-url, 409 plugin-slug-taken).
- Implement server-side plugin registry search/filter endpoints and corresponding client-side UI.
- Ensure soft-deleted plugins are excluded from GET results and address any related dependencies.
- Expand automated test coverage and address deferred error handling and integration gaps.