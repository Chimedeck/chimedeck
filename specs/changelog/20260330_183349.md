## Update
- Integrated a global error handler middleware (`handleUnhandledError`) into the server, ensuring all unhandled exceptions are captured and API responses use the standard error envelope shape.
- Updated `server/index.ts` to wrap the main router call in a try-catch, routing errors through the new middleware.

## New
- Added `server/common/middlewares/errorHandler.ts` implementing Sentry capture and robust error envelope/fallback logic.
- Created unit tests in `tests/unit/server/common/middlewares/errorHandler.test.ts` covering envelope shape, Sentry capture, and edge cases.
- Added changelog documenting the integration and deferred items.

## Technical Debt
- No automated tests for Sentry server integration; Sentry event capture not verifiable without a dedicated error-triggering endpoint.
- Privacy hardening and error payload redaction deferred.
- Cannot test fallback serialization or non-Error thrown values in live API (no endpoint triggers unhandled errors).

## What Should Be Done Next
- Add an endpoint to deliberately trigger unhandled server errors for integration testing.
- Implement privacy hardening/redaction in error payloads sent to Sentry.
- Add/extend automated tests for Sentry event capture and fallback error handling.
- Document error handling and Sentry integration usage in README if not already covered.
- Manual verification of Sentry event receipt in dashboard when DSN is enabled.