## Update
- None. No changes to existing code or features; all work in this iteration was additive.

## New
- Added `db/migrations/0106_webhooks.ts`: Migration creating `webhooks` and `webhook_deliveries` tables, using `string` (varchar) for all ID and FK columns to match project conventions. Migration verified to run cleanly.
- Added `server/extensions/webhooks/common/eventTypes.ts`: Exports `WEBHOOK_EVENT_TYPES` (22 event types, including notification-system aliases) and `WebhookEventType` union type as the single source of truth for webhook event types.

## Technical Debt
None

## What Should Be Done Next
- Implement webhook registration API and delivery logic using the new tables and event type constants.
- Add tests for webhook registration, event emission, and delivery.
- Document webhook event types and delivery guarantees in architecture specs.
