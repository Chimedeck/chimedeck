# Changelog — 2026-03-18T07:55:11Z

## Sprint 89 Iteration 3 — Client Permission Guard + Guest Invite Modal

### Update

- `src/extensions/Board/slices/boardGuestsSlice.ts` — Added `guestType: GuestType` to `BoardGuest` interface. Updated `inviteBoardGuest` mutation to accept optional `guestType` (defaults to `'VIEWER'`). Added `updateBoardGuest` mutation (`PATCH /api/v1/boards/:boardId/guests/:userId`). Exported `useUpdateBoardGuestMutation`.
- `src/extensions/Board/components/BoardMembersPanel/GuestsTab.tsx` — Added `GuestType` state for the invite form. Added Viewer/Member toggle button group below the email input. The selected role is sent with the invite request. Success message reflects the chosen role. Role resets to `VIEWER` after a successful invite.

### New

- `src/extensions/Board/mods/guestPermissions.ts` — Client-side permission helper. Exports `GuestType` type and `canBoardGuestWrite(guestType)`. Returns `true` for `MEMBER` and non-guest callers (`null`/`undefined`), `false` for `VIEWER`. Mirrors server-side logic.

### Technical Debt

- `GuestsTab.tsx` serves as the invite modal for iteration 3. A dedicated `GuestInvite` extension can be extracted if the invite flow grows more complex.

### What Should Be Done Next

- Iteration 4: Add `guestType` badge in `GuestList`/`GuestsTab` for existing guests and allow ADMIN+ to change type inline. Apply `canBoardGuestWrite` guards to card create button, comment input, attachment upload, card edit, and member assign controls.
