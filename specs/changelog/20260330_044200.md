## Update
- Added `handlebars@4.7.9` and `@types/handlebars@4.1.0` to package.json dependencies.

## New
- `server/extensions/email/templates/render.ts`: new `renderTemplate({ templateName, data })` async helper that loads `.html` Handlebars templates from the `html/` subfolder via `Bun.file`, compiles them, and caches the compiled functions in a module-level `Map` to avoid repeated disk I/O.

## Technical Debt
- No HTML template files exist yet; `renderTemplate` will throw at runtime until `html/*.html` files are created (Iteration 2+).

## What Should Be Done Next
- Iteration 2: Extract verification email HTML into `server/extensions/email/templates/html/verification.html` and update `verificationEmail.ts` to call `await renderTemplate(...)`.
