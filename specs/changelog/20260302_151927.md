## Update
- Expanded `src/extensions/Auth/duck/authDuck.ts` to use typed API wrappers, added `signupThunk` and `refreshTokenThunk`, and handled new reducer cases.
- Replaced stub implementations in `LoginPage.tsx` and `SignupPage.tsx` with full-featured, accessible forms and error handling.
- Updated `src/App.tsx` to dispatch `refreshTokenThunk` on mount for session restoration.

## New
- Added `src/extensions/Auth/api/auth.ts` for typed auth API calls.
- Created reusable components: `PasswordInput.tsx`, `AuthDivider.tsx`, `OAuthButton.tsx`, `LoginForm.tsx`, and `SignupForm.tsx`.
- Added `src/extensions/Auth/translations/en.json` for all UI strings and error messages.
- Added Playwright test specs: `specs/tests/auth-login.md` and `specs/tests/auth-signup.md`.

## Technical Debt
- OAuth buttons are rendered but not fully functional; feature flag logic is stubbed with TODOs.
- Error mapping may need refinement for all backend error codes.
- Could not run Playwright tests due to dev server permission issues; test coverage is unverified.

## What Should Be Done Next
- Manually start the dev server (`bun run dev`) and run Playwright tests to verify authentication flows.
- Complete OAuth provider integration and feature flag wiring.
- Refine error mapping and add more granular i18n if needed.
- Begin E2E coverage for refresh token and PrivateRoute scenarios.