## Update
None

## New
- Added `tests/e2e/attachment-upload.spec.ts`: Playwright E2E spec covering multipart upload, URL attachments, MIME/size validation, unauthenticated/invalid requests, and UI checks for attachment section and download links.
- Added `tests/e2e/payment-card-price.spec.ts`: Playwright E2E spec covering card money field updates, partial/clearing updates, JWT auth, validation errors, monetisation flag toggling, and UI price badge checks.

## Technical Debt
- All tests in both new spec files fail due to a `TypeError` in `registerAndLogin` (tests/e2e/_helpers.ts): login response does not provide the expected `data.access_token`.
- No E2E tests from sample-project/ to reference; conventions inferred from existing tests.

## What Should Be Done Next
- Fix the login API or test helper so it returns the expected `data.access_token` for E2E tests.
- Re-run Playwright E2E tests to verify new specs pass once login is fixed.
- Review and update selectors/flows if UI for attachment upload or card price has changed.
- Ensure server is running and endpoints are implemented for full test coverage.
