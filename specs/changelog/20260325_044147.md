# Changelog — 20260325_044147

## Update
- `server/extensions/attachment/api/index.ts`: Registered `PATCH /api/v1/attachments/:id` route.
- `src/extensions/Attachments/types.ts`: Replaced `url: string | null` with `view_url: string | null`; added `alias: string | null`; added `PatchAttachmentRequest` type.
- `src/extensions/Attachments/api.ts`: Imported `PatchAttachmentRequest`; added `patchAttachment({ attachmentId, alias })` helper.

## New
- `server/extensions/attachment/api/patch.ts`: PATCH endpoint for `/api/v1/attachments/:id`. Authenticates caller, verifies board membership, validates alias (non-empty string, max 255 chars), persists to DB, returns updated attachment row in API envelope. Never exposes raw S3 URLs.
- `tests/e2e/sprint-117-attachment-alias.md`: Playwright MCP markdown tests covering: valid alias → 200 + updated row; empty alias → 400; whitespace-only alias → 400; unauthenticated → 401; alias >255 chars → 400; list response includes `alias` field.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 3: Add inline Edit action to `AttachmentItem` and wire `onRename` through `AttachmentPanel` using `patchAttachment`.
