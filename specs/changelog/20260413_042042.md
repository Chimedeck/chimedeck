# Changelog — 2026-04-13T04:20:42Z

## Update
- `RegisterPluginModal.tsx`: header close button migrated from raw `<button>` to `IconButton` (ghost variant); footer cancel button migrated to `Button` (ghost/sm).
- `EditPluginModal.tsx`: same header close + footer cancel migration as RegisterPluginModal.
- `CardModal/index.tsx`: error-state inline "Close" link migrated from raw `<button>` to `Button` (link variant); added `Button` import.
- `MemberAssignModal.tsx`: header close button migrated to `IconButton`; member assign/remove buttons migrated to `Button` (primary/danger variants).

## New
- `IconButton` and `Button` primitives now used consistently across all plugin and card modal close/action controls.

## Technical Debt
- Category tag remove buttons in RegisterPluginModal and EditPluginModal remain as raw `<button>` — these are inline form-field controls (chip badges), not modal close/action buttons, and will be addressed in a future sweep iteration.
- Pre-existing lint and typecheck errors in unrelated files (`featureFlagsSlice.ts`, etc.) are not addressed here.

## What Should Be Done Next
- Iteration 4: Migrate raw buttons in ApiTokenPage, MemberList, WorkspaceListPage, AcceptInvitePage to Button variants.
