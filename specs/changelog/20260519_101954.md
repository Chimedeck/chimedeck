## Update
- None. No changes made to existing code or features in this iteration.

## New
- Created `specs/trello-adapter-normalization/matrix.md` documenting a gap analysis for all Trello Actions endpoints.
- Matrix table compares required keys (from checklist) vs. current implementation for each endpoint, with PASS/PARTIAL/MISMATCH status and notes.

## Technical Debt
- None. No shortcuts or new issues introduced in this documentation-only iteration.

## What Should Be Done Next
- Implement and test missing or partial Actions endpoints as identified in the matrix (e.g., `GET /actions/{id}/organization`, reactions endpoints).
- Add contract tests for Actions endpoints to verify Trello compatibility.
- Address any ambiguous or conditionally-present keys flagged in the matrix.
- Ensure test credentials are valid and login flow works for Playwright-based tests.