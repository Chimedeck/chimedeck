# Changelog — 2026-04-13T04:09:19Z

## Update
- Migrated all raw `<button>` elements in `CommentItem`, `Attachment/AttachmentItem`, `AttachmentUrlModal`, and `Attachments/AttachmentItem` to use `Button` or `IconButton` primitives from the design system.

## New
- `Attachment/components/AttachmentItem.tsx` now imports and uses `Button` (secondary/sm for open/download) and `IconButton` (ghost, danger-colored) for the delete action.
- `Attachment/components/AttachmentUrlModal.tsx` cancel and submit actions now use `Button` (secondary/sm and primary/sm respectively).
- `Comment/components/CommentItem.tsx` edit, delete, and reply inline text actions now use `Button` with `variant="link"` and scoped className overrides to preserve muted/danger color intent.
- `Attachments/components/AttachmentItem.tsx` rename save/cancel and delete confirmation buttons now use `Button` (link variant); delete trigger icon button now uses `IconButton`. The critical `onMouseDown` blur-prevention handlers are preserved.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 3: Migrate plugin modal and card modal close/action raw buttons to `IconButton`/`Button`.
