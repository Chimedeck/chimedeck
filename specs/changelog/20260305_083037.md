# Sprint 38 §2–4: Plugin Data Board Isolation & Resource Validation

## Update
- `server/extensions/plugins/api/plugin-data/get.ts`: Now requires `boardId` query param (returns 400 if missing). Validates that the `resourceId` belongs to the given `boardId` before querying (returns 403 on mismatch). Includes `board_id` in all DB where clauses.
- `server/extensions/plugins/api/plugin-data/set.ts`: Now requires `boardId` in the request body (returns 400 if missing). Same resource-board validation (403 on mismatch). Includes `board_id` in the unique-check query and in inserts.

## New
- `server/extensions/plugins/common/validateResourceBelongsToBoard.ts`: Helper that checks a `card` or `list` resourceId belongs to the specified `boardId` by querying the DB. Throws `ResourceBoardMismatchError` on mismatch. Board and member scopes are handled as no-ops or direct equality check.

## Technical Debt
- The `resolvePlugin` helper is duplicated between `get.ts` and `set.ts`; could be extracted to a shared module.

## What Should Be Done Next
- Iteration 6: Audit `usePluginBridge.ts` to confirm `boardId` is always forwarded in DATA_GET/DATA_SET messages, then run end-to-end smoke tests.
