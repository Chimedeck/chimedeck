## Update
- Registered new API endpoints for email change and confirmation in `server/extensions/auth/api/index.ts`.
- Updated `src/extensions/Auth/translations/en.json` with new keys for change email flows.
- Added new API methods to `src/extensions/Auth/api/auth.ts`.
- Registered `confirmEmailChange` reducer in `src/store/index.ts`.
- Updated `src/routing/index.tsx` to add `/confirm-email-change` route.

## New
- Migration `db/migrations/0025_email_change.ts` adds `pending_email`, `email_change_token`, and `email_change_token_expires_at` columns to `users` table.
- `server/extensions/auth/api/changeEmail.ts`: POST endpoint for requesting email change (with password re-validation, rate limiting, and conditional confirmation flow).
- `server/extensions/auth/api/confirmEmailChange.ts`: GET endpoint for confirming email change via token, with race-condition re-check and session invalidation.
- `server/extensions/email/templates/emailChangeConfirmation.ts`: Email template for confirmation link.
- `src/extensions/Auth/containers/ConfirmEmailChangePage/ConfirmEmailChangePage.tsx` and `.duck.ts`: Confirmation page and Redux slice.
- `src/extensions/Auth/components/ChangeEmailForm.tsx`: Form for submitting new email and password.
- `src/extensions/Auth/components/EmailChangePending.tsx`: Banner for pending confirmation.
- Test specs: `specs/tests/change-email.md`, `change-email-taken.md`, `change-email-no-flag.md`.

## Technical Debt
None

## What Should Be Done Next
- Implement Sprint 41 (Forgot Password/Password Reset) and Sprint 42 (AWS credential split).
- Set up and configure the "mcp-evaluator" Playwright project to enable automated test runs for new flows.
- Thoroughly test edge cases: race conditions, session invalidation, and error handling for email change flows.
- Monitor for user feedback and refine UI/UX for the new email change process.