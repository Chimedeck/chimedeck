## Update
- Integrated graph/list sync logic into state transition PUT and list rename/delete flows.
- Enhanced validator and serializer to support robust sync and rules output.
- WebSocket event `state_transition_updated` is now emitted after successful PUT.
- Expanded and updated tests for sync, rules, and WS event flows.

## New
- Implemented GET `/api/v1/boards/:boardId/state-transitions/rules` endpoint returning normalized rules JSON.
- Added `stripDeletedNodes` and related helpers for board/list sync in `sync.ts`.
- Created/extended test files for rules endpoint, sync logic, and WS event emission.

## Technical Debt
- Error envelope formats remain inconsistent across endpoints.
- Lint/typecheck/test suite has unrelated failures outside stateTransitions scope.

## What Should Be Done Next
- Implement `/copy` endpoint and further UI integration.
- Standardize error envelope formats across all endpoints.
- Address repo-wide lint/typecheck/test failures unrelated to stateTransitions.
- Expand UI-based tests once login form is available.