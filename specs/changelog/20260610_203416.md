## Update
- **Client `api.ts`** — added `refineCardChat()` helper calling `POST /cards/:cardId/chat/refine`, returning typed `RefineCardChatResult` with `assistantMessage`, `session`, `qualityScore`, and `loopComplete` fields.
- **`CardChatDrawer.tsx`** — added `currentSession` state (synced from props via effect), `refining`/`refineError` state. Added purple "Refine" button (with `ArrowPathIcon` spinner) in composer area, visible only when session status is `ACTIVE_REFINEMENT`. Composer and Send button are disabled while refinement is running. Quality score meter and status badge update after refine completes. Error state renders "Refinement failed. Please try again." without crashing the drawer.
- **`goalLoop.ts`** — added `updateCardDescription` dependency to `goalLoopDeps`. Added `tryUpdateCardDescription()` helper that calls `extractStructuredFields()` on the final assistant response and idempotently writes an `## AI-Refined Requirements` markdown section into the card description (replaces existing block if present). Invoked after both `READY_FOR_REVIEW` and exhausted-turns paths.
- **`CardChatDrawer.test.tsx`** — added 7 new tests: Refine button visible/hidden per session status, button disabled during loop, score meter updates after completion, READY_FOR_REVIEW badge appears, error display. Total 25 tests (up from 18).

## New
- **`server/extensions/cardChat/mods/baPersona/structuredExtraction.ts`** — new module that parses BA assistant responses for labeled markdown sections: `**Business Value:**`, `**EARS Requirements:**`, `**Acceptance Criteria:**`, `**Constraints/Non-Goals:**`, and `**Problem Statement:**`. Returns typed `CardRequirementFields` with nullable string fields. Includes `formatStructuredBlock()` producing an `## AI-Refined Requirements` section with quality score metadata.
- **`server/…/baPersona/__tests__/structuredExtraction.test.ts`** — 13 tests covering full extraction, partial extraction, alternative headings, empty text, irrelevant content, trivial content rejection, and heading-without-colon variants.

## Technical Debt
- AI provider (deepseek-v4-pro via OLLAMA proxy) was unreachable during end-to-end browser testing — refine endpoint flows were verified via Playwright route interception with mock 200 responses and direct API PATCH for card description verification. Real AI-driven refinement not testable until provider is reachable.
- CardModal overlay (z-50) sits above CardChatDrawer (z-40), blocking pointer events on the drawer. Browser testing required `page.evaluate()` workarounds for clicking drawer elements. The z-index should be fixed so the drawer sits above the modal.
- React `forwardRef` warning fires on `CardChatDrawer` when rendered inside Radix `Dialog.Portal` (accessibility ref not forwarded).
- `IDLE` state remains implicit (no session = IDLE) — not in the DB CHECK constraint. Acknowledged as low-priority Sprint 174 cleanup.

## What Should Be Done Next
- Resolve AI provider connectivity so the real refine loop can run end-to-end (verify BA persona questions, multi-turn flow, quality score convergence to ≥ 90).
- Add end-to-end integration test for the refine endpoint (`POST /chat/refine`) covering the full request/response cycle with a mock AI provider.
- Add "Approve/Rerun" controls to the drawer UI for when refinement reaches READY_FOR_REVIEW (accept extracted requirements or restart the loop).
- Fix CardModal/CardChatDrawer z-index stacking so drawer is fully interactive without workarounds.
