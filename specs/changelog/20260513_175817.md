# Update
- Enforced one-modal-at-a-time behavior in card description edit mode for link/config popovers.
- Updated outside-click handling so open link modals close when user clicks outside.

# New
- Opening the link config modal now closes link insert and asset picker popovers first.
- Opening link insert popover now closes the link config modal first.
- Opening asset picker now closes link insert and link config modals first.

# Technical Debt
- Popover coordination is still component-local state wiring; this can be centralized into a single modal manager for cleaner long-term maintenance.

# What Should Be Done Next
- Add interaction tests that verify:
  - outside click closes open link config/edit modal
  - only one popover/modal is open at any time in description edit mode
