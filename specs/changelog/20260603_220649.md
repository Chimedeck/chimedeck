## Update
- Tightened SpecsWorkspacePage conflict recovery state handling around stale saves and manifest loading.
- Added a persistent conflict recovery banner with an explicit reload action.
- Preserved file state across manifest reloads and cleared conflict state after reload.

## New
- Added reducer coverage for save conflict handling and conflict-clearing transitions.
- Added a dedicated conflict recovery state path for SpecsWorkspacePage.

## Technical Debt
- Live browser verification is still blocked by the file-load path: the workspace tree renders, but the selected file remains stuck on `Loading...` so the editor never mounts.
- The full 412 stale-save/reload flow could not be exercised end to end in the browser session.

## What Should Be Done Next
- Fix the Specs file-load path so selected files advance out of `Loading...`.
- Re-run the browser conflict/reload flow once the editor mounts reliably.
- Expand coverage for the reload path and stale-precondition recovery UX.
