## Update
- Updated `Makefile` to add `test-e2e` (runs Playwright E2E tests) and `test-all` (runs full test suite via orchestration script) targets.
- Updated `.PHONY` targets to include new test commands.

## New
- Added `run-all-tests-full.sh` script to orchestrate unit (bun), e2e (Playwright), and MCP scenario tests sequentially.
  - Supports `SKIP_UNIT`, `SKIP_E2E`, `SKIP_MCP` env vars for selective skipping.
  - Forwards `BASE_URL` as `TEST_BASE_URL` to test runners.
  - Soft-skips missing test modes/tools with clear messages.
  - Prints emoji summary table and exits nonzero on failure.

## Technical Debt
None

## What Should Be Done Next
- Validate that all intended test modes are recognized and executed (especially Playwright scenario/E2E tests).
- Move or convert any misplaced test files as per Sprints 111/112.
- Address deferred features: docs wiring, board views, uuidv7 migration.
- Monitor for CI integration and portability issues across environments.
