## Update
- Added `onInsertComment` prop and Comment button to `AttachmentItem.tsx` for inserting attachment markdown into comments.
- Updated `ActivityFeed.tsx` to use proxy `view_url` for all attachment links and thumbnails.
- Modified `AttachmentPanel.tsx` to wire `onInsertComment` and handle markdown insertion via ref.
- Enhanced `CardModal.tsx` to create and pass `insertMarkdownRef` to both `AttachmentPanel` and `ActivityFeed`.
- Extended `CommentEditor.tsx` to support external markdown insertion via `insertMarkdownRef`.
- Updated translations in `en.json` for new comment action labels.

## New
- Added Playwright MCP scenario file: `tests/e2e/sprint-117-attachment-comment-action.md` covering UI and insertion behavior for attachment comments.

## Technical Debt
- Playwright `.md` scenario is not a runnable test; needs conversion to `.ts` for execution.
- Minor pre-existing type errors remain unrelated to these changes.

## What Should Be Done Next
- Convert the Playwright `.md` scenario to a `.ts` test file for automated execution.
- Proceed with deferred features: avatar proxy, S3 URL rewrite, and further proxy/alias enhancements.
- Monitor for any issues with markdown insertion in edge layouts or editors.