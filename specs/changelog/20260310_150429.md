## Update
- Registered CalendarView in BoardPage for CALENDAR view type.
- Added `optimisticUpdateCardField` reducer to `boardSlice.ts` for drag-to-reschedule support.
- Fixed minor syntax errors in `server/extensions/board/api/create.ts` and `patch.ts`.

## New
- Implemented CalendarView feature:
  - `src/extensions/CalendarView/types.ts` (types, constants)
  - `src/extensions/CalendarView/CalendarCardChip.tsx` (draggable chip)
  - `src/extensions/CalendarView/CalendarDayCell.tsx` (day cell, overflow)
  - `src/extensions/CalendarView/CalendarMonthGrid.tsx` (6-week grid, navigation)
  - `src/extensions/CalendarView/CalendarView.tsx` (root, toolbar, drag-to-reschedule)
- Added `tests/e2e/calendar-view.spec.ts` with 5 passing E2E Playwright tests for CalendarView flows.

## Technical Debt
- None

## What Should Be Done Next
- Implement CalendarView weekly grid and drag-to-reschedule (Sprint 53, part 2).
- Add TimelineView and register in BoardViewSwitcher.
- Expand E2E tests for edge cases (e.g., timezones, invalid due dates).
- Address Playwright/Bun test runner compatibility for broader E2E coverage.
- Add E2E tests for session revocation and multi-device flows.