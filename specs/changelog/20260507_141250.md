## Update
- Extended checklist item drag-and-drop so items can move between different checklist groups inside the same card modal.
- Centralized checklist item drag handling at the card checklist level, while preserving checklist group reordering.
- Added explicit drop-zone support per checklist so users can drop into checklists even when the destination is empty.

## New
- Added optimistic state transition for cross-checklist checklist-item moves in card detail state.
- Added API support in card-detail client patch wrappers for checklist item `checklist_id` updates.
- Added server-side PATCH support for `checklist_id` on checklist items with validation that destination checklist exists and belongs to the same card.

## Technical Debt
- CardModal component still exceeds configured complexity threshold (pre-existing issue), and should be decomposed into smaller view/controller chunks.

## What Should Be Done Next
- Add integration coverage for moving checklist items across checklists, including empty destination checklist and rollback-on-failure cases.
- Emit an activity event for checklist-item checklist-group moves so board/card activity feeds capture this action.
