## Update
- Standardised ALL `<input>`, `<textarea>`, and `<select>` elements across the entire `src/` tree to use the canonical semantic token className pattern: `bg-bg-overlay text-base placeholder:text-subtle border border-border rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent`.
- Replaced all hardcoded `bg-gray-*`, `bg-white`, `bg-slate-*` fills on form elements with `bg-bg-overlay`.
- Replaced all `focus:ring-indigo-500` and `focus:ring-blue-500` on inputs with `focus:ring-primary`.
- Replaced all `placeholder-gray-*`, `placeholder-slate-*`, and `placeholder:text-muted` on inputs with `placeholder:text-subtle`.
- Removed all now-redundant `dark:` override variants on form elements (semantic tokens handle both modes).
- Fixed prose color classes in `CardDescriptionTiptap.tsx` and `CardDescription.tsx`: removed `text-gray-900/700` and `bg-gray-50` from prose containers; replaced with `text-base` and `bg-bg-overlay`. The `dark:prose-invert` Tailwind variant was already present on all `.prose` usages and is confirmed correct.
- Added themed scrollbar CSS to `src/index.css` using `::-webkit-scrollbar` rules and CSS custom properties (`--bg-overlay`, `--border`, `--border-strong`).

## New
- Added `specs/tests/sprint-120-inputs-and-prose.md`: Playwright MCP markdown test spec covering input placeholder colour in both themes, prose-invert rendering, themed scrollbars, and theme-toggle consistency.

## Technical Debt
- `Attachment/components/AttachmentUrlModal.tsx` uses inline `style={}` props instead of Tailwind — not converted in this iteration. Needs a full component refactor.
- A few pre-existing TypeScript errors unrelated to theme migration remain in `authDuck.ts`, `attachmentMarkdown.ts`, `Automation/api.ts`, and `Activity/components/ActivityFeed.tsx`.
- `CardDescriptionTiptap.tsx` and `CardDescription.tsx` still use `hover:bg-gray-100` in the click-to-edit region (hover state for surface, not an input element) — deferred to Iteration 3 contrast audit.

## What Should Be Done Next
- Iteration 3: Full text/contrast audit — walk every screen in both light and dark modes, fix remaining invisible/washed-out text (text-white on light surfaces, error/success hardcoded red/green), and run the grep verification commands from sprint-120.md to confirm zero non-exception hardcoded colour classes remain.
