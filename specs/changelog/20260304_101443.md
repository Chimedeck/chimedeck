## Update
- Modified `server/extensions/card/api/update.ts` to support PATCHing `amount` and `currency` fields on cards, with validation and activity event emission.
- Updated `server/extensions/board/api/get.ts` to include `amount` and `currency` in card responses.
- Ensured `card.money.updated` is present in `server/extensions/activity/config/visibleEventTypes.ts` (from previous iteration).

## New
- Added migration `db/migrations/0019_card_money.ts` to add `amount` (decimal 15,4) and `currency` (varchar 3) columns to the `cards` table.
- PATCH /api/v1/cards/:id now accepts and validates `amount` and `currency`, defaults currency to USD, and clears both fields when `amount: null`.
- Emits `card.money.updated` activity event and `card_updated` WebSocket event on money changes.

## Technical Debt
- None

## What Should Be Done Next
- Add Playwright/browser UI tests for card money fields and activity feed.
- Implement global middleware and rate-limiting.
- Address minor TypeScript errors and missing actor name in activity rows.
