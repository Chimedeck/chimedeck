# Sprint 96 — Iteration 2: Translations, GlobalNotificationToggle heading, email footer URL

## Update
- Replaced old translation keys (`UserProfile.notificationPreferences`, `UserProfile.notifications`) with new keys: `UserProfile.notificationsSubheading`, `UserProfile.notificationsMasterNote`, and `GlobalNotificationToggle.heading` in `en.json`.
- Updated `GlobalNotificationToggle.tsx` to include a self-contained `<h2>` heading using the new translation key.
- Changed email notification footer URL in `shared.ts` from `#notifications` to `?tab=notifications` for correct deep-linking.

## New
- Added translation keys for improved notification settings UI clarity and discoverability.
- Introduced a dedicated heading for the master notification toggle in the UserProfile settings page.

## Technical Debt
- No automated Playwright or Bun tests exist for UserProfile notification UI or email footer flows; only markdown specs are present. Manual verification or new test implementation is required.

## What Should Be Done Next
- Scaffold Playwright test files for UserProfile notification UI and email footer flows based on existing markdown specs.
- Implement automated tests to cover notification tab labels, master toggle heading, and email footer deep-linking.
- Perform manual QA if automated tests are not yet available.
