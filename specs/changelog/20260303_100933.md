# Changelog — 20260303_100933

## Update

### Search 500 Error Fixed
- **Root cause**: `server/extensions/search/api/query.ts` was referencing a non-existent `archived` boolean column on the `boards` table. Boards use a `state` enum (`'ACTIVE'` | `'ARCHIVED'`) instead.
- **Fix**: Changed board search query to select `state` instead of `archived`, and filter with `WHERE state = 'ACTIVE'` instead of `WHERE archived = false`.
- **Files**: `server/extensions/search/api/query.ts`

### Board Settings Click-Outside Close
- **Problem**: Opening the ··· board settings dropdown had no way to dismiss it except clicking the button again.
- **Fix**: Added an invisible `role="presentation"` full-screen backdrop div (`fixed inset-0 z-10`) behind the dropdown. Clicking anywhere outside the menu triggers `setMenuOpen(false)`.
- **Files**: `src/extensions/Board/components/BoardHeader.tsx`

## New

### Search Modal Integration (completed from previous agent session)
- `SearchModal` from `src/extensions/Search/` is now rendered in `AppShell` with a global Cmd+K / Ctrl+K keyboard shortcut.
- Sidebar now shows a 🔍 Search button with ⌘K hint.
- Navigation on result selection: boards → `/boards/:id`, cards → `/boards/:boardId?card=:cardId`
- Server card search results now include `board_id` so direct card deep-links work.
- **Files**: `src/layout/AppShell.tsx`, `src/extensions/Workspace/components/Sidebar.tsx`, `src/extensions/Search/api.ts`, `server/extensions/search/api/query.ts`

### Board Archive/Delete Actions (completed from previous agent session)
- `BoardPage.tsx` was not passing `onArchive` / `onDelete` props to `BoardHeader`, so the settings dropdown rendered no items.
- Added `handleBoardArchive` and `handleBoardDelete` handlers; wired them up to the header.
- **Files**: `src/extensions/Board/containers/BoardPage/BoardPage.tsx`

## Technical Debt

- The `connected` prop on `BoardHeader` is marked deprecated but not yet removed — will need a follow-up cleanup sprint.
- Search does not yet support trigram (fuzzy) matching — only exact `to_tsquery` full-text. Cards/boards need the `pg_trgm` extension and `GIN` index for fuzzy prefix search.
- `SearchModal` uses a polling/debounce model; could be moved to a React Query or SWR cache to reduce redundant fetches.

## What Should Be Done Next

1. **Fuzzy search**: Enable `pg_trgm` extension and add composite `GIN` indexes on `search_vector || title` so short partial queries (e.g. "sp") return results.
2. **Search result highlighting**: Add a server-side `ts_headline()` snippet so matched terms are highlighted in the search results UI.
3. **Card deep-link on search select**: Verify the `?card=:id` query param is being read by `BoardPage` and opens the card detail modal automatically on navigation.
4. **Board settings deprecation cleanup**: Remove the `connected` prop from `BoardHeader` and its callers.
5. **List archive/unarchive**: The list options menu shows "Archive" but the unarchive flow (restoring an archived list) has not been tested.
