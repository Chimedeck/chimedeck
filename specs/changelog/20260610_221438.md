## Update
- **Fixed SprintGeneration client test imports.** Changed `bun:test` → `vitest` and `mock()` → `vi.fn()` in `ApproveRerunControls.test.tsx` and `GeneratedDiffSummary.test.tsx` (7+5 tests). These files used the Bun-native `mock` API which vitest can't resolve; now pass correctly under vitest.
- **Wired CardModal callbacks to ActivityFeed for Sprint 176.** Added `onFileClick`, `onApprove`, `onRerun`, `onEdit` props to `CardModal` component (type definition, destructuring, pass-through to `ActivityFeed`). Defined stub handler implementations in `CardModal/index.tsx` container (`handleFileClick`, `handleEditRunApprove`, `handleEditRunRerun`, `handleEditRunEdit`) with `console.debug` + `TODO` markers for real API wiring. This completes the UI integration so sprint-gen and as-built activity events render with rich controls.
- **Added 21 unit tests for CardActivity renderers.** Created `ActivityEventRenderer.test.tsx` covering `isRichEventType` (16 tests: all 12 registered sprint-gen/as-built event types + 3 false positives + empty string) and `ActivityEventRenderer` component dispatch (5 tests: SprintGenEvent, AsBuiltEvent, null for unrecognised types, callback pass-through).
- **Extended isolated test runner to cover pathGuard and tierPolicy tests.** Added `aiEditOrchestrator/mods/pathGuard/__tests__/index.test.ts`, `sprintGeneration/mods/tierPolicy/__tests__/index.test.ts`, and `quotaEnforcer.test.ts` to `run-cardchat-tests.sh`. These pass in isolation but fail in bulk due to Bun `mock.module` cross-file caching (same pattern as cardChat tests). Runner now covers 14 files / 141 tests.

## New
- `src/extensions/CardActivity/components/__tests__/ActivityEventRenderer.test.tsx` — 21 unit tests for the rich event renderer discriminator and component.

## Technical Debt
- CardModal container handlers (`handleFileClick`, `handleEditRunApprove`, `handleEditRunRerun`, `handleEditRunEdit`) are stubs — they log to console but don't call actual API endpoints. Real wiring needs POST/PATCH to the ai/edit orchestrator API.
- `quotaEnforcer.ts` still uses module-scoped `quotaEnforcerDeps` that tests mutate directly — should be refactored to factory/constructor pattern.
- `generateArtifacts` round-robin heuristic may duplicate requirements on the last sprint when `ceil(N/sprintCount)` leaves an empty trailing bucket.
- 46 stateTransitions tests fail when run together (Bun `mock.module` cross-file issue) — pre-existing, not sprint 171-176.
- 6 `document is not defined` errors only occur with `bun test` (no jsdom); all client tests pass correctly under vitest.

## What Should Be Done Next
- Implement real `handleEditRunApprove`/`handleEditRunRerun`/`handleEditRunEdit` handlers calling the AI edit orchestrator API endpoints.
- Wire `requiresHumanApproval` from tier/phase metadata into `ApproveRerunControls` to conditionally show approve/edit controls.
- Add SprintArtifactLinks component tests.
- Resolve pre-existing stateTransitions test isolation (add to script or convert to factory pattern).
- Deploy server and verify activity events appear in card feed with rich rendering in staging.
