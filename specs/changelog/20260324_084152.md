## Update
- None (new module, no existing code modified).

## New
- Added `server/extensions/healthCheck/common/config/healthCheck.ts`: centralised config for the probe engine, reading `HEALTH_CHECK_ENABLED`, `HEALTH_CHECK_TIMEOUT_MS`, and `HEALTH_CHECK_AMBER_THRESHOLD_MS` from the top-level env module.
- Added `server/extensions/healthCheck/mods/probe/classify.ts`: pure function mapping `(httpStatus, responseTimeMs, error, amberThresholdMs)` → `'green' | 'amber' | 'red'`. No I/O, fully deterministic.
- Added `server/extensions/healthCheck/mods/probe/runProbe.ts`: HTTP GET probe with configurable timeout (AbortController), SSRF guard (DNS pre-check + private IP blocklist covering RFC-1918, loopback, link-local, IPv6 ULA), and redirect detection via `response.redirected`.
- Added `server/extensions/healthCheck/mods/probe/persistResult.ts`: INSERTs a probe result into `board_health_check_results` and returns the camelCase persisted row.
- Added `server/extensions/healthCheck/mods/probe/index.ts`: composes `runProbe → persistResult` into a single `probe({ healthCheckId, url })` call; re-exports types and helpers for downstream API layers.
- Added `tests/unit/server/extensions/healthCheck/mods/probe/classify.test.ts`: 18 unit tests covering all sprint-115 edge cases — green fast, green slow→amber, 3xx→amber, 4xx→red, 5xx→red, timeout→red, network error→red, SSRF rejection shape, custom threshold, error-precedence guard.

## Technical Debt
- `runProbe.ts` uses `redirect: 'follow'` and `response.redirected` to detect 3xx. This means the caller sees `httpStatus: 301` for any redirect chain rather than the true intermediate code. Acceptable for the current classification rules; revisit if per-redirect-code behaviour is needed.
- DNS pre-check in `assertPublicHost` performs a single A/AAAA lookup; a host with multiple addresses (some private, some public) could bypass the guard on a subsequent lookup by the OS resolver. Acceptable for now — document and address in a later security hardening pass.

## What Should Be Done Next
- Iteration 3: Implement Health Check REST API endpoints (presets, CRUD, probe on-demand) and mount the extension behind `HEALTH_CHECK_ENABLED`.
