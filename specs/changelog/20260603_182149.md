# Update

- Implemented Sprint 168 iteration 9 backend contract for board-level GitHub Project URL settings.
- Added `GET/PATCH /api/v1/boards/:id/settings/integrations` routing and handlers in board API.
- Added board activity/audit emission for integration URL changes with previous/new hash+reference payload.

# New

- Added migration `0121_board_github_project_url.ts` introducing nullable `boards.github_project_url`.
- Added `server/extensions/board/mods/githubProjectUrl.ts` for URL validation, canonical normalization, and audit reference/hash derivation.
- Added `server/extensions/board/api/__tests__/integrations.test.ts` covering valid/invalid URL paths, RBAC enforcement, normalization behavior, and audit emission.
- Added `board_github_project_url_updated` to activity visible event allowlist.

# Technical Debt

- Repo-wide lint/typecheck baseline is currently red due to pre-existing issues outside this sprint scope (CLI lint config mismatch and unrelated TypeScript errors); this iteration keeps its coverage focused on the new endpoint/tests.

# What Should Be Done Next

- Iteration 10 should wire the Board Settings UI field (`Github Project Url`) to these new integrations endpoints, including disabled state for unauthorized callers and inline 422 validation messaging.
