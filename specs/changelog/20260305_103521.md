## Update
- Modified `src/extensions/Plugins/api.ts` to add `registerPlugin(body)` (POST `/api/v1/plugins`) and the `RegisterPluginBody` interface.
- Updated `PluginDashboardPage.duck.ts` to include `registerPluginThunk`, `clearRegisterState` action, and new state for `registerStatus`, `registerError`, and `newApiKey` with selectors.
- Enhanced `PluginDashboardPage.tsx` to use `isPlatformAdmin`, enable "Add a Plugin" for admins, and wire up registration and API key modals with correct open/close/refresh logic.
- Added `VITE_PLATFORM_ADMIN_EMAILS` to `.env` (not committed).

## New
- Created `src/extensions/Auth/utils/isPlatformAdmin.ts` to check if the current user's email matches any in `VITE_PLATFORM_ADMIN_EMAILS`.
- Added `src/extensions/Plugins/modals/RegisterPluginModal.tsx`: full-featured plugin registration modal with validation, slug auto-derivation, category tags, and error handling.
- Added `src/extensions/Plugins/modals/ApiKeyRevealModal.tsx`: modal for one-time API key display with copy and warning.

## Technical Debt
- No Playwright or MCP evaluator tests exist for plugin dashboard or registration flows; only a manual test plan is present.
- Platform admin logic is still based on email env var, not DB roles.
- Playwright e2e tests remain blocked by Bun/ESM issues.

## What Should Be Done Next
- Author Playwright specs for plugin dashboard, registration, and API key reveal flows.
- Implement PluginSearchBar and dashboard search/filter UI.
- Refactor admin checks to use DB roles instead of env var.
- Add registry caching, SDK versioning, plugin authentication, static serving, and admin UI as per architecture spec.