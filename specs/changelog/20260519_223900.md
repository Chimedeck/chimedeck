# Update

- Added explicit 404 handling for state transitions GET/PUT when a board exists but has no active lists, matching iteration edge-case requirements.
- Preserved existing feature-flag and validation behavior for `/api/v1/boards/:boardId/state-transitions`.

# New

- Added API tests to assert `GET` and `PUT` return `404` with `name: "board-not-found"` when active board lists are missing.

# Technical Debt

- `stateTransitionsRouter` currently contains route handling beyond Iteration 2 scope (`/rules`), which should be split into phase-specific mounting for clearer sprint boundaries.

# What Should Be Done Next

- Continue with Iteration 3: implement and verify `/rules` endpoint behavior, list sync hooks, and websocket broadcast integration against sprint acceptance criteria.
