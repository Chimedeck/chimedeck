## Update
- Fixed comment reaction notification copy showing a heart fallback after refresh by persisting reaction emoji in the notifications table.
- Updated the notifications list API to return the stored reaction emoji so client rendering is stable across realtime and paginated fetch paths.
- Updated notification UI typing/rendering to use the typed notification emoji field instead of an untyped cast.

## New
- Added migration `0115_notifications_add_emoji.ts` to add nullable `notifications.emoji`.

## Technical Debt
- Existing `NotificationItem` accessibility lint warning remains (`div` with `role="button"`), unrelated to this fix.

## What Should Be Done Next
- Run DB migration in all environments before deploying this fix so newly created reaction notifications persist emoji.
- Backfill historical `comment_reaction` notifications with emoji when recoverable from related activity events, otherwise they will continue showing fallback emoji.
