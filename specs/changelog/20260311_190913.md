## Update
- Added `label` and `configSchema` fields to `TriggerHandler` in `server/extensions/automation/common/types.ts`.
- Updated `engine/registry.ts` to expose `getAllTriggerHandlers()`.
- Modified `server/index.ts` to register triggers at startup.
- Installed `zod@4.3.6` for schema validation.

## New
- Implemented 13 card trigger modules under `server/extensions/automation/engine/triggers/card/` (`created`, `movedToList`, `movedFromList`, `labelAdded`, `labelRemoved`, `memberAdded`, `memberRemoved`, `dueDateSet`, `dueDateRemoved`, `checklistCompleted`, `allChecklistsCompleted`, `archived`, `commentAdded`).
- Added `engine/triggers/validate.ts` for trigger config validation.
- Added `engine/triggers/index.ts` to register all handlers.
- Created `tests/integration/automation/triggers.test.ts` (44 passing tests).

## Technical Debt
- None

## What Should Be Done Next
- Implement board and list triggers.
- Add trigger-types discovery API.
- Expand integration tests for edge cases and negative scenarios.
- Continue with multipart upload endpoints and further attachment features as planned.