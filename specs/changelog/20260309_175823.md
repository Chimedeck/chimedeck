## Update
- Updated `server/index.ts` to enhance `/api/v1/flags` endpoint, now returning `sesEnabled`, `adminInviteEmailEnabled`, and `adminEmailDomains`.
- Registered `adminInvite` and `featureFlags` reducers in `src/reducers.ts` and `src/store/index.ts`.
- Modified `src/layout/AppShell.tsx` to fetch feature flags on mount and render the `InviteExternalUserModal` globally.
- Updated `src/extensions/Workspace/components/Sidebar.tsx` to show the "Invite External User" nav item only for admin-domain users.

## New
- Added `src/extensions/AdminInvite/types.ts` for feature-specific TypeScript types.
- Created `src/extensions/AdminInvite/api.ts` with `adminInviteApi.createUser()` for the admin invite API.
- Implemented `src/extensions/AdminInvite/adminInvite.slice.ts` for modal and credential state management.
- Built `src/extensions/AdminInvite/InviteExternalUserModal.tsx` with full form, password strength bar, send-email toggle, and error handling.
- Added `src/extensions/AdminInvite/CredentialSheet.tsx` for displaying/copying credentials post-invite.
- Wrote `src/extensions/AdminInvite/README.md` documenting feature usage and architecture.
- Introduced `src/slices/featureFlagsSlice.ts` for global feature flag state.
- Created `tests/integration/adminInvite/adminInvite.ui.test.ts` with 17 passing Playwright UI tests.

## Technical Debt
- Playwright/E2E tests for admin/invite flows are present, but broader E2E coverage for related flows (e.g., password reset UI, AWS credential refactoring) remains deferred.
- No pre-existing test failures were addressed; one unrelated test in `Auth store` remains failing.

## What Should Be Done Next
- Monitor and address deferred E2E coverage for password reset UI and AWS credential refactoring.
- Review and improve accessibility and translations for new AdminInvite UI components.
- Consider refactoring test setup to resolve pre-existing `Auth store` test failures.
- Continue to monitor for edge cases in admin invite flows as feature is adopted.