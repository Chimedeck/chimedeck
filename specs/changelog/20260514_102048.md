## Update
- Fixed card move no-op handling in server move API so dropping a card back into the same list at the same index does not trigger persistence, events, or notification fan-out.
- Aligned card move event dispatch with activity behavior by emitting card.moved only when a card actually changes lists.

## New
- Added a same-list/index no-op guard in card move endpoint logic to return current card data immediately.

## Technical Debt
- The current no-op detection performs a same-list ordered card id lookup. If drag volume grows significantly, this can be optimized by deriving current index from preloaded state in a single query path.

## What Should Be Done Next
- Add API-level tests for PATCH /cards/:id/move covering:
  - same-list same-index no-op (no event/notification)
  - same-list different-index reorder behavior
  - cross-list move behavior (event/notification emitted)
