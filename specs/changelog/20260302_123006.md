## Update
- Updated `server/extensions/card/api/index.ts` and `get.ts` to support includes for labels, members, and checklist items in card GET responses.
- Extended `src/extensions/Card/api.ts`, `routes.ts`, `types.ts`, and `translations/en.json` to support new endpoints and UI strings for card extended fields.
- Integrated new CardModal sections and components into existing card UI flows.

## New
- Added `db/migrations/0007_card_extended.ts` for labels, card_labels, card_members, and checklist_items tables.
- Implemented `server/extensions/label/api/` (CRUD for labels, workspace-scoped).
- Added `server/extensions/card/api/labels.ts`, `members.ts`, `checklist.ts`, `dueDate.ts` for card sub-resources.
- Enforced max 20 labels per card in `server/extensions/card/mods/labels/validate.ts`.
- Created 7 new frontend components: `LabelChip`, `LabelPicker`, `CardMemberAvatars`, `MemberAssignModal`, `ChecklistProgress`, `ChecklistItem`, `ChecklistSection`.
- Added 4 new CardModal sections: `LabelsSection`, `MembersSection`, `ChecklistSection`, `DueDateSection`.
- Added 15 new tests in `specs/tests/card-extended-fields.test.ts` (all passing).

## Technical Debt
- Notification system for due dates is stubbed only.
- Attachments, comments, and activities for cards are deferred to a future iteration.
- Playwright MCP evaluator project is not configured; UI e2e tests not run.

## What Should Be Done Next
- Implement attachments, comments, and activities for cards.
- Integrate notification system for due dates.
- Configure and run Playwright MCP evaluator for UI e2e coverage.
- Continue frontend integration and polish for CardModal and board views.