## Update
- Registered new `csrfGuard` middleware globally in `server/index.ts` to validate `Origin`/`Referer` headers on all mutating requests.
- Updated `server/config/env.ts` to add `APP_BASE_URL` (with fallback to `APP_URL`).
- Added `Secure` flag to all `refresh_token` `Set-Cookie` headers in auth API files, ensuring `SameSite=Strict; Secure; HttpOnly`.

## New
- Created `server/middlewares/csrfGuard.ts` to enforce CSRF protection via origin header checks.
- Added `tests/e2e/csrf-guard.md` manual test plan for CSRF guard scenarios.

## Technical Debt
- Playwright E2E tests for CSRF guard are only documented in Markdown, not executable.
- Global middleware registration and input sanitization are still pending.

## What Should Be Done Next
- Convert `tests/e2e/csrf-guard.md` into an executable Playwright `.ts` test file.
- Implement input sanitization and global security middleware.
- Add global rate-limiting and revisit deferred security features.
- Ensure all mutating routes remain covered by CSRF guard and cookie flags are consistently set.