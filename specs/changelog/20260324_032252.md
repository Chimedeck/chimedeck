# Changelog — 20260324_032252

## Update
- Converted 5 remaining `.md` scenario files in `tests/e2e/` to Playwright `.spec.ts` files and deleted the originals.

## New
- `tests/e2e/business-logic-invariants.spec.ts` — API-level tests for Sprint 56 invariants: archived board blocks mutations (403 `board-is-archived`); workspace last-owner removal/demotion returns 422 `workspace-must-have-one-owner`.
- `tests/e2e/card-description-inline-edit.spec.ts` — UI tests for inline click-to-edit description: enter edit mode by click, Ctrl+Enter save, Escape cancel, Save/Cancel buttons, empty placeholder, keyboard accessibility.
- `tests/e2e/csrf-guard.spec.ts` — API-level tests for CSRF origin-header guard middleware: correct origin passes, mismatched Origin/Referer blocked (403 `csrf-origin-mismatch`), no-origin allowed, GET exempt, cookie flags.
- `tests/e2e/delete-confirmation.spec.ts` — API + UI tests for delete-confirmation flag: 409 without `confirm:true` on non-empty board/list, 204 with `confirm:true`, empty board/list deleted immediately, UI confirmation dialogs.
- `tests/e2e/input-sanitization.spec.ts` — API-level tests for XSS prevention: card title, card description, board title/description, list name, comment content, custom field `value_text`.

## Technical Debt
- UI tests (card-description-inline-edit, delete-confirmation) use best-effort selectors (`data-testid`, `aria-label`, `role="dialog"`). If testids drift, selectors may need updating.
- `business-logic-invariants.spec.ts` — the "second OWNER" role-change test accepts 422 as valid since the invitation flow may not be fully implemented; marked with TODO.

## What Should Be Done Next
- Iteration 8: Create `tests/README.md` documenting the three-directory structure and how to run each suite.
