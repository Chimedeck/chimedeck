## Update
- Replaced plugin registry API stubs in `server/extensions/plugins/api/index.ts` with real handler dispatch for all CRUD operations.

## New
- Implemented GET `/api/v1/plugins` (list) and GET `/api/v1/plugins/:id` (detail) endpoints with admin/public visibility rules.
- Added POST `/api/v1/plugins` for admin-only plugin registration, including `api_key` generation and manifest capability fetch (fail-open).
- Added PATCH `/api/v1/plugins/:id` for admin-only partial updates.
- Added DELETE `/api/v1/plugins/:id` for admin-only soft-delete (`is_active=false`).
- Created five new handler files: `registry/list.ts`, `registry/get.ts`, `registry/create.ts`, `registry/update.ts`, `registry/delete.ts`.

## Technical Debt
- No Playwright or e2e test coverage for plugin registry endpoints.
- Dev server and Playwright MCP evaluator project are not configured/running, so automated tests were not executed.
- Pre-existing unrelated TypeScript errors remain unresolved.

## What Should Be Done Next
- Implement client-side dashboard UI, Redux duck, and board integration for plugin management.
- Add Playwright/e2e tests for all plugin registry API flows (happy/error paths).
- Address advanced plugin authentication, registry caching, and deferred UI/test work from previous sprints.
- Fix dev server and Playwright MCP evaluator configuration to enable automated test runs.