# Sprint 122 Iteration 2 — Multi-Instance Notification Delivery via Pub/Sub

## Update
- Updated `server/extensions/realtime/api/index.ts` to call `subscribeUserChannel` after registering a user socket and `unsubscribeUserChannel` after deregistering, ensuring correct WebSocket lifecycle integration.
- Updated all `publishToUser` call sites in:
  - `server/extensions/activity/mods/mapActivityToNotification.ts`
  - `server/extensions/notifications/mods/createNotifications.ts`
  - `server/extensions/notifications/mods/boardActivityDispatch.ts`
  - `server/extensions/events/mods/publishBoardDeleted.ts`
  to properly `await` the now-async function or return its promise in `.then()` chains.

## New
- No new files or features added; all changes were updates to existing notification delivery and WebSocket lifecycle logic.

## Technical Debt
- None introduced by this iteration. All changes align with architecture requirements and existing async/await patterns. Pre-existing test failures and backend auth errors remain unrelated to this work.

## What Should Be Done Next
- Investigate and resolve backend 500 errors on `/api/v1/auth/refresh` to unblock UI login and further testing.
- Review and address pre-existing test failures unrelated to notification delivery.
- Monitor for any missed `publishToUser` call sites or edge cases in WebSocket lifecycle handling in future iterations.