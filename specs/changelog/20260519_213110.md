## Update
- PUT /state-transitions now normalizes and syncs persisted graph data with current board lists before saving.
- validator.ts enforces strict graph/list sync: returns 422 with name: "state-transition-graph-out-of-sync" if node labels are stale.
- Updated tests for validator and PUT error handling.

## New
- findOutOfSyncNodeLabel helper in validator.ts to detect out-of-sync node labels.
- Test cases for out-of-sync graph/list validation and error envelope.
- Changelog entry for this iteration.

## Technical Debt
- Error envelope formats remain inconsistent across modules.
- Real-time sync/event handling complexity not fully addressed.

## What Should Be Done Next
- Investigate/fix login flow or backend connectivity (login page stuck, see test-homepage-console-errors.log).
- Implement /rules and /copy endpoints if not yet done.
- Add list rename/delete sync hooks and websocket broadcast on updates.
- Ensure error envelope consistency across all modules.