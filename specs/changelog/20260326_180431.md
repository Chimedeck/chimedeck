# Changelog — 20260326_180431

## Update

Bulk semantic token migration — replaced hardcoded `bg-slate-*`, `bg-gray-*`, `text-white`, `text-slate-*`, `text-gray-*`, `bg-red-*`, `bg-green-*`, `bg-indigo-*` with theme-aware design tokens across 56 files.

**Tokens applied:**
- Background: `bg-bg-base`, `bg-bg-surface`, `bg-bg-overlay`, `bg-bg-sunken`
- Text: `text-base`, `text-subtle`, `text-muted`, `text-link`, `text-inverse`
- Border: `border-border`, `border-border-strong`
- Semantic: `bg-primary`, `bg-primary-hover`, `bg-success`, `bg-danger`, `text-success`, `text-danger`

**Files changed:**
- Auth pages (Login, Signup, ForgotPassword, VerifyEmail, ConfirmEmailChange, ResetPassword) — containers and form components
- Card components (MemberAvatarPopover, MemberAssignModal, CardMetaStrip, CardModalBottomBar, CardMembers, OneLineToolbar, CardActionMenu, ChecklistProgress, CommandMenu, CardReferenceChip, ActivityFeed, CardMemberAvatars, CardLabelChips, CardDatesPicker, CardModal/index, EditorHelpModal)
- Board components (BoardStateChip, VisibilityBadge)
- Plugin components (EditPluginModal, RegisterPluginModal, PluginDashboardPage, ApiKeyRevealModal, PluginRegistryTable, PluginAllowedDomainsPanel, CardPluginSection, CardPluginBadges, CardDetailPluginBadges, CardPluginButtons)
- AdminInvite (CredentialSheet, InviteExternalUserModal)
- List (AddListForm)
- BoardViews (BoardCommentsPanel, BoardArchivedCardsPanel)
- Calendar (CalendarView, CalendarWeekGrid, CalendarDayCell, CalendarMonthGrid, CalendarCardChip)
- Timeline (TimelineRow, TimelineHeader)
- CustomFields (BoardCustomFieldsPanel)
- UserProfile (EditProfilePage, GlobalNotificationToggle)

**Fixed broken JSX comments** inside ternary expressions in:
- `VerifyEmailPage.tsx` (invalid `{/* */}` inside ternary branch)
- `VerificationPending.tsx` (same pattern)

**JSX comments converted to inline `// [theme-exception]`** on all primary-button `text-white` instances.

## New

- `src/common/tokens/index.ts` — centralized semantic token class-string constants
- `specs/tests/sprint-120-contrast-audit.md` — Playwright MCP contrast audit test suite

## Technical Debt

- `TableRow.tsx` and `WorkspaceDashboard/index.tsx` still have broken JSX comments inside ternary/map expressions causing TypeScript errors — these are pre-existing and not part of this sprint's scope

## What Should Be Done Next

- Fix remaining broken JSX comments in `TableRow.tsx` and `WorkspaceDashboard/index.tsx`
- Run Playwright contrast audit tests from `specs/tests/sprint-120-contrast-audit.md`
- Audit remaining hardcoded colors in files not covered by this sprint
