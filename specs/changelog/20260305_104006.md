# Changelog — 2026-03-05T10:40:06Z

## Update
- Extended `fetchAvailablePlugins` in `api.ts` to accept search, category, and pagination params; now builds query string accordingly.
- Updated `PluginDashboardPage.duck.ts` to add `searchQuery`, `selectedCategory`, and `categories` state; added reducers and thunks to handle search/filter and category fetching.
- Modified `PluginDashboardPage.tsx` to render the new search bar, wire up search/category changes, and display empty state UI with a Clear button.

## New
- Added `PluginSearchBar.tsx` component: debounced search input (300ms), category dropdown, and callback wiring for search/filter changes.
- Implemented `fetchCategories` API utility for retrieving plugin categories from the server.

## Technical Debt
- No automated Playwright/e2e tests for dashboard search/filter due to Bun/ESM issues.
- Admin logic remains env-based, not DB roles; error handling and registry caching deferred.

## What Should Be Done Next
- Implement Playwright/e2e tests for dashboard search/filter once Bun/ESM issues are resolved.
- Migrate admin logic to DB roles and improve error handling.
- Add registry caching, SDK versioning, plugin authentication, and static serving as outlined in requirements.
