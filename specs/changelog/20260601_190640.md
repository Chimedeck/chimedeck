## Update
- Tightened subscription enforcement so board member adds, guest invites, and attachment upload starts now return `402 limit-reached` at plan caps.
- Extended quota usage aggregation to cover invited members, guests, and storage bytes for guard evaluation.
- Fixed the card workspace resolver path used by attachment uploads so storage checks can run against the correct workspace.
- Kept `SUBSCRIPTIONS_ENABLED=false` as a bypass for all subscription guards.

## New
- Added limit enforcement for invited-member, guest, and storage quota flows using the existing `limitGuard` contract.
- Added live coverage for the over-limit write paths and the read-only-over-limit behavior.
- Captured a screenshot artifact for the attachment over-limit state during retest.

## Technical Debt
- `bun run typecheck` still reports unrelated pre-existing repo errors outside this iteration's quota changes.
- Storage accounting is still tied to the current persisted-size model and may need revisiting if upload semantics change.

## What Should Be Done Next
- Finish the remaining subscription UI/billing work.
- Revisit the broader usage aggregation approach to reduce direct-count coupling.
- Track down and fix the existing card-scoped custom-field lookup issue noted in prior changelogs.
