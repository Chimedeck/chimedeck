## Update
- Updated `server/extensions/trelloCompat/serializers/board.ts` to use persisted `short_id` for `shortLink`/`shortUrl` when present, preserving fallback logic.
- Extended `server/extensions/trelloCompat/helpers/assertTrelloShape.ts` to validate board-specific nested contract requirements (prefs, labelNames, limits, key field types).
- Updated `specs/trello-adapter-normalization/matrix.md` with Iteration 4 statuses: `GET /boards/{id}` = PASS, `GET /boards/{id}/{field}` = PARTIAL.

## New
- Added `server/extensions/trelloCompat/tests/unit/server/extensions/trelloCompat/boards.contract.test.ts` to provide contract test coverage for board normalization.

## Technical Debt
- No contract test coverage existed previously for board normalization; this is now partially addressed but may require further edge case fixtures.
- UI contract tests are blocked due to login failure with test credentials; unable to verify end-to-end flows.

## What Should Be Done Next
- Verify and fix the dev server user database or test credentials to unblock UI contract tests.
- Expand board contract test fixtures to cover more edge cases (archived boards, missing optional fields, etc.).
- Review and address any remaining gaps in board normalization per `endpoint-response-checklist.md`.
- Continue updating matrix and contract tests for other Trello endpoints as needed.
