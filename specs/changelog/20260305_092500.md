# Changelog 2026-03-05 09:25

## Update
- Modified `src/extensions/Board/containers/BoardSettings/BoardSettings.tsx` to add a Plugins navigation entry that navigates to `/boards/:boardId/settings/plugins`.

## New
- `src/extensions/Plugins/config/pluginsConfig.ts` — feature config with API base paths.
- `src/extensions/Plugins/api.ts` — fetch wrappers for `fetchBoardPlugins`, `fetchAvailablePlugins`, `enablePlugin`, `disablePlugin`.
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.duck.ts` — Redux duck with thunks, optimistic enable/disable, and rollback logic.
- `src/extensions/Plugins/hooks/useBoardPlugins.ts` — thin hook wrapping duck selectors and dispatchers.
- `src/extensions/Plugins/components/PluginCapabilityChips.tsx` — compact capability chip list.
- `src/extensions/Plugins/components/PluginCard.tsx` — single plugin row (icon, name, description, capabilities, enable/disable button).
- `src/extensions/Plugins/components/PluginList.tsx` — renders Active and Available sections.
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.tsx` — board admin-only dashboard page; redirects on 403 from server.
- `src/extensions/Plugins/routes.ts` — registers `/boards/:boardId/settings/plugins` route.
- `src/extensions/Plugins/reducers.ts` — re-exports `pluginDashboardReducer`.
- `src/extensions/Plugins/README.md` — extension documentation.
- Modified `src/reducers.ts` — added `pluginDashboardReducer` export.
- Modified `src/extensions.ts` — registered plugin routes in the global extension registry.
- Modified `src/store/index.ts` — added `pluginDashboard: pluginDashboardReducer` to the store.

## Technical Debt
- `WorkspaceModal.test.tsx` uses a partial mock store; the test was already broken before this iteration and now references one more missing field (`pluginDashboard`). The test mock should be updated to use a full store factory or `Partial<RootState>`.

## What Should Be Done Next
- Iteration 8: Plugin Iframe Host + postMessage Bridge — inject hidden iframes per active plugin in BoardView, implement `usePluginBridge` context, validate message origins.
