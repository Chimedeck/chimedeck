# Changelog — 20260319_171803

## Update

- Migrated `src/extensions/TableView` to use per-feature translations. All hardcoded English UI strings in `TableView.tsx`, `TableHeader.tsx`, and `TableRow.tsx` are now sourced from `translations/en.json` via bracket notation.

## New

- `src/extensions/TableView/translations/en.json` — 15 translation keys covering: column headers (Title, List, Labels, Members, Due Date, Start Date, Value), empty state ("No cards in this board"), sort aria-labels, filter placeholder, Export CSV button label, and open-card aria-label prefix.
- `specs/tests/sprint-93-tableview-i18n.md` — Playwright MCP test plan verifying all TableView UI copy renders from translations, including column headers, empty state, card title aria-labels, and sort interactions.

## Technical Debt

- `TableView.filterPlaceholder` and `TableView.exportCsvButton` keys are defined in `en.json` but the filter input and CSV export button are not yet implemented in the UI. Keys are reserved for when those features are added.

## What Should Be Done Next

- Implement the filter input and Export CSV button in `TableView.tsx` using the reserved translation keys.
- Proceed to Sprint 94 i18n: Mention + Notifications (Iteration 5).
