# Changelog — 20260316_092942

## Update
- `src/extensions/Card/components/CardModal.tsx` — removed the fixed right sidebar (Members, Labels, Start Date, Due Date, Value, Actions sections). Replaced it with a toggleable Activity column (`activityVisible` state, default `true`). Added `CardModalBottomBar` at the bottom of the modal. Moved plugin detail badges and CardValue into an inline row in the main column.

## New
- `src/extensions/Card/components/CardModalBottomBar.tsx` — new sticky bottom action bar for the card detail modal. Left side: Power-ups popover (renders `CardPluginButtons` with `variant="sidebar"`), Automations popover (renders `CardButtonsSection`), and Actions dropdown (Archive/Unarchive, Copy link, Delete in red). Right side: Activity toggle button that shows/hides the right activity column. All three left popovers close on outside click and Escape key.
- `specs/tests/card-modal-bottom-bar.md` — Playwright test spec for the bottom bar: activity toggle on/off, Actions dropdown options, Escape-to-close, outside-click dismissal.

## Technical Debt
- `CardActionMenu.tsx` remains in the codebase but is no longer imported. It can be deleted in a future cleanup iteration once confirmed no other consumer uses it.
- `CardSidebarSection.tsx` remains in the codebase but is no longer used by `CardModal.tsx`. Can be deleted in iteration 7 after final sidebar cleanup.

## What Should Be Done Next
- Iteration 6: Major CardModal restructure — integrate `ResizablePanels` (left content / right activity), wire up activity panel toggle from `CardModalBottomBar`, confirm no residual sidebar references.
- Iteration 7: Delete `CardActionMenu.tsx` and `CardSidebarSection.tsx` after verifying no remaining consumers. Add value/plugin badges to the left column (already partially done in this iteration).
