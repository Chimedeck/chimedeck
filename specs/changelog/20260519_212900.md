## Update
- Updated `PUT /api/v1/boards/:boardId/state-transitions` to always sync persisted graph data with active board lists before save (strip deleted nodes/edges and refresh renamed node labels).
- Added strict request validation for stale node labels in submitted graphs, returning `422 state-transition-graph-out-of-sync` with mismatch details.

## New
- Added validator helper `findOutOfSyncNodeLabel(...)` to detect graph/list label drift.
- Added tests for out-of-sync label detection and the new PUT validation response.

## Technical Debt
- Repository-wide lint/typecheck baselines are still failing due unrelated pre-existing issues outside State Transitions scope.

## What Should Be Done Next
- Continue Iteration 4 (copy endpoint refinements) and subsequent enforcement/UI iterations as planned.
