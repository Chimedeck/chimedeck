## Update
- None (no changes to existing files; all additions are new).

## New
- Added `db/migrations/0003_workspace.ts`: Knex migration for `workspaces`, `memberships`, and `invites` tables with correct schema, foreign keys, and role enum.
- Added `server/middlewares/permissionManager.ts`: RBAC middleware with `Role` type, role comparison helpers, `requireWorkspaceMembership`, and `requireRole` enforcing role hierarchy and error conventions.
- Added `server/middlewares/permissionManager.test.ts`: Unit tests for role comparison and RBAC middleware, covering all hierarchy and error edge cases (all tests passing).

## Technical Debt
- None.

## What Should Be Done Next
- Integrate RBAC middleware into relevant API endpoints.
- Implement workspace-related endpoints and business logic.
- Add Playwright "mcp-evaluator" project to Playwright config and run end-to-end tests for workspace and RBAC flows.
- Complete advanced feature flag/remote provider logic and Redis integration as noted in previous technical debt.
