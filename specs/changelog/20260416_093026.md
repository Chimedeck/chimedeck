## Update
None

## New
- Added `db/migrations/0108_attachment_dimensions.ts` migration to introduce nullable `width` and `height` integer columns to the `attachments` table. Migration is fully reversible and does not affect existing data.

## Technical Debt
None

## What Should Be Done Next
- Apply and verify the migration in all environments.
- Update attachment creation and update logic to populate `width` and `height` where possible.
- Extend API and UI to display or utilize attachment dimensions as needed in future iterations.