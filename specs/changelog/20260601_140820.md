## Update
- Updated `server/config/env.ts` to add Stripe keys and `SUBSCRIPTIONS_ENABLED` flag; fixed duplicate `WEBHOOKS_ENABLED` key.
- Extended `server/extensions/subscription/common/types.ts`, `subscriptionRepo.ts`, and `serializer.ts` to support new API flows and contracts.
- Updated `server/index.ts` to mount the new subscription API router.

## New
- Implemented workspace-scoped subscription API endpoints:
  - `server/extensions/subscription/api/createCheckout.ts` (POST /api/subscription/checkout)
  - `server/extensions/subscription/api/createPortal.ts` (POST /api/subscription/portal)
  - `server/extensions/subscription/api/getWorkspaceSubscription.ts` (GET /api/subscription)
  - `server/extensions/subscription/api/index.ts` (router wiring)
- Added `workspaceResolver.ts` for centralized workspace lookup and OWNER/ADMIN auth.
- Added integration tests: `tests/integration/subscriptionApi.test.ts` (access matrix, flag-off, error handling).

## Technical Debt
None

## What Should Be Done Next
- Implement Stripe webhook sync, idempotency, and tier update logic.
- Add more robust Stripe error mocking and test coverage for edge cases.
- Review and extend workspace context fallback logic if needed.
- Document API usage and error contracts for frontend integration.
