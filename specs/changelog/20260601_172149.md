## Update
- Wired conditional feature-gate middleware into the server request pipeline.
- Added normalized request workspace resolution and subscription entitlement lookup for gate checks.
- Extended subscription tier config with feature flags used by plan enforcement.
- Added tests for gate mapping and middleware allow/block behavior.

## New
- New `server/config/feature-gates.ts` route-to-feature map.
- New `server/common/requestContext.ts` helper for resolving workspace context from requests.
- New `server/middlewares/featureGate.ts` middleware returning `402 feature-not-in-plan` when access is denied.
- New test coverage in `server/config/feature-gates.test.ts` and `server/middlewares/featureGate.test.ts`.

## Technical Debt
- No upstream `sample-project/` reference was available in this workspace for comparison.
- The middleware still depends on subscription rows being maintained correctly for each workspace.

## What Should Be Done Next
- Add resource limit guards for plan-restricted actions.
- Add workspace-shared rate limiting.
- Build the billing UI and upgrade modal flow for blocked feature access.
