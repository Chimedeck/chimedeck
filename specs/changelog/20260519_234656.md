## Update
- Updated `server/extensions/stateTransitions/api/put.ts` to reconcile persisted transition graphs with current board lists before save, so stale list deletions and renames are auto-synced when PUT updates only `enabled` (or omits `graph`).

## New
- Added `specs/tests/stateTransitions-rules-endpoint.yml` defining rules-endpoint verification scenarios (empty graph, complex derivation, stale rename/delete sync, feature flag off).
- Added `specs/tests/stateTransitions-management-rules.yml` defining management verification scenarios (PUT websocket broadcast, list rename sync, list delete sync, feature flag off).

## Technical Debt
- PUT still treats caller-provided `graph` as authoritative for list-label drift and returns out-of-sync errors when client payload labels do not match current list titles.

## What Should Be Done Next
- In the next iteration, complete the `/copy` endpoint and enforcement integration from Sprint 154+ while preserving list-sync guarantees introduced here.
