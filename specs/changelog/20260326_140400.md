## Update
- Added semantic color aliases to `tailwind.config.ts` for brand, surface, border, semantic state, and text colors using CSS custom properties.
- Refactored `src/common/components/Button.tsx` to use the new `buttonVariants` from theme config, removing the `cva` dependency and centralizing styling.

## New
- Created `src/config/theme.ts` as the single source of truth for button variants and sizes, exporting `buttonVariants`, `ButtonVariant`, and `ButtonSize`.
- Added Playwright MCP test spec `specs/tests/sprint-118-button-variants.md` covering all button variants and color changes in dark/light mode.

## Technical Debt
- Playwright MCP test spec was not recognized as a valid test file by Playwright; needs conversion to `.ts` or `.js` format.
- Pre-existing type errors in unrelated files remain unresolved.

## What Should Be Done Next
- Convert the Playwright test spec to a valid `.ts`/`.js` file and rerun tests.
- Investigate and resolve any baseline type errors.
- Continue with the next planned iteration.