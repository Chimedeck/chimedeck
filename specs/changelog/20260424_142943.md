# Update
- Fixed card member assignment live-sync so CardItem member avatars update without page refresh.
- Realtime `card_activity_created` handling now reacts to `card_member_assigned` / `card_member_unassigned` by refreshing member state for the affected card.
- Card modal member assign/remove flows now update board/card slices optimistically and rollback on API failure.

# New
- Added `cardDetailSliceActions.remoteSyncMembers` to synchronize modal member state from realtime-driven server refreshes.

# Technical Debt
- `CardModal/index.tsx` and `cardDetailSlice.ts` still contain pre-existing lint debt unrelated to this fix.

# What Should Be Done Next
- Add an integration/e2e scenario where User A assigns/removes a member and User B sees CardItem member avatars update live (no refresh).
- Consider a dedicated lightweight endpoint (or websocket payload enrichment) for card-member snapshots to avoid full card-detail fetches on member activity events.
