## Update
- Updated `TimelineRow.tsx` to render `TimelineBar` for each scheduled card and call `useTimelineDrag`.
- Updated `TimelineView.tsx` to pass `addToast` to `TimelineRow`.
- Extended `types.ts` with TimelineBar and drag/resize types, and added `addToast` to `TimelineRowProps`.

## New
- Added `TimelineBar.tsx`: renders colored bars for cards with start/due dates, with drag/resize handles and TODO for dependency arrows.
- Added `useTimelineDrag.ts`: manages drag/resize logic, optimistic UI, PATCH, and error revert/toast.
- Added E2E tests: `timeline-bar.spec.ts` (bar rendering) and `timeline-drag.spec.ts` (drag/resize, PATCH, error handling).

## Technical Debt
- E2E tests for TimelineView fail at login (dev server runs, but login page/email input not found).
- TimelineView: dependency arrows not implemented (TODO in TimelineBar.tsx).
- Deferred: E2E infra instability, CalendarView API/TS bugs, TimelineView performance.

## What Should Be Done Next
- Investigate/fix login page or E2E infra so tests can run UI flows.
- Implement dependency arrows in TimelineBar.
- Address deferred TimelineView features and E2E issues from previous sprints.
- Review and improve error handling, accessibility, and performance for TimelineView drag/resize.