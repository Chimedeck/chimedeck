# Changelog — 20260302_000000

## Update

- Added `specs/architecture/technical-decisions.md` — captures all architecture choices that were open in the existing requirements (database, event store, WebSocket runtime, Redis pub/sub, auth token design, fractional indexing algorithm, file storage, API URL structure, state management, testing strategy, infrastructure, observability, CSRF, rate limiting).

## New

- `specs/sprints/sprint-plan.md` — master sprint overview table, domain model coverage map, and final acceptance checklist.
- `specs/sprints/sprint-01.md` — Project setup: Docker Compose, PostgreSQL, Redis, MinIO, Bun server skeleton, CI workflow.
- `specs/sprints/sprint-02.md` — Authentication: email/password, JWT RS256, refresh token rotation, OAuth Google + GitHub.
- `specs/sprints/sprint-03.md` — Workspace lifecycle + RBAC: create workspace, invite/accept flow, role enforcement on every route.
- `specs/sprints/sprint-04.md` — Board lifecycle: create/rename/archive/delete/duplicate, board load response with `includes`.
- `specs/sprints/sprint-05.md` — List management: CRUD, fractional lexicographic reorder, collision resolution.
- `specs/sprints/sprint-06.md` — Card core: CRUD, move between lists, duplicate, archived state.
- `specs/sprints/sprint-07.md` — Card extended fields: workspace-scoped labels, member assignment, checklist items.
- `specs/sprints/sprint-08.md` — Real-time infrastructure: Event store (Postgres), BoardSnapshot, Bun WebSocket server, Redis pub/sub fan-out, HTTP polling fallback, presence TTL.
- `specs/sprints/sprint-09.md` — Real-time collaboration (client): Redux optimistic update pattern, WS event reconciliation, conflict toast, offline mutation queue.
- `specs/sprints/sprint-10.md` — Comments (editable, versioned, soft-delete) and immutable Activity log with cursor-paginated feed.
- `specs/sprints/sprint-11.md` — Attachments: pre-signed S3 PUT upload flow, external URL, virus scanning worker, orphan cleanup, signed read URLs.
- `specs/sprints/sprint-12.md` — Search (PostgreSQL tsvector/GIN) and presence indicators (Redis TTL + WS `presence_update` events).
- `specs/sprints/sprint-13.md` — Observability (OTEL traces + metrics), rate limiting (Redis sliding window), security hardening (CSP, CSRF, input sanitization, audit), virtualization, mobile responsive, load tests.

## Technical Debt

- Activity backfill (stubs written in sprints 02–07) must be replaced in sprint 10 — tracked explicitly in sprint-10 scope.
- `board_snapshots` snapshot policy (N=50) is a hardcoded default; should be configurable via `server/config/env.ts`.

## What Should Be Done Next

- Begin sprint 01 implementation following [sprint-01.md](../sprints/sprint-01.md).
- Review Redis key namespaces in `technical-decisions.md §5` and lock down TTL values in `server/config/env.ts` before sprint 02.
- Confirm S3 provider (AWS S3 vs Cloudflare R2) before sprint 11 to decide on SDK choice (`@aws-sdk/client-s3` is compatible with both).
