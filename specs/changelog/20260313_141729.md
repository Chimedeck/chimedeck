## Update
- Refactored `createNotificationsForMentions` in `server/extensions/notifications/mods/createNotifications.ts` to check `preferenceGuard` per recipient before inserting notification rows or publishing WebSocket events.
- If `NOTIFICATION_PREFERENCES_ENABLED` is false, guard is skipped and all recipients are notified (backward compatibility).
- If `preferenceGuard` throws (e.g., DB error), notification is delivered (fail open, opt-out model).

## New
- Added `specs/tests/notification-preferences-mention-guard.md`: Playwright MCP spec with 5 scenarios covering mention notification delivery based on in-app preference, feature flag, self-mention, and DB error cases.

## Technical Debt
- Playwright test spec is in Markdown and not recognized as a valid test by Playwright; needs conversion to `.ts` or `.js`.
- No UI or end-to-end tests for managing notification preferences yet.

## What Should Be Done Next
- Convert the Markdown test spec to a valid Playwright test script and re-run tests.
- Build UI for managing notification preferences.
- Wire preference checks into all other notification types (not just mentions).
- Add SES/email logic and ensure end-to-end test coverage for notification preferences.