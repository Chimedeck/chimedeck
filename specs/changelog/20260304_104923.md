## Update
- Added `MonetizationType` type and `monetization_type` field to the client `Board` type in `api.ts`.
- Updated `BoardHeader` to support opening a settings panel.
- Updated `BoardPage` to render a slide-in BoardSettings panel, handle monetization save with optimistic update and rollback, and pass `monetizationType` to `BoardCanvas`.
- Updated `BoardCanvas` and `ListColumn` to thread `monetizationType` and `listName` props to `CardItem`.
- Updated `CardItem` to accept and pass through `monetizationType` and `listName` props.
- Added `optimisticUpdateBoardMonetization` reducer action in `boardSlice.ts`.

## New
- Created `src/extensions/Board/config/payToPaidConfig.ts` exporting a predicate for qualifying columns for pay-to-paid payment buttons.
- Created `src/extensions/Board/containers/BoardSettings/BoardSettings.tsx` with a Monetization radio group (None / Pre-paid / Pay to Paid), Save button, error display, and RBAC-aware disabled state.

## Technical Debt
- Playwright/e2e tests for Board monetization UI and PATCH/GET endpoints are missing; no Playwright config or test suite is present.
- RBAC and event sourcing for monetization changes are not fully reviewed or tested.
- Automated API verification for monetization_type is deferred.

## What Should Be Done Next
- Set up Playwright and write e2e tests for BoardSettings and monetization flows.
- Review and implement RBAC and event sourcing for board monetization changes.
- Monitor for client/server contract drift as backend evolves.
- Implement Stripe payment flows and UI in the next iteration.
