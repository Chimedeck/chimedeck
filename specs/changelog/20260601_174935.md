## Update
- Added centralized subscription limit enforcement for workspace, board, and list creation.
- Wired the new guard into workspace, board, and list create handlers.
- Extended shared usage aggregation to expose the counts required by the new guard.

## New
- Added `server/middlewares/limitGuard.ts` for reusable quota checks and standard `402 limit-reached` responses.
- Added tests for the limit guard and create flows under workspace, board, and list APIs.
- Verified the limit flows in the browser against live backend responses.

## Technical Debt
- Usage aggregation is still workspace-wide and direct-count based.
- Limit enforcement depends on subscription rows and tier IDs staying aligned with the canonical tier map.

## What Should Be Done Next
- Add invited-member, guest, and storage limit enforcement.
- Reuse the shared limit guard for any remaining gated create/update flows.
- Expand validation coverage for additional plan-gating scenarios.
