# Changelog — 2026-03-05T09:02:15Z

## Update
- Mounted the new plugin API router in `server/index.ts` under `/api/v1/plugins`.
- Added a static route in `server/index.ts` to serve the SDK bundle at `/sdk/jh-instance.js` if present.

## New
- Created `server/extensions/plugins/config/index.ts` to centralize plugin system environment variables and settings.
- Added `server/extensions/plugins/middlewares/board-admin-guard.ts` middleware to enforce board admin checks, returning 403 `{ name: 'not-board-admin' }` on failure.
- Added `server/extensions/plugins/api/index.ts` as the main plugin API router skeleton, with stubs for all major plugin route groups.

## Technical Debt
- No automated Playwright or e2e tests exist for the new plugin API or SDK routes; coverage for these flows is missing and must be added in a future iteration.
- Board admin guard logic assumes workspace membership/role structure is stable; may require updates if user/board schema changes.

## What Should Be Done Next
- Implement all plugin API endpoints and wire up sub-routers in `server/extensions/plugins/api/`.
- Build and serve the SDK bundle at `/sdk/jh-instance.js` with full TrelloPowerUp API compatibility.
- Add Playwright/e2e tests for plugin API, board-admin-guard, and SDK routes.
- Complete client-side integration: Plugin Dashboard UI, iframe host, Redux bridge, and UI injection points.
- Review and update board admin guard logic if workspace/user schema changes.