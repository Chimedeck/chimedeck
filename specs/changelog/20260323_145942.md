## Update
- Updated `package.json` to add `@modelcontextprotocol/sdk@1.27.1` as a dependency via `bun add`.

## New
- Created `server/extensions/mcp/config.ts`: Reads `HORIFLOW_TOKEN` and `HORIFLOW_API_URL` from environment; exits with a clear, actionable error if `HORIFLOW_TOKEN` is missing.
- Created `server/extensions/mcp/apiClient.ts`: Provides a generic `apiCall<T>()` fetch wrapper that injects the `Authorization: Bearer` header and normalises error responses.
- Created `server/extensions/mcp/index.ts`: Entry point for the MCP server using `McpServer` and `StdioServerTransport` (Bun runtime, no tools registered yet).

## Technical Debt
None

## What Should Be Done Next
- Implement and register the first MCP server tool modules (e.g., notification, user, or project tools).
- Add integration and E2E tests for MCP server endpoints once tools are available.
- Monitor and address the recurring PostgreSQL connection pool instability affecting test reliability.
- Expand config and API client for future extensibility as new features are added.
