## Update
Fixed the `SpecsWorkspacePage` load flow so switching back to a cached specs file exits the loading state immediately without clobbering unsaved editor content. Added regression coverage for the cache-switch path and kept the stale-save/reload flow covered.

## New
Added browser-backed verification for the cached-file switch behavior in the specs workspace.

## Technical Debt
The live docs/specs backend can still return `502` for manifest-backed boards, so the docs tab remains dependent on a healthy backend setup.

## What Should Be Done Next
Resume the deferred docs save/commit work: ETag delta-save preconditions, commit metadata/footer handling, client save orchestration, and stale-precondition recovery.
