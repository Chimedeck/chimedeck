# Changelog - 2026-06-03 13:00:00

## Update

- Updated board chat design documentation to include the detailed right sidebar UI structure from the latest reference mock.
- Expanded chat-related sprint specs to align implementation with concrete UI zones, controls, and interaction states.

## New

- Added `Board Chat Sidebar UI Specification (Reference)` section to `DESIGN.md` under feature expansion track.
- Added sidebar layout parity requirements to `specs/sprints/sprint-164.md` (sticky header/composer, timeline structure, responsive drawer behavior).
- Added guest-access visual and lock-state requirements to `specs/sprints/sprint-165.md`.
- Added AI assist suggestion card and explicit create-card CTA/dismiss flow details to `specs/sprints/sprint-167.md`.

## Technical Debt

- The reference UI screenshot is not yet stored as a versioned design asset in the repository; current documentation uses textual spec only.
- Exact icon set mapping and spacing token values for the sidebar are not yet codified in a dedicated component spec file.

## What Should Be Done Next

- Add a repository-stored image asset for the board chat sidebar mock and link it from docs.
- Create a component-level spec for `BoardChatDrawer`, `GuestAccessControls`, and `AiSuggestionCard` with props/state contracts.
- Add E2E scenarios for guest locked state, guest view/use toggles, and AI suggestion -> create-card confirmation flow.
