# Changelog — 2026-04-15T03:42:59Z

## Update
- Added `tests/unit/webhooks/SignatureVerificationSnippet.test.tsx` — the only missing test file from Iteration 9's plan.

## New
- **SignatureVerificationSnippet tests** (35 new tests):
  - Expand/collapse state machine: starts collapsed, toggles correctly, handles rapid toggling
  - Copy-to-clipboard state machine: sets copied immediately, resets after timeout, passes snippet text
  - Translation key coverage: verifies all 10 required `SignatureSnippet.*` keys are present and non-empty; expand/collapse and copy/copied labels are distinct
  - Code block content: verifies snippet references HMAC-SHA256, `Webhook-Signature` header, `timingSafeEqual`, `t` and `v0` extraction, signed payload format, replay protection via `toleranceSeconds=300`, raw body note, and 401 rejection

## Technical Debt
- None introduced.

## What Should Be Done Next
- All 9 iterations of Sprint 135 + 136 are complete. Total test suite: 98 unit + 49 integration = 147 tests, all passing.
