# Update
- Grouped consecutive notification rows for the same card when the events are discussion-related (`mention` and `card_commented`) into a single stacked card block.
- Preserved newest-first ordering inside each stack so the latest comment/mention appears at the top.

# New
- Added contiguous grouping logic in the notification panel that merges only adjacent, same-card discussion notifications.
- Extended notification item rendering to support multi-entry stack mode with per-entry click, delete, preview, and reaction controls.

# Technical Debt
- Stacking is currently a purely visual aggregation in the client and does not include a server-side grouped payload shape.
- Notification rows with different activity types (e.g. reactions, assignments) still render independently by design.

# What Should Be Done Next
- Add an integration test to assert stack grouping boundaries: same card + adjacent + mention/comment only.
- Add a UI test to verify stack interactions (mark read, delete, react) still target the specific stacked entry.
