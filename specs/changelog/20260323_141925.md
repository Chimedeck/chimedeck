## Update
- Integrated `BoardNotificationTypePreferences` into the "User settings" section of `BoardSettings.tsx`.
- Verified all supporting infrastructure (RTK slice, translations, store registration) was present and functional.
- Confirmed server endpoints and reducers for notification preferences are correctly registered.

## New
- No new features or files added; only UI integration of an existing component.

## Technical Debt
None.

## What Should Be Done Next
- Update Playwright config to include `tests/e2e/` so all E2E tests are discovered and run.
- Complete any deferred client-side polish, additional translations, or integration tests for notification preferences.
- Implement and wire new email templates for `cardUpdated`, `cardDeleted`, and `cardArchived` if not already done.
