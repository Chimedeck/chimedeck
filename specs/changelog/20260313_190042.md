## Update
- Added `GUEST` to `Role` type and `callerRole` to `Workspace` interface in `src/extensions/Workspace/api.ts`.
- Updated `Sidebar.tsx` to hide Members link and add guest badge for GUEST users.
- Updated `WorkspacePage.tsx` to show Access Restricted panel for GUEST users.
- Updated `routes.ts` to add `/workspaces/:workspaceId/boards` route for WorkspaceDashboard.

## New
- Created `src/extensions/Workspace/slices/workspaceSlice.ts` with selectors for user workspace role.
- Created `src/extensions/Workspace/containers/WorkspaceDashboard/index.tsx` for guest-aware board listing.
- Added Playwright MCP test spec: `specs/tests/sprint-80-iteration-8-guest-workspace-view.md`.
- Added changelog: `specs/changelog/20260313_195000.md`.

## Technical Debt
- Playwright test spec is not runnable; Playwright did not detect any tests. Needs conversion or config update.
- Some unrelated TypeScript errors remain (pre-existing).

## What Should Be Done Next
- Fix Playwright test spec so it is runnable and recognized by Playwright.
- Implement guest client permission guards and auth-aware route guards (Sprint 80, Iteration 9).
- Address any remaining TypeScript errors.
- Continue guest invite flow and guest-specific UI improvements.