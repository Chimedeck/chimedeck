## Update
- Fixed link edit behavior so display text is no longer stripped in the first link display mode (URL mode) when users provide custom text.
- Applied the same behavior to both card description rich editor and comment editor for consistency.

## New
- URL mode now preserves user-entered display text and only falls back to raw URL text when no label is provided.
- Link edit popover now keeps existing link text prefilled instead of clearing it when the current mode is URL.

## Technical Debt
- Link mode behavior is duplicated between description and comment editors; consider extracting shared link mutation helpers to avoid drift.

## What Should Be Done Next
- Add focused UI tests for link mode editing:
  - URL mode + custom display text persists after save
  - URL mode without display text falls back to raw URL
  - Switching between URL/button/card retains expected label behavior
