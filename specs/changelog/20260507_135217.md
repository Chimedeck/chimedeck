## Update
- Fixed comment reaction notification fan-out resilience in `createReactionNotification` so duplicate-key insert collisions no longer silently drop notifications.
- Added conflict recovery logic that refreshes an existing `comment_reaction` notification row (actor, emoji, timestamps, unread state) when a dedupe constraint blocks insertion.

## New
- No new feature surface; behavior now reliably re-surfaces existing comment reaction notifications instead of failing silently.

## Technical Debt
- Notification insert/update conflict handling is still localized in one module; a shared helper for idempotent notification writes could reduce repeated DB error handling patterns.

## What Should Be Done Next
- Add a server test that simulates duplicate-key collisions for `comment_reaction` notifications and asserts the row is updated to `read=false` with refreshed `created_at`.
- Verify production DB uniqueness constraints for `notifications` to confirm the intended dedupe model and document it in migration comments.
