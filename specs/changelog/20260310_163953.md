## Update
- Extended `customFieldsRouter` in `server/extensions/customFields/api/index.ts` to register card-scoped value routes.
- Updated `GET /api/v1/cards/:id` handler in `server/extensions/card/api/get.ts` to always include `customFieldValues` under the `includes` key.

## New
- Implemented `server/extensions/customFields/api/cardValues.ts` with GET/PUT/DELETE endpoints for card-scoped custom field values, supporting all field types and type validation.
- Added Playwright MCP e2e test file `tests/e2e/custom-field-values-api.md` covering upsert, retrieve, delete, includes, board mismatch, type errors, and auth rejection scenarios.

## Technical Debt
- Playwright does not recognize `.md` test files; e2e tests are not executed until converted to a supported format.
- Some business logic invariants are only client-enforced; server-side enforcement is pending.

## What Should Be Done Next
- Convert `tests/e2e/custom-field-values-api.md` to a supported Playwright test format (e.g., `.ts`) and run tests.
- Implement server-side enforcement for all business logic invariants.
- Address edge cases: field definition deletion, permission checks, and error handling for malformed IDs.
- Begin client UI for custom fields and further security features.