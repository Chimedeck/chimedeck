## Update
- Updated `server/extensions/plugins/api/index.ts` to mount new board plugin management endpoints (list, enable, disable) with correct method/path routing.
- Ensured no new TypeScript errors in plugin-related files; all pre-existing errors are unrelated to this iteration.

## New
- Added `server/extensions/plugins/api/board-plugins/list.ts`: Implements `GET /api/v1/boards/:boardId/plugins` to return active plugins for a board with full plugin metadata.
- Added `server/extensions/plugins/api/board-plugins/enable.ts`: Implements `POST /api/v1/boards/:boardId/plugins` to enable a plugin for a board (admin only), with error handling for already-enabled and not-active plugins.
- Added `server/extensions/plugins/api/board-plugins/disable.ts`: Implements `DELETE /api/v1/boards/:boardId/plugins/:pluginId` to soft-disable a plugin (sets `disabled_at`).

## Technical Debt
- Plugin API endpoints are not covered by e2e or Playwright MCP tests due to missing `mcp-evaluator` project in Playwright config.
- No client-side integration, dashboard UI, or SDK implementation yet.
- Advanced plugin authentication and plugin data storage APIs are deferred.

## What Should Be Done Next
- Add the `mcp-evaluator` project to Playwright config and implement e2e tests for plugin API flows.
- Implement plugin API endpoint logic for registry CRUD and plugin data storage.
- Build the plugin SDK bundle and static route.
- Develop the admin dashboard UI and client-side plugin integration (iframe host, Redux, UI injection).