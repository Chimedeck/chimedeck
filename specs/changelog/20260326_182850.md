## Update
- Migrated all remaining hardcoded Tailwind color classes (e.g., bg-gray-*, text-gray-*, text-white, text-red-*, text-green-*) to semantic token classes across Board, Card, Label, List, Automation, Attachments, Plugins, Workspace, CustomFields, and UserProfile components.
- Fixed critical invisible text bug in MemberAvatarPopover (text-white on bg-white) by switching to theme token.
- Updated all Auth containers to use theme-aware background classes (bg-bg-base/bg-bg-surface) instead of hardcoded bg-slate-950/900.
- Replaced all error/success text color classes with semantic tokens (text-danger, text-success).
- Added inline // [theme-exception] tags to all legitimate text-white usages on colored backgrounds for grep-verifiable exceptions.
- Eliminated all non-exception hardcoded bg-slate-*/bg-gray-* usages.

## New
- Created `src/common/tokens/index.ts` to centralize exported class-string constants for inputs, panels, badges, and error boxes.
- Expanded `specs/tests/sprint-120-contrast-audit.md` to cover 18 test suites for all major screens in both light and dark themes.

## Technical Debt
- Some pre-existing TypeScript errors remain on the server side, unrelated to this sprint's changes.
- Playwright test for contrast audit remains in markdown format; not yet automated as a .spec.ts file.
- Design decision deferred for mapping certain light-mode surface colors to new semantic tokens.

## What Should Be Done Next
- Convert `specs/tests/sprint-120-contrast-audit.md` to an automated Playwright `.spec.ts` test for CI compatibility.
- Review and finalize design decisions for light-mode surface token mappings.
- Manually walkthrough all screens in both themes to catch any missed contrast issues.
- Audit any new or deferred components/screens for hardcoded color classes in future sprints.