# Changelog — 2026-03-10 15:03

## Update
- `src/extensions/Board/slices/boardSlice.ts` — added `optimisticUpdateCardField` reducer action for optimistic card field updates (used by CalendarView drag-to-reschedule)
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx` — registered CalendarView component for the `CALENDAR` view type
- `server/extensions/board/api/create.ts` — fixed pre-existing syntax error (unescaped single quotes in error string)
- `server/extensions/board/api/patch.ts` — fixed pre-existing syntax error (unescaped single quotes in error string)

## New
- `src/extensions/CalendarView/types.ts` — type definitions: `CalendarMode`, `MAX_CHIPS_PER_DAY = 3`, component prop interfaces
- `src/extensions/CalendarView/CalendarCardChip.tsx` — draggable compact card chip (title + label colour dot, HTML5 drag API)
- `src/extensions/CalendarView/CalendarDayCell.tsx` — day cell with drag-over/drop target and `+N more` overflow expand/collapse
- `src/extensions/CalendarView/CalendarMonthGrid.tsx` — 6-week monthly grid (always 42 cells starting on Sunday) with prev/next navigation
- `src/extensions/CalendarView/CalendarView.tsx` — root component: toolbar note, month mode, optimistic drag-to-reschedule via `PATCH /api/v1/cards/:id`
- `tests/e2e/calendar-view.spec.ts` — 5 Playwright E2E tests: grid renders, card placement, no-due-date note, prev/next nav, +N more overflow

## Technical Debt
- Drag-to-reschedule uses the HTML5 drag API; a migration to dnd-kit (used in the Kanban board) would improve consistency
- CalendarView currently only supports Month mode; Week view is planned for Iteration 5

## What Should Be Done Next
- Iteration 5: `CalendarWeekGrid.tsx` (7-column week view, prev/next week) and `useCalendarDrag.ts` (refined drag-to-reschedule with optimistic update + revert on failure)
