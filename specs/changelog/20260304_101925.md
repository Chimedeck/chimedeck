## Update
- Added `amount` and `currency` fields to the `Card` interface in `src/extensions/Card/api.ts`.
- Updated `patchCard` in `src/extensions/Card/api/cardDetail.ts` to support updating `amount` and `currency`.
- Modified `applyOptimisticCardUpdate` in `src/extensions/Card/slices/cardDetailSlice.ts` to handle `amount` and `currency` fields.
- Enhanced `CardItem.tsx` to display a money badge when a card has a value set.
- Updated `CardModal.tsx` and its container to support editing and saving card value.

## New
- Created `CardMoneyBadge.tsx` for displaying formatted card value badges.
- Created `CardValue.tsx` sidebar section for editing card value and currency with validation and save action.

## Technical Debt
- None

## What Should Be Done Next
- Free up port 3000 to allow the dev server to start.
- Add a Playwright test script to `package.json` for automated UI testing of the card value feature.
- Implement and verify server-side filtering and sideloading of activities per the Sprint 29 plan.
- Create missing activity config and API files as outlined in the implementation plan.
