## Update
- Registered attachment API router in `server/index.ts`.
- Updated `server/config/env.ts` to include S3 and virus scan config.

## New
- Migration `db/migrations/0011_attachments.ts`: creates `attachments` table with FILE/URL type, status, S3 fields, and cascade delete.
- S3 modules: `common/config/s3.ts`, `presignPut.ts`, `presignGet.ts`, `headObject.ts`, `deleteObject.ts`.
- Virus scan modules: `enqueue.ts`, `worker.ts`, `update.ts` (flag-controlled, event-driven).
- Orphan cleanup worker: `orphanCleanup.ts` (removes abandoned uploads and S3 objects).
- API endpoints: request upload URL, confirm upload, add external URL (with SSRF guard), get signed URL, delete attachment (with S3 cleanup).
- Frontend: `AttachmentSection`, `AttachmentItem`, `AttachmentUploader`, `AttachmentUrlModal`, `AttachmentStatusBadge`, and `useAttachmentUpload` hook.
- Tests: SSRF validator (IPv4/IPv6), orphan cleanup TTL, upload flow, external URL, and delete cascade.

## Technical Debt
- None.

## What Should Be Done Next
- Integrate Playwright MCP evaluator for UI flow tests (ensure project is configured).
- Monitor S3/virus scan error handling in production for edge cases.
- Consider distributed/scalable virus scan queue in future sprints if needed.
- Backfill activity log for attachment mutations if required by audit/compliance.