# Changelog — 2026-03-23T05:00:00Z

## Update
- `src/extensions/Board/translations/en.json`: Added 6 new translation keys for the board notification type preferences matrix and reset actions.
- `src/reducers.ts`: Exported `boardNotificationTypePreferencesApi` from the new slice.
- `src/store/index.ts`: Registered `boardNotificationTypePreferencesApi` reducer and middleware.

## New
- `src/extensions/Notifications/NotificationPreferences/boardNotificationTypePreferences.slice.ts`: RTK Query API slice exposing `getBoardTypePreferences` (GET), `updateBoardTypePreference` (PATCH), and `resetBoardTypePreferences` (DELETE) endpoints for the board-level per-type override table.
- `src/extensions/Board/containers/BoardSettings/BoardNotificationTypePreferences.tsx`: Two-column (In-App, Email) toggle matrix component for all 9 notification types. Board-level overrides (`source='board'`) show an indigo ring. Email column hidden when `SES_ENABLED`/`EMAIL_NOTIFICATIONS_ENABLED` is false. "Reset to defaults" button removes all board-level overrides with a confirmation step.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 7: Mount `BoardNotificationTypePreferences` inside `BoardSettings.tsx` under the User settings section, below `BoardNotificationToggle`. Verify full happy-path end-to-end via Playwright.
