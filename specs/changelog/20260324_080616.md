# Changelog — 20260324_080616

## Update
- `PluginDashboardPage.tsx` replaced single `PluginList` layout with two-section layout: "Enabled on this board" and "Discover Plugins", using new `EnabledPluginRow` and `DiscoverPluginRow` components.
- `useBoardPlugins.ts` updated to call `fetchDiscoverablePluginsThunk` (board-specific `/available` endpoint) instead of the global registry for the Discover section.
- `PluginDashboardPage.duck.ts` all `fetchAvailablePluginsThunk` calls in the page replaced with `fetchDiscoverablePluginsThunk` for correct board-scoped discovery.

## New
- `src/extensions/Plugins/api.ts` — added `fetchBoardAvailablePlugins` calling `GET /api/v1/boards/:boardId/plugins/available`
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.duck.ts` — added `fetchDiscoverablePluginsThunk` using the board-specific endpoint
- `src/extensions/Plugins/components/EnabledPluginRow.tsx` — new row component for enabled plugins (icon, name/author, Settings gear, Disable button)
- `src/extensions/Plugins/components/DiscoverPluginRow.tsx` — new row component for discoverable plugins (icon, name, description, capabilities, categories, Enable button)
- `src/extensions/Plugins/components/DiscoverPluginSearch.tsx` — debounced search input + category dropdown for the Discover section
- `src/extensions/Plugins/translations/en.json` — added `plugins.dashboard.enabled.*` and `plugins.dashboard.discover.*` translation keys
- `specs/tests/board-plugin-discovery.md` — Playwright MCP scenario covering all board plugin discovery flows

## Technical Debt
- `PluginList.tsx` and `PluginCard.tsx` are now only used in legacy contexts (if any); consider removing if no other consumers remain.
- The `fetchAvailablePluginsThunk` (global registry) is still in the duck but no longer called from PluginDashboardPage — can be removed in a cleanup pass.

## What Should Be Done Next
- Remove `PluginList` / `PluginCard` if confirmed unused after dashboard overhaul.
- Execute the `board-plugin-discovery.md` Playwright MCP scenario against the running dev server once MCP config is available.
- Add optimistic-enable loading state per row (currently `loading` prop is wired but no per-plugin loading tracking exists in state).
