## Update
- Implemented Sprint 169 iteration 11 backend repository-fetch service scaffolding under `server/extensions/board/mods/githubRepository`.
- Added GitHub App configuration wiring in `server/config/env.ts` and board-scoped config in `server/extensions/board/common/config/githubRepository.ts`.
- Added unit coverage for repository download flow and GitHub App token lifecycle.

## New
- `downloadRepositoryFromProjectUrl({ projectUrl, boardId?, refresh? })` service with URL validation, board/url cache keying, in-flight dedupe, and refresh handling.
- GitHub App auth helpers for app JWT signing, installation lookup, installation token mint/refresh, and repository default branch resolution.
- Server-side `simple-git` wrapper for clone/fetch/checkout operations with authenticated GitHub access.
- New dependency: `simple-git`.

## Technical Debt
- Organization/user-scope GitHub project URLs are intentionally rejected in the download service because they do not uniquely resolve to one repository target yet.
- Repository cache and token cache are process-local; multi-instance cache coordination is deferred.

## What Should Be Done Next
- Iteration 12: add specs manifest/file-read APIs with traversal protection, file-size limits, ETag/If-None-Match, and board-member access checks.
