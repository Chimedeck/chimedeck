# Changelog — 2026-03-23T17:45:00Z

## Update
- Created `src/extensions/DeveloperDocs/containers/McpDocsPage/McpDocsPage.tsx` — static documentation page for the Horiflow MCP server.

## New
- **McpDocsPage**: Full static docs page with sticky left Table of Contents and 6 sections:
  1. **Overview** — explains stdio vs HTTP transports
  2. **Authentication** — token generation and usage per transport
  3. **Connecting** — Claude Desktop and Cursor config, environment variables table
  4. **Endpoint Reference** — HTTP `/api/mcp` session lifecycle, curl examples, error codes
  5. **Available Tools** — summary table of all 9 MCP tools with endpoint mapping
  6. **Tool Details** — per-tool parameter tables for all 9 tools: `move_card`, `write_comment`, `create_card`, `edit_card_description`, `set_card_price`, `invite_to_board`, `search_cards`, `search_board`, `get_card`
- Mirrors the layout, primitives, and style of `PluginDocsPage.tsx` exactly (Section, H2, H3, P, Code, Pre, Table, Badge, NavItem, Divider components).

## Technical Debt
- Route, sidebar NavItem, and translation key are wired in Iteration 4.

## What Should Be Done Next
- Wire `/developer/mcp` route in `src/routing/index.tsx`.
- Add "MCP Docs" NavItem to `src/layout/Sidebar.tsx` and `src/extensions/Workspace/components/Sidebar.tsx`.
- Add `"Sidebar.mcpDocsLabel": "MCP Docs"` to `src/common/translations/en.json`.
- Run Playwright navigation test to verify the page renders with all sections and 9 tools.
