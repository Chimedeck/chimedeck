# Changelog — 2026-03-25T04:47:37Z

## Update

- `AttachmentItem.tsx` — Added `onRename?: (id, alias) => void` prop; inline rename input that replaces the name span when editing; Enter/blur commits, Escape cancels, empty value rejected with red border; Edit (pencil) button hidden for temp upload rows; Cancel button prevents accidental blur-submit.
- `AttachmentItem.tsx` — Fixed: `attachment.url` → `attachment.view_url` for the open/download action and video lightbox source; display name now uses `alias ?? name`.
- `AttachmentPanel.tsx` — Added `handleRename` callback using `patchAttachment` with optimistic alias update and rollback on server error; wired `onRename={handleRename}` to server-persisted `AttachmentItem` rows; fixed temp attachment object to use `view_url: null` and `alias: null` instead of the removed `url` field.
- `translations/en.json` — Added keys: `attachment.edit`, `attachment.item.action.edit.ariaLabel`, `attachment.rename.placeholder`, `attachment.rename.cancel`.

## New

- `tests/e2e/sprint-117-attachment-edit.md` — Playwright MCP markdown tests covering: Edit button visibility, input pre-fill, Escape reverts, Enter saves, blur saves, empty input rejected, Cancel button, no-alias fallback to original name.

## Technical Debt

- `AttachmentThumbnail.tsx` still references removed `url` field — pre-existing from Iteration 2, to be addressed in Iteration 4 (view_url migration sweep).
- `attachmentMarkdown.ts` similarly references `url` — pre-existing, scoped to Iteration 7.

## What Should Be Done Next

- Iteration 4: Add Comment action button to `AttachmentItem`; wire `onInsertComment` through `AttachmentPanel` / `CardModal` to the `CommentEditor`; remove any remaining raw `url` usage in `AttachmentItem`/`AttachmentPanel`.
