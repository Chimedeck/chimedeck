## Update
- Modified `server/index.ts` to mount the `healthCheckExtensionRouter` for REST API exposure, guarded by the `HEALTH_CHECK_ENABLED` flag.

## New
- Added `server/extensions/healthCheck/common/validateUrl.ts`: URL validator for health checks (http/https-only, blocks credentials, private/loopback IPs, and internal hostnames).
- Added `server/extensions/healthCheck/api/presets.ts`: Implements `GET /api/v1/health-check/presets` for preset service list.
- Added `server/extensions/healthCheck/api/list.ts`: Implements `GET /api/v1/boards/:boardId/health-checks` with latest result per entry.
- Added `server/extensions/healthCheck/api/create.ts`: Implements `POST /api/v1/boards/:boardId/health-checks` with validation, duplicate detection, and SSRF protection.
- Added `server/extensions/healthCheck/api/remove.ts`: Implements `DELETE /api/v1/boards/:boardId/health-checks/:id`.
- Added `server/extensions/healthCheck/api/index.ts`: Registers all health check API endpoints.
- Added `server/extensions/healthCheck/index.ts`: Mounts API routes, guarded by feature flag.

## Technical Debt
None

## What Should Be Done Next
- Add or fix Playwright MCP tests to cover all new health check endpoints and flows (CRUD, presets, error cases).
- Implement on-demand probe endpoints and rate limiting (deferred to next iteration).
- Review and refine URL validation logic for edge cases as needed.
- Ensure integration tests verify feature-flag behavior and error/response shapes.