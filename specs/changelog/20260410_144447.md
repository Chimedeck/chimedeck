## Update
- Extended `Comment` interface to include `reactions` (array of `ReactionSummary`).
- Updated `CommentItem.tsx`, `CommentThread.tsx`, `ActivityFeed.tsx`, and `CardModal.tsx` to support and render emoji reactions.
- Added Redux actions and optimistic update logic for add/remove reaction in `cardDetailSlice.ts`.
- Integrated real-time WS sync for comment reactions in `useBoardSync.ts`.
- Added translation keys for reaction UI in `translations/en.json`.

## New
- `src/extensions/Comment/api.ts`: API helpers for `addReaction` and `removeReaction`.
- `src/extensions/Comment/components/CommentReactions.tsx`: Emoji pill row component.
- `src/extensions/Comment/components/EmojiPickerPopover.tsx`: Emoji picker popover using `@emoji-mart/react`.

## Technical Debt
None

## What Should Be Done Next
- Add/extend Playwright MCP UI tests for edge cases (e.g., error handling, accessibility).
- Improve accessibility for emoji picker and pills (keyboard navigation, ARIA).
- Internationalize any remaining hardcoded UI strings.
- Defer threaded replies and their UI to a future iteration.
