# Changelog — 2026-03-23 08:05

## Update
- `server/extensions/mcp/index.ts`: Registered `move_card` and `write_comment` tools on the MCP server instance.

## New
- `server/extensions/mcp/tools/moveCard.ts`: Implements the `move_card` MCP tool. Accepts `cardId`, `targetListId`, and optional `position`. Calls `PATCH /api/v1/cards/:cardId/move` via `apiCall`. Returns structured error on API failure.
- `server/extensions/mcp/tools/writeComment.ts`: Implements the `write_comment` MCP tool. Accepts `cardId` and `text`. Calls `POST /api/v1/cards/:cardId/comments` via `apiCall`. Returns structured error on API failure.

## Technical Debt
- Remaining 4 tools (`create_card`, `edit_card_description`, `set_card_price`, `invite_to_board`) are deferred to Iterations 3 and 4.

## What Should Be Done Next
- Iteration 3: Implement `create_card` and `edit_card_description` tools.
