## Update
- Migrated all `src/extensions/**/*.tsx` files to use semantic token classes for color (e.g., `bg-bg-base`, `text-base`, `border-border`) instead of hardcoded palette classes.
- Removed all `dark:` overrides from extension components.
- Wrapped bare styled `<button>` elements in the shared `<Button>` component for consistency.
- Tagged intentional palette usage (e.g., label chips, avatar rings) with `// [theme-exception]` comments.

## New
- Added `specs/tests/sprint-119-extensions-light-mode.md`: Playwright MCP markdown test spec for Attachments, Labels, and Mentions extension panels in both light and dark themes.

## Technical Debt
- Some List components retain `bg-gray-100`, `text-gray-700/800/900` for light surfaces due to lack of semantic token equivalents (pending design decision, tagged as `[theme-exception]`).
- Playwright `.md` specs are not auto-run; require manual execution or conversion to `.spec.ts` for automation.
- Pre-existing type errors in unrelated files remain unresolved.

## What Should Be Done Next
- Audit all remaining `src/extensions/` files for any missed hardcoded color classes and migrate as needed.
- Remove any remaining `dark:` overrides if found.
- Convert Playwright markdown test specs to `.spec.ts` files for automated execution.
- Standardize input/form components, prose-invert, and scrollbar theming (deferred to next iteration).
- Resolve design questions around light-mode surface tokens and update exceptions accordingly.