## Update
None

## New
- Added six new MCP scenario files in `specs/tests/`:
  - `attachment-upload.md`: Covers multipart upload flow, URL attachment, MIME/size validation, and authentication.
  - `list-crud.md`: Covers create, rename, reorder, archive, and delete list operations with validation.
  - `label-crud.md`: Covers workspace label CRUD, assign/remove label from card, and validation.
  - `payment-card-price.md`: Covers set, partial update, and clear card money fields with currency validation.
  - `presence-board.md`: Covers active user presence API, TTL expiry, subscribe/unsubscribe, and access control.
  - `events-card-log.md`: Covers card activity log for create/move/assign events, append-only, newest-first.
- All files follow the established scenario format: Title, Overview, Pre-conditions, numbered Steps with Assert markers, and Expected Result.

## Technical Debt
- All new MCP scenario tests fail due to a missing `.vscode/mcp.json` config file (pre-existing environment issue).
- No scenario-specific bugs detected; failures are unrelated to the new test files.

## What Should Be Done Next
- Add or configure `.vscode/mcp.json` to enable MCP Playwright tests to run successfully.
- Re-run MCP scenario tests after resolving the config issue to verify new flows.
- Review and refine scenario files for clarity and completeness once tests are passing.
