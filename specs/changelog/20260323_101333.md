## Update
- Refactored `EditProfilePage.tsx` to use a two-tab layout (Profile, Notifications) with URL-driven state via `useSearchParams` and ARIA roles for accessibility.
- Moved notification settings to the Notifications tab; Profile tab now only shows profile-related content.
- Added `UserProfile.tabProfile` and `UserProfile.tabNotifications` translation keys to `en.json` for tab labels.

## New
- Introduced a tabbed navigation UI for EditProfilePage, defaulting to the Profile tab and supporting direct linking to Notifications via `?tab=notifications`.

## Technical Debt
- No Playwright or other automated tests exist for EditProfilePage tab navigation or notification settings flows. Manual testing required.

## What Should Be Done Next
- Scaffold Playwright tests for EditProfilePage tab navigation and notification settings flows.
- Review and update documentation/specs to reflect the new tabbed layout.
- Monitor for user feedback on tab usability and accessibility.