# Changelog — 20260316_091232

## Update
- `src/extensions/Card/components/CardModal.tsx` — imported `CardMetaStrip` and inserted it as a horizontal strip directly below the title/breadcrumb, above the archived card banner and the body.

## New
- `src/extensions/Card/components/CardMetaStrip.tsx` — compact horizontal metadata strip showing: label chips (up to 3 + overflow badge), member initials avatars (up to 3 + overflow badge), and start/due date pill buttons. Each section opens an inline popover (label picker with create support, member picker, date input). Empty states show `+ Labels` / `+ Members` placeholder buttons. Respects `disabled` prop throughout. Closes popovers on Escape or outside click.
- `src/extensions/Card/components/CardMetaStrip.mcp.md` — Playwright MCP test spec covering: strip renders below title, label chips display, `+ Labels` popover opens/closes, label attach, member avatars + overflow, member picker, start/due date popovers, date clearing, and disabled/archived state.

## Technical Debt
- The sidebar sections for Members, Labels, Start Date, and Due Date still exist in `CardModal.tsx` — they will be removed in iteration 6 when the full two-column layout restructure is applied.

## What Should Be Done Next
- Iteration 4: CardDescription inline click-to-edit (remove explicit Edit button, view mode renders markdown, click to open textarea).
