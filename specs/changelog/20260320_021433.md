# Changelog — 2026-03-20T02:14:33Z

## Update
- `server/extensions/notifications/mods/boardActivityDispatch.ts`: Changed recipient query from `memberships` (workspace-scoped) to `board_members` (board-scoped). Added Sprint 95 opt-out guards that filter recipients before dispatching any channel.

## New
- `server/extensions/notifications/mods/boardPreferenceGuard.ts`: Helper that checks `board_notification_preferences` for a user/board pair. Returns `true` (enabled) when no row exists (opt-out model).
- `server/extensions/notifications/mods/globalPreferenceGuard.ts`: Helper that checks `user_notification_settings` for a user's global notification master toggle. Returns `true` (enabled) when no row exists (opt-out model).

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 3: Add GET/PATCH `/api/boards/:boardId/notification-preference` endpoints so clients can read and update the per-board opt-out preference.
