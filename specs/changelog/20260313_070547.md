# Changelog — 20260313_070547

## Update
- None

## New
- `db/migrations/0037_notification_preferences.ts`: Adds the `notification_preferences` table with `user_id` (UUID FK → users, CASCADE), `type` (TEXT), `in_app_enabled` (BOOLEAN DEFAULT TRUE), `email_enabled` (BOOLEAN DEFAULT TRUE), `updated_at` (TIMESTAMPTZ), a UNIQUE constraint on `(user_id, type)`, and an index on `user_id`. Supports clean rollback via `dropTableIfExists`.

## Technical Debt
- Migration is numbered 0037 (not 0031 as originally planned) because 0031 was already taken by the `user_board_view_prefs` migration from Sprint 52.

## What Should Be Done Next
- Iteration 2: Implement GET and PATCH `/api/v1/notifications/preferences` endpoints, the `preferenceGuard` helper, and wire routes into the notifications API index (Sprint 70 API work).
