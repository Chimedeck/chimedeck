## Update
- Initialized Sentry client SDK in `src/main.tsx` at app bootstrap, gated by `VITE_SENTRY_CLIENT_ENABLED` and DSN presence.
- Updated `src/config/index.ts` to export `sentryReplayEnabled` from `VITE_SENTRY_REPLAY_ENABLED`.
- Updated `README.md` with Sentry client bootstrap instructions, feature flags, and manual smoke test checklist.

## New
- Added `src/common/monitoring/sentryClient.ts` to encapsulate Sentry init logic and provide `captureError`/`captureMessage` helpers.
- Created `src/common/monitoring/README.md` documenting client monitoring integration, feature flags, usage, and privacy policy.
- Added `VITE_SENTRY_REPLAY_ENABLED` to environment variable documentation.
- Created this changelog entry: `specs/changelog/20260330_181517.md`.

## Technical Debt
None

## What Should Be Done Next
- Implement error boundary and advanced privacy filtering (beforeSend) in Iteration 3.
- Add automated tests for Sentry event capture and Replay masking.
- Document and verify PII masking in Replay events for all user flows.
- Address pre-existing unrelated type errors in `Sidebar.tsx` and `featureFlagsSlice.ts`.