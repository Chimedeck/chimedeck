## Update
- Added the board-chat `create_board_card` tool flow end-to-end, including tool detection, validation, and card creation dispatch.
- Refactored card creation through a shared `server/extensions/card/mods/create.ts` path so the API and assistant use the same persistence logic.
- Extended card-created activity payloads with `board-chat-assist` source metadata.
- Updated the assist provider to send OpenAI-compatible `tools` payloads and normalize tool-call responses.
- Added and updated focused tests for provider payloads, tool routing, invalid tool payloads, and activity source persistence.
- Updated the sprint 167 spec and added an end-to-end board-chat assist scenario doc.

## New
- `server/extensions/board/mods/chat/assist/createBoardCard.ts`
- `server/extensions/card/mods/create.ts`
- `server/extensions/board/mods/chat/assist/__tests__/index.test.ts`
- `specs/tests/39-board-chat-assist-create-card.md`
- Tool-call support in the board-chat assist response shape
- Assistant-origin metadata on created card activity events

## Technical Debt
- Duplicate create-card prompts still created duplicate cards in the live browser retest, so idempotency is not fully enforced across requests.
- The browser retest required a temporary mock assist provider because the local environment did not have a usable `CHAT_ASSIST_*` provider configuration.
- Repo-wide typecheck still has unrelated pre-existing failures outside this iteration.

## What Should Be Done Next
- Fix cross-request idempotency for `create_board_card` so repeated tool calls reuse the same card result.
- Add the remaining assistant-side card state rendering for `suggested`, `confirmed`, and `dismissed`.
- Continue the later sprint work for GitHub Project URL storage, repository/spec loading, and the Documentation tab markdown editor with save/commit sync.
