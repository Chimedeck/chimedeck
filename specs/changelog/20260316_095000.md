# Changelog — 2026-03-16T09:50:00Z

## Update
- `src/extensions/Card/components/CardModal.tsx`: Replaced the static two-column flex layout with `ResizablePanels`. The modal body now uses `<ResizablePanels left={...} right={...}>` when the activity panel is visible, and falls back to a single scrollable column when it is hidden. Widened the modal from `max-w-3xl` (48rem) to `max-w-5xl` (64rem) to give both columns adequate space. Added `import ResizablePanels from './ResizablePanels'` and updated the comment block.
- `specs/tests/card-detail-modal.md`: Updated the test spec to reflect the new two-column layout, remove the outdated sidebar reference, and add steps for verifying the ResizablePanels drag handle, MetaStrip below title, sticky bottom bar, no ··· button, and mobile single-column behaviour.

## New
- Two-column resizable card modal layout: drag handle between left (content) and right (ActivityFeed) columns with localStorage persistence of the column ratio.
- Mobile fallback: columns stack vertically on viewports < 768px with no drag handle.

## Technical Debt
- The left-column content is duplicated in both branches of the `activityVisible` conditional. A future refactor could extract it into a named component or variable to reduce repetition.

## What Should Be Done Next
- Iteration 7: Place `CardValue` and `CardDetailPluginBadges` badges in a horizontal badge row in the left column, remove or clean up any remaining `CardSidebarSection` usages, and do a full acceptance criteria check across all sprint-81 deliverables.
