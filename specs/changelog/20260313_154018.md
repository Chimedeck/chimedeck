# Changelog — 2026-03-13T15:40:18Z

## Update
- Replaced all hardcoded dark-only Tailwind classes with dual-mode (light base + `dark:` override) classes per sprint-75 mapping in:
  - `src/layout/AppShell.tsx` (shell bg, topbars, borders, text)
  - `src/extensions/Workspace/components/Sidebar.tsx` (sidebar, nav items, dropdowns, user menu, badges, empty-state button)
  - `src/extensions/Board/containers/BoardListPage/BoardListPage.tsx` (headings, filter chips, empty/loading text)
  - `src/extensions/Board/components/BoardCard.tsx` (card surface, title, star button)
  - `src/extensions/Workspace/containers/WorkspaceListPage/WorkspaceListPage.tsx` (headings, workspace tiles, empty state)
  - `src/extensions/Workspace/containers/WorkspacePage/WorkspacePage.tsx` (headings, Members section)
- Confirmed that `Header.tsx`/`Sidebar.tsx` under `src/layout/` and dashboard files referenced in the plan do not exist; all layout logic is inline in the above files.

## New
- No new files or features added in this iteration.

## Technical Debt
- Playwright "mcp-evaluator" project is missing from config; E2E UI tests for theme switching and automation actions remain unverified.
- Minimal API action config validation and card attachments/comments/activities are still deferred.

## What Should Be Done Next
- Audit and update theme classes in board, card, modal, search, and notification components.
- Wire ThemeToggle into the Header.
- Add/enable E2E UI tests for automation actions and notification preferences.
- Address any visual regressions or missed class mappings from this audit.
