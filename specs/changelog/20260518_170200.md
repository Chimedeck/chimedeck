## Update
- Implemented Sprint 143 Trello boards compatibility routes under `server/extensions/trelloCompat/api/boards/index.ts`, covering board CRUD, field reads, lists/cards/members/labels/memberships/actions sub-resources, and board member add/remove flows.
- Updated Trello compat root router to mount and execute the new boards router from `server/extensions/trelloCompat/api/index.ts`.
- Extended Trello compat error helpers in `server/extensions/trelloCompat/common/errors.ts` with a dedicated unauthorized-permission response matching Trello's 401 shape.

## New
- Added `server/extensions/trelloCompat/serializers/board.ts` to map ChimeDeck boards to Trello board response shape.
- Added `server/extensions/trelloCompat/serializers/list.ts` to map ChimeDeck lists to Trello list response shape with Trello-compatible `pos`.
- Added `tests/integration/trelloCompat/boards.test.ts` with endpoint coverage for:
  - board CRUD
  - default list creation on board creation
  - lists and cards sub-resource filtering
  - members/labels/memberships reads
  - member add/remove
  - non-admin delete permission rejection
  - field-level board reads

## Technical Debt
- `GET /trello/1/boards/{id}/actions` currently returns an empty array placeholder; full activity/action mapping remains for Sprint 148.
- Member role handling in `PUT /trello/1/boards/{id}/members/{idMember}` currently persists `admin|normal` via `board_members`; richer observer/guest role parity will be refined in future Trello-member/org iterations.

## What Should Be Done Next
- In Iteration 3 (Sprint 144), implement dedicated card serializer and `/trello/1/cards/*` routes, then reuse that serializer in board card sub-resource responses for tighter Trello parity.
- Add DB-backed integration coverage for Trello compat routes once shared test DB seeding is available, to complement current handler-level mocked integration tests.
