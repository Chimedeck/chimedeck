## Update
- Updated `server/extensions/mcp/registerTools.ts` to import and register the new `get_card` tool.
- Expanded `server/extensions/mcp/README.md` to document all three tools (`search_cards`, `search_board`, `get_card`), including input/output schemas and usage examples.

## New
- Added `server/extensions/mcp/tools/getCard.ts`: Implements the `get_card` MCP tool, following the exact pattern of `moveCard.ts` (Zod schema, `apiClient` usage, error/success response shape).
- Added `server/extensions/mcp/tests/getCard.md`: Playwright scenario test for `get_card`, covering success and error cases.

## Technical Debt
- Playwright test runner does not recognize `getCard.md` ("No tests found"). Test config or syntax may need adjustment.
- No MCP flows have been fully validated due to test runner issues.

## What Should Be Done Next
- Review and update Playwright config to ensure all scenario tests are detected and executed.
- Validate and, if needed, correct the test syntax in `getCard.md` and other scenario files.
- Proceed to Sprint 108: Implement `McpDocsPage` component and integrate route/sidebar.