## Update
- Updated `server/config/env.ts` to add OAuth and APP_URL env vars.
- Updated `server/index.ts` to mount auth and users routers; made `router()` async.

## New
- Added `db/migrations/0002_auth.ts` for `users` and `refresh_tokens` tables.
- Added `server/common/db.ts` (shared Knex connection pool).
- Added `server/mods/cache/index.ts` (in-memory TTL cache for nonces/rate limiting).
- Added `server/extensions/auth/` extension: JWT RS256, bcrypt, OAuth (Google/GitHub), all API handlers, and middleware.
- Added `server/extensions/users/api/` with `GET/PATCH /api/v1/users/me`.
- Added 2 test files (6 tests: token issue/verify/tamper, password hash/verify/reject).
- Installed `jose` (JWT RS256) and `uuid` packages.

## Technical Debt
- Feature flag remote providers (Flagsmith, FeatBit) and storage module (S3 client factory) remain stubbed.
- Activity backfill and board_snapshots policy are incomplete.
- Storage module (S3/LocalStack) and CORS middleware are still pending.
- Vite/client build pipeline is referenced but not yet installed.
- E2E Playwright MCP tests failed due to missing `.vscode/mcp.json` config.

## What Should Be Done Next
- Tests for user/auth flows.
- Integrate remote feature flag providers and storage module.
- Complete activity backfill and board_snapshots policy.
- Add CORS middleware and Vite/client build pipeline.
- Continue with next sprint tasks as per `specs/sprints/sprint-plan.md`.