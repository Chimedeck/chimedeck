## Update
- Refactored `CardDescriptionTiptap.tsx` to use the new `OneLineToolbar` shared component, replacing inline toolbar logic.
- Rewrote `CommentEditor.tsx` to use a Tiptap-based rich text editor with the shared one-line toolbar, replacing the previous textarea/MentionInput implementation.
- Expanded `rich-text-toolbar-single-line-overflow.md` with new Playwright MCP scenarios for CommentEditor (single-line, overflow, keyboard navigation, edit flow).

## New
- Added `OneLineToolbar.tsx`, a reusable single-line toolbar component for Tiptap editors, supporting overflow via a stubbed **+** menu and full accessibility markup.

## Technical Debt
- Playwright MCP test file is not runnable due to config/path issues; tests not executed this iteration.
- Overflow menu in toolbar is a stub; full command menu and @mention support are deferred.
- Accessibility and responsive layout require further verification.

## What Should Be Done Next
- Fix Playwright test configuration and ensure all toolbar scenarios run and pass.
- Implement the full + overflow command menu and search/filter logic in the toolbar.
- Add @mention support back to CommentEditor.
- Further test and refine accessibility and responsive behavior for the toolbar and editors.