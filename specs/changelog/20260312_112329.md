## Update
- Enhanced `AutomationList.tsx`, `ButtonsTab.tsx`, and `ScheduleItem.tsx` to display a real-time updating run-count chip for each automation, subscribing to `automation_ran` WebSocket events and maintaining local run deltas.
- Updated `LogPanel/index.tsx` to prepend new run log rows in real time on `automation_ran` events (page 1 only), and to clear synthetic rows on page navigation.
- Modified `AutomationPanel/index.tsx` to pass the `automations` list to `LogPanel` for name resolution.
- Extended `tests/e2e/automation/logPanel.md` with scenarios for real-time updates, run-count chip states, and deduplication.

## New
- Added `RunCountChip.tsx`, a compact badge component for automation run counts (grey at 0, emerald when >0, caps at "999+").
- Implemented real-time UI updates for automation run logs and run-count chips across Automation, Button, and Schedule panels.

## Technical Debt
- Playwright test run failed with "No tests found"; Playwright configuration or test file naming may need adjustment.
- No user flows were tested; no PASS/FAIL results available for new features.

## What Should Be Done Next
- Fix Playwright configuration or test file naming to ensure tests are recognized and executed.
- Add or update tests to verify new real-time features and UI states.
- Consider handling missed WebSocket events (e.g., on reconnect) by fetching latest run logs.
- Review accessibility and color contrast of run-count chips.
- Decide on notification behavior if Log tab is not open during new events.
