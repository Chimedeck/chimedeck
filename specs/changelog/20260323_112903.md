## Update
- Wired `card_updated`, `card_deleted`, and `card_archived` notification types into `emailDispatch.ts` with correct template selection and data mapping.
- Updated `boardActivityDispatch.ts` to build and dispatch email notifications for these three types, matching the conventions for `card_commented`.
- Verified that the unsubscribe link in `shared.ts` already uses the correct deep-link (`/settings/profile?tab=notifications`).

## New
- No new files or features added; all changes were to existing notification dispatch and template selection logic.

## Technical Debt
- No Playwright or MCP tests exist for card update, archive, or delete notification email flows. These must be implemented before automated regression or acceptance testing is possible.
- Pre-existing unrelated TypeScript errors remain in the codebase.

## What Should Be Done Next
- Implement Playwright tests in `tests/e2e/` for card update, archive, and delete notification email flows, including content and unsubscribe link verification.
- Proceed with board-level notification preferences and related API/UI work as planned for the next iteration.
