## Update
- Modified `server/extensions/attachment/workers/thumbnail.ts` to add early return for GIFs: now persists width and height to the DB for GIF images, but skips thumbnail generation and does not write `thumbnail_key`.
- Ensured all other image types (JPEG, PNG, WebP) retain existing thumbnail and dimension logic.

## New
- No new files or features added; all changes are updates to existing logic.

## Technical Debt
None

## What Should Be Done Next
- Add/expand tests to verify correct handling of GIFs (dimensions persisted, no thumbnail generated).
- Review error handling for corrupt or unsupported images.
- Consider UI/API changes if GIF-specific logic needs to be surfaced to clients.