# Update
- Fixed assignment/unassignment notification copy to use target-user-aware text instead of always using `you`.

# New
- Notifications list API now enriches board-activity notifications with `target_user_id` and `target_user_name` by reading activity payload (`userId`/`previousUserId` + `assigneeName`).
- Realtime fanout for activity-driven notifications now includes `target_user_id` and `target_user_name` fields in `notification_created` payload.
- Notification row copy rendering now uses current user id + target user metadata to choose between:
  - `assigned you` / `removed you`
  - `assigned {target}` / `removed {target}`

# Technical Debt
- `NotificationItem.tsx` has broad pre-existing unsafe-type lint debt; this change only adjusted assignment copy logic and relies on the existing component typing baseline.

# What Should Be Done Next
- Add an integration test for notification copy variants (target=current user vs target=other user) across card/checklist assignment and unassignment.
- Consider gradually tightening `NotificationItem.tsx` types to eliminate existing no-unsafe lint noise and make future copy changes safer.
