## Update
- Added OTel metric instruments (`realtime.conflicts` counter, `realtime.propagation_delay_ms` histogram) to `server/mods/observability/metrics.ts` and updated the Metrics interface.
- Modified `server/mods/events/write.ts` to include `emittedAt` timestamp in WebSocket events for propagation delay measurement.
- Updated `server/extensions/card/api/move.ts` to call `recordConflict()` on position collision.
- Registered the new metrics endpoint in `server/index.ts`.
- Extended `src/extensions/Realtime/client/socket.ts` and `useWebSocket.ts` to support and report propagation delay.

## New
- Created `server/extensions/realtime/mods/conflictHandler.ts` for OTel/legacy conflict counting.
- Added `server/extensions/realtime/api/metrics.ts` with `POST /api/v1/metrics/propagation` endpoint.
- Added Playwright E2E test: `tests/e2e/otel-metrics.spec.ts` for endpoint and metrics flows.

## Technical Debt
- All E2E tests for the new endpoint failed (404); endpoint not reachable—likely routing issue.
- Card move smoke test failed at user registration (422); no board actions tested.
- Deferred: full wiring of metrics, plugin UI, registry caching, queue clearing, and invariants.

## What Should Be Done Next
- Investigate and fix metrics endpoint routing/registration (ensure `/api/v1/metrics/propagation` is available).
- Resolve user registration failure (422) in E2E tests to enable board actions.
- Complete wiring of OTel metrics for conflict/propagation delay and queue overflow handling.
- Expand and fix E2E coverage for all new flows and edge cases.
- Expose test hooks for queue state and overflow.
