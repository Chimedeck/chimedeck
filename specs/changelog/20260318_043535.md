# Changelog — 2026-03-18T04:35:35Z

## Update
- `src/extensions/Board/containers/BoardPage/BoardPage.tsx` — `handleBoardDelete` and `BoardDeleteDialog.onConfirm` now redirect to `/workspace/:workspaceId/boards` (workspace-scoped boards list) instead of the generic `/workspaces` route. Both code paths pass `{ state: { successToast: 'Board deleted' } }` via React Router navigate so the destination page can display a success toast.
- `src/extensions/Board/containers/BoardListPage/BoardListPage.tsx` — Reads `location.state.successToast` on mount via `useLocation` and shows the message as an info-level toast. Adds `ToastRegion` render, `toasts` state, `addToast`/`dismissToast` helpers, and clears the router state after consumption to prevent re-showing on back-navigation.

## New
- Panels (settings, members, automation) are closed before the board-deletion redirect so no overlay is visible on the destination page.
- `BoardDeleteDialog.onConfirm` gains try/catch error handling: shows "Failed to delete board." error toast if the confirmed deletion API call fails, rather than an unhandled promise rejection.
- `specs/tests/board-delete-auto-refresh.md` — BDAR-UI-03, BDAR-UI-03b, BDAR-UI-03c, BDAR-UI-04, and BDAR-UI-04b scenarios fully written for Iteration 3. Acceptance checklist items for Iteration 3 marked complete.

## Technical Debt
- Pre-existing TypeScript mismatch between `ToastItem['variant']` (`'info' | 'conflict' | 'error'`) and the `addToast` prop type expected by `CalendarView`/`TimelineView` (`'error' | 'conflict' | 'success'`) remains; this is outside the scope of Sprint 87.

## What Should Be Done Next
- Iteration 4: Wire workspace board list to invalidate in real-time when `board_deleted` events arrive via WebSocket, and propagate the removal to other connected users without a page reload.
