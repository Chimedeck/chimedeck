## Update
- Updated list rename and delete handlers to keep `board_state_transitions.graph_data` synchronized with live board lists.
- Updated state transitions router matching so `PUT` is accepted only on the base `.../state-transitions` route (not `.../rules`).
- Updated sync helpers to expose `updateNodeLabels` directly (with backward-compatible alias).
- Updated graph validation to keep unknown-list detection on `PUT` aligned with `state-transition-node-unknown-list` behavior.

## New
- Added websocket coverage test for `state_transition_updated` emission after successful state transition `PUT`:
  - `server/extensions/stateTransitions/api/__tests__/put_ws.test.ts`
- Added integration test coverage for list rename/delete sync hooks:
  - `server/extensions/stateTransitions/common/__tests__/integration_list_sync.test.ts`
- Added/updated sync unit coverage for `stripDeletedNodes` and `updateNodeLabels`:
  - `server/extensions/stateTransitions/common/__tests__/sync.test.ts`

## Technical Debt
- Repository-wide lint and typecheck still report pre-existing unrelated failures outside the state transitions scope.

## What Should Be Done Next
- Continue Iteration 4 scope: implement and test `POST /api/v1/boards/:boardId/state-transitions/copy`.
