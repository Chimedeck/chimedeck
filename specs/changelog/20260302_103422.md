## Update
- Refactored `Dockerfile` to a multi-stage build (deps → build → production) with non-root `app` user and optimized layer caching.
- Updated `docker-compose.yml` to use `Dockerfile.dev`, mount source for hot-reload, add improved healthcheck, and support optional Redis profile.
- Expanded `package.json` scripts for dev, build, lint, typecheck, test, and docker flows; added `concurrently` as a devDependency.
- Enhanced `.env.example` with Docker/build-related variables (`APP_PORT`, `NODE_ENV`, `DOCKER_IMAGE`, `IMAGE_TAG`).
- Split CI workflow into `quality` and `docker-build` jobs with buildx and cache support.

## New
- Added `Dockerfile.dev` for development with hot-reload (`bun --hot`).
- Created `docker-compose.prod.yml` for production deployments (pre-built image, no hardcoded secrets, always-on Redis).
- Introduced `Makefile` for common Bun and Docker commands.

## Technical Debt
- Feature flag remote providers (Flagsmith, FeatBit) and storage module (S3 client factory) remain stubbed.
- Activity backfill and board_snapshots policy are incomplete.
- Docker Compose healthchecks are basic and may need further refinement.

## What Should Be Done Next
- Implement the storage module (S3/LocalStack client factory) as per sprint-02.md.
- Add CORS middleware and the `composePromises` utility.
- Finalize feature flag remote provider integrations.
- Begin authentication feature work as planned for Sprint 03.