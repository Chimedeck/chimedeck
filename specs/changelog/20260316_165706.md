## Update
- Refactored `src/extensions/Card/components/CardModal.tsx` to use `<ResizablePanels>` for a two-column layout, replacing the static flex body.
- Increased modal width from `max-w-3xl` to `max-w-5xl` for improved two-column usability.
- Updated `specs/tests/card-detail-modal.md` to remove outdated sidebar assertions and add checks for new layout, sticky bottom bar, and drag handle.

## New
- Introduced conditional rendering in `CardModal.tsx` for two-column layout with independent scroll for ActivityFeed.
- Added test assertions for two-column layout, sticky bottom bar, and mobile responsiveness in `card-detail-modal.md`.

## Technical Debt
None

## What Should Be Done Next
- Verify if the dev server is running and accessible.
- Identify and run the correct Playwright test command for CardModal flows.
- Ensure all UI changes meet accessibility and mobile requirements.
- Refactor or remove any remaining sidebar-dependent code/components.