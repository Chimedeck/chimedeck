## Update
- Replaced all stub sections in `DesignSystemPage.tsx` with live implementations for Color Tokens, Typography, Buttons, Icon Buttons, Badges, and Form Inputs.
- Enhanced Color Tokens section to group and display all semantic CSS variables from `index.css` with group headings.
- Updated `components/index.ts` to export new helpers.

## New
- Added `BadgeSample.tsx` with 6 badge variants (default, primary, success, danger, warning, info) using semantic tokens.
- Added `FormInputSample.tsx` with text input (4 states), select (2 states), checkbox (3 states), and radio group samples, using shared `Input` and token-styled native controls.
- Added CodeSnippet examples for all new sections.

## Technical Debt
- NotificationPreferencesPanel’s ToggleSwitch still uses a raw button (`[switch-exception]`), pending refactor to a design-system primitive.
- Clipboard copy in CodeSnippet deferred to Iteration 12.
- Pre-existing lint/type errors in unrelated files (e.g., vite.config.ts) remain out of scope.

## What Should Be Done Next
- Fix `/design-system` route so the page is available in the running frontend (currently 404).
- Complete DesignSystem content polish and acceptance in Iteration 12.
- Add clipboard copy to CodeSnippet.
- Manually verify all migrated button flows and accessibility.
- Refactor ToggleSwitch to use a design-system primitive.
- Address any regressions or missed edge-case raw button usages.