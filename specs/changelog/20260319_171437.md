## Update
- Extracted all hardcoded UI strings from TimelineView components (`TimelineView.tsx`, `TimelineBar.tsx`, `TimelineZoomControl.tsx`, `TimelineRow.tsx`) to use `translations['Key']` bracket notation for i18n, following CalendarView/CustomFields patterns.
- Updated aria-labels and dynamic labels to reference translation keys.

## New
- Added `src/extensions/TimelineView/translations/en.json` with 13 translation keys for TimelineView UI copy.
- Created `specs/tests/sprint-93-timelineview-i18n.md` Playwright MCP test plan to verify TimelineView i18n coverage.
- Added changelog documenting the migration and test plan.

## Technical Debt
- Dynamic string interpolation (e.g., scheduled count suffix) uses basic template replacement; a helper is needed for more complex cases.
- Some translation keys (e.g., `Quarter`, `addDateRange`) are reserved but not yet wired up.
- No runtime locale switching implemented; only English supported.
- Date abbreviation internationalisation is deferred.

## What Should Be Done Next
- Diagnose and run Playwright tests for TimelineView i18n (project misconfiguration: "mcp" not found).
- Extract i18n for TableView, Mention, Notifications, UserProfile, AdminInvite, Realtime, OfflineDrafts, BoardViews, and Common/Layout in future iterations.
- Implement interpolation helper and runtime locale switching.
- Review ambiguous or reused strings for consistency and product/UX alignment.
- Ensure accessibility text extraction is complete across all features.
