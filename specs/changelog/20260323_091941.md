# Changelog — 2026-03-23T09:19:41Z

## Update
- Added `cli/README.md` documenting the `horiflow` CLI.

## New
- `cli/README.md`: Covers global install via Bun, authentication (token generation + `HORIFLOW_TOKEN` env var), `HORIFLOW_API_URL` override for local dev, global flags table, full command reference for all 6 sub-commands (`move-card`, `comment`, `create-card`, `edit-description`, `set-price`, `invite`) with flag tables and examples, and a scripting tip using `--json` + `jq`.

## Technical Debt
- None.

## What Should Be Done Next
- Sprint 104 and Sprint 105 are both complete. The next iteration could focus on end-to-end integration tests or publishing the CLI package.
