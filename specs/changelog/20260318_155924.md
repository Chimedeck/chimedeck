## Update
- Board GET API now includes `callerGuestType` in the response for client-side permission checks.
- `Board` interface extended to include `callerGuestType`.
- Propagation of `isViewerGuest` from BoardPage → BoardCanvas → ListColumn; Add Card button hidden for VIEWER guests.
- CardModalContainer and CardModal updated to pass `isViewerGuest` to ActivityFeed and AttachmentPanel; CommentEditor and upload controls hidden for VIEWER guests.
- GuestsTab updated to show guest type badge and allow inline type change for ADMINs, with error feedback.

## New
- Inline guest type (VIEWER/MEMBER) toggle in GuestsTab for ADMINs.
- UI badge for guest type in GuestsTab.
- Client-side permission guards for VIEWER guests: Add Card, CommentEditor, and AttachmentPanel write controls hidden.
- 13 new test scenarios (34–46) in `specs/tests/sprint-89-guest-role-split.md` covering all new UI and permission behaviors.
- Changelog file `specs/changelog/20260318_162000.md` created for this iteration.

## Technical Debt
- Playwright MCP project config is missing; no Playwright tests executed for this iteration.
- Playwright test matcher includes Markdown files, causing syntax errors.
- Some pre-existing type errors remain in board list and related files.

## What Should Be Done Next
- Fix Playwright configuration: add missing "mcp" project and exclude Markdown files from test matcher.
- Run and verify Playwright tests for all guest role split scenarios.
- Address pre-existing type errors in board list and related files.
- Consider UI polish, bulk guest management, and advanced error handling in future iterations.