## Update
- Moved Playwright spec `sprint-106-mcp-http-init.spec.ts` from `specs/tests/` to `tests/e2e/` as `mcp-http-init.spec.ts`.
- Fixed bug in test: updated `loginAndGetJwt(email, password)` to `loginAndGetJwt(request, email, password)` to match helper signature.
- Deleted the original `.spec.ts` from `specs/tests/` (no `.spec.ts` files remain there).
- Verified new test location loads with no import errors.

## New
- No new features or files added; this iteration only moved and fixed an existing test.

## Technical Debt
None

## What Should Be Done Next
- Run the full Playwright test suite to confirm all e2e tests pass after the move.
- Review other test files for similar helper signature mismatches.
- Continue migrating any remaining misplaced tests to their correct locations if found.