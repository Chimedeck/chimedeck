## Update
- Added Sprint 166 board chat write-path plumbing for raw message persistence and best-effort embedding storage.
- Registered `POST /api/v1/boards/:boardId/chat/messages` in the board router.

## New
- `db/migrations/0120_board_chat_storage.ts` for chat threads, messages, and message vectors.
- `server/extensions/board/mods/chat/messages/*` for write, embedding, and retry helpers.
- `server/extensions/board/api/chatMessages/*` for message creation.
- Tests for raw persistence, retry enqueueing, and API validation/access checks.

## Technical Debt
- Retry delivery is queued via pubsub, but there is no dedicated worker yet.
- Embedding provider config is present but still expects a live HTTP embedding endpoint.

## What Should Be Done Next
- Add the history and semantic search endpoints for Sprint 166.
- Wire a background worker for the embedding retry queue once the read path lands.
