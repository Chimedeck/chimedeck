# Changelog — 20260415_033753

## Update
- Fixed `useWorkspaceId` return type in `WebhooksRegisterPage.tsx` to satisfy `exactOptionalPropertyTypes` strict mode (`match[1] ?? ''`).
- Updated `index.ts` to export all container components including `EditWebhookModal`, `DeleteWebhookDialog`, and `SignatureVerificationSnippet`.

## New
- **`SignatureVerificationSnippet.tsx`**: Collapsible panel (collapsed by default) with ARIA `aria-expanded` toggle. Expands to reveal:
  - Prose explaining the `Webhook-Signature: t=<ts>,v0=<hex>` header format and replay-protection guidance.
  - A full JavaScript `verifyWebhookSignature` implementation using `crypto.timingSafeEqual`, matching the server-side `sign.ts` logic.
  - Copy-to-clipboard button that shows "Copied ✓" for 2 s then resets.
- **`en.json`** additions: `SignatureSnippet.copy`, `SignatureSnippet.copied`, `SignatureSnippet.signatureFormatTitle`, `SignatureSnippet.signatureFormatBody`, `SignatureSnippet.signatureFormatDetail`, `SignatureSnippet.replayProtectionTitle`, `SignatureSnippet.replayProtectionBody`.
- `WebhooksRegisterPage` now renders `<SignatureVerificationSnippet />` below the webhook table / empty state.

## Technical Debt
- No syntax highlighting library (e.g. `highlight.js`) added; code block is plain `<pre><code>` styled with monospace. Can be upgraded if syntax colouring is needed.

## What Should Be Done Next
- Iteration 9: write unit tests for `SignatureVerificationSnippet`, `RegisterWebhookModal`, and the full UI CRUD integration flow.
