## Update
- `src/extensions/Plugins/api.ts` — Added `registerPlugin(body)` API function (POST /api/v1/plugins) and `RegisterPluginBody` interface
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.duck.ts` — Added `registerPluginThunk`, `clearRegisterState` action, and `registerStatus`/`registerError`/`newApiKey` state fields with selectors
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.tsx` — Wired up platform admin check, RegisterPluginModal, and ApiKeyRevealModal
- `.env` — Added `VITE_PLATFORM_ADMIN_EMAILS` for client-side admin gating

## New
- `src/extensions/Auth/utils/isPlatformAdmin.ts` — Utility to check if a user email is in the `VITE_PLATFORM_ADMIN_EMAILS` env var (comma-separated)
- `src/extensions/Plugins/modals/RegisterPluginModal.tsx` — Full plugin registration form with all fields, slug auto-derivation, category tag input, icon preview, and inline validation
- `src/extensions/Plugins/modals/ApiKeyRevealModal.tsx` — One-time API key reveal modal with copy button and "never shown again" warning

## Technical Debt
- None introduced

## What Should Be Done Next
- Iteration 4: PluginSearchBar component + search/filter wiring into the Plugin Dashboard
