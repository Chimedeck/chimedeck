## Update
- Added `isStarred` field and star/unstar API functions to `Board/api.ts`.
- Updated `BoardCard.tsx` to include a star toggle button with filled yellow star icon.
- Enhanced `BoardListPage.duck.ts` with star/unstar thunks, starred filter toggle, and visible boards selector.
- Modified `BoardListPage.tsx` to support starred filter chip and star/unstar actions.
- Refactored `BoardPage.tsx` to add tabbed interface for Board, Activity, Comments, and Archived Cards panels.

## New
- Created `src/extensions/BoardViews/` feature module:
  - `types.ts`: Board views types/interfaces.
  - `api.ts`: RTK Query endpoints for board activity, comments, archived cards, and star/unstar.
  - `BoardActivityPanel.tsx`: Activity feed panel.
  - `BoardCommentsPanel.tsx`: Comments panel with card context.
  - `BoardArchivedCardsPanel.tsx`: Archived cards list with restore action.

## Technical Debt
- Playwright E2E tests for BoardViews UI and starred filter could not be executed due to Bun/Playwright incompatibility and config issues.
- Some pre-existing TypeScript errors remain in BoardListPage.duck.ts (not introduced by this iteration).
- Optimistic UI for star/unstar and restore actions lacks rollback on API failure.

## What Should Be Done Next
- Fix Playwright/Bun test runner setup to enable E2E and MCP test execution.
- Add/verify Playwright tests for BoardViews panels and starred filter flows.
- Implement advanced error handling and rollback for optimistic UI actions.
- Address any remaining uuidv4 usage and dead code in board/card modules.
- Consider UI polish and persistence for starred filter chip across navigation.