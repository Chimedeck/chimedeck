## Update
None

## New
- Added `.umbra/production-fleet.yml`: CI workflow for the `production-fleet` branch, sets `USE_SSH=TRUE`, `DEPLOYMENT_MODE=fleet`, and includes all required fleet secrets. Runs `scripts/build.sh` then `scripts/deploy.sh`.
- Added `.umbra/production-asg.yml`: CI workflow for the `production-asg` branch, sets `USE_SSH=FALSE`, `DEPLOYMENT_MODE=asg-bluegreen`, `ENV_NAME=production`, `USE_RDS=TRUE`, and includes all required Terraform state secrets. Runs `scripts/build.sh` then `scripts/deploy.sh`.

## Technical Debt
None

## What Should Be Done Next
- Implement remaining Terraform infrastructure modules and scripts as described in the requirements and architecture specs.
- Continue CI/CD pipeline automation for additional environments or deployment strategies as needed.
- Review and update secrets documentation if new secrets are introduced in future iterations.
