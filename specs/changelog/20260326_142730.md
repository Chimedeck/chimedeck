## Update
- Migrated `src/containers/TopbarContainer/TopbarContainer.tsx`: replaced `bg-gray-900` with `bg-bg-surface`, `border-white/10` with `border-border`, `text-white` on the app name span with `text-base`. Replaced the bare `<button>` logout element with the `<Button variant="ghost" size="sm">` component from `src/common/components/Button.tsx`.

## New
- Added `specs/tests/sprint-119-containers-light-mode.md`: Playwright MCP test spec (4 scenarios) verifying the TopbarContainer renders with light surfaces, readable text, and ghost-styled logout button in light mode, and correctly in dark mode.

## Technical Debt
- `src/containers/FooterContainer/FooterContainer.tsx` returns `null` — it has no markup to migrate. No action needed until the footer is expanded in a future sprint.

## What Should Be Done Next
- Continue with Iteration 5: Shared Components Token Migration (Sprint 119, Part C) — audit `src/components/` for hardcoded colour patterns and replace with semantic tokens.
