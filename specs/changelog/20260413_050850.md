# Iteration 8: Sprint 134 foundation — feature flag + route wiring

## Update
- `server/config/env.ts`: Added `DESIGN_SYSTEM_ENABLED` flag. Defaults to `true` in non-production environments; defaults to `false` in production (opt-in via `DESIGN_SYSTEM_ENABLED=true`).
- `src/config/index.ts`: Added `designSystemEnabled` client flag. Evaluates to `true` when Vite is in dev mode (`import.meta.env.DEV`) or when `VITE_DESIGN_SYSTEM_ENABLED=true` is explicitly set.
- `src/routing/index.tsx`: Conditionally lazy-loads and registers the `/design-system` route only when `config.designSystemEnabled` is true. Route is absent from production bundles by default.

## New
- `src/extensions/DesignSystem/routes.ts`: Exports `DESIGN_SYSTEM_PATH = '/design-system'` constant.
- `src/extensions/DesignSystem/DesignSystemPage.tsx`: Stub component (returns null); full implementation deferred to Iteration 9.

## Technical Debt
- `DesignSystemPage.tsx` is a stub and must be replaced with the full scaffold in Iteration 9.

## What Should Be Done Next
- Iteration 9: Build the full `DesignSystemPage` with AppShell-based layout, sticky nav scaffold, and helper components (`Section`, `CodeSnippet`, `ColorSwatch`, `TypographySample`).
