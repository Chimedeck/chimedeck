# Sprint 120 Part B — Text & Contrast Full Audit + Verification

## Update

### Critical bug fix
- **`MemberAvatarPopover`**: Resolved invisible-text bug where member name rendered as `text-white` on a `bg-white` popover surface. Converted container to `bg-bg-surface border-border`, member name to `text-base`, hover row to `hover:bg-bg-overlay`.

### Auth pages and form components → semantic tokens
- All six auth page containers (`LoginPage`, `SignupPage`, `ForgotPasswordPage`, `VerifyEmailPage`, `ConfirmEmailChangePage`, `ResetPasswordPage`): `bg-slate-950` → `bg-bg-base`, `bg-slate-900` → `bg-bg-surface`, `border-slate-800` → `border-border`, `text-white` headings → `text-base`. Auth pages now respond to the app theme toggle.
- `LoginForm`, `SignupForm`, `ChangeEmailForm`, `VerificationPending`: submit buttons `bg-indigo-600` → `bg-primary hover:bg-primary-hover`; `text-white` on buttons tagged `// [theme-exception]`.
- `OAuthButton`: `bg-slate-800` container → `bg-bg-overlay border-border text-base`; `ring-indigo-500` → `ring-primary`.
- `PasswordInput`: `hover:text-slate-200` → `hover:text-base`; focus ring → `ring-primary`.

### Error/success color tokens
- Error boxes in `EditPluginModal`, `RegisterPluginModal`, `PluginDashboardPage`: `bg-red-900/30 border-red-700 text-red-300` → `bg-danger/10 border-danger/40 text-danger`.
- `ApiKeyRevealModal`: code display `text-green-300` → `text-success`.
- `CredentialSheet`: success state `bg-green-900/30 text-green-300` → `bg-success/10 text-success`.
- `BoardStateChip`: active state `bg-green-100 text-green-700` → `bg-success/10 text-success`.
- `VisibilityBadge`: PUBLIC `bg-green-600/80 text-green-100` → `bg-success/80 text-inverse`; WORKSPACE `bg-blue-600/80 text-blue-100` → `bg-primary/80 text-inverse`.
- `MemberAssignModal`: unassign button `bg-red-100 text-red-600` → `bg-danger/10 text-danger`; assign button `bg-blue-100 text-blue-600` → `bg-primary/10 text-link`.

### Card components
- **CardMetaStrip**: due-date chips (done/overdue/due-soon/normal) → semantic tokens (`bg-success/10`, `bg-danger/10`, `bg-bg-overlay`); checkboxes → `bg-success`, `bg-danger`; dividers `bg-gray-200` → `bg-border`; overflow avatar ring `ring-white` → `ring-bg-surface`.
- **CardModalBottomBar**: `border-gray-200 bg-gray-50/50` → `border-border bg-bg-surface/50`; all hover states `hover:bg-gray-100 dark:hover:bg-slate-800` → `hover:bg-bg-overlay`.
- **CardMembers**, **MemberAssignModal**: hover rows → `hover:bg-bg-overlay`.
- **OneLineToolbar**: borders, kbd background, dividers, hover states all converted.
- **CardActionMenu**, **ChecklistProgress**, **CommandMenu**, **CardReferenceChip**: gray fills → semantic tokens.
- **ActivityFeed**: actor display name `text-white` → `text-base`.
- **CardLabelChips**, **LabelChip**: JSX block-comment exceptions moved to inline `// [theme-exception]` on className lines.
- **CardDatesPicker**: `!bg-slate-500 !text-white` start-date selection → `!bg-bg-sunken !text-base`.
- **CardModal**: error panel `bg-slate-900` → `bg-bg-surface`.

### AddListForm
- Closed-state button and open-state form: `bg-white/40 dark:bg-slate-900/40 border-gray-300 dark:border-slate-700` → `bg-bg-surface/40 border-border`. Submit button → `bg-primary hover:bg-primary-hover text-white // [theme-exception]`.

### Board views and panels
- **BoardCommentsPanel**, **BoardArchivedCardsPanel**: card surfaces `bg-slate-800` → `bg-bg-surface`; `text-gray-200` → `text-base`.
- **CalendarView** (Day, Week, Month, Card chip): all hardcoded `bg-slate-*`, `text-slate-*`, `border-slate-*` → semantic tokens.
- **TimelineView**: sticky columns, header, `border-slate-700` → `border-border bg-bg-surface`; zoom control border/hover converted.
- **Sidebar**: `bg-slate-50` → `bg-bg-surface`.

### Plugins and extensions
- **PluginRegistryTable**: `bg-slate-50` thead → `bg-bg-sunken`.
- **PluginAllowedDomainsPanel**, **CardPluginSection**, **CardPluginBadges**, **CardDetailPluginBadges**, **CardPluginButtons**: all slate hardcodes → semantic tokens.
- **PluginDashboardPage**: page wrapper `bg-slate-900 text-slate-100` → `bg-bg-base text-base`.
- **PluginRegistryPage**: `bg-indigo-600` CTA → `bg-primary hover:bg-primary-hover`; `dark:border-red-800 dark:bg-red-900/20` error box → `border-danger/30 bg-danger/10`.
- **PluginRegistryRow**: `bg-red-600` delete button → `bg-danger hover:opacity-90`.

### Automation components
- Step indicators `bg-blue-600 text-white` (active) → `bg-primary text-white // [theme-exception]`; completed state `bg-emerald-600 text-white` → `bg-success text-white // [theme-exception]`.
- Day/frequency selector active state, submit buttons: `bg-blue-600` → `bg-primary`.
- Danger buttons `bg-red-600/bg-red-700` → `bg-danger`.
- `IconPicker` selected state `ring-blue-400` → `ring-primary/50`.
- `BoardButtonBuilder`: active state `bg-blue-600 border-blue-500` → `bg-primary border-primary`.

### Other components
- **MentionList**: dropdown `bg-slate-800 border-slate-700` → `bg-bg-surface border-border`.
- **CustomFieldBadge**: `bg-slate-800 border-slate-700` → `bg-bg-overlay border-border`.
- **DropdownFieldEditor**: popup `bg-slate-900 border-slate-600` → `bg-bg-surface border-border`; checkmark `text-white` tagged `// [theme-exception]`.
- **BoardCustomFieldsPanel**: `hover:text-white` → `hover:text-base`.
- **EditProfilePage**: `bg-slate-950 text-slate-100` → `bg-bg-base text-base`; hover states → `hover:text-base hover:border-border`.
- **GlobalNotificationToggle**: toggle track `bg-slate-700` → `bg-bg-overlay`; active track `bg-slate-600` → `bg-bg-sunken`.
- **CommentItem**: avatar `text-white` tagged `// [theme-exception]` (on colored avatar bg).
- **SearchResultItem**: avatar `text-white` tagged `// [theme-exception]` (on colored avatar bg).
- **Attachments** (AttachmentThumbnail, AttachmentPanel, CardAttachmentPreview): media overlay `text-white/70`, `text-white/80`, `text-white/90` all tagged `// [theme-exception]` (on dark media backgrounds).
- `activityEventLabels.ts`: dot colors `bg-slate-500/600` → `bg-bg-sunken`.

### `src/config/theme.ts`
Added `// [theme-exception]` inline on all three `text-white` button variant strings (primary, danger, success).

### Workspace components
- `CreateWorkspaceModal`, `WorkspaceDashboard`, `WorkspacePage`, `AcceptInvitePage`, `WorkspaceListPage`: all `bg-primary text-white` CTA buttons tagged `// [theme-exception]` inline.
- `ListDeleteDialog`: `bg-red-600 text-white` → `bg-danger text-white // [theme-exception]`.
- `AddListButton`: `bg-primary text-white` tagged `// [theme-exception]`.

## New

- **`src/common/tokens/index.ts`**: New centralized semantic token class-string reference file. Exports `inputClasses`, `overlayPanelClasses`, `neutralBadgeClasses`, `successBadgeClasses`, `dangerBadgeClasses`, `errorBoxClasses`, `listItemHoverClasses` constants.
- **`specs/tests/sprint-120-contrast-audit.md`**: Updated with 18 Playwright MCP test suites — login, signup, workspace, sidebar, board view, card modal, theme-toggle-while-open, notifications, inputs, member popover, calendar, timeline, table, profile — in both light and dark modes. Added terminal verification commands.

## Technical Debt

- Playwright MCP test spec is a markdown walkthrough; must be converted to `.spec.ts` files for automated execution.
- Pre-existing TypeScript errors on the server side (unrelated to this migration) remain unresolved.
- Orange due-soon chip still uses `bg-orange-500/10 text-orange-600` — no semantic `warning` text token exists; kept with `// [theme-exception]`.

## What Should Be Done Next

1. Run `grep -rn 'text-white\b' src/` — should return zero lines without `// [theme-exception]` (verified ✅).
2. Convert `specs/tests/sprint-120-contrast-audit.md` Playwright MCP scenarios to `.spec.ts` automated tests.
3. Consider adding an ESLint rule to prohibit new `bg-gray-*`/`bg-slate-*`/`text-white` without `// [theme-exception]`.
4. Add a `text-warning` semantic token to `tailwind.config.ts` and `src/index.css` for the orange due-soon chip.
