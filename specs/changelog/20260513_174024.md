# Update
- Changed edit-mode link default display behavior to mode 2 (button display) instead of mode 1.
- Updated link insertion flows to apply the button-display class by default.

# New
- Links inserted from paste-with-preview in card description now default to button display mode.
- Links inserted/updated through the link insert popover now default to button display mode.

# Technical Debt
- Existing legacy links without display classes are now interpreted as mode 2 in UI state, but historical markdown content may still include raw URL text from earlier versions.

# What Should Be Done Next
- Add a migration pass (optional) to normalize legacy link marks/classes when documents are opened in edit mode.
- Add regression tests for default-mode selection and class assignment across paste, insert, and edit flows.
