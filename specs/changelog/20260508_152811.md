# Changelog - 2026-05-08 15:28:11

## Update
- Fixed list (column) creation permissions so board guest members can create new columns.
- `POST /boards/:boardId/lists` now accepts:
  - workspace MEMBER/ADMIN/OWNER (existing behavior)
  - workspace GUEST with board `guest_type=MEMBER` (newly allowed)

## New
- Reused existing permission helper `requireMemberOrBoardGuestMember` in list create handler for parity with other board write actions that already support member guests.

## Technical Debt
- Other list write endpoints (rename/archive/delete/reorder/sort/color) may still use stricter role checks and could be reviewed for consistency with guest member write policy.

## What Should Be Done Next
- Add API tests for list creation covering:
  - GUEST + board MEMBER => 201
  - GUEST + board VIEWER => 403
  - non-board guest => 403
- Audit list mutation endpoints for the same guest-member rule where intended.
