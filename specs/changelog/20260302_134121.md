## Update
- Refactored `server/mods/cache/index.ts` to use an adapter pattern for cache providers.
- Updated `server/index.ts` to add WebSocket upgrade and real-time endpoints.
- Modified `server/extensions/board/api/index.ts` to mount events and presence routes.
- Replaced all `[event]` log stubs in 15 mutation handlers with real `writeEvent()` calls for event sourcing compliance.

## New
- Added `db/migrations/0008_events.ts` to create `events` and `board_snapshots` tables.
- Implemented event store and snapshot logic in `server/mods/events/write.ts`, `read.ts`, `snapshot/write.ts`, `snapshot/read.ts`, and `snapshot/policy.ts`.
- Introduced `CacheProvider` abstraction and adapters in `server/mods/cache/types.ts`, `adapters/nodeCache.ts`, and `adapters/redis.ts`.
- Added `PubSubProvider` abstraction and adapters in `server/mods/pubsub/types.ts`, `index.ts`, `publisher.ts`, `subscriber.ts`, `adapters/inMemory.ts`, and `adapters/redis.ts`.
- Created WebSocket room management and real-time modules in `server/extensions/realtime/mods/rooms/`, `heartbeat.ts`, `auth.ts`, and `api/index.ts`.
- Added events and presence endpoints for polling and real-time sync.

## Technical Debt
- Redis-based tests are skipped due to missing Redis in the current environment.
- Playwright MCP evaluator tests are not configured and were not run; user flows remain untested.
- Due date notification system and push logic remain stubbed.

## What Should Be Done Next
- Configure and run Playwright MCP evaluator tests for user flow validation.
- Implement due date notification push and complete notification system.
- Expand test coverage for multi-node Redis and presence expiry scenarios.
- Continue with comments, activity log, attachments, search, and observability features as scheduled in future sprints.