## Update
- Refactored `src/extensions/TableView/TableView.tsx` to extract all user-visible strings to `translations/en.json` using bracket notation.
- Updated `TableRow.tsx` to use translation key for the open-card aria-label prefix.
- Audited `TableHeader.tsx`; no user-visible hardcoded strings found, so no changes made.

## New
- Added `src/extensions/TableView/translations/en.json` with 15 translation keys for all TableView UI copy.
- Created `specs/tests/sprint-93-tableview-i18n.md` with a Playwright MCP test plan for TableView i18n.
- Added this changelog entry documenting the TableView i18n migration.

## Technical Debt
- No Playwright `.test.ts(x)` files exist for TableView; only a markdown test plan is present, so tests were not executed.
- `.md` files in the test directory cause Playwright runner errors; these should be moved or ignored.
- String interpolation and locale-aware date formatting are still handled ad-hoc or not implemented.

## What Should Be Done Next
- Implement Playwright `.test.ts(x)` files for TableView i18n as described in the test plan.
- Move or ignore `.md` files in the test directory to prevent Playwright errors.
- Refactor string interpolation and date formatting to be more robust and locale-aware.
- Continue i18n migration for remaining features (Mention, Notifications, UserProfile, etc.).
