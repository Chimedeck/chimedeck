# 20260317_174259

## Update

- Changed the card description attachment button to persist file references as stable `attachment:<name>` placeholders instead of expiring presigned URLs.
- Hydrated card description markdown against the current card attachments before rendering, editing, and retrying offline saves.
- Inserted uploaded files back into the rich-text description editor at the original cursor position so image and file links stay inline.
- Updated the description toolbar paperclip flow to open the same insert-attachment picker used by comments, including selecting existing card attachments inline.

## New

- Added a shared attachment markdown utility under `src/common/utils` so card descriptions and comments use the same placeholder encode/decode logic.

## Technical Debt

- Placeholder lookup is still name-based, so duplicate attachment names on the same card can resolve ambiguously.
- `CardDescriptionTiptap` still carries a pre-existing Sonar cognitive-complexity warning despite the extraction cleanup done in this change.

## What Should Be Done Next

- Add a focused UI test that uploads an attachment through the description toolbar, saves, reloads the card, and verifies the rendered markdown rehydrates with a fresh attachment URL.
- If duplicate names become a real issue, move the placeholder format from attachment name to a stable attachment identifier.
