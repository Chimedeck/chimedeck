## Update
- Modified `server/extensions/board/api/index.ts` to route PATCH `/api/v1/boards/:id` requests to the new `handlePatchBoard` handler.
- No changes required for the GET endpoint; it now includes the new `monetization_type` field automatically.

## New
- Added `db/migrations/0020_board_monetization.ts` to introduce a nullable `monetization_type` column to the `boards` table.
- Created `server/extensions/board/types.ts` defining `MonetizationType` and updating the `Board` type.
- Implemented `server/extensions/board/api/patch.ts` to handle PATCH requests for updating `monetization_type` (with zod validation, admin-only, event emission, and error handling).

## Technical Debt
- Playwright MCP tests for PATCH/GET `/api/v1/boards/:id` could not be executed due to port 3000 conflicts and system-level permission errors.
- No automated verification of the new API behavior was performed in this iteration.

## What Should Be Done Next
- Resolve port 3000 conflicts and system permission issues to enable Playwright MCP tests.
- Add/verify tests for PATCH and GET `/api/v1/boards/:id` covering all monetization_type scenarios (valid, null, invalid values).
- Review RBAC and event sourcing compliance for the new field.
- Monitor for any migration or API regressions in production environments.