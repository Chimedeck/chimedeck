## Update
- Wrapped `<App />` in `ErrorBoundary` in `src/main.tsx` to catch render errors and display recovery UI.
- Enhanced `sentryClient.ts` with a `beforeSend` filter to drop noisy/unactionable browser errors before sending to Sentry.
- Updated `src/common/monitoring/README.md` to document ErrorBoundary usage and noise filtering logic.

## New
- Added `src/common/monitoring/ErrorBoundary.tsx` implementing a Sentry-based error boundary with fallback UI and recovery actions.

## Technical Debt
None

## What Should Be Done Next
- Tune the list of ignored error patterns as new noisy errors are discovered.
- Consider adding automated tests for ErrorBoundary behavior.
- Monitor for any privacy or PII leakage in error reporting.
- Evaluate user experience of fallback UI and adjust as needed.