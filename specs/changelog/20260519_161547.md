## Update
- Added server-side state transition enforcement checks for card moves via `validateCardMove({ boardId, fromListId, toListId })`.
- Wired enforcement into `PATCH /api/v1/cards/:id/move` so blocked transitions return `422` with `name: state-transition-forbidden` and allowed next states.
- Added board rules cache invalidation call in state transitions `PUT` handler.

## New
- New `server/extensions/stateTransitions/enforcement/` module:
  - `index.ts` for enforcement guard logic.
  - `rules.ts` for 60-second in-memory board rule cache and invalidation helpers.
- New `StateTransitionForbiddenError` in `server/extensions/stateTransitions/common/errors.ts`.
- Extended state transition tests with enforcement scenarios and cache invalidation-after-PUT coverage.

## Technical Debt
- Full repository lint/typecheck/test baselines are currently failing due unrelated pre-existing issues outside this iteration scope.
- Rule/name fallback currently relies on graph node labels when list names are absent from graph state.

## What Should Be Done Next
- In iteration 6, add blocked-move activity log emission and Trello-compat error mapping for `StateTransitionForbiddenError`.
- Add integration tests around `card/api/move.ts` endpoint response payloads for forbidden transitions.
