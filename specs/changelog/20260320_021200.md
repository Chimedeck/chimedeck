# Changelog — 2026-03-20T02:12:00Z

## Update
- None

## New
- `db/migrations/0086_board_notification_preferences.ts`: Creates `board_notification_preferences` table with `id` (PK), `user_id` (FK→users), `board_id` (FK→boards), `notifications_enabled` (bool, default true), `updated_at`. UNIQUE constraint on `(user_id, board_id)`. Opt-out model: missing row means notifications enabled.
- `db/migrations/0087_user_notification_settings.ts`: Creates `user_notification_settings` table with `user_id` (PK, FK→users), `global_notifications_enabled` (bool, default true), `updated_at`. Opt-out model: missing row means global notifications enabled.

## Technical Debt
- None

## What Should Be Done Next
- Iteration 2: Fix `boardActivityDispatch.ts` to query board members instead of workspace members, and add `boardPreferenceGuard.ts` / `globalPreferenceGuard.ts` helpers to filter recipients by notification preferences.
