# Changelog — 2026-03-24 17:18:45

## Update

- `src/extensions/Board/containers/BoardPage/BoardPage.tsx` — Added **Health Check** as the fifth board tab. The tab is conditionally rendered when `HEALTH_CHECK_ENABLED` is true. Imports `HealthCheckTab` and `HEALTH_CHECK_ENABLED` from the HealthCheck extension. The `BoardTab` type now includes `'health-check'`.

## New

- `src/extensions/HealthCheck/containers/HealthCheckTab/HealthCheckTab.tsx` — Full tab panel container. Connects to Redux (entries, status, lastCheckedAt), dispatches `fetchHealthChecksThunk` on mount, wires `useHealthCheckAutoRefresh` (60 s countdown) and `useHealthCheckProbe` (per-row on-demand probe), renders `HealthCheckRow` list, `HealthCheckEmptyState` when empty, `HealthCheckCountdown` in the header, and the `AddServiceModal`.

- `src/extensions/HealthCheck/routes.ts` — Exports `HEALTH_CHECK_TAB_ID = 'health-check'`, `BOARD_TAB_PARAM = 'tab'`, and the `isHealthCheckTabActive(search)` helper so board tab routing has no magic strings.

- `src/extensions/HealthCheck/README.md` — Documents the extension: feature flag, board tab integration, folder structure, status classification, auto-refresh behaviour, and add-service modal.

## Technical Debt

- `BoardPage.tsx` has pre-existing TypeScript errors at the `addToast` prop passed to `CalendarView` / `TimelineView` (type mismatch introduced before this iteration). Not introduced here; not fixed to avoid scope creep.

## What Should Be Done Next

- Iteration 10: Write the Playwright MCP E2E spec (`specs/tests/health-check-tab-ui.md`) covering sprint-116.md acceptance criteria (AC-1 through AC-10).
