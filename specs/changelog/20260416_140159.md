## Update
- Fixed workspace selection drift when navigating to boards through global search by syncing active workspace before route change.
- Added a board-level safety sync so the workspace shell updates from loaded board data on `/boards/:boardId` routes.

## New
- No new endpoints, schema changes, or UI modules.

## Technical Debt
- Board and app shell still rely on global active workspace state that can temporarily diverge in other deep-link entry points without workspace metadata.

## What Should Be Done Next
- Add a focused test that covers cross-workspace search navigation to `/boards/:id` and verifies sidebar workspace stability during delayed board fetch.
- Consider deriving active workspace from route/board context centrally to reduce duplicated sync logic.
