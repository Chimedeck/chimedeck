## Update
- Mounted the workspace API router in `server/index.ts` alongside auth and users routers.

## New
- Implemented full workspace server extension under `server/extensions/workspace/`:
  - CRUD handlers: create, get, update, delete.
  - Invite handlers: create, inspect, accept (with expiry/double-accept logic).
  - Member management: list, updateRole, remove (with owner invariants).
  - Invite mods: create, validate, consume (token generation, expiry, usage).
  - Centralized invite config in `common/config/invite.ts` (TTL, max uses).
  - Regex-based router composition for all workspace endpoints.

## Technical Debt
- None.

## What Should Be Done Next
- Implement Playwright integration tests for all workspace, invite, and membership flows as per sprint-04 and requirements specs.
- Verify RBAC and owner invariants via automated tests.
- Document API endpoints and invariants for client integration.
- Monitor for edge cases in invite and membership flows during testing.
