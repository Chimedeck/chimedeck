## Update
- Integrated state transition graph sync with list rename/delete hooks in list API modules.
- Updated state transitions routing to support `/rules` endpoint and restrict `PUT` to base path.
- Enhanced validator to preserve unknown-list validation path for API compatibility.

## New
- Added `server/extensions/stateTransitions/api/getRules.ts` for GET `/rules` endpoint.
- Added `server/extensions/stateTransitions/common/sync.ts` with `updateNodeLabels` and `stripDeletedNodes` helpers.
- Created/updated tests: `put_ws.test.ts`, `integration_list_sync.test.ts`, `sync.test.ts` for WS, integration, and unit coverage.

## Technical Debt
- Error envelope format is not fully unified across modules.
- Real-time sync and event handling complexity remains; further refactoring may be needed as features expand.

## What Should Be Done Next
- Fix login flow to unblock UI and end-to-end state transition tests.
- Implement copy endpoint, enforcement logic, and additional real-time/collab features in the next iteration.
- Continue improving error handling and real-time sync robustness.