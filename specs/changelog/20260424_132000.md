# Changelog 20260424_132000

## Update

- Fixed attachment internal-link classification regression by expanding card URL parsing to handle both current and legacy internal route formats.
- Updated `parseInternalCardUrl` on both server and client attachment panel logic so internal card links resolve to `referenced_card_id` and render under related cards instead of external links.

## New

- Added unit test coverage for `parseInternalCardUrl` to validate:
  - `/c/:cardId[/slug]`
  - `/boards/:boardId/cards/:cardId[/slug]`
  - `/b/:boardId?card=:cardId`
  - non-card URL rejection

## Technical Debt

- Internal URL parsing is duplicated between server (`addUrl.ts`) and client (`AttachmentPanel.tsx`).
- A shared parsing utility module should be introduced to keep behavior synchronized and prevent drift.

## What Should Be Done Next

- Add a small client unit test for the attachment panel parser logic to guard against future route-shape regressions.
- Validate manually in production/staging with both modern and legacy copied card links to confirm they consistently appear in the related-card section.
