# Changelog — 20260319_045702

## Update
- All five Comment extension components now use `translations['Key']` bracket notation instead of hardcoded English strings.

## New
- `src/extensions/Comment/translations/en.json` — 49 translation keys covering all visible UI copy in the Comment extension: section title, empty state, editor placeholder and labels, action buttons (Edit, Delete, Update, Comment), draft/offline status messages, asset picker header and item labels, aria-labels, and error messages.
- `specs/tests/sprint-90-iteration-1-comment-i18n.md` — Playwright MCP markdown test with 6 scenarios covering the full Comment user flow (empty state, add, edit, delete, asset picker, no regressions).

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 2: Activity Extension i18n — create `src/extensions/Activity/translations/en.json` and update all Activity components to use `translations['Key']`.
