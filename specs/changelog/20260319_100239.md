# Changelog — 2026-03-19 10:02:39

## Update

- Replaced all hardcoded English UI strings in `src/extensions/CalendarView/` with `translations['Key']` bracket-notation lookups:
  - `CalendarView.tsx`: `aria-label="Calendar mode"`, `"Month"`, `"Week"`, `"Cards without a due date are not shown."`
  - `CalendarMonthGrid.tsx`: `aria-label="Previous month"`, `aria-label="Next month"`
  - `CalendarWeekGrid.tsx`: `aria-label="Previous week"`, `aria-label="Next week"`
  - `CalendarDayCell.tsx`: `aria-label="Today"`, `"+N more"` overflow button, `"Show less"` collapse button
  - `CalendarCardChip.tsx`: `"Card: "` prefix in chip `aria-label`

## New

- `src/extensions/CalendarView/translations/en.json` — per-feature translation file with 15 keys covering all CalendarView UI strings:
  - Navigation aria-labels (`ariaPrevMonth`, `ariaNextMonth`, `ariaPrevWeek`, `ariaNextWeek`)
  - Mode toggle labels (`monthView`, `weekView`, `ariaMode`)
  - Day cell labels (`todayButton`, `overflowMore`, `showLess`)
  - Card chip prefix (`cardAriaPrefix`)
  - Toolbar note (`noDueDateNote`)
  - Reserved keys from sprint spec (`noCards`, `unscheduled`, `allDayLabel`) for future use
- `specs/tests/sprint-93-calendarview-i18n.md` — Playwright MCP test plan for CalendarView i18n verification

## Technical Debt

- `CalendarView.noCards` (`"No cards due this month"`), `CalendarView.unscheduled` (`"Unscheduled"`), and `CalendarView.allDayLabel` (`"All day"`) are defined in en.json but not yet rendered in the UI — they are reserved for planned empty-state and unscheduled-section features.

## What Should Be Done Next

- Iteration 3: TimelineView i18n — extract hardcoded strings from `src/extensions/TimelineView/` and create its `translations/en.json`.
- When the unscheduled cards section and empty-state are added to CalendarView, wire up `noCards`, `unscheduled`, and `allDayLabel` keys.
