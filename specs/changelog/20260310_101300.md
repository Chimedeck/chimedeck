## Update
- Replaced all `uuidv4()` usages in `server/extensions/auth/` (login, register, adminCreateUser, verifyEmail, oauth/callback, token/refresh) with a central `generateId()` helper using `uuidv7()`.
- Updated imports to use `generateId` from `server/common/uuid.ts`.
- Verified zero remaining `uuidv4` references in `server/extensions/` and `server/mods/`.
- Confirmed server starts without import/runtime errors.

## New
- Added `server/common/uuid.ts` module exporting `generateId()` (wraps `uuidv7()` from the uuid package).

## Technical Debt
- None

## What Should Be Done Next
- Replace `uuidv4` in `db/seeds/` and any remaining files outside `server/extensions/` and `server/mods/`.
- Implement board stars/followers, board-level views, starred boards UI, and related tests.
- Complete uuidv7 migration for all code paths (including seeds/tests if needed).