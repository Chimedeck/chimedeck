## Update
- Modified `src/extensions/Card/components/CardModal.tsx` to integrate the new `CardMetaStrip` below the title/breadcrumb, wiring all label/member/date props.

## New
- Added `src/extensions/Card/components/CardMetaStrip.tsx`: compact meta strip showing up to 3 label chips (+ overflow), 3 member avatars (+ overflow), start/due date buttons, and "+ Labels"/"+ Members" when empty. Integrated existing popovers for label/member/date editing. All actions respect the `disabled` prop.
- Created `src/extensions/Card/components/CardMetaStrip.mcp.md`: Playwright MCP test spec covering UI and popover integration scenarios.

## Technical Debt
- None

## What Should Be Done Next
- Convert CardMetaStrip.mcp.md scenarios into executable Playwright test code (.ts).
- Run Playwright tests for CardMetaStrip and verify UI flows.
- Defer CardDescription inline editing and CardModalBottomBar integration to next iteration.
