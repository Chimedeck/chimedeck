# 20260323_093737 — Sprint 106 Iteration 1: MCP Tool Registration Extraction & Session Store

## Update
- `server/extensions/mcp/index.ts`: Refactored to use `registerMcpTools()` instead of inline tool registrations.

## New
- `server/extensions/mcp/registerTools.ts`: Shared helper that registers all 6 MCP tools (move-card, write-comment, create-card, edit-description, set-price, invite-to-board) on any `McpServer` instance.
- `server/extensions/mcp/http/sessions.ts`: In-memory session store (`Map<string, McpSession>`) with 30-minute TTL eviction loop (runs every 5 minutes). Exports `sessions` map and `McpSession` interface for use by the HTTP handler (Iteration 2).

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 2: Implement `server/extensions/mcp/http/index.ts` (mcpHttpHandler) and wire it into `server/index.ts`.
