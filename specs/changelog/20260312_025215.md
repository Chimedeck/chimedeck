# Changelog — 20260312_025215

## Update

- `src/extensions/Automation/components/AutomationPanel/index.tsx` — wired in `RuleBuilder` for both create and edit modes; `editingRule` state tracks `null` (list view), `undefined` (new rule), or `Automation` (edit mode).

## New

- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/index.tsx` — multi-step rule builder; handles create and edit; calls `createAutomation` / `updateAutomation` on save.
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/TriggerPicker.tsx` — searchable dropdown fetching trigger types from `GET /automation/trigger-types`.
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/TriggerConfig.tsx` — dynamic form fields for the selected trigger's `configSchema`.
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/configFieldRenderer.tsx` — shared helper rendering a single config field (select / boolean / number / text fallback).
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/ActionList.tsx` — drag-and-drop ordered action list using `@dnd-kit/core`; inline config toggle per action.
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/ActionPicker.tsx` — grouped, searchable dropdown fetching action types from `GET /automation/action-types`.
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/ActionConfig.tsx` — dynamic config form for a selected action type.
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/ActionItem.tsx` — single draggable action row with drag handle (`@dnd-kit/sortable`), config summary, and delete button.
- `src/extensions/Automation/components/AutomationPanel/RuleBuilder/RuleBuilderFooter.tsx` — rule name input + Save / Cancel buttons.
- `src/extensions/Automation/reducers.ts` — `useOptimisticAutomations` hook for optimistic enable/disable toggle and delete with rollback on API error.
- `tests/e2e/automation/rulesPanel.md` — Playwright MCP test covering open panel, create rule, toggle enable/disable, delete rule, cancel creation, "coming soon" tabs, and drag-to-reorder.

## Technical Debt

- `TriggerConfig` and `ActionConfig` share nearly identical rendering logic via `configFieldRenderer`; if more field types are added, consider extracting a dedicated `DynamicForm` component.
- `useOptimisticAutomations` in `reducers.ts` duplicates some state management that could be folded into an RTK Query cache update in a future refactor.

## What Should Be Done Next

- Sprint 66: Card Buttons UI + server run endpoint (`CardButtonsSection`, `CardButtonItem`, `AddCardButtonButton`, `CardButtonBuilder`, shared `IconPicker`, `POST /api/v1/cards/:cardId/automation-buttons/:automationId/run`).
