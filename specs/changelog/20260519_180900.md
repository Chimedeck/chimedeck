## Update
- Added Graph Editor header **Copy to Board** action with a new modal flow that loads workspaces, filters target boards to ADMIN/OWNER access, supports `copyEnabled`, and executes the copy mutation.
- Extended the state transitions client API with `copyStateTransitions` and copy response metadata (`skippedNodes`, `copyEnabled`).
- Extended server `POST /api/v1/boards/:boardId/state-transitions/copy` to accept `copyEnabled`, preserve target `enabled` when requested, and return metadata with skipped node count.
- Mounted state transitions graph editor entry points in Kanban via banner/rules actions and query param (`openStateTransitionsEditor=1`) support.
- Added dismissible **Transitions Active** banner logic scoped per board session key (`state-transitions-banner-dismissed-{boardId}`).

## New
- Added `CopyTransitionsModal.tsx`.
- Added `TransitionsActiveBanner.tsx`.
- Added `useTransitionsBanner.ts` hook + helper tests.
- Added manual QA specs:
  - `specs/tests/stateTransitions/copy-to-board-modal.md`
  - `specs/tests/stateTransitions/transitions-active-banner.md`
- Added/updated state transitions translations for copy modal, toast copy, and active banner text/actions.
- Added server test coverage for `copyEnabled=false` behavior and copy metadata assertions.

## Technical Debt
- Repository-wide lint/typecheck baseline remains noisy outside this scope; validation for this iteration was run with focused StateTransitions tests and targeted lint scope.

## What Should Be Done Next
- Add integration/UI tests that cover the copy modal workspace/board filtering and banner dismissal persistence through real browser interactions.
