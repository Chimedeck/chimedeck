## Update
- Added the Board Settings UI wiring for the GitHub Project URL integration.
- Passed board/workspace permission context into settings so the field can be gated in the UI.
- Added typed board integrations API helpers and translation strings for the new setting.
- Added test coverage for the new settings component and its save/validation flows.

## New
- `src/extensions/Board/containers/BoardSettings/GithubProjectUrlSetting.tsx`
- `src/extensions/Board/containers/BoardSettings/__tests__/BoardSettings.githubProjectUrl.test.tsx`
- GitHub Project URL field with load, edit, clear, validation, and save behavior.
- Visible-but-disabled state for users who can view the setting but should not edit it.

## Technical Debt
- The non-admin workspace-member path still behaves inconsistently: the field remains editable and does not show the unauthorized state in live UI.
- Save flow relies on the existing optimistic update pattern and should be revisited if settings interactions expand.

## What Should Be Done Next
- Fix the non-admin gating bug for the GitHub Project URL setting.
- Continue with iteration 11: repository fetch service with GitHub App authentication.
