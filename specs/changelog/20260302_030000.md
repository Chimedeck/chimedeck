# Changelog — 20260302_030000

## Update

- `README.md` — extended with application setup instructions, Docker Compose usage, feature flag docs, and available scripts.
- `.gitignore` — already covered by existing file.
- `package.json` — removed `@prisma/client` + `prisma`; added `knex@^3.1.0`, `pg@^8.13.0`, `@types/pg@^8.11.0`, `@aws-sdk/client-s3@^3.0.0`, `@aws-sdk/s3-request-presigner@^3.0.0`; db scripts updated (`db:migrate` → `knex migrate:latest`, `db:rollback`, removed `db:studio` / `db:generate`).
- `docker-compose.yml` — replaced `minio` service with `localstack` (`localstack/localstack:latest`, port 4566, `SERVICES: s3`); volume renamed `minio_data` → `localstack_data`; S3 env vars updated to `AWS_ACCESS_KEY_ID=test` / `AWS_SECRET_ACCESS_KEY=test` / `S3_ENDPOINT=http://localstack:4566`.
- `.env.example` — MinIO vars removed; AWS S3 vars added (`AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `S3_ENDPOINT`); `FLAG_USE_LOCAL_STORAGE=true` section added.
- `server/config/env.ts` — `S3_ACCESS_KEY` / `S3_SECRET_KEY` replaced with `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`; `S3_ENDPOINT` default changed to `''`.
- `server/mods/flags/defaults.ts` — added `USE_LOCAL_STORAGE: true` (safe dev default; switches storage target between LocalStack and AWS S3).
- `Dockerfile` — removed `RUN bunx prisma generate`; `COPY prisma/` → `COPY db/`.
- `specs/architecture/technical-decisions.md` — §2 rewritten (Prisma → Knex rationale); §8 rewritten (MinIO → LocalStack + `USE_LOCAL_STORAGE` flag table).
- `specs/architecture/architecture.md` — stack table and server folder structure updated to reflect Knex and LocalStack.
- All sprint spec files (sprint-plan.md, sprint-01.md through sprint-08.md, sprint-13.md) — Prisma DSL schema blocks replaced with Knex TypeScript migration blocks; MinIO references replaced with LocalStack.

## New

- `package.json` — Bun runtime, scripts (`dev`, `lint`, `typecheck`, `db:migrate`, `test`), dependencies (`knex`, `pg`, `node-cache`, `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`), devDependencies (`eslint`, `typescript`, `prettier`, `bun-types`).
- `tsconfig.json` — TypeScript strict mode, `moduleResolution: bundler`, targets Bun.
- `eslint.config.js` — ESLint flat config with `@typescript-eslint/strict-type-checked`, `no-console` warning, unused-var error.
- `.prettierrc` — Prettier config (single quotes, semi, 100 print width).
- `docker-compose.yml` — services: `app` (Bun), `postgres` (PostgreSQL 16 with healthcheck), `localstack` (S3-compatible local storage), `redis` (Redis 7, optional via `profiles: [redis]`).
- `.env.example` — all required and optional env vars documented, including Redis and non-Redis modes, feature flag sources, and `FLAG_USE_LOCAL_STORAGE`.
- `Dockerfile` — Bun image, installs deps, copies `db/`, starts server.
- `server/index.ts` — `Bun.serve` entry point; loads feature flags, routes `/health` and `/api/v1/flags`, applies security headers and request logging.
- `server/config/env.ts` — single file centralising all `Bun.env` access.
- `server/config/app.ts` — app config (port, isDev).
- `server/config/flags.ts` — wires env vars into `flagsConfig` consumed by flags module.
- `server/mods/logger.ts` — request logging middleware.
- `server/mods/helmet.ts` — security headers middleware.
- `server/middlewares/parser/index.ts` — JSON body parser.
- `server/mods/openapi/index.ts` — route registry stub for documentation.
- `server/mods/flags/types.ts` — `FlagProvider` interface and `FlagContext` type.
- `server/mods/flags/defaults.ts` — hardcoded fallback values for all known flags (including `USE_LOCAL_STORAGE`).
- `server/mods/flags/index.ts` — singleton `flags` object backed by `CompositeFlagProvider`.
- `server/mods/flags/providers/env.ts` — reads `FLAG_<KEY>=true|false` from `Bun.env`.
- `server/mods/flags/providers/json.ts` — parses JSON file at `FEATURE_FLAGS_JSON_PATH`, hot-reloads every 30 s.
- `server/mods/flags/providers/flagsmith.ts` — Flagsmith SDK stub (no-op until SDK installed).
- `server/mods/flags/providers/featbit.ts` — FeatBit SDK stub (no-op until SDK installed).
- `server/mods/flags/providers/composite.ts` — merges sources: defaults → json → env → remote.
- `server/mods/flags/flags.test.ts` — unit tests for flag defaults (2 passing).
- `db/knexfile.ts` — Knex config; reads `DATABASE_URL`, points migrations at `db/migrations/`, uses `pg` client, loads `.ts` extensions via Bun.
- `db/migrations/0001_init.ts` — empty baseline migration establishing Knex migration history.
- `.github/workflows/ci.yml` — CI: install, typecheck, lint, test, docker build.

## Technical Debt

- `flagsmith.ts` and `featbit.ts` are stubs; actual SDK calls are commented out with `TODO`. SDKs will be wired in when a remote feature flag provider is chosen.
- `server/middlewares/parser/index.ts` uses a loose type signature to avoid a Bun/undici type conflict; should be tightened once `bun-types` exposes a proper `Request.clone()` return type.
- Storage module (reading `USE_LOCAL_STORAGE` flag and returning a configured `@aws-sdk/client-s3` instance pointing at either LocalStack or AWS) has not been implemented yet — needed from Sprint 02 onward for file upload routes.
- `db:seed` script reference in `package.json` points to `db/seeds/run.ts` which does not exist yet.

## What Should Be Done Next

**Sprint 02 — Build Tooling & Docker (iteration 2):**
Read `specs/sprints/sprint-02.md` for full scope. Key tasks:

- Verify and finalise `Dockerfile` (multi-stage build, non-root user, `COPY db/`).
- Confirm `docker-compose.yml` dev setup: LocalStack health check, `app` depends-on order.
- Add `composePromises` / functional composition utility to `server/utils/`.
- Add CORS middleware (`server/middlewares/cors/`) with origin allow-list from env.
- Implement the storage factory module (`server/mods/storage/`) — returns a configured S3 client pointing at LocalStack (`FLAG_USE_LOCAL_STORAGE=true`) or AWS (`false`).
- Ensure `bun run typecheck`, `bun test`, and `docker compose build` all pass as acceptance criteria.

**Subsequent sprints (03–08):** Authentication → Workspaces → Boards → Lists → Cards (basic) → Cards (extended). Each sprint's data model is a Knex migration file in `db/migrations/`.
