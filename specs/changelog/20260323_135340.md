## Update
- Extended `server/extensions/auth/middlewares/authentication.ts` to support `hf_` API token authentication: hashes incoming tokens, checks against `api_tokens` joined with `users`, denies if revoked/expired, updates `last_used_at` non-blocking.
- JWT authentication path remains unchanged; both methods are now handled via prefix dispatch.

## New
- Added `specs/tests/api-token-auth.md`: Playwright MCP test spec covering valid, revoked, expired, and cross-user API token scenarios.

## Technical Debt
None.

## What Should Be Done Next
- Investigate why Playwright did not recognize or execute the new `.md` test spec; verify test file format and Playwright config for correct test discovery.
- Update `playwright.config.ts` to include `tests/e2e/` and/or ensure `.md` specs are supported.
- Complete deferred client-side polish and additional integration tests for notification preferences UI.
- Implement and wire new email templates for `cardUpdated`, `cardDeleted`, and `cardArchived` (Sprint 99 outstanding).
- Continue with the next planned iteration per sprint plan.
