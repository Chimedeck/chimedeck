# Changelog — 2026-03-18T12:15:00Z

## Update
- `src/extensions/Card/config/activityEventsConfig.ts`: Added four new event types (`card_created`, `card_moved`, `card_member_assigned`, `card_member_unassigned`) to the `VISIBLE_ACTIVITY_EVENT_TYPES` allowlist.
- `src/extensions/Card/config/activityEventLabels.ts`: Updated `getActivityEventMeta` to accept an optional `ActivityEventContext` parameter (with `resolveName` and `currentUserId`) and added human-readable label rendering for all four new event types. Dynamic list names from payload are interpolated for `card_moved`; self-assignment/removal copy uses "themselves" when `actor_id === currentUserId`.
- `src/extensions/Card/containers/CardModal/ActivityFeed.tsx`: Passes `ActivityEventContext` (member name resolver + current user id) to `getActivityEventMeta` so labels resolve board member names and detect self-assignments.

## New
- `specs/tests/card-activity-create-move-assign.md`: Added Section 8 with 10 Playwright MCP UI assertion scenarios covering correct label copy for all four event types, self-assignment edge cases, graceful fallback for unknown events, and member-left-the-board defensive rendering.

## Technical Debt
- None introduced.

## What Should Be Done Next
- Iteration 9: Wire realtime activity updates so open card modals receive new activity rows without a page refresh.
