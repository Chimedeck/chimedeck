## Update
- Added board chat guest-permission controls to the drawer, including locked states for non-owner/non-admin users.
- Wired the board page to pass guest/admin context into the chat drawer so the UI can enforce access correctly.
- Updated guest chat permission handling so view/use toggles stay coupled and rollback cleanly on failed saves.
- Added server-side guest chat permission guards with deny-by-default behavior and typed 403 responses.
- Expanded board chat tests to cover owner, guest, and locked-member permission flows.

## New
- New `src/extensions/BoardChat/api.ts` helpers for fetching and patching board chat permissions.
- New reusable `server/extensions/board/middlewares/chatPermissions.ts` guest access guards.
- New pure permission helpers in `server/extensions/board/mods/chatPermissions.ts`.
- New changelog and test coverage for the board chat permission matrix.

## Technical Debt
- Board chat history is still not backed by a full message storage/retrieval implementation.
- The drawer still depends on live permission fetching, so it should be revisited with future pagination/history work.

## What Should Be Done Next
- Implement board chat message storage and retrieval for the drawer history.
- Add send/composer backend wiring and pagination or infinite scroll for chat history.
- Revisit the chat drawer once the live message pipeline exists to align empty, loading, and error states with real data.
