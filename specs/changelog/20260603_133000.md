# Changelog - 2026-06-03 13:30:00

## Update

- Updated documentation to define the markdown editor entrypoint as a board-level `Documentation` tab/button.
- Locked tab placement so `Documentation` appears directly next to `Health Check`.

## New

- Added `Documentation` tab navigation requirements to `specs/sprints/sprint-170.md`, including route and visibility rules.
- Updated Sprint 170 summary wording in `specs/sprints/sprint-plan.md` to include the new tab entrypoint.
- Updated `DESIGN.md` GitHub docs workflow to include `Documentation` tab placement next to `Health Check`.

## Technical Debt

- Tab overflow behavior is not yet specified for boards with many enabled tabs on small widths.
- Fallback UX when `GitHub Project URL` is missing is currently hidden-state only; discoverable setup CTA can be added later.

## What Should Be Done Next

- Add responsive tab overflow rules (scroll/"More" menu) to board navigation specs.
- Add an optional empty setup CTA in board settings when `Documentation` is hidden due to missing GitHub URL.
- Add E2E scenario to assert `Health Check` and `Documentation` adjacency ordering.
