## Update
None

## New
- Added three Playwright E2E spec files for Iteration 16:
  - `tests/e2e/search-filters.spec.ts`: Covers keyword search, type filter, board-scoped search, 401 guard, UI search box, and UI type toggle.
  - `tests/e2e/list-management.spec.ts`: Covers list creation, renaming, reordering (drag), archiving, 401 guard, UI add-list, UI rename, and drag-to-reorder.
  - `tests/e2e/presence-board.spec.ts`: Covers GET/POST/DELETE presence endpoints, two-user presence propagation, 401 guard, and two-browser-context UI avatar test.
- All 20 tests are recognized by Playwright and soft-skip if the server is not running or endpoints are unimplemented.

## Technical Debt
- E2E tests depend on the dev server being available and returning expected responses; tests currently fail if the server is down or login endpoint is broken.
- No additional test data isolation beyond unique names per run; may need further isolation for parallel runs.

## What Should Be Done Next
- Ensure the dev server is running and the login endpoint returns the expected response, then re-run all E2E tests.
- Investigate and fix the `TypeError: Cannot read properties of undefined (reading 'access_token')` in `registerAndLogin` if it persists.
- Review and update selectors/flows in E2E specs if the UI has changed since last reference.
- Expand test data isolation if flakiness is observed in CI or parallel runs.