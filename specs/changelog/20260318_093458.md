## Update
- Extended `src/extensions/Search/api.ts` with types and a `searchBoard()` function for board-scoped search.
- Modified `src/extensions/Board/components/BoardHeader.tsx` to accept and pass `searchToken` and `onSearchResultSelect` props, rendering the search bar when a token is present.
- Updated `src/extensions/Board/containers/BoardPage/BoardPage.tsx` to provide `accessToken` as `searchToken` to `BoardHeader`.
- Expanded `specs/tests/board-scoped-search.md` with 10 UI scenarios and updated acceptance criteria checklist.

## New
- Added `src/extensions/Board/components/BoardSearchBar.tsx`: a fully accessible, debounced, board-scoped search bar with clear/escape, min-char guard, dropdown results, and empty state panel.

## Technical Debt
- No automated Playwright UI tests exist for BoardSearchBar or board search flows; only documentation/specs were updated. Manual or new test implementation is required for verification.

## What Should Be Done Next
- Implement Playwright UI tests for BoardSearchBar and board-scoped search scenarios as outlined in `specs/tests/board-scoped-search.md`.
- Ensure all new flows are covered by automated tests and update documentation/specs as needed.
- Review accessibility and UX for edge cases and polish as required.