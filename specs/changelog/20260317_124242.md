## Update
- Expanded `specs/tests/offline-description-draft-recovery.md` with 17 new client-side scenarios (IndexedDB, API wrapper, reconciliation, error handling).

## New
- Created `src/extensions/OfflineDrafts/storage.ts`: IndexedDB draft storage layer.
- Created `src/extensions/OfflineDrafts/api.ts`: Typed client API wrapper for server draft endpoints.
- Created `src/extensions/OfflineDrafts/reconcile.ts`: Last-write-wins merge logic for local/server drafts.

## Technical Debt
- No Playwright E2E tests yet for offline draft flows—test scenarios exist only in Markdown.
- Error handling for IndexedDB failures is silent; UI may not surface local persistence issues.

## What Should Be Done Next
- Implement Playwright E2E tests for offline draft flows in `tests/e2e/` (e.g., `offline-draft-recovery.spec.ts`).
- Integrate offline draft infrastructure into `CardDescriptionTiptap` and `CommentEditor` components.
- Review and improve error handling/reporting for local storage failures.