## Update
- Added the Sprint 170 save/commit workflow support across board specs handling, including delta-save requests with `If-Match` preconditions and specs-only write handling.
- Updated the developer docs workspace to autosave editor changes and expose commit actions from the Documentation tab.
- Extended board specs API coverage for save and commit behavior, plus the associated client workspace tests.

## New
- New board specs endpoints for saving markdown files and creating commits under `/api/v1/boards/:id/github/specs/file` and `/api/v1/boards/:id/github/specs/commit`.
- New shared specs write/commit helpers and cache invalidation support in the board server modules.
- New end-to-end flow coverage for the specs edit/save/commit path.

## Technical Debt
- Live browser verification of the specs workflow remains blocked when GitHub App configuration is missing, so the Documentation tab cannot fully load in that environment.
- The repo still has unrelated baseline typecheck noise outside this iteration's changes.

## What Should Be Done Next
- Finish the remaining GitHub App-backed live verification for the specs save/commit flow once backend configuration is available.
- Revisit any remaining Sprint 170 polish around stale-precondition UX and commit metadata if further iteration work is needed.
