## Update
- Standardized state transition rules API responses to camelCase keys.
- Updated `GET /api/v1/boards/:boardId/state-transitions/rules` serialization from snake_case to camelCase for all rule fields.
- Updated frontend state transition rules typings and guard logic to consume camelCase fields.
- Updated server and client test fixtures/assertions for the camelCase response contract.

## New
- No new endpoints were added; this iteration changed payload field naming for the existing rules endpoint.

## Technical Debt
- Internal server rule model in enforcement code still uses snake_case for derived rule objects; only the API response contract is camelCase.
- If desired, internal model naming could be migrated in a separate refactor to reduce dual-shape cognitive overhead.

## What Should Be Done Next
- Add explicit OpenAPI schema for rules payload fields (currently generic response schema) to document camelCase contract in Swagger response body models.
- Add a contract test that snapshots rules endpoint JSON keys to prevent accidental snake_case regressions.
