## Update
- Extended `server/extensions/activity/mods/createActivityEvent.ts` to add emitters for `card_moved`, `card_member_assigned`, and `card_member_unassigned` events, with type-safe payloads and no-op guards.
- Updated `server/extensions/card/api/move.ts` to emit `card_moved` activity events after card moves, including list name resolution and event payload construction.
- Refactored `server/extensions/card/api/members.ts` to emit assignment/unassignment activity events only on actual changes, with improved context resolution and idempotency.
- Added new event types to `server/extensions/activity/config/visibleEventTypes.ts` so they appear in activity feeds.

## New
- Created `specs/tests/card-activity-create-move-assign.md` with detailed scenarios for card move, assignment, and unassignment activity event emission, including edge cases and payload schema assertions.

## Technical Debt
- Playwright E2E tests for these scenarios are not yet implemented as executable `.ts` files; only markdown specs exist.
- Some type errors and Playwright/Bun incompatibility persist elsewhere in the codebase.
- Focus trap for mobile drawer remains custom and may have edge cases.

## What Should Be Done Next
- Scaffold and implement Playwright `.ts` test files for card activity event flows described in the markdown spec.
- Complete E2E coverage for board star/follow and board deletion flows.
- Migrate all UUID usage to `uuidv7`.
- Implement client UI for activity feed rendering and real-time updates.
- Address outstanding type errors and Playwright/Bun compatibility issues.
