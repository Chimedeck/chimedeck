# Changelog — 20260310_065046

## Update
- DB migration `0031_user_board_view_prefs.ts` added and applied. Creates `user_board_view_prefs` table with `user_id`, `board_id`, `view_type` (KANBAN/TABLE/CALENDAR/TIMELINE enum), `updated_at`, and unique constraint on `(user_id, board_id)`.

## New
- `server/extensions/boardView/` — new server extension for view preference persistence:
  - `types.ts` — `ViewType` enum and `ViewPreference` interface
  - `model.ts` — `getViewPreference` and `upsertViewPreference` DB access functions
  - `api/get.ts` — `GET /api/v1/boards/:id/view-preference` handler (auth required, defaults to KANBAN)
  - `api/put.ts` — `PUT /api/v1/boards/:id/view-preference` handler (auth required, validates view type)
  - `api/index.ts` — route dispatcher matching the view-preference sub-path
  - `index.ts` — extension entry point re-exporting the router
  - `README.md` — API and DB schema documentation
- `tests/integration/boardViewPreference.test.ts` — 6 handler-level tests covering auth guards and basic validation (all pass)

## Technical Debt
- The `tests/integration/board/starFollow.test.ts` and `boardVisibility.test.ts` files contain 11 pre-existing test failures (checking `body.name` instead of `body.error.code`). These existed before this sprint and are unrelated to the view preference work.

## What Should Be Done Next
- Iteration 2: Implement `src/extensions/BoardViewSwitcher/` (React UI) with RTK Query API client and Redux slice. Mount switcher in board page toolbar.
