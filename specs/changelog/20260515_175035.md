## Update
- Fixed realtime `comment_reaction` notifications so comment preview content arrives in the live `notification_created` payload without requiring a refresh.
- Updated notification Redux handling to upsert existing notifications by `id` when websocket events arrive, instead of ignoring duplicates.

## New
- `comment_reaction` live payload now includes `comment_content` sourced from the reacted comment.
- Realtime upsert now preserves existing hydrated fields (`comment_content`, `comment_reactions`) when incremental websocket payloads omit them.

## Technical Debt
- `notification_created` payload assembly is currently duplicated across multiple server modules (`createNotifications`, `boardActivityDispatch`, `createReactionNotification`) and can drift in field completeness.

## What Should Be Done Next
- Extract a shared server-side notification payload mapper to keep websocket payload fields consistent across all notification types.
- Add an integration test that verifies `comment_reaction` preview appears immediately after a reaction event (without refetch).
