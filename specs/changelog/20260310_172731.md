# Changelog — 20260310_172731

## Update

- `server/extensions/board/middlewares/requireBoardWritable.ts` — updated error code from `board-archived` to `board-is-archived` (403) to match spec
- `server/extensions/card/middlewares/requireCardWritable.ts` — same error code update
- `server/extensions/card/api/delete.ts` — added archived board guard (this handler lacked the check)
- `server/extensions/comment/api/create.ts` — added archived board guard after board resolution
- `server/extensions/comment/api/update.ts` — added archived board guard after board resolution
- `server/extensions/comment/api/delete.ts` — added archived board guard after board resolution
- `server/extensions/customFields/api/cardValues.ts` — added archived board guard to both `handleUpsertCardFieldValue` and `handleDeleteCardFieldValue`
- `server/extensions/workspace/api/members/remove.ts` — changed error code from `workspace-must-have-owner` (409) to `workspace-must-have-one-owner` (422) per spec
- `server/extensions/workspace/api/members/updateRole.ts` — same error code and status update

## New

- `server/middlewares/archivedBoardGuard.ts` — standalone middleware utility with `requireBoardNotArchived(boardId)`, `resolveBoardFromCard(cardId)`, and `resolveBoardFromComment(commentId)` helpers for use in any handler that needs to guard mutations on archived boards
- `tests/e2e/business-logic-invariants-1.md` — Playwright MCP markdown test covering 403 on archived board mutations and 422 when removing/demoting the last workspace OWNER

## Technical Debt

- The `requireBoardWritable` and `requireCardWritable` middlewares already enforced the archived-board guard for lists and most card mutations; only the DELETE card, comment mutations, and custom-field-value mutations were missing the check. The new `archivedBoardGuard.ts` provides a canonical helper for future handlers that don't go through those middleware paths.

## What Should Be Done Next

- Iteration 6: Implement delete confirmation flag (Sprint 56) — server-side 409 for board/list DELETE without `confirm:true`, plus client-side dialog updates.
