# Changelog — 20260319_054814

## Update

- Updated all `.tsx` files in `src/extensions/Plugins/components/` and `src/extensions/Plugins/modals/` to replace hardcoded English strings with `translations['Key']` bracket notation, importing `../translations/en.json` statically.
- Updated `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.tsx` to use translations for all visible UI copy (page title, back button, register button, loading state, no-match/clear-search copy).

## New

- Created `src/extensions/Plugins/translations/en.json` with 110+ keys covering all Plugins extension UI namespaces:
  - `plugins.dashboard.*` — Dashboard page header, loading, no-match states
  - `plugins.searchBar.*` — Search placeholder and category dropdown
  - `plugins.list.*` — Active/available section headings and empty states
  - `plugins.card.*` — Enable/Disable/Edit/Settings button labels and aria-labels
  - `plugins.capabilities.*` — Chip labels for all plugin capabilities
  - `plugins.allowedDomains.*` — Allowed Domains panel heading, description, save states
  - `plugins.registerModal.*` — Register modal title, field labels, placeholders, validation, server errors, buttons
  - `plugins.editModal.*` — Edit modal title, field labels, placeholders, validation, server errors, buttons, whitelisted domains
  - `plugins.apiKeyModal.*` — API Key reveal modal title, warning, copy/done buttons
  - `plugins.modal.*` — Generic plugin iframe modal default title and close aria-label
  - `plugins.popup.*` — Plugin popup default title and close aria-label
- Created `specs/tests/sprint-92-iteration-5-plugins-i18n.md` — Playwright MCP markdown test covering all 14 Plugins UI flows.

## Technical Debt

None introduced.

## What Should Be Done Next

- Consider extracting `by` (author attribution) into a more expressive interpolated key if i18n needs full sentence support in other locales.
- Future: when a second locale is added, replace the static JSON import with a locale-aware loader.
