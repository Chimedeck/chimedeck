# Changelog — 20260319_095046

## Update

- **CustomFields i18n (Sprint 93 — Phase 4, Iteration 1)**  
  All hardcoded English UI strings in the CustomFields extension have been extracted into a new per-feature translations file and replaced with `translations['Key']` bracket-notation lookups. No i18n library was introduced; the existing static-import pattern is used throughout.

## New

- `src/extensions/CustomFields/translations/en.json` — new translation file containing 38 keys covering all UI copy in the CustomFields extension: panel title, empty state, field type labels, form field placeholders, button labels, aria-labels, loading states, show-on-card toggle, dropdown options editor copy, and card value editor copy.

- `specs/tests/sprint-93-customfields-i18n.md` — Playwright MCP test plan for verifying that all CustomFields UI copy is rendered from translations (no hardcoded English strings).

## Technical Debt

- `CustomFields.deleteConfirm` uses a `.replace('{fieldName}', field.name)` pattern for dynamic interpolation (no i18n library). A proper interpolation helper should be introduced in a future sprint if more keys require variable substitution.

## What Should Be Done Next

- Iteration 2: Sprint 93 — CalendarView i18n (create `src/extensions/CalendarView/translations/en.json` and update all `.tsx` files in that extension).
- Run Playwright MCP against `specs/tests/sprint-93-customfields-i18n.md` to confirm all CustomFields UI copy is served from translations.
