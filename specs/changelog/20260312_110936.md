## Update
- Updated `server/extensions/automation/config/index.ts` to add `monthlyQuota` (reads `AUTOMATION_MONTHLY_QUOTA`, defaults to 1000).
- Registered new GET routes in `server/extensions/automation/api/index.ts` for run log, board-wide run history, and quota endpoints.

## New
- `server/extensions/automation/api/runs.ts`: Implements GET /api/v1/boards/:boardId/automations/:automationId/runs (paginated, status filter).
- `server/extensions/automation/api/boardRuns.ts`: Implements GET /api/v1/boards/:boardId/automation-runs (last 200 runs, paginated).
- `server/extensions/automation/api/quota.ts`: Implements GET /api/v1/boards/:boardId/automation-quota (returns usedRuns, maxRuns, resetAt, percentUsed; emits quota_warning event at ≥80%).
- `tests/integration/automation/runs.test.ts`: Integration tests for run log endpoints (pagination, status filter, cap).
- `tests/integration/automation/quota.test.ts`: Integration tests for quota endpoint (usage, reset, config, edge cases).

## Technical Debt
- No Playwright E2E tests exist for new automation run log, board-wide run history, or quota UI flows.
- Deferred UI/UX and WebSocket integration for quota warnings.

## What Should Be Done Next
- Add Playwright `.spec.ts` E2E tests for automation run log, board-wide run history, and quota UI flows.
- Implement UI Log tab, badges, and WebSocket quota warning integration.
- Address deferred error handling and technical debt from previous sprints.
