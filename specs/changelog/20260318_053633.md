# Changelog — 2026-03-18T05:36:33Z — Sprint 88 Iteration 11: Regression and acceptance hardening

## Update

- `specs/tests/card-activity-create-move-assign.md`: Added Sprint/Feature/Status metadata header. Added a comprehensive 33-item **Acceptance Checklist** covering all four event types, feed ordering, UI rendering, realtime delivery, and notification fan-out. Added **Section 11 — Regression and acceptance hardening** with 10 new Playwright MCP scenarios (11.1–11.10) covering: API error state rendering, large-feed non-truncation, failed-creation no-activity guarantee, non-blocking activity write failure, VIEWER read access, non-member 403, visible-event-types allowlist enforcement, cross-card realtime isolation, idempotent unassign, and a full 11.10 end-to-end acceptance smoke test.
- `specs/tests/board-delete-auto-refresh.md`: Added **Section: Regression and acceptance hardening** with 5 new Playwright MCP scenarios (BDAR-REG-01–BDAR-REG-05) covering: VIEWER role denial, settings panel teardown before redirect, members panel teardown before redirect, 404 on second delete attempt (idempotency), and a full BDAR-REG-05 end-to-end acceptance smoke test spanning both sprints.

## New

No new production files. All changes are spec/documentation updates.

## Technical Debt

None introduced. The regression scenarios surface two areas worth monitoring in future iterations:
- Activity feed pagination: currently all rows are returned in a single response; a cursor-based paginator should be added if high-volume boards become common.
- `writeActivity` failure swallowing: the non-blocking fire-and-forget pattern means a persistent DB failure would silently drop activity events; an async retry or dead-letter queue should be considered.

## What Should Be Done Next

- Sprint 87 and Sprint 88 are fully specified and implemented. Run the full Playwright MCP test suite against the dev server to validate all acceptance criteria.
- Address the technical debt items above in a future performance/reliability iteration.
- Archive `.task` file if desired now that all 11 iterations are complete.
