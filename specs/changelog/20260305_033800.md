# Changelog — 2026-03-05T03:38:00Z

## Update

- `src/extensions/Plugins/api.ts` — extended `fetchAvailablePlugins` to accept `q`, `category`, `page`, and `perPage` params; added `fetchCategories()` for GET /api/v1/plugins/categories.
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.duck.ts` — added `searchQuery`, `selectedCategory`, and `categories` state; added `setSearchQuery`, `setSelectedCategory`, `clearSearch` reducers; added `fetchCategoriesThunk`; updated `fetchAvailablePluginsThunk` to forward search/filter params.
- `src/extensions/Plugins/containers/PluginDashboardPage/PluginDashboardPage.tsx` — renders `PluginSearchBar` above available plugins, wires search/category changes to dispatch, fetches categories on mount, shows "No plugins match your search." empty state with a Clear button.

## New

- `src/extensions/Plugins/components/PluginSearchBar.tsx` — debounced (300ms) search input + category dropdown; syncs to parent via `onSearchChange` / `onCategoryChange` callbacks.

## Technical Debt

- Automated Playwright e2e tests for search/filter deferred due to Bun/ESM issues.

## What Should Be Done Next

- Add pagination UI controls to the Available Plugins section.
- Add e2e tests once Bun/ESM test environment is resolved.
