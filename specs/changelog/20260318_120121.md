## Update
- Modified `server/extensions/card/api/create.ts` to emit a `card_created` activity event after successful card creation, ensuring the event is only written on success and includes all required payload fields.
- Updated `server/extensions/activity/config/visibleEventTypes.ts` to include `'card_created'` in the visible event types, allowing it to appear in activity feeds.

## New
- Added `server/extensions/activity/mods/createActivityEvent.ts`, providing a typed `emitCardCreated` wrapper around `writeActivity` for consistent, auditable event emission.
- Created `specs/tests/card-activity-create-move-assign.md` with detailed scenario requirements for card activity event flows (not yet executable as Playwright tests).

## Technical Debt
- The new test spec is written in requirements/scenario format, not as an executable Playwright test file. Needs conversion to Playwright's test syntax for automation.
- Pre-existing TypeScript errors remain elsewhere in the codebase, but none were introduced by these changes.

## What Should Be Done Next
- Convert the scenario spec (`card-activity-create-move-assign.md`) into an executable Playwright test file to enable automated verification.
- Proceed to implement `card_moved` and assignment/unassignment activity events in future iterations.
- Begin client-side rendering and notification mapping for activity events.