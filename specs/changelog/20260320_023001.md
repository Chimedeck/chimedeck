# Changelog — 20260320_023001

## Update
- `server/extensions/notifications/api/index.ts`: Registered the new `globalPreferenceRouter` for `/api/v1/user/notification-settings`.
- `src/extensions/UserProfile/containers/EditProfilePage/EditProfilePage.tsx`: Added a "Notifications" section with `GlobalNotificationToggle` above the existing notification preferences section.
- `src/extensions/UserProfile/translations/en.json`: Added `notifications`, `notificationsLabel`, `notificationsAriaLabel`, `notificationsLoadError`, and `notificationsUpdateError` keys.

## New
- `server/extensions/notifications/api/globalPreference/get.ts`: `GET /api/v1/user/notification-settings` — returns the authenticated user's global notification setting (defaults to enabled via opt-out model).
- `server/extensions/notifications/api/globalPreference/update.ts`: `PATCH /api/v1/user/notification-settings` — upserts the authenticated user's global notification setting.
- `server/extensions/notifications/api/globalPreference/index.ts`: Router wiring the two global preference endpoints.
- `src/extensions/UserProfile/containers/EditProfilePage/GlobalNotificationToggle.tsx`: Pill-switch toggle component that fetches/updates the global notification setting with optimistic UI and rollback.
- `specs/tests/board-notification-preference.md`: Playwright MCP test spec for per-board notification toggle (Board Settings panel).
- `specs/tests/global-notification-setting.md`: Playwright MCP test spec for global notification toggle (Profile Settings page).

## Technical Debt
- None introduced.

## What Should Be Done Next
- Run the Playwright MCP tests in `specs/tests/global-notification-setting.md` against a live dev server to confirm end-to-end behaviour.
- Consider exposing a feature flag for the global notification toggle similar to `NOTIFICATION_PREFERENCES_ENABLED` if gradual rollout is needed.
