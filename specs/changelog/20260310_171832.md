## Update
- Integrated `CustomFieldsSection` into `CardModal.tsx` below the description for in-modal editing of all custom fields.
- Updated `CardItem.tsx` and `CardTile.tsx` to render custom field badges for fields with `show_on_card=true` and a value.
- Server: Added `GET /cards/:id/custom-field-values` endpoint and fixed payload format to accept `value_text`, `value_number`, etc. matching client types.
- Passed `boardId` prop from `BoardCanvas` through `SortableListColumn` to `CardItem` for badge rendering context.

## New
- `CustomFieldValueEditor.tsx`: Type-aware input component for editing TEXT, NUMBER, DATE, CHECKBOX, and DROPDOWN custom fields.
- `CustomFieldBadge.tsx`: Badge component for displaying custom field values on card tiles.
- `CustomFieldsSection.tsx`: Section for card modal, rendering all custom field editors.
- `CardCustomFieldBadges.tsx`: Renders badges for custom fields in `CardItem` (board tile).
- `tests/e2e/custom-fields-ui.md`: Playwright MCP test covering editing and badge display for all field types.

## Technical Debt
- Playwright did not discover Markdown-based tests; test file may require config or format adjustment.
- Some pre-existing TypeScript errors remain unrelated to these changes.
- E2E coverage for card value API and badge rendering is pending test runner config fix.

## What Should Be Done Next
- Verify Playwright config to ensure Markdown-based tests are discovered and executed.
- Implement deferred card value API and card GET includes if not already complete.
- Add E2E tests for board star/follow and plugin context.
- Complete uuidv4→uuidv7 migration for all IDs.
- Begin Sprint 56: Business logic invariants (archived board guard, workspace owner invariant).