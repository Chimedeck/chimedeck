# Changelog — 2026-03-05T09:20:08Z

## Update
- No changes to existing server routes were required; the static SDK route (/sdk/jh-instance.js) was already present and correctly configured to serve application/javascript.

## New
- Added `server/extensions/plugins/sdk/jh-instance.ts`: Implements the `jhInstance` browser SDK for plugin iframe communication, including `initialize()`, `iframe()`, and `FrameContext` APIs, plus a TrelloPowerUp compatibility shim.
- Built and published `public/sdk/jh-instance.js`: Minified browser bundle for use by plugin iframes and external integrations.
- Created `specs/tests/plugins-sdk-mcp-evaluator.md`: Manual test plan for plugin SDK flows, covering initialization, context, compatibility, and edge cases.

## Technical Debt
- No automated Playwright or MCP evaluator tests exist for the plugin SDK; only a manual test plan was added.
- SDK versioning and advanced plugin authentication/data storage are deferred.

## What Should Be Done Next
- Implement automated Playwright/MCP evaluator tests for plugin SDK and iframe flows.
- Build the client-side plugin dashboard UI and Redux integration.
- Complete advanced plugin authentication, registry caching, and data storage features.
- Establish SDK versioning and update documentation as the SDK evolves.
