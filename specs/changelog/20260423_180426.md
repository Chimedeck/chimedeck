## Update
- Fixed board/card activity rows for `card_link_attached` so they no longer fall back to the generic "performed card_link_attached" message.
- Added explicit activity copy that includes which link/card was attached via a computed `linkTarget` (referenced card title, attachment name, or URL).
- Extended URL-attachment activity payload generation to include `linkUrl` and `referencedCardTitle` for card-link attachments.

## New
- Added translation key `activity.action.card_link_attached` for board activity feed rendering.

## Technical Debt
- Existing historical activity rows created before this change may not have `referencedCardTitle`; UI safely falls back to attachment name or URL.

## What Should Be Done Next
- Add a regression test covering URL attachment to internal card URL and assert board activity renders referenced card title.
- Consider enriching historical `card_link_attached` payload rows via a one-off backfill if complete historical fidelity is required.
