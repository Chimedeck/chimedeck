## Update
- Modified `server/extensions/healthCheck/api/index.ts` to register new probe endpoints and ensure correct routing order (probe-all before wildcard).

## New
- Added `server/extensions/healthCheck/mods/rateLimiter.ts`: In-memory rate limiter (5s window) with helpers for per-check and per-board limits.
- Added `server/extensions/healthCheck/api/probe.ts`: Implements `POST /api/v1/boards/:boardId/health-checks/:id/probe` endpoint with rate limiting and probe execution.
- Added `server/extensions/healthCheck/api/probeAll.ts`: Implements `POST /api/v1/boards/:boardId/health-checks/probe-all` endpoint with per-board rate limiting and concurrent probe execution.

## Technical Debt
- Rate limiting is in-memory only and not persistent across process restarts.
- No automated Playwright or MCP tests exist for the new endpoints or rate limiting flows.

## What Should Be Done Next
- Write Playwright E2E tests for probe/probe-all endpoints, rate limiting, and error cases.
- Document rate limiter limitations and error shapes in developer docs.
- Consider persistent/shared rate limiting if needed for production.
- Review SSRF and URL validation logic for probe endpoints.