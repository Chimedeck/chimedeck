# Sprint 120 — Text and Contrast Full Audit (Fixes)

## Update

### Critical contrast fix
- **MemberAvatarPopover**: Fixed invisible text bug — member name was `text-white` on a `bg-white` surface. Converted container to `bg-bg-surface border-border`, member name to `text-base`, hover to `hover:bg-bg-overlay`. Removed stray `dark:` variants.

### Auth pages → semantic tokens
All six auth page containers (`LoginPage`, `SignupPage`, `ForgotPasswordPage`, `VerifyEmailPage`, `ConfirmEmailChangePage`, `ResetPasswordPage`) converted:
- `bg-slate-950` → `bg-bg-base`, `bg-slate-900` → `bg-bg-surface`, `border-slate-800` → `border-border`
- `text-white` headings/brand name → `text-base`
- Removed all `{/* [theme-exception] text-white on dark slate-900 bg */}` block JSX comments (no longer applicable with semantic tokens)

### Auth form components → semantic tokens
- **LoginForm**, **SignupForm**: `bg-indigo-600 hover:bg-indigo-500` submit button → `bg-primary hover:bg-primary-hover`; inline `// [theme-exception]` on className line
- **ChangeEmailForm**: heading `text-white` → `text-base`; button → `bg-primary hover:bg-primary-hover`
- **VerificationPending**: `hover:text-white` close button → `hover:text-base`; button → `bg-primary hover:bg-primary-hover`; fixed invalid `{/* */}` JSX comment inside ternary
- **OAuthButton**: `bg-slate-800 hover:bg-slate-700 border-slate-700 text-slate-200` → `bg-bg-overlay hover:bg-bg-sunken border-border text-base`; `ring-indigo-500` → `ring-primary`
- **PasswordInput**: `hover:text-slate-200` → `hover:text-base`; `ring-indigo-500` → `ring-primary`

### Color tokens (text-red-*, text-green-* → semantic)
- **EditPluginModal**, **RegisterPluginModal**, **PluginDashboardPage**: error boxes `bg-red-900/30 border-red-700 text-red-300` → `bg-danger/10 border-danger/40 text-danger`
- **ApiKeyRevealModal**: `text-green-300` → `text-success`
- **CredentialSheet**: `bg-green-900/30 text-green-300` → `bg-success/10 text-success`; `bg-slate-950 text-slate-200` → `bg-bg-base text-base`; `border-slate-700` → `border-border`; `hover:bg-slate-800` → `hover:bg-bg-overlay`
- **BoardStateChip**: `bg-green-100 text-green-700` → `bg-success/10 text-success`
- **VisibilityBadge**: PUBLIC `bg-green-600/80 text-green-100` → `bg-success/80 text-inverse`; WORKSPACE `bg-blue-600/80 text-blue-100` → `bg-primary/80 text-inverse`
- **MemberAssignModal**: `bg-red-100 text-red-600 hover:bg-red-200` → `bg-danger/10 text-danger hover:bg-danger/20`; blue counterpart → `bg-primary/10 text-link hover:bg-primary/20`

### Card components → semantic tokens
- **CardMetaStrip**: dates chips (done/overdue/due-soon/hasDate/default), checkboxes, dividers, popovers, ring colors all converted; `dark:` variants removed; inline `// [theme-exception]` added to all `text-white` on colored backgrounds
- **CardModalBottomBar**: `hover:bg-gray-100 dark:hover:bg-slate-800` → `hover:bg-bg-overlay`; `border-gray-200 bg-gray-50/50` → `border-border bg-bg-surface/50`; all popover backgrounds → `bg-bg-surface border-border`
- **CardMembers**: `bg-white border-gray-200` → `bg-bg-surface border-border`; hover → `hover:bg-bg-overlay`
- **OneLineToolbar**: borders, hover states, dividers, kbd bg, active state converted
- **CardActionMenu**: `hover:bg-gray-100` → `hover:bg-bg-overlay`
- **ChecklistProgress**: `bg-gray-200` track → `bg-bg-overlay`
- **CommandMenu**: `bg-gray-100` → `bg-bg-overlay`
- **CardReferenceChip**: `border-gray-300 bg-gray-100` → `border-border bg-bg-overlay`
- **ActivityFeed**: actor name `text-white` → `text-base`; avatar div moved to inline `// [theme-exception]`
- **CardMemberAvatars**: overflow count `bg-gray-400 ring-white` → `bg-bg-overlay text-muted ring-bg-surface`; individual avatar `ring-slate-800` → `ring-bg-surface`
- **CardLabelChips**: JSX block comment → inline `// [theme-exception]` on className
- **LabelChip**: JSX block comment → inline `// [theme-exception]` on className
- **CardDatesPicker**: `!bg-slate-500 !text-white` → `!bg-bg-sunken !text-base`; selected date kept with inline `// [theme-exception]`
- **CardModal/index**: `bg-slate-900` error panel → `bg-bg-surface`

### AddListForm → semantic tokens
Closed-state button and open-state form both converted to `bg-bg-surface border-border`. Submit → `bg-primary hover:bg-primary-hover` with inline `// [theme-exception]`. Cancel → `hover:bg-bg-overlay`.

### PluginDashboardPage → semantic tokens
Page wrapper `bg-slate-900 text-slate-100` → `bg-bg-base text-base`; back button hover `hover:text-slate-200` → `hover:text-base`.

### BoardViews panels
- **BoardCommentsPanel** and **BoardArchivedCardsPanel**: card surfaces `border-slate-700 bg-slate-800` → `border-border bg-bg-surface`; `text-gray-200` → `text-base`; link buttons → `text-link hover:bg-bg-overlay`

### Calendar, Timeline, CustomFields, UserProfile, EditorHelpModal → semantic tokens
All hardcoded `border-slate-*`, `bg-slate-*`, `text-slate-*` in these views converted to semantic tokens (`border-border`, `bg-bg-surface/base/overlay/sunken`, `text-base/subtle/muted`). `dark:` variants removed.

### Plugins components → semantic tokens
- **PluginRegistryTable**: `bg-slate-50` → `bg-bg-sunken`
- **PluginAllowedDomainsPanel**: `border-slate-700 bg-slate-850` → `border-border bg-bg-sunken`
- **CardPluginSection**: `border-slate-700 bg-slate-800/50` → `border-border bg-bg-overlay/50`
- **CardPluginBadges**: `bg-slate-600/30` → `bg-bg-overlay/30`
- **CardDetailPluginBadges**: `bg-slate-700/50 border-slate-600/40` → `bg-bg-overlay/50 border-border/40`
- **CardPluginButtons**: `bg-slate-700 text-slate-200 hover:bg-slate-600` → `bg-bg-overlay text-base hover:bg-bg-sunken`

### InviteExternalUserModal
Residual `bg-slate-700` strength bar and `hover:bg-slate-800 hover:text-white` cancel button converted.

## New

- `src/common/tokens/index.ts`: Centralized semantic token class-string reference with exported constants (`inputClasses`, `overlayPanelClasses`, `neutralBadgeClasses`, `successBadgeClasses`, `dangerBadgeClasses`, `errorBoxClasses`, `listItemHoverClasses`)
- `specs/tests/sprint-120-contrast-audit.md`: Full Playwright MCP test spec covering 7 suites — login page, workspace dashboard, board view, card modal, theme toggle, error/success states, and input placeholders — in both light and dark modes

## Technical Debt

- Several files outside the audit scope (`Automation/`, `Attachments/`, `Comment/`, `Notification/`, `Workspace/`, `Search/`) still have `text-white` on hardcoded colored backgrounds (`bg-blue-600`, `bg-red-600`, `bg-indigo-600`, dynamic colors). These are valid uses but lack `// [theme-exception]` inline tags. A follow-up audit pass is recommended.
- `DropdownFieldEditor.tsx` has `text-white` on a color swatch — valid, needs `// [theme-exception]`
- TimelineView still has some hardcoded `bg-blue-600` today-highlight markers — tagged `// [theme-exception]` already

## What Should Be Done Next

1. Run Playwright contrast tests from `specs/tests/sprint-120-contrast-audit.md` against both light and dark modes
2. Extend `// [theme-exception]` audit to remaining uncovered extensions (`Automation`, `Attachments`, `Comment`, `Search`)
3. Consider adding `bg-primary-hover` to tailwind.config.ts `theme.extend.colors` if not already present (check that `hover:bg-primary-hover` compiles)
4. Add ESLint rule to prevent new `bg-gray-*` / `bg-slate-*` / `text-white` without `// [theme-exception]` annotation
