## Update
- BoardHeader now hides member avatar stack and settings menu for GUEST users.
- BoardSettings and BoardMembersPanel return null for GUEST users, removing access to settings and member management.
- BoardPage passes isGuest prop to BoardHeader, BoardSettings, and BoardMembersPanel; uses spread for optional handlers to satisfy exactOptionalPropertyTypes.
- Routing updated to wrap /workspace/:workspaceId with GuestBlockedRoute, redirecting GUEST users from restricted routes.

## New
- GuestBlockedRoute: Route guard component that redirects GUEST users from /workspace/:workspaceId to /workspaces/:id/boards.
- Playwright MCP test spec (sprint-80-iteration-9-guest-permissions.md) covering guest permission scenarios.
- Changelog for Iteration 9.

## Technical Debt
- Playwright MCP .md test files are not runnable with standard Playwright tooling; no custom runner detected.
- Some pre-existing TypeScript errors remain unrelated to these changes.

## What Should Be Done Next
- Clarify or implement a runner for MCP .md Playwright test files, or convert tests to standard Playwright format.
- Continue with guest permission enforcement in API and server-side checks if not already complete.
- Address pre-existing TypeScript errors in CalendarView/TimelineView.
- Monitor for edge cases in guest role detection and route protection.