## Update
- Extended `Notification` API type to support new board activity types and `list_title` for card moves.
- Updated `NotificationItem.tsx` to render type-specific icons and copy for `card_created`, `card_moved`, and `card_commented` events.
- Modified notification list API and WS payloads to include `list_title` for card move events.
- Clarified `useNotificationSync.ts` hook documentation for type handling.

## New
- Added Playwright MCP spec: `specs/tests/board-activity-notifications-ui.md` covering all notification types, icons, copy, real-time WS updates, and edge cases.

## Technical Debt
- Playwright MCP UI spec is not detected by Playwright runner ("No tests found"); test format or runner config may need adjustment.
- Minor event type naming inconsistencies remain (e.g., `comment_added` vs. `card.comment_added`).

## What Should Be Done Next
- Ensure Playwright recognizes and executes the new `.md` UI spec, or migrate to `.ts/.js` if required.
- Address event type naming consistency across API, WS, and UI.
- Automate and validate full end-to-end notification preference and board activity flows.
- Defer further notification UI enhancements and advanced filtering to the next iteration.