## Update
- Updated `server/extensions/board/api/create.ts` to wrap board creation in a transaction and auto-insert the creator as ADMIN in the new `board_members` table.

## New
- Added `db/migrations/0040_board_members.ts` migration to create the `board_members` table with appropriate constraints and indexes.
- Added `specs/tests/sprint-78-iteration-1-board-members-migration.md` Playwright MCP test to verify the creator is listed as ADMIN in `board_members` after board creation.

## Technical Debt
- Playwright test was not recognized by the runner; test configuration or file path needs investigation.
- Deferred: Board-level access control middleware, API endpoints for managing board members, and UI integration.

## What Should Be Done Next
- Fix Playwright test discovery so the new test runs and validates the migration.
- Implement board-level access control middleware and endpoints.
- Add UI for managing board members and roles.
- Address any technical debt or errors noted in recent changelogs.