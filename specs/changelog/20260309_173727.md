## Update
- Added `ALLOWED_EMAIL_DOMAINS` and `EMAIL_DOMAIN_RESTRICTION_ENABLED` to `server/config/env.ts` with validation and documentation.
- Updated `register.ts` and `changeEmail.ts` to enforce domain restriction and return `{ name: 'email-domain-not-allowed' }` error if violated.
- Enhanced `SignupForm.tsx` and `ChangeEmailForm.tsx` to map the new error code to user-friendly messages.
- Extended Auth translations to include new error keys for domain restriction.

## New
- Created `server/extensions/auth/common/emailDomain.ts` with helpers for domain extraction and allowlist checking.
- Added `tests/integration/auth/emailDomainRestriction.test.ts` covering all domain restriction scenarios (10 tests, all passing).

## Technical Debt
- None

## What Should Be Done Next
- Run Playwright MCP evaluator and E2E tests using Node.js instead of Bun to resolve loader compatibility issues.
- Proceed to implement Sprint 44 and 45 features after confirming all tests pass.
- Monitor for any missed edge cases in domain parsing or error mapping during further QA.