## Update
- `useAttachmentUpload.ts`: Changed `onSuccess` callback to return the full `Attachment` object instead of just the ID. Updated both upload flows to pass the full payload.
- `OneLineToolbar.tsx`: Added optional `onAttach` prop and inserted a paperclip button for triggering file selection.
- `CardDescriptionTiptap.tsx`: Integrated attachment upload, inline preview, and cancel/error handling. Updated toolbar usage and editor state management.
- `CommentEditor.tsx`: Added attachment upload support, inline preview, and error/cancel handling, mirroring the card description editor.

## New
- `InlineUploadPreview.tsx`: Reusable component for showing upload progress, thumbnail/icon, filename, size, and error/cancel actions inline in editors.
- 6 new Playwright test scenarios in `rich-text-toolbar-single-line-overflow.md` for attachment upload preview, progress, cancel, error, and multiple files.

## Technical Debt
- None

## What Should Be Done Next
- Fix Playwright project configuration and rerun attachment upload tests.
- Consider refactoring shared upload preview logic if more editors require this feature.
- Review accessibility and error messaging for all upload flows.
- Monitor for edge cases with simultaneous uploads and large files.