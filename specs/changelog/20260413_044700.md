# Changelog — 2026-04-13 04:47:00

## Update
- **ErrorBoundary**: replaced raw `<button>` elements with `Button` (primary / secondary variants).
- **ListHeader**: replaced title, options (···), and all dropdown menu action buttons with `Button ghost` / `Button danger` variants.
- **ListColumn**: replaced "+ Add a card" raw button with `Button ghost` variant preserving full-width layout.
- **ResetPasswordPage**: replaced submit button with `Button primary`, preserving loading/disabled state.
- **TimelineView**: replaced Today toolbar button with `Button secondary`.
- **TimelineZoomControl**: replaced zoom-level toggle buttons with `Button ghost` + active-state className override, preserving `aria-pressed` and `data-testid`.
- **TimelineRow**: replaced unscheduled-card chip buttons with `Button ghost` variant.
- **TableRow**: replaced card title link-style button with `Button ghost` + underline hover classes.
- **NotificationPreferencesPanel**: documented the `role="switch"` toggle as a `[switch-exception]` — raw button intentionally retained due to transform-based thumb layout incompatible with Button's `justify-center` base class.
- **CardPluginButtons**: added `// [plugin-button-exception]` comments on both sidebar and chip plugin-injected raw buttons.

## New
- None.

## Technical Debt
- NotificationPreferencesPanel's ToggleSwitch could be extracted as a dedicated `ToggleSwitch` design-system primitive to avoid the raw-button exception in future.

## What Should Be Done Next
- Iteration 7: Consolidation sweep — verify all Sprint 133 listed files are migrated or explicitly excepted; fix any regressions; run targeted manual smoke tests.
