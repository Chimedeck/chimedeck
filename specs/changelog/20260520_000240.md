## Update
- Integrated state transition graph sync hooks into list rename/delete flows (update.ts, delete.ts).
- Updated stateTransitions API index to mount /rules endpoint.
- Enhanced serializer and sync helpers for rules normalization and list sync.
- Ensured websocket broadcast on successful stateTransitions PUT.

## New
- Added GET /api/v1/boards/:boardId/state-transitions/rules endpoint (getRules.ts).
- Created sync helpers for list rename/delete (sync.ts).
- Added websocket utility for state_transition_updated event (ws.ts).
- Added test specs: specs/tests/stateTransitions-rules.md, specs/tests/stateTransitions-sync.md.

## Technical Debt
- Legacy error envelope shape ({ error: { ... } }) remains in some paths; not fully migrated to { name, data }.
- Partial sync edge cases (e.g., missing node on rename/delete) handled minimally; revisit for robustness.

## What Should Be Done Next
- Implement /copy endpoint and enforcement guard.
- UI integration for state transitions rules and sync feedback.
- Complete error envelope migration for all endpoints.
- Expand tests for additional edge cases and websocket event flows.