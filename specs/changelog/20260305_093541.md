## Update
- Mounted `<PluginIframeContainer boardId={boardId ?? ''} />` in `BoardPage.tsx` to inject plugin iframes when viewing a board.

## New
- Added `src/extensions/Plugins/iframeHost/usePluginBridge.ts`: React hook for postMessage protocol, origin validation, and plugin bridge context.
- Added `src/extensions/Plugins/iframeHost/PluginIframeHost.tsx`: Renders a hidden, sandboxed iframe for each active plugin.
- Added `src/extensions/Plugins/iframeHost/PluginIframeContainer.tsx`: Fetches active plugins, renders one iframe per plugin, and provides bridge context.

## Technical Debt
- Playwright plugin flow tests are missing or not discoverable; MCP evaluator project not found and ESM loader errors prevent automated test runs.
- `plugin_auth_tokens` table remains unused; WorkspaceModal tests are still broken due to missing mock store fields.

## What Should Be Done Next
- Implement server-side plugin APIs, iframe host/bridge backend, Redux/API wrappers, and plugin data storage.
- Add UI injection points, modal/popup flows, and automated Playwright tests for plugin flows.
- Ensure plugin authentication, registry caching, and robust teardown of plugin iframes/bridges.
- Fix Playwright test configuration and ensure compatibility with Bun runtime.