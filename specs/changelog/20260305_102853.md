## Update
- Extended `server/extensions/plugins/api/registry/list.ts` to support search (`q`), filter by `category` and `isPublic`, and pagination (`page`, `perPage`, max 50).
- All plugin list queries now exclude soft-deleted plugins (`is_active=false`).
- Added metadata (`total`, `totalPage`, `page`, `perPage`) to plugin list responses.
- Updated `server/extensions/plugins/api/index.ts` to register `/categories` route before `/:pluginId` to prevent route collision.

## New
- Implemented `GET /api/v1/plugins/categories` endpoint in `server/extensions/plugins/api/registry/categories.ts` to return a deduplicated array of all active plugin categories.

## Technical Debt
- No automated e2e or Playwright tests for plugin registry endpoints; Playwright tests failed to run due to Bun/ESM loader incompatibility.
- Admin check is email-based from env, not DB-driven.
- Duplicated `isRegistryAdmin` helper exists.
- Deferred: SDK versioning, registry caching, client modal/bridge features.

## What Should Be Done Next
- Fix Playwright/Bun compatibility to enable automated test runs.
- Add/enable e2e tests for plugin registry API and SDK.
- Refactor admin checks to use DB roles, deduplicate helpers.
- Implement SDK versioning, registry caching, and deferred client features.
- Monitor for edge cases in category extraction and pagination.