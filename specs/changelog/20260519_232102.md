## Update
- Adjusted state transitions GET/PUT handling so boards with zero active lists now return/persist an empty graph instead of returning `404 board-not-found`.
- Tightened graph response serialization to emit only contract fields (`nodes`, `edges`, `notes` item properties) for API output.

## New
- Added serializer coverage to verify extra graph fields are stripped from API graph responses.
- Updated API unit tests for GET/PUT to assert empty-graph behavior when a board has no active lists.

## Technical Debt
- `validateGraphShape` still accepts unknown extra fields on input objects before serialization strips them; if strict input rejection is desired, validator-level no-extra-key checks should be added.

## What Should Be Done Next
- Keep Sprint 153 iteration boundaries by finishing any remaining GET/PUT acceptance coverage in the top-level `tests/api/stateTransitions` suite for the zero-lists edge case as well.
