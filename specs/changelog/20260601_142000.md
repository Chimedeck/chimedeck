## Update
- Extended subscription routing with a dedicated Stripe webhook endpoint at `POST /api/subscription/webhook`.
- Expanded `subscriptionRepo` with Stripe lookup helpers and webhook idempotency tracking helpers used by sync processing.

## New
- Added `server/extensions/subscription/api/webhook.ts` with Stripe signature verification, error envelopes, and webhook processing flow.
- Added `server/extensions/subscription/common/syncFromStripe.ts` to process Stripe subscription events idempotently and persist workspace tier/status sync.
- Added `server/extensions/subscription/common/priceTierMap.ts` to map Stripe price IDs to internal tiers with `tier_1` fallback.
- Added tests:
  - `tests/server/extensions/subscription/api/webhook.test.ts`
  - `tests/server/extensions/subscription/common/syncFromStripe.test.ts`

## Technical Debt
- Webhook idempotency uses in-process event tracking; persistent cross-process idempotency storage still needs a dedicated DB-backed event log.

## What Should Be Done Next
- Add a migration-backed Stripe webhook event ledger so idempotency survives restarts and horizontal scaling.
- Extend event coverage when additional Stripe billing events are needed (e.g. invoice-driven transitions).
