# Changelog — 20260319_051151

## Update
- `src/extensions/Attachment/components/AttachmentUploader.tsx` — replaced hardcoded "Uploading… {progress}%" and "Drop a file here or click to browse" with `translations[...]` bracket notation.
- `src/extensions/Attachment/components/AttachmentItem.tsx` — replaced "Open", "Download", and "Delete attachment" aria-label with translation keys.
- `src/extensions/Attachment/components/AttachmentStatusBadge.tsx` — replaced BADGE_LABELS object values ("Scanning…", "Ready", "Rejected") with translation keys.
- `src/extensions/Attachment/components/AttachmentSection.tsx` — replaced "Attachments" heading and "+ Add URL" button label with translation keys.
- `src/extensions/Attachment/components/AttachmentUrlModal.tsx` — replaced modal title, field labels ("Label", "URL"), error messages, and "Cancel"/"Add"/"Adding…" button labels with translation keys.
- `src/extensions/Attachments/components/AttachmentDropZone.tsx` — replaced "Drop files to attach" overlay text with translation key.
- `src/extensions/Attachments/components/AttachmentThumbnail.tsx` — replaced all four aria-labels ("Close image preview", "Preview {name}", "Close video player", "Play {name}") with translation keys.
- `src/extensions/Attachments/components/AttachmentItem.tsx` — replaced STATUS_LABELS object values, "Open link"/"Play video"/"Download file" aria-labels, and "Delete?"/"Yes"/"No" confirmation text with translation keys.
- `src/extensions/Attachments/components/InlineUploadPreview.tsx` — replaced "Upload failed", "Uploaded", "Queued", "Uploading {fileName}", "Remove"/"Dismiss"/"Cancel upload" tooltip and aria-label with translation keys.
- `src/extensions/Attachments/components/UploadProgressBar.tsx` — replaced default label "Uploading…" with translation key.
- `src/extensions/Attachments/components/AttachmentPanel.tsx` — replaced "Attachments" heading, "Attach file" button, empty-state message, "Images"/"Videos" sub-headings, URL/name input placeholders, "Attach"/"Cancel" form buttons, and "Attach a link" button with translation keys.

## New
- `src/extensions/Attachment/translations/en.json` — created with 19 string keys for the Attachment extension (section title, uploader, item actions, status badge labels, URL modal).
- `src/extensions/Attachments/translations/en.json` — created with 38 string keys for the Attachments extension (drop zone, thumbnails, item status/actions, inline upload, progress bar, panel).
- `specs/tests/sprint-90-iteration-3-attachment-i18n.md` — Playwright MCP test covering all 9 acceptance criteria for Attachment(s) i18n.

## Technical Debt
None introduced.

## What Should Be Done Next
- Iteration 4: Automation extension i18n — create `src/extensions/Automation/translations/en.json` and update all Automation components to use translation keys.
