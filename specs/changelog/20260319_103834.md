# Changelog — 20260319_103834

## Update

- `src/extensions/Notification/translations/en.json`: Extended with new keys: `Notifications.markAllRead` (updated text to "Mark all as read"), `Notifications.clearAll`, `Notifications.settingsLink`, `Notifications.ariaOpenPanel`, `Notifications.ariaClosePanel`, `Notifications.ariaUnread`, `Notifications.loading`, `Notifications.today`, `Notifications.earlier`.
- `src/extensions/Notification/components/NotificationBell.tsx`: Replaced hardcoded `aria-label` strings with `translations['Notifications.ariaOpenPanel']` and `translations['Notifications.ariaUnread']` (interpolated with count).
- `src/extensions/Notification/components/NotificationPanel.tsx`: Replaced hardcoded `aria-label`, `<h2>` text, "Mark all read", "You have no notifications.", "Loading…", "Load more" with translations bracket-notation.
- `src/extensions/Notification/components/NotificationItem.tsx`: Replaced hardcoded `aria-label="Dismiss notification"` with `translations['Notifications.deleteAriaLabel']`.
- `src/extensions/Notifications/NotificationPreferences/NotificationPreferencesPanel.tsx`: Replaced hardcoded "Loading notification preferences…", table headers ("Notification", "In-App", "Email"), toggle aria-labels, email disabled tooltip, and error toast message with translations bracket-notation.

## New

- `src/extensions/Mention/translations/en.json`: Created with keys `Mention.ariaList`, `Mention.searchPlaceholder`, `Mention.noResults`, `Mention.chipAriaRemove`.
- `src/extensions/Notifications/translations/en.json`: Created with keys `NotificationPreferences.columnNotification`, `NotificationPreferences.columnInApp`, `NotificationPreferences.columnEmail`, `NotificationPreferences.loading`, `NotificationPreferences.emailDisabledTooltip`, `NotificationPreferences.updateError`.
- `src/extensions/Mention/MentionList.tsx`: Added import of Mention translations and replaced hardcoded `aria-label="Mention suggestions"` with `translations['Mention.ariaList']`.
- `specs/tests/sprint-94-mention-notifications-i18n.md`: Playwright MCP test plan for Mention + Notifications i18n verification.

## Technical Debt

- `src/common/components/MentionInput/MentionSuggestions.tsx` still has hardcoded `aria-label="Mention suggestions"`. This will be addressed in Iteration 8 (Common + Layout i18n).
- `src/extensions/Notification/components/NotificationItem.tsx` — the `buildCopy()` function generates notification copy strings dynamically without using translations. These are data-driven strings (actor names, card titles) rather than UI labels, so they are out of scope for this iteration, but should be reviewed in a future i18n pass.

## What Should Be Done Next

- Iteration 6: UserProfile + AdminInvite i18n — create/extend `UserProfile/translations/en.json` and `AdminInvite/translations/en.json`.
- Iteration 7: Realtime + OfflineDrafts + BoardViews i18n.
- Iteration 8: Common + Layout i18n (includes fixing `MentionSuggestions.tsx` hardcoded string).
