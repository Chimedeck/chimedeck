## Update
- None. No changes to existing code; all work was additive.

## New
- Added `db/migrations/0118_state_transitions.ts` migration: creates `board_state_transitions` table with `board_id` (FK, unique), `enabled` (default `false`), `graph_data` (JSONB, default empty graph), timestamps, and index.
- Added `STATE_TRANSITIONS_ENABLED` feature flag to `server/config/featureFlags.ts`, reading from `Bun.env.STATE_TRANSITIONS_ENABLED`.
- Added `StateTransitionGraph`, `StateTransitionNode`, `StateTransitionEdge`, and `StateTransitionNote` TypeScript interfaces to `server/extensions/stateTransitions/common/types.ts`.

## Technical Debt
- None.

## What Should Be Done Next
- Implement API endpoints and validation for state transitions.
- Add UI and integration tests for state transitions (blocked until login/auth is fixed).
- Ensure feature flag is respected in all relevant flows.
- Migrate any legacy board state logic to use the new schema/types.
