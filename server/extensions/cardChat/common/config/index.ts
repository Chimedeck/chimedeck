// Card-chat config — centralizes feature flag key and placeholder for
// future AI provider configuration (Sprint 171+).

/** Feature flag key gating the card-scoped chat feature. */
export const INNER_CARD_CHAT_FLAG_KEY = 'INNER_CARD_CHAT_ENABLED';
