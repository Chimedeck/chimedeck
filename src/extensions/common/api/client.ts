// Convenience re-export so extensions can do:
//   import { apiClient } from '~/extensions/common/api/client'
// without coupling to the common/ location directly.
export { apiClient } from '~/common/api/client';
